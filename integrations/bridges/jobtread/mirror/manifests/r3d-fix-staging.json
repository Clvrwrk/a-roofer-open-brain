{
  "run_name": "jt-acculynx-mirror",
  "workdir": "/Users/chussey/Documents/a-roofers-open-brain/integrations/bridges/jobtread/mirror",
  "max_parallel": 1,
  "tasks": [
    {
      "key": "r3d-fix-staging",
      "task_type": "code-fix",
      "spec": "You are a staging-data repair worker for jt-acculynx-mirror, second repair pass. The executor now fails on exactly two payload defects; you fix them and run a proactive required-field sweep so there is no third pass. BOUNDARY: UPDATE only jt_mirror.pending_write rows with status in ('failed','staged'); NEVER touch status 'executed' or 'skipped'; Supabase SQL only, no external APIs; one deliverable: /Users/chussey/Documents/a-roofers-open-brain/integrations/bridges/jobtread/mirror/r3d-fix-staging/fix-report.md; no credential values anywhere. SQL access: set -a; source /Users/chussey/.config/cleverwork/master.env; set +a; curl -s -X POST https://api.supabase.com/v1/projects/rnhmvcpsvtqjlffpsayu/database/query -H \"Authorization: Bearer $SUPABASE_ACCESS_TOKEN\" -H 'Content-Type: application/json' -d '{\"query\":\"<SQL>\"}'. AUTHORITATIVE OP SIGNATURES (from the official docs snapshot — trust these, do not re-derive): createCostType({isTaxable, isTimeTrackable, margin, name, organizationId}); createCostItem({costCodeId, costTypeId, name, quantity, unitCost, unitPrice, unitId, organizationId, ...}); createAccount({name, type, organizationId, notify, ...}); createLocation({accountId, address, name, contactId, ...}); createJob({locationId, name, number, customFieldValues, companycamId, ...}); createDocument({jobId, accountId, type, name, lineItems, issueDate, ...}); createDailyLog({jobId, date, notes, notify, ...}); updateJob({id, customFieldValues, ...}). REPAIRS: (1) cost_types (4 failed rows): payload already has isTaxable; add \"isTimeTrackable\": false (jsonb_set), leave margin absent, reset status='staged', error=null, attempt=0. Rationale to note: these derive from QBO expense accounts; time tracking is not mirrored. (2) catalog_items (11 failed + 112 staged = all 123 need costCodeId): first inspect the 13 executed cost_codes rows (select source_ref, payload->>'name', payload->>'number' from jt_mirror.pending_write where domain='cost_codes' and status='executed') and jt_mirror.crosswalk for their jt ids. Assign every catalog item a costCodeId of the form {\"$ref\": \"<exact source_ref of the chosen cost_codes row>\"} so the executor resolves it via crosswalk. Mapping rule, deterministic: choose the cost code whose name/number best matches the item's semantics (labor-dominant items — labor_cost > material_cost or item_type indicates labor — to the labor-like code; material items to the material/COGS-like code; else the most general code). Document the exact rule and the per-code item counts in your report. Reset the 11 failed rows to status='staged', error=null, attempt=0. (3) notify sweep: update any staged payload where payload->>'notify'='true' to false; report how many (expected: 0 — verify). (4) PROACTIVE REQUIRED-FIELD SWEEP (report-only unless mechanically fixable): for each staged domain, verify payloads contain the required keys per the signatures above (customer_accounts: name+type+organizationId; locations: accountId-$ref+address; jobs: locationId-$ref+name; documents: jobId-$ref+type+name; daily_logs: jobId-$ref+date+notes; job_custom_values: id-$ref+customFieldValues). Fix mechanically derivable gaps (e.g., missing account type='customer'), list anything you cannot derive in ## Unresolved with the count — do NOT invent data. VERIFY with live SQL before finishing: zero failed rows; catalog_items staged=123 all with costCodeId; cost_types staged=4 with both booleans. DELIVERABLE fix-report.md sections: ## Repairs Applied, ## Cost Code Assignment (rule + distribution table), ## Required-Field Sweep (per domain: pass/gaps), ## Unresolved, ## Verdict.",
      "check": "python3 /Users/chussey/Documents/a-roofers-open-brain/integrations/bridges/jobtread/mirror/checks/r3d_fix_check.py",
      "expect_files": [
        "/Users/chussey/Documents/a-roofers-open-brain/integrations/bridges/jobtread/mirror/r3d-fix-staging/fix-report.md"
      ],
      "timeout_s": 1800,
      "verified": "cost_types complete, all 123 catalog items carry resolvable costCodeId refs, notify clean, required-field sweep done"
    }
  ]
}
