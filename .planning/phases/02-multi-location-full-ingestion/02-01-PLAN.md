---
phase: 02-multi-location-full-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/cleverwork-roofer/168-acculynx-watermark-ddl.sql
  - schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql
  - schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql
autonomous: true
requirements: [REQ-03, REQ-04]
must_haves:
  truths:
    - "acculynx_sync_watermark has account_key + per-resource cursor columns and a UNIQUE(account_key, resource) constraint"
    - "All 11 target tables carry account_key, market, last_seen_by_api, archived_at, archive_reason columns (live in prod DB)"
    - "v_acculynx_reconciliation returns api_count vs brain_count vs delta_pct per (account_key, resource)"
  artifacts:
    - path: "schemas/cleverwork-roofer/168-acculynx-watermark-ddl.sql"
      provides: "Watermark DDL capture + account_key dimension + UNIQUE(account_key,resource)"
      contains: "account_key"
    - path: "schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql"
      provides: "Additive account_key/diff columns on the 11 target tables"
      contains: "last_seen_by_api"
    - path: "schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql"
      provides: "v_acculynx_reconciliation view"
      contains: "v_acculynx_reconciliation"
  key_links:
    - from: "schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql"
      to: "live prod DB rnhmvcpsvtqjlffpsayu"
      via: "Supabase MCP apply_migration"
      pattern: "account_key"
    - from: "schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql"
      to: "acculynx_sync_watermark.last_api_count"
      via: "view subquery"
      pattern: "v_acculynx_reconciliation"
---

<objective>
Lay the schema foundation for multi-location ingestion: capture the live shapes of the
11 pre-existing empty AccuLynx target tables and the watermark table, then add (additively)
the account_key dimension, diff-detection columns, extended watermark cursors, and a
reconciliation view — and APPLY all three migrations to the live prod DB.

Purpose: Every downstream row must be FK-traceable to its source account (no cross-account
bleed, REQ-03) and every unfed resource (REQ-04) needs a place to land with archive-not-delete
diff semantics (hard rule 1). Verification of populated tables in later plans is meaningless
until these columns exist live.

Output: Migrations 168 (watermark), 169 (resource tables), 170 (reconciliation view), each
written to schemas/ AND applied to the live DB via Supabase MCP apply_migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md
@.planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0 (BLOCKING): Introspect live schemas of the 11 target tables + watermark</name>
  <read_first>
    - .planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md (section "What Exists vs. What Needs Building" — the 11-table list + Open Question 1)
    - skills/cleverwork-roofer/acculynx-api/SKILL.md (acculynx_invoices known columns: job_id, invoice_number, due_date, current_invoice_state, total_price, balance_due)
    - schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql (account_key slug values, market/state columns)
  </read_first>
  <action>
    Using Supabase MCP against project rnhmvcpsvtqjlffpsayu (READ-ONLY — no apply_migration in this task):
    call `list_tables` and `execute_sql` with `SELECT table_name, column_name, data_type, is_nullable FROM information_schema.columns WHERE table_schema='public' AND table_name IN ('acculynx_contacts','acculynx_job_contacts','acculynx_contact_emails','acculynx_contact_phones','acculynx_estimates','acculynx_invoices','acculynx_invoice_lines','acculynx_job_financials','acculynx_job_insurance','acculynx_insurance_carriers','acculynx_job_milestone_history','acculynx_sync_watermark') ORDER BY table_name, ordinal_position`.
    Also query existing PK/unique constraints: `SELECT tc.table_name, tc.constraint_type, kcu.column_name FROM information_schema.table_constraints tc JOIN information_schema.key_column_usage kcu USING (constraint_name) WHERE tc.table_schema='public' AND tc.table_name = ANY(...same list...)`.
    Record the exact live column list + constraints for each table into the SUMMARY as the ground-truth schema. This is the input contract for Task 1's migration 169. Do NOT assume column shapes from research — research flagged these as MEDIUM-confidence/UNKNOWN. Note which tables already have account_key (ADD COLUMN IF NOT EXISTS will no-op those) and the existing unique-constraint name on acculynx_sync_watermark (needed for the DROP CONSTRAINT IF EXISTS in mig 168).
    This is BLOCKING: migration 169 cannot be written correctly without it.
  </action>
  <verify>
    <automated>MISSING — read-only MCP introspection; manual verify: SUMMARY contains a column list for all 12 tables and the watermark's existing unique-constraint name</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY records column_name + data_type for all 12 tables (11 target + acculynx_sync_watermark) from live information_schema
    - SUMMARY records the existing unique/PK constraint name on acculynx_sync_watermark (the value to DROP CONSTRAINT IF EXISTS in mig 168)
    - SUMMARY notes for each of the 11 target tables whether account_key already exists live
  </acceptance_criteria>
  <done>Live ground-truth schema for all 12 tables captured in SUMMARY; the watermark's current unique constraint name is known.</done>
</task>

<task type="auto">
  <name>Task 1: Write migrations 168 (watermark) + 169 (resource tables) + 170 (reconciliation view)</name>
  <files>schemas/cleverwork-roofer/168-acculynx-watermark-ddl.sql, schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql, schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql</files>
  <read_first>
    - schemas/cleverwork-roofer/166-acculynx-catalog-probe-ddl.sql (header comment style, ADD COLUMN IF NOT EXISTS, index, RLS+grant patterns — lines 1-13, 49-61)
    - schemas/cleverwork-roofer/119-canonical-uom-columns.sql (multi-column ADD COLUMN IF NOT EXISTS + CREATE OR REPLACE VIEW patterns)
    - schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql (RLS + grant pattern, account_key slug domain)
    - .planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md (migration sections for 168/169/170 — exact pattern excerpts)
    - The Task 0 SUMMARY ground-truth column lists (do not write 169 without these)
  </read_first>
  <action>
    Migration 168 (acculynx_sync_watermark): additive `ALTER TABLE public.acculynx_sync_watermark ADD COLUMN IF NOT EXISTS` for: account_key text NOT NULL DEFAULT 'wichita', last_page_index integer, last_walked_job_id text, last_api_count integer, last_sync_at timestamptz. Then replace the unique constraint: `DROP CONSTRAINT IF EXISTS <existing-name-from-Task-0>` then `ADD CONSTRAINT acculynx_sync_watermark_account_resource UNIQUE (account_key, resource)`. Add `CREATE INDEX IF NOT EXISTS idx_acculynx_sync_watermark_account ON public.acculynx_sync_watermark(account_key)`. Enable RLS + grant select to authenticated, service_role.
    Migration 169 (11 target tables): for EACH of the 11 tables, one `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` block adding: account_key text, market text, last_seen_by_api timestamptz, archived_at timestamptz, archive_reason text. Use `CREATE TABLE IF NOT EXISTS` ONLY for any table Task 0 found missing live (none expected; CREATEs are no-ops). Add `CREATE INDEX IF NOT EXISTS idx_<table>_account ON public.<table>(account_key)` per table. RLS + grant select per table. Header comment must state: tables pre-exist live, CREATEs are no-ops, real effect is the new columns (hard rule 1, additive+idempotent). Do NOT add NOT NULL on account_key here (tables may hold rows; backfill happens at ingest); enforce non-null via the ingest stamping + a documented later constraint.
    Migration 170: `CREATE OR REPLACE VIEW public.v_acculynx_reconciliation` selecting from acculynx_sync_watermark w: w.account_key, w.resource, w.last_api_count AS api_count, w.last_sync_at, a per-resource `brain_count` via a CASE on w.resource counting non-archived rows in the matching target table (jobs, contacts, estimates, invoices, invoice_lines, job_financials, job_insurance, job_milestone_history, job_contacts), and delta_pct = round(100.0*ABS(brain_count - last_api_count)/NULLIF(last_api_count,0),1). Grant select to authenticated, service_role.
    ALL SQL additive + idempotent (hard rule 1): no DROP TABLE/COLUMN, no TRUNCATE, no type changes. Header comments follow the 166 style (number, title, Phase 2 + REQ-03/REQ-04, additive rationale).
  </action>
  <verify>
    <automated>grep -v '^--' schemas/cleverwork-roofer/168-acculynx-watermark-ddl.sql | grep -ci 'add column if not exists' | awk '{exit ($1>=5)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i 'account_key' schemas/cleverwork-roofer/168-acculynx-watermark-ddl.sql` returns a match
    - `grep -i 'acculynx_sync_watermark_account_resource' schemas/cleverwork-roofer/168-acculynx-watermark-ddl.sql` returns a match (new UNIQUE constraint)
    - `grep -ci 'last_seen_by_api' schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql` returns >= 11 (one per target table)
    - `grep -i 'v_acculynx_reconciliation' schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql` returns a match
    - No `drop table`, `drop column`, `truncate`, or `alter column ... type` in any of the three files (case-insensitive grep returns nothing)
  </acceptance_criteria>
  <done>Three migration files written, additive+idempotent, covering watermark extension, the 11 target tables' diff columns, and the reconciliation view, grounded in Task 0's live shapes.</done>
</task>

<task type="auto">
  <name>Task 2 (BLOCKING): Apply migrations 168/169/170 to the live prod DB via Supabase MCP</name>
  <read_first>
    - schemas/cleverwork-roofer/168-acculynx-watermark-ddl.sql (the SQL to apply — Task 1 output)
    - schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql
    - schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql
    - CLAUDE.md (hard rule 1; migrations applied via Supabase MCP apply_migration, committed under schemas/)
  </read_first>
  <action>
    Apply each migration to live prod DB rnhmvcpsvtqjlffpsayu via Supabase MCP `apply_migration` (name + SQL body), in order 168 → 169 → 170. Migration names: `acculynx_watermark_ddl`, `acculynx_resource_tables_ddl`, `acculynx_reconciliation_view`. apply_migration is idempotent here (all statements are IF NOT EXISTS / OR REPLACE), so re-running is safe.
    This is the BLOCKING gate required by the phase: build/type checks and Edge Function deploys pass WITHOUT these columns being live, and any later row-population verification would false-positive against tables missing the new columns. Nothing in Plan 03/04 that reads these columns may run before this task succeeds.
    After applying, run `execute_sql` to confirm: account_key column exists on acculynx_contacts; the UNIQUE(account_key,resource) constraint exists on acculynx_sync_watermark; v_acculynx_reconciliation is queryable (`SELECT * FROM v_acculynx_reconciliation LIMIT 1`).
  </action>
  <verify>
    <automated>MISSING — applied via Supabase MCP; manual verify: execute_sql confirms account_key on acculynx_contacts, UNIQUE(account_key,resource) on watermark, and SELECT from v_acculynx_reconciliation succeeds</automated>
  </verify>
  <acceptance_criteria>
    - `execute_sql "SELECT 1 FROM information_schema.columns WHERE table_name='acculynx_contacts' AND column_name='account_key'"` returns a row
    - `execute_sql "SELECT 1 FROM information_schema.columns WHERE table_name='acculynx_contacts' AND column_name='last_seen_by_api'"` returns a row
    - `execute_sql "SELECT conname FROM pg_constraint WHERE conname='acculynx_sync_watermark_account_resource'"` returns the constraint
    - `execute_sql "SELECT * FROM public.v_acculynx_reconciliation LIMIT 1"` executes without error
  </acceptance_criteria>
  <done>All three migrations live on prod DB; account_key + diff columns + reconciliation view confirmed present via execute_sql. The verification gate for Plans 03/04 is satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo SQL → live prod DB | Migration SQL crosses into the shared prod brain via Supabase MCP apply_migration |
| account_key column | The single FK that ties every future row to one and only one AccuLynx account |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Migration 169 missing NOT NULL/constraint lets a row land with NULL or wrong account_key (cross-account bleed at the data layer) | mitigate | account_key column added on all 11 tables (mig 169); ingest stamps it on every row (Plan 03); reconciliation view segments strictly by account_key (mig 170) |
| T-02-02 | Denial of Service | Destructive or non-idempotent SQL corrupts the shared prod brain | mitigate | All DDL is ADD COLUMN IF NOT EXISTS / CREATE OR REPLACE / DROP CONSTRAINT IF EXISTS only; acceptance grep asserts no DROP TABLE/COLUMN/TRUNCATE/type-change (hard rule 1) |
| T-02-03 | Information Disclosure | Secret value accidentally written into a watermark/target column | accept | No secret column is added by these migrations; only env_secret_name (name) lives in acculynx_accounts (mig 165). DDL adds no key-bearing column |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Phase 2 introduces no package installs (RESEARCH Package Legitimacy Audit: N/A); no install task in this plan |
</threat_model>

<verification>
- All three migration files exist under schemas/cleverwork-roofer/ with additive+idempotent SQL.
- Live DB confirms account_key + last_seen_by_api on the target tables, the new watermark UNIQUE constraint, and a queryable v_acculynx_reconciliation.
- No destructive SQL anywhere (grep gate).
</verification>

<success_criteria>
- Migrations 168/169/170 written AND applied to prod DB rnhmvcpsvtqjlffpsayu.
- account_key + market + last_seen_by_api + archived_at + archive_reason live on all 11 target tables.
- acculynx_sync_watermark carries account_key + per-resource cursors with UNIQUE(account_key, resource).
- v_acculynx_reconciliation queryable. This unblocks Plans 03 and 04.
</success_criteria>

<output>
Create `.planning/phases/02-multi-location-full-ingestion/02-01-SUMMARY.md` when done.
Include the Task 0 live-schema ground-truth (column lists for all 12 tables) — Plan 03 reads it.
</output>
