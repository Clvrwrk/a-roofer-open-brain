---
phase: 02-multi-location-full-ingestion
plan: 03
type: tdd
wave: 2
depends_on: ["02-01", "02-02"]
autonomous: true
files_modified:
  - supabase/functions/acculynx-sync/lib/accounts.ts
  - supabase/functions/acculynx-sync/lib/watermark.ts
  - supabase/functions/acculynx-sync/lib/diff.ts
  - supabase/functions/acculynx-sync/resources/jobs.ts
  - supabase/functions/acculynx-sync/resources/contacts.ts
  - supabase/functions/acculynx-sync/resources/estimates.ts
  - supabase/functions/acculynx-sync/resources/job-walk.ts
  - supabase/functions/acculynx-sync/index.ts
requirements: [REQ-03, REQ-04]
must_haves:
  truths:
    - "All Wave 0 tests pass (GREEN): production-only fan-out, watermark keyed (account_key,resource), mark-not-delete, invoice two-level walk, budget-stop resumption"
    - "Every upserted row across every resource is stamped account_key + market from acculynx_accounts (no cross-account bleed)"
    - "jobs uses recordStartIndex+ModifiedDate date window; contacts/estimates use pageStartIndex full sweep; sub-resources via job-walk resumable by last_walked_job_id"
    - "Invoices are a two-level walk: /jobs/{id}/invoices then /invoices/{invoiceId} → invoice_lines"
  artifacts:
    - path: "supabase/functions/acculynx-sync/resources/jobs.ts"
      provides: "Date-windowed incremental jobs sync (recordStartIndex)"
      contains: "ModifiedDate"
    - path: "supabase/functions/acculynx-sync/resources/contacts.ts"
      provides: "Full-sweep contacts sync (pageStartIndex)"
      contains: "pageStartIndex"
    - path: "supabase/functions/acculynx-sync/resources/job-walk.ts"
      provides: "Per-job sub-resource walk + invoice two-level + budget resumption"
      contains: "last_walked_job_id"
    - path: "supabase/functions/acculynx-sync/index.ts"
      provides: "Serial account fan-out entrypoint wiring all resources"
      contains: "loadProductionAccounts"
  key_links:
    - from: "supabase/functions/acculynx-sync/index.ts"
      to: "lib/accounts.loadProductionAccounts + resolveKey"
      via: "serial account loop"
      pattern: "for.*accounts"
    - from: "supabase/functions/acculynx-sync/resources/job-walk.ts"
      to: "acculynx_invoices + acculynx_invoice_lines"
      via: "two-level invoice walk upsert"
      pattern: "invoices/\\$\\{"
    - from: "supabase/functions/acculynx-sync/resources/jobs.ts"
      to: "acculynx_jobs (account_key, market stamped)"
      via: "upsert onConflict id,account_key"
      pattern: "account_key"
---

<objective>
Implement the lib/ and resource/ modules so all Wave 0 tests go GREEN: the serial fan-out
over production accounts, per-resource watermarks, mark-not-delete diff, and the four resource
syncs (jobs date-window, contacts/estimates full-sweep, job-walk for the per-job sub-resources
including the invoice two-level walk). Wire them into the index.ts entrypoint.

Purpose: This is the core REQ-03 (8-account fan-out with account stamping, no cross-account
bleed) + REQ-04 (the 8 unfed resources) delivery. The schema (Plan 01) is live and the
contracts (Plan 02 tests) are RED; this plan turns them GREEN.

Output: working acculynx-sync function: serial account loop, 4 resource modules, GREEN test
suite. (Production fan-out enablement + deploy + reconciliation are Plan 04.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md
@.planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md
@supabase/functions/acculynx-read-sweep/index.ts
@supabase/functions/acculynx-read-sweep/sweep.ts
@.planning/phases/02-multi-location-full-ingestion/02-01-SUMMARY.md
@.planning/phases/02-multi-location-full-ingestion/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement lib/ (accounts, watermark, diff) — turn the lib Wave 0 tests GREEN</name>
  <files>supabase/functions/acculynx-sync/lib/accounts.ts, supabase/functions/acculynx-sync/lib/watermark.ts, supabase/functions/acculynx-sync/lib/diff.ts</files>
  <read_first>
    - supabase/functions/acculynx-sync/lib/accounts.test.ts (the contract to satisfy — Plan 02 Task 2)
    - supabase/functions/acculynx-sync/lib/watermark.test.ts
    - supabase/functions/acculynx-sync/lib/diff.test.ts
    - .planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md (lib/accounts.ts, lib/watermark.ts, lib/diff.ts sections — exact Supabase query/upsert/update excerpts)
    - supabase/functions/acculynx-read-sweep/index.ts (createClient pattern, .from().select().eq().order() and upsert onConflict patterns)
    - .planning/phases/02-multi-location-full-ingestion/02-01-SUMMARY.md (live column ground-truth for watermark + target tables)
  </read_first>
  <behavior>
    - loadProductionAccounts: returns only environment='production' AND is_active=true rows, ordered by account_key; sandbox excluded.
    - resolveKey: Deno.env.get(env_secret_name); undefined → caller skips with console.warn of the NAME (never the value).
    - readWatermark(sb, accountKey, resource): filters .eq('account_key',accountKey).eq('resource',resource).maybeSingle().
    - advanceWatermark(sb, row): upsert onConflict 'account_key,resource'; sets last_sync_at.
    - markNotSeen(sb, table, accountKey, sweepStartedAt): .update({archived_at:now, archive_reason:'not_seen_in_api'}).eq('account_key',...).is('archived_at',null).lt('last_seen_by_api',sweepStartedAt); never .delete().
  </behavior>
  <action>
    Replace the Plan 02 stubs with real bodies following the PATTERNS analog excerpts. createClient is passed in (sb param) so tests inject a mock — do not instantiate a client inside these helpers. loadProductionAccounts selects account_key, env_secret_name, label, market, state and throws on query error. resolveKey returns Deno.env.get(acct.env_secret_name) and the caller (index.ts) handles undefined with the skip-on-missing warn (warn the NAME only — hard rule 2). watermark helpers use onConflict 'account_key,resource' (the UNIQUE constraint added in mig 168). markNotSeen uses .update() exclusively (hard rule 1) and best-effort console.warn on error (non-fatal). Keep all helpers pure of network calls.
  </action>
  <verify>
    <automated>deno test supabase/functions/acculynx-sync/lib/ --allow-env</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/acculynx-sync/lib/ --allow-env` exits 0 (all lib tests GREEN)
    - `grep -E "\.update\(" supabase/functions/acculynx-sync/lib/diff.ts` matches AND `grep -E "\.delete\(" supabase/functions/acculynx-sync/lib/diff.ts` returns nothing
    - `grep "account_key,resource" supabase/functions/acculynx-sync/lib/watermark.ts` returns a match (onConflict tuple)
    - `grep -E "environment.*production" supabase/functions/acculynx-sync/lib/accounts.ts` returns a match
  </acceptance_criteria>
  <done>lib accounts/watermark/diff implemented; lib Wave 0 tests GREEN; mark-not-delete and tuple-keyed watermark verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement resource modules: jobs, contacts, estimates (with account stamping)</name>
  <files>supabase/functions/acculynx-sync/resources/jobs.ts, supabase/functions/acculynx-sync/resources/contacts.ts, supabase/functions/acculynx-sync/resources/estimates.ts</files>
  <read_first>
    - .planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md (resources/jobs.ts, resources/contacts.ts, resources/estimates.ts sections — URL construction + page-loop + upsert excerpts)
    - supabase/functions/acculynx-read-sweep/index.ts (lines 168-257 page loop; lines 73-99 acculynxGet 429 retry — extract/reuse with apiKey as explicit param)
    - supabase/functions/acculynx-read-sweep/sweep.ts (paginationParam — reuse; never hardcode the param name globally)
    - docs/65-acculynx-read-capability-matrix.md (per-endpoint pagination: /jobs=recordStartIndex, /contacts=pageStartIndex, /estimates=pageStartIndex; item keys)
    - skills/cleverwork-roofer/acculynx-api/SKILL.md (endpoint paths, item-key shapes)
    - .planning/phases/02-multi-location-full-ingestion/02-01-SUMMARY.md (live column shapes for acculynx_jobs/contacts/estimates upsert conflict targets)
  </read_first>
  <behavior>
    - jobs.ts: GET /jobs?dateFilterType=ModifiedDate&startDate={watermark}&pageSize=25&recordStartIndex={offset}&sortBy=ModifiedDate&sortOrder=Ascending; paginate; upsert acculynx_jobs onConflict 'id,account_key'; stamp account_key + market on every row; advance watermark.last_modified_date per page; stop on empty page or budget.
    - contacts.ts: GET /contacts?pageSize=50&pageStartIndex={idx}; full sweep; upsert acculynx_contacts onConflict (id,account_key per Task0 shape); stamp account_key + last_seen_by_api on every row; advance last_page_index; on empty items → sweep complete (reset cursor).
    - estimates.ts: GET /estimates?pageSize=50&pageStartIndex={idx}; identical full-sweep shape to contacts; upsert acculynx_estimates; stamp account_key + job_id (from estimate jobId) + last_seen_by_api.
  </behavior>
  <action>
    Each module exports an async fn taking (sb, sbClient deps, acct, apiKey, deadline) — apiKey is an EXPLICIT parameter (never a module-level constant; prevents cross-account bleed, Pitfall 3). Reuse the acculynxGet(url, apiKey) 429-retry helper and PACE_MS=130 sleep from the analog; do NOT hand-roll retry/backoff (Don't Hand-Roll table). Use the correct pagination param per resource via paginationParam() or the known literal (jobs→recordStartIndex, contacts/estimates→pageStartIndex) — never a single global param (Pitfall 2). Stamp account_key: acct.account_key and market: acct.market from acculynx_accounts on EVERY upserted row (Pitfall 7: market comes from the account, NOT the job address). Set last_seen_by_api: now() on every upserted row (feeds diff detection). Budget check Date.now() >= deadline → stop and let next run resume from watermark. Upsert conflict targets per the live shapes in 02-01-SUMMARY (do not assume id-only; it is (id,account_key)).
  </action>
  <verify>
    <automated>deno check supabase/functions/acculynx-sync/resources/jobs.ts supabase/functions/acculynx-sync/resources/contacts.ts supabase/functions/acculynx-sync/resources/estimates.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ModifiedDate" supabase/functions/acculynx-sync/resources/jobs.ts` matches AND `grep "recordStartIndex" supabase/functions/acculynx-sync/resources/jobs.ts` matches
    - `grep "pageStartIndex" supabase/functions/acculynx-sync/resources/contacts.ts` matches AND `grep "pageStartIndex" supabase/functions/acculynx-sync/resources/estimates.ts` matches
    - `grep "account_key" supabase/functions/acculynx-sync/resources/jobs.ts` AND `...contacts.ts` AND `...estimates.ts` each match (stamping)
    - `grep -E "apiKey" supabase/functions/acculynx-sync/resources/jobs.ts` shows apiKey as a parameter (no module-level key constant)
    - `deno check` on all three files passes
  </acceptance_criteria>
  <done>jobs (date-window), contacts + estimates (full sweep) implemented with per-row account/market stamping, correct per-resource pagination param, reused 429 retry, and budget-resume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement job-walk (sub-resources + invoice two-level + budget resumption) and wire index.ts fan-out — full suite GREEN</name>
  <files>supabase/functions/acculynx-sync/resources/job-walk.ts, supabase/functions/acculynx-sync/index.ts</files>
  <read_first>
    - supabase/functions/acculynx-sync/resources/job-walk.test.ts (the contract: invoice two-level + budget-stop watermark — Plan 02)
    - .planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md (resources/job-walk.ts + index.ts sections — HATEOAS harvest, two-level invoice walk, budget-check, serial fan-out)
    - .planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md (Pattern 4 job-walk; Pattern 5 diff; Pitfall 5 resumption; Pitfall 6 invoice two-level; pagination table for per-job endpoints)
    - supabase/functions/acculynx-read-sweep/index.ts (lines 101-119 HATEOAS harvest; Deno.serve POST guard; json() helper)
    - docs/65-acculynx-read-capability-matrix.md (per-job endpoint shapes: /jobs/{id}/contacts array, /financials single object, /insurance single, /milestone-history items array, /invoices list)
    - supabase/functions/acculynx-sync/lib/accounts.ts, lib/watermark.ts, lib/diff.ts (the helpers to wire)
  </read_first>
  <behavior>
    - job-walk.ts: iterate acculynx_jobs for the account sorted by created_date ASC, resuming from watermark.last_walked_job_id. Per job: GET /jobs/{id}/contacts → upsert acculynx_job_contacts; /financials → acculynx_job_financials; /insurance → acculynx_job_insurance; /milestone-history → acculynx_job_milestone_history; /invoices (level 1, pageStartIndex) → upsert acculynx_invoices headers, then per invoice GET /invoices/{invoiceId} (level 2) → upsert acculynx_invoice_lines. Stamp account_key + job_id on every row. Advance last_walked_job_id AFTER each job; if Date.now() >= deadline, break (watermark already advanced → clean resume).
    - index.ts: Deno.serve POST-guard; loadProductionAccounts; SERIAL loop (no Promise.all — Pitfall 4 IP limit) — for each account resolve key via Deno.env.get(env_secret_name), skip-on-missing warn, then run jobs → contacts → estimates → job-walk under one shared deadline; write a sync summary.
  </behavior>
  <action>
    Implement job-walk per Pattern 4 + the invoice two-level walk (Pitfall 6): level-1 /jobs/{jobId}/invoices list → header upsert to acculynx_invoices; level-2 GET /invoices/{encodeURIComponent(invoiceId)} → detail/lines upsert to acculynx_invoice_lines. URL-encode all GUID path params (ASVS V5). Advance last_walked_job_id after EACH job, before the budget break, so resumption picks up the next job (Pitfall 5). Reuse acculynxGet(url, apiKey) and PACE_MS; apiKey explicit param. After a COMPLETE account sweep of a full-sweep resource (contacts/estimates) call lib/diff.markNotSeen — but only run job-walk diff after last_walked_job_id reaches the account's max job (research Pattern 5 scope limitation).
    Wire index.ts: serial account loop calling lib/accounts.loadProductionAccounts; resolve key; if missing, console.warn the secret NAME and continue; run the resource modules in order under the shared deadline; on each account, pass acct + apiKey explicitly to every resource fn (no shared module-level key). Write a summary row/log per account at the end. Run the FULL suite and make resources/job-walk.test.ts GREEN.
  </action>
  <verify>
    <automated>deno test supabase/functions/acculynx-sync/ --allow-env --allow-net=localhost</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/acculynx-sync/ --allow-env --allow-net=localhost` exits 0 (FULL suite GREEN, including job-walk.test.ts)
    - `grep -E "invoices/\$\{" supabase/functions/acculynx-sync/resources/job-walk.ts` shows the level-2 /invoices/{id} call (two-level walk)
    - `grep "last_walked_job_id" supabase/functions/acculynx-sync/resources/job-walk.ts` matches (resumption watermark)
    - `grep "loadProductionAccounts" supabase/functions/acculynx-sync/index.ts` matches AND `grep -E "Promise\.all" supabase/functions/acculynx-sync/index.ts` returns nothing (serial fan-out, Pitfall 4)
    - `deno check supabase/functions/acculynx-sync/index.ts` passes
  </acceptance_criteria>
  <done>job-walk with invoice two-level + budget resumption implemented; index.ts serial fan-out wires all resources; full suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| account loop → AccuLynx API | Each iteration must use ONLY its own account's key; serial, one key active at a time |
| function → 8 production keys | All 8 keys resolved from Supabase secrets; never logged; never cross-applied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Cross-account key bleed in the fan-out loop (closure/await/shared-var bug) | mitigate | apiKey passed as explicit parameter to every resource fn and to acculynxGet; no module-level key; account_key stamped per row; Plan 04 integration asserts no account_key mismatch |
| T-02-05 | Information Disclosure | Secret value logged on skip-on-missing or error | mitigate | console.warn references env_secret_name (the NAME) only; resolved value never logged/stored (hard rule 2) |
| T-02-06 | Tampering | Diff/archival deletes client atoms | mitigate | job-walk + full-sweep diff calls lib/diff.markNotSeen (.update only); never .delete (hard rule 1) |
| T-02-07 | Denial of Service | Parallel accounts exceed 30 req/s IP limit → 429 storm | mitigate | Serial account loop (no Promise.all); PACE_MS=130 (~8 req/s) per call; reused acculynxGet honors Retry-After + backoff |
| T-02-08 | Input Validation | Unencoded GUID path params in /jobs/{id}, /invoices/{id} | mitigate | encodeURIComponent on every path param (ASVS V5) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs; existing jsr imports only (RESEARCH audit N/A) |
</threat_model>

<verification>
- Full Deno suite GREEN: deno test supabase/functions/acculynx-sync/ --allow-env --allow-net=localhost.
- Serial fan-out (no Promise.all); per-row account_key+market stamping on all resources.
- Invoice two-level walk; job-walk resumable by last_walked_job_id; diff is mark-not-delete.
- Correct per-resource pagination param (jobs recordStartIndex; contacts/estimates pageStartIndex).
</verification>

<success_criteria>
- REQ-03: serial fan-out over production accounts with account_key + market stamped on every row, no cross-account bleed (key passed explicitly).
- REQ-04: contacts, job-contacts, estimates, invoices, invoice-lines, job-financials, job-insurance, milestone-history all have working sync paths with per-(account,resource) watermarks.
- All Wave 0 tests GREEN.
</success_criteria>

<output>
Create `.planning/phases/02-multi-location-full-ingestion/02-03-SUMMARY.md` when done.
</output>
