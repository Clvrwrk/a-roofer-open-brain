---
phase: 02-multi-location-full-ingestion
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-03"]
files_modified:
  - supabase/functions/acculynx-sync/index.ts
  - scripts/acculynx-reconcile-check.sql
autonomous: false
requirements: [REQ-03, REQ-04]
must_haves:
  truths:
    - "All 8 production AccuLynx keys are confirmed set as Supabase Edge secrets and each returns 200 on a ping"
    - "The new resources were proven against the sandbox account FIRST (no production first-tries)"
    - "acculynx-sync is deployed and a production fan-out run (DIRECT invocation, not the buggy pg_net path) has populated contacts/estimates/sub-resources with account_key stamped"
    - "v_acculynx_reconciliation shows delta_pct within tolerance (<5%) for synced full-sweep resources (contacts/estimates); no row has a wrong account_key"
    - "Diff detection (not-seen-in-API archival) fires this phase for full-sweep resources (contacts/estimates); job-walk SUB-resource diff is queued and completes over Phase 3 cron runs (Pattern 5 — backfill needs hundreds of runs), so it is NOT a Phase 2 completion gate"
  artifacts:
    - path: "supabase/functions/acculynx-sync/index.ts"
      provides: "Deployed fan-out function (buildCommit confirmed)"
      contains: "loadProductionAccounts"
    - path: "scripts/acculynx-reconcile-check.sql"
      provides: "Reconciliation gate query"
      contains: "delta_pct"
  key_links:
    - from: "acculynx-sync (deployed)"
      to: "acculynx_contacts / acculynx_estimates / acculynx_job_* tables"
      via: "production fan-out run (direct invoke)"
      pattern: "account_key"
    - from: "v_acculynx_reconciliation"
      to: "acculynx_sync_watermark.last_api_count vs brain_count"
      via: "delta_pct"
      pattern: "delta_pct"
---

<objective>
Take the implemented function to production safely: verify all 8 production keys, prove the
new resource syncs against the SANDBOX account first (Chris's no-first-tries mandate), deploy,
run the production fan-out, and reconcile row counts by account/market.

Purpose: REQ-03/REQ-04 are only "done" when real data lands for all 8 accounts with no
cross-account bleed and counts reconcile (Phase 2 success criteria 1–4). This plan is the
production gate; it is non-autonomous because the sandbox→production promotion requires a
human verify (no production first-tries — PROJECT.md constraint).

Output: deployed acculynx-sync, populated target tables stamped by account, and a passing
reconciliation check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md
@.planning/phases/02-multi-location-full-ingestion/02-01-SUMMARY.md
@.planning/phases/02-multi-location-full-ingestion/02-03-SUMMARY.md
@skills/cleverwork-roofer/acculynx-api/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1 (BLOCKING): Verify all 8 production keys are set and each pings 200; sandbox-first resource test run</name>
  <read_first>
    - .planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md (Environment Availability — 8 prod keys UNVERIFIED; Open Question 3 ping gate; Open Question 4 sandbox data depth)
    - schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql (the 8 production env_secret_name values + sandbox)
    - skills/cleverwork-roofer/acculynx-api/SKILL.md (a cheap GET to ping per account, e.g. /company-settings or /diagnostics; auth header shape)
    - supabase/functions/acculynx-sync/index.ts (the function to invoke against sandbox)
    - .claude/skills/coolify/ and .claude/skills/workos-agent-auth/ (deploy + agent-auth context for invoking the function)
  </read_first>
  <action>
    (a) Confirm secrets: run `supabase secrets list` (CLI 2.105.0) and verify all 8 production names from migration 165 are present (PE_CC_FLORIDA_…, _COLORADO_, _GEORGIA_, _KANSAS_CITY_, _TEXAS_, _WICHITA_, _INSURANCE_PROGRAM_, _MULTI_FAMILY_COMMERCIAL_) plus PE_CC_SANDBOX_. For each present key, ping AccuLynx with a single cheap GET (e.g. /company-settings) and confirm HTTP 200 — log per-account status (200 / missing / non-200). Do NOT log key values. Any missing/failing key is recorded; that account is skipped by the fan-out (resolveKey skip-on-missing) but the gap must be surfaced to the human in Task 2.
    (b) Sandbox-first proof: invoke the deployed/local acculynx-sync limited to the sandbox account only (temporarily scope the loop to account_key='sandbox', or invoke with a sandbox-only filter) and confirm the new resource fetch logic runs: contacts return rows (sandbox reported 53 in Phase 1), and the job-walk fetch logic executes for the 1 sandbox job without error. Accept that estimates/invoices may return 0 in sandbox (research Open Question 4) — validate the fetch path is correct by code/log, not by data presence. This is the no-first-tries gate: production fan-out must not run until the sandbox proof passes.
  </action>
  <verify>
    <automated>MISSING — uses Supabase CLI + live API pings; manual verify: SUMMARY shows per-account ping status for all 8 keys + a successful sandbox-only run with contacts rows fetched</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY lists each of the 8 production env_secret_name values with a ping result (200 / missing / non-200) — values never logged
    - SUMMARY confirms a sandbox-only run fetched contacts (>0 rows) and executed the job-walk path for the sandbox job without error
    - Any missing/failing production key is explicitly listed as a known gap to surface in the Task 2 checkpoint
  </acceptance_criteria>
  <done>All 8 keys' presence/ping status known; the new resource sync proven against sandbox first; gaps surfaced. Production fan-out is gated behind the Task 2 checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human gate — approve production fan-out + deploy</name>
  <what-built>
    acculynx-sync is implemented (Plan 03, full suite GREEN), the schema is live (Plan 01),
    all 8 production keys were checked for presence + 200 ping, and the new resource syncs were
    proven against the SANDBOX account first (Task 1). No production location account has been
    written to yet.
  </what-built>
  <how-to-verify>
    1. Review the Task 1 per-account ping table in 02-04-SUMMARY (which of the 8 keys are live).
    2. Confirm the sandbox-only run results (contacts fetched, job-walk path exercised, no errors).
    3. State the deploy change + user-visible impact + rollback (Live⇄Dev deploy gate, CONVENTIONS §13):
       "Deploying acculynx-sync to fan out ingestion across N production accounts; impact: target
       tables begin populating with account_key-stamped rows; rollback: revert the function to its
       prior deployed revision and the additive columns remain harmless."
    4. Decide whether to enable ALL present accounts or a subset for the first production run.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally naming the subset of accounts to enable) or describe required changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy acculynx-sync, run the production fan-out (direct invoke), and reconcile by account/market</name>
  <read_first>
    - supabase/functions/acculynx-sync/index.ts (remove any sandbox-only scoping added in Task 1; restore full production fan-out per the human's approved account set)
    - scripts/acculynx-reconcile-check.sql (the reconciliation gate query — flags delta_pct > 5 or NULL)
    - schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql (v_acculynx_reconciliation definition — synced resources only; contact_emails/contact_phones excluded)
    - .claude/skills/coolify/ (deploy; buildCommit confirmation via /healthz) — note acculynx-sync is a Supabase Edge Function, deploy via `supabase functions deploy acculynx-sync`, not Coolify; confirm with `supabase functions list`
    - .planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md (Pattern 6 reconciliation; tolerance <2% flag / >5% investigate; Pattern 4/5 backfill takes many runs; "pg_net reconciliation is Phase 3" — v_acculynx_cron_outcomes perpetual-pending is OUT OF SCOPE here)
  </read_first>
  <action>
    (a) Deploy: restore the full production fan-out in index.ts (remove the Task-1 sandbox scoping), then `supabase functions deploy acculynx-sync` to project rnhmvcpsvtqjlffpsayu; confirm with `supabase functions list` that acculynx-sync is deployed. Commit the function source under supabase/functions/acculynx-sync/.
    (b) Run the fan-out via DIRECT invocation only — `supabase functions invoke acculynx-sync --no-verify-jwt` (or an equivalent direct HTTP POST to the function URL with the service token). DO NOT trigger or rely on the existing pg_net/pg_cron path: it has the known perpetual-`pending` reconciliation bug (v_acculynx_cron_outcomes never settles — out of scope until Phase 3), so completion through that path is UNOBSERVABLE and a silent prod failure could read as "pending" forever. Confirm each invocation's success directly from the invoke response (HTTP 200 + the function's own summary payload), NOT from v_acculynx_cron_outcomes. Because the job-walk backfill is O(jobs × ~5 calls) and exceeds one 110s budget, expect multiple invocations — invoke repeatedly until watermarks stop advancing, OR document that subsequent runs will complete the backfill (Phase 2 success gate is "a full backfill HAS RUN / is running windowed", not single-run completion — research Pattern 4).
    (c) Verify population + no cross-account bleed via Supabase MCP execute_sql (the source of truth, NOT the cron-outcomes view): `SELECT account_key, count(*) FROM acculynx_contacts WHERE account_key IS NOT NULL GROUP BY account_key` returns rows for the enabled accounts; assert no row carries an account_key outside the enabled set; spot-check that acculynx_jobs rows' market matches acculynx_accounts.market for that account_key (Pitfall 7).
    (d) Reconcile: run scripts/acculynx-reconcile-check.sql against the live DB; confirm delta_pct < 5 for synced full-sweep (account, contacts/estimates) pairs (or document which are mid-backfill and expected to converge). The reconciliation view excludes the intentionally-unsynced contact_emails/contact_phones (Plan 01), so they will not appear as false failures. Record the reconciliation table in the SUMMARY.
  </action>
  <verify>
    <automated>MISSING — deploy + direct `supabase functions invoke` + MCP execute_sql; manual verify: invoke returns HTTP 200 with summary, execute_sql shows account_key-grouped counts >0 for enabled accounts, no out-of-set account_key, and reconcile-check delta_pct < 5 (or documented mid-backfill). Completion confirmed via execute_sql population, NOT v_acculynx_cron_outcomes (pg_net reconciliation is Phase 3).</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions list` shows acculynx-sync deployed; function source committed under supabase/functions/acculynx-sync/
    - The fan-out was run via DIRECT `supabase functions invoke acculynx-sync --no-verify-jwt` (SUMMARY records the invoke response HTTP status + summary); the pg_net/pg_cron path was NOT used as the completion signal (Phase 3 scope)
    - `execute_sql "SELECT account_key, count(*) FROM acculynx_contacts WHERE account_key IS NOT NULL GROUP BY account_key"` returns >0 rows for each enabled account
    - `execute_sql "SELECT count(*) FROM acculynx_contacts WHERE account_key NOT IN (<enabled set>)"` returns 0 (no cross-account bleed)
    - `execute_sql` over acculynx_estimates / acculynx_invoices / acculynx_job_financials / acculynx_job_insurance / acculynx_job_milestone_history / acculynx_job_contacts shows the job-walk populated them (non-empty for accounts that have such data) OR the SUMMARY documents mid-backfill state
    - scripts/acculynx-reconcile-check.sql run shows delta_pct < 5 for completed full-sweep (account, contacts/estimates) pairs (mid-backfill / job-walk sub-resource pairs documented; job-walk diff completes over Phase 3 cron runs)
    - SUMMARY states explicitly: full-sweep diff (contacts/estimates not-seen-in-API archival) is exercised this phase; job-walk sub-resource diff is queued for Phase 3 backfill completion (Pattern 5) and is NOT a Phase 2 gate — so the verifier does not fail SC3 on job-walk
  </acceptance_criteria>
  <done>acculynx-sync deployed; production fan-out (direct invoke) populated the unfed resources with account_key/market stamped; reconciliation within tolerance or documented mid-backfill; no cross-account bleed; completion proven via execute_sql, not the Phase-3 pg_net path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy → production | First write to production location accounts; gated behind human approval (no first-tries) |
| 8 keys → 8 accounts | Each production run must keep keys account-isolated; reconciliation detects bleed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Cross-account key bleed surfaces only at production scale | mitigate | Post-run execute_sql asserts no account_key outside the enabled set + market matches account (integration gate); serial fan-out from Plan 03 |
| T-02-05 | Information Disclosure | Key value logged during ping/deploy | mitigate | Pings log status codes + secret NAME only; `supabase secrets list` shows names, never values; no key echoed (hard rule 2) |
| T-02-07 | Denial of Service | Production fan-out across 8 keys hits the 30 req/s IP limit | mitigate | Serial account loop + PACE_MS pacing (Plan 03); first production run optionally scoped to a subset per the human checkpoint |
| T-02-09 | Repudiation | A production deploy with no stated impact/rollback | mitigate | Task 2 checkpoint requires explicit change + impact + rollback statement before deploy (CONVENTIONS §13 deploy gate) |
| T-02-12 | Repudiation | Completion read from the buggy pg_net/v_acculynx_cron_outcomes path (perpetual pending) masks a silent prod failure | mitigate | Fan-out run via DIRECT `supabase functions invoke`; completion confirmed via execute_sql row counts, never v_acculynx_cron_outcomes (pg_net reconciliation is Phase 3) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH audit N/A) |
</threat_model>

<verification>
- All 8 keys' ping status recorded; sandbox-first proof done before any production write.
- Human checkpoint approved before deploy (no production first-tries).
- acculynx-sync deployed; fan-out run via direct invoke; target tables populated with account_key/market stamping; no cross-account bleed.
- v_acculynx_reconciliation delta_pct < 5 for completed full-sweep pairs (or mid-backfill documented); completion proven via execute_sql, not pg_net.
</verification>

<success_criteria>
- Phase 2 SC1: fan-out across production keys; every row stamped account/market; no cross-account bleed (execute_sql asserts).
- Phase 2 SC2: contacts/job-contacts/estimates/invoices/invoice-lines/job-financials/job-insurance/milestone-history populated for accounts that have them, with per-resource watermarks.
- Phase 2 SC3: windowed historical backfill has run / is running (job-walk resumable across runs); full-sweep diff (contacts/estimates) fires this phase, job-walk sub-resource diff completes over Phase 3 cron runs (Pattern 5) — not a single-phase gate.
- Phase 2 SC4: row counts by account/market reconcile within <5% tolerance for full-sweep resources (v_acculynx_reconciliation).
</success_criteria>

<output>
Create `.planning/phases/02-multi-location-full-ingestion/02-04-SUMMARY.md` when done.
Include the per-account ping table and the final reconciliation table.
</output>
</output>
