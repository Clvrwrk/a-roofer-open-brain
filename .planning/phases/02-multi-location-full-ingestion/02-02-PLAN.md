---
phase: 02-multi-location-full-ingestion
plan: 02
type: tdd
wave: 1
depends_on: []
autonomous: true
files_modified:
  - supabase/functions/acculynx-sync/index.ts
  - supabase/functions/acculynx-sync/lib/accounts.ts
  - supabase/functions/acculynx-sync/lib/watermark.ts
  - supabase/functions/acculynx-sync/lib/diff.ts
  - supabase/functions/acculynx-sync/lib/accounts.test.ts
  - supabase/functions/acculynx-sync/lib/watermark.test.ts
  - supabase/functions/acculynx-sync/lib/diff.test.ts
  - supabase/functions/acculynx-sync/resources/contacts.test.ts
  - supabase/functions/acculynx-sync/resources/jobs.test.ts
  - supabase/functions/acculynx-sync/resources/job-walk.test.ts
  - scripts/acculynx-reconcile-check.sql
requirements: [REQ-03, REQ-04]
must_haves:
  truths:
    - "The acculynx-sync Edge Function source exists in the repo (located-or-recreated), not just deployed"
    - "Wave 0 failing tests assert: fan-out filters to production only, account_key stamped, watermark keyed (account_key,resource), markNotSeen uses .update() not .delete(), invoice two-level walk, budget-stop advances watermark"
    - "resources/contacts.test.ts + resources/jobs.test.ts assert per-row account_key+market stamping, correct pagination param (contacts=pageStartIndex, jobs=recordStartIndex), and budget-stop — these are RED here, GREEN in Plan 03"
    - "A reconciliation SQL smoke check exists (content/grep verified here; functionally gated in Plan 04 Task 3 against the live view)"
  artifacts:
    - path: "supabase/functions/acculynx-sync/lib/accounts.test.ts"
      provides: "REQ-03 fan-out + stamping unit tests (RED)"
      contains: "production"
    - path: "supabase/functions/acculynx-sync/lib/diff.test.ts"
      provides: "REQ-04 mark-not-delete unit test (RED)"
      contains: "archived_at"
    - path: "supabase/functions/acculynx-sync/resources/contacts.test.ts"
      provides: "REQ-04 contacts pageStartIndex + account_key/market stamping + budget-stop (RED)"
      contains: "pageStartIndex"
    - path: "supabase/functions/acculynx-sync/resources/jobs.test.ts"
      provides: "REQ-03/04 jobs recordStartIndex + account_key/market stamping + budget-stop (RED)"
      contains: "recordStartIndex"
    - path: "supabase/functions/acculynx-sync/resources/job-walk.test.ts"
      provides: "REQ-04 invoice two-level + budget-stop watermark test (RED)"
      contains: "invoice"
    - path: "scripts/acculynx-reconcile-check.sql"
      provides: "delta_pct smoke query against v_acculynx_reconciliation"
      contains: "v_acculynx_reconciliation"
  key_links:
    - from: "supabase/functions/acculynx-sync/lib/accounts.ts"
      to: "acculynx_accounts (environment='production')"
      via: "loadProductionAccounts query"
      pattern: "environment.*production"
    - from: "supabase/functions/acculynx-sync/lib/accounts.ts"
      to: "Deno.env.get(env_secret_name)"
      via: "runtime secret resolution"
      pattern: "Deno\\.env\\.get"
---

<objective>
Establish the acculynx-sync Edge Function in the repo and write the Wave 0 FAILING tests
(RED) that define the contracts Plan 03 must satisfy. Locate-or-recreate the deployed v10
function source (research Open Question 2 — it may not be committed), then scaffold the
lib/ module signatures and write pure unit tests that fail until Plan 03 implements them.

Purpose: TDD gate. The phase's anti-shallow rules require every behavior to have an automated
verify; research's Validation Architecture lists 5 Wave 0 gaps. These tests encode the
hard-rule invariants (no cross-account bleed, mark-not-delete, secret-name-only resolution)
AND the per-resource behavioral contracts (correct pagination param, account_key/market
stamping, budget-stop) as executable assertions BEFORE implementation, so Plan 03 cannot drift
behind a grep-on-file-content gate.

Output: acculynx-sync/ skeleton (index.ts entrypoint + lib/ signatures), 6 failing test
files (3 lib + contacts + jobs + job-walk), and the reconciliation SQL smoke script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md
@.planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md
@supabase/functions/acculynx-read-sweep/sweep.ts
@supabase/functions/acculynx-read-sweep/sweep.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locate-or-recreate the acculynx-sync Edge Function source in the repo</name>
  <files>supabase/functions/acculynx-sync/index.ts, supabase/functions/acculynx-sync/lib/accounts.ts, supabase/functions/acculynx-sync/lib/watermark.ts, supabase/functions/acculynx-sync/lib/diff.ts</files>
  <read_first>
    - .planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md (Open Question 2; Recommended Project Structure; System Architecture Diagram — REQUIRED reading before any recreation)
    - .planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md (index.ts / lib/accounts.ts / lib/watermark.ts / lib/diff.ts sections — exact analog excerpts)
    - supabase/functions/acculynx-read-sweep/index.ts (the live analog: Deno.serve POST guard, createClient pattern, PACE_MS=130, RUNTIME_BUDGET_MS=110000, acculynxGet 429 retry, json() helper)
    - supabase/functions/acculynx-read-sweep/sweep.ts (paginationParam, redactSample — reuse, do not reinvent)
    - skills/cleverwork-roofer/acculynx-api/SKILL.md (endpoint paths, pagination params per resource — consult, never guess)
  </read_first>
  <action>
    First attempt to LOCATE the deployed v10 source: try `supabase functions download acculynx-sync` (Supabase CLI 2.105.0 is available).
    CONDITIONAL STOP: if `supabase functions download acculynx-sync` FAILS or returns nothing (function predates repo structure per research A6), do NOT silently jump to a from-scratch recreation — surface the download failure to the human in the SUMMARY (note the exact CLI error) and proceed to recreation ONLY after reading the 02-RESEARCH.md System Architecture Diagram + Recommended Project Structure first (recreation must match that architecture, not be invented). If the download SUCCEEDS, commit the downloaded source under supabase/functions/acculynx-sync/ as the starting point and skip recreation.
    Create the entrypoint index.ts: Deno.serve POST-only guard, createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, {auth:{persistSession:false}}), constants PACE_MS=130 and RUNTIME_BUDGET_MS=110000, a started/deadline budget, and the json() response helper — all per the read-sweep analog. Import paginationParam and redactSample from ../acculynx-read-sweep/sweep.ts (reuse; do NOT reinvent the pagination param split or PII redaction). Do NOT import assertSandbox/SANDBOX_SECRET_NAME — this function fans out to production keys.
    Create lib/ module STUBS with real exported signatures but minimal/throwing bodies so the Wave 0 tests can import and fail meaningfully:
      - lib/accounts.ts: `export async function loadProductionAccounts(sb)` querying acculynx_accounts WHERE environment='production' AND is_active=true ORDER BY account_key, selecting account_key, env_secret_name, label, market, state; `export function resolveKey(acct)` returning Deno.env.get(acct.env_secret_name) with skip-on-missing console.warn. Secret NAME only — never log the value (hard rule 2).
      - lib/watermark.ts: `export async function readWatermark(sb, accountKey, resource)` and `export async function advanceWatermark(sb, row)` upserting onConflict 'account_key,resource'.
      - lib/diff.ts: `export async function markNotSeen(sb, table, accountKey, sweepStartedAt)` using sb.from(table).update({archived_at, archive_reason:'not_seen_in_api'}).eq('account_key',...).is('archived_at',null).lt('last_seen_by_api', sweepStartedAt) — MUST use .update(), never .delete() (hard rule 1).
    Stubs may throw "not implemented" in bodies; signatures and the production-only query string must be real (the tests assert on them). Promote apiKey to an explicit parameter everywhere it is used (no module-level shared key — prevents cross-account bleed, research Pitfall 3).
  </action>
  <verify>
    <automated>deno check supabase/functions/acculynx-sync/index.ts supabase/functions/acculynx-sync/lib/accounts.ts supabase/functions/acculynx-sync/lib/watermark.ts supabase/functions/acculynx-sync/lib/diff.ts</automated>
  </verify>
  <acceptance_criteria>
    - supabase/functions/acculynx-sync/index.ts exists and `deno check supabase/functions/acculynx-sync/index.ts supabase/functions/acculynx-sync/lib/accounts.ts supabase/functions/acculynx-sync/lib/watermark.ts supabase/functions/acculynx-sync/lib/diff.ts` type-checks ALL FOUR files clean (so a stub signature error surfaces here, not as a misleading compile failure inside the Wave 0 RED)
    - `grep -E "environment.*production" supabase/functions/acculynx-sync/lib/accounts.ts` returns a match
    - `grep "Deno.env.get" supabase/functions/acculynx-sync/lib/accounts.ts` returns a match (secret-name resolution)
    - `grep -E "\.update\(" supabase/functions/acculynx-sync/lib/diff.ts` returns a match AND `grep -E "\.delete\(" supabase/functions/acculynx-sync/lib/diff.ts` returns nothing (mark-not-delete)
    - SUMMARY records whether the source was downloaded (located) or recreated; if download failed, the CLI error is recorded and the recreation cites the 02-RESEARCH System Architecture Diagram
  </acceptance_criteria>
  <done>acculynx-sync skeleton in repo with real lib signatures; all four core files type-check clean; production-only fan-out query and mark-not-delete diff signature present; download-vs-recreate path recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Wave 0 FAILING unit tests (lib + resources/contacts + resources/jobs + job-walk) + reconciliation SQL smoke</name>
  <files>supabase/functions/acculynx-sync/lib/accounts.test.ts, supabase/functions/acculynx-sync/lib/watermark.test.ts, supabase/functions/acculynx-sync/lib/diff.test.ts, supabase/functions/acculynx-sync/resources/contacts.test.ts, supabase/functions/acculynx-sync/resources/jobs.test.ts, supabase/functions/acculynx-sync/resources/job-walk.test.ts, scripts/acculynx-reconcile-check.sql</files>
  <read_first>
    - supabase/functions/acculynx-read-sweep/sweep.test.ts (Deno.test structure, jsr:@std/assert@1 imports, one assertion-per-behavior; the existing 6/6-passing analog)
    - .planning/phases/02-multi-location-full-ingestion/02-RESEARCH.md (Validation Architecture → Wave 0 Gaps; Phase Requirements → Test Map; Pitfall 2 pagination split; Pattern 5 diff scope)
    - .planning/phases/02-multi-location-full-ingestion/02-PATTERNS.md (lib/*.test.ts section — pure unit tests, inject mock sb, no live DB/network)
    - supabase/functions/acculynx-sync/lib/accounts.ts (signatures from Task 1 to import)
    - supabase/functions/acculynx-sync/lib/diff.ts (markNotSeen signature)
  </read_first>
  <behavior>
    - accounts.test.ts: loadProductionAccounts(mockSb) issues a query filtered to environment='production' (assert the mock .eq was called with 'environment','production') and never returns the sandbox row; resolveKey reads Deno.env.get(env_secret_name) and returns undefined→skip when the secret is unset (no throw, warn path).
    - watermark.test.ts: advanceWatermark upserts with onConflict 'account_key,resource'; readWatermark filters by both account_key AND resource (assert both .eq calls). A watermark for ('kansas_city','contacts') is independent of ('florida','contacts').
    - diff.test.ts: markNotSeen calls sb.from(table).update(...) with archive_reason='not_seen_in_api' and archived_at set, scoped .eq('account_key',...).is('archived_at',null).lt('last_seen_by_api',...); it NEVER calls .delete() (assert the mock .delete spy was not invoked).
    - contacts.test.ts (BEHAVIORAL — RED here, GREEN in Plan 03 Task 2): via a mock fetch + mock sb, syncContacts (a) requests a URL whose pagination param is `pageStartIndex` (NOT recordStartIndex — reuse paginationParam() or assert the literal); (b) stamps account_key AND market from the passed acct on EVERY upserted row (assert every record in the .upsert payload carries acct.account_key and acct.market, and last_seen_by_api set); (c) stops the page loop when Date.now() >= deadline (inject a deadline already in the past → assert no further fetch beyond the budget check).
    - jobs.test.ts (BEHAVIORAL — RED here, GREEN in Plan 03 Task 2): via mock fetch + mock sb, syncJobs (a) requests a URL whose pagination param is `recordStartIndex` (NOT pageStartIndex) and includes dateFilterType=ModifiedDate; (b) stamps account_key AND market on EVERY upserted row; (c) budget-stops when Date.now() >= deadline. Reuse paginationParam() for the param assertion so the contacts/jobs split (Pitfall 2) is proven, not assumed.
    - job-walk.test.ts: the invoice walk fetches /jobs/{id}/invoices (level 1) then /invoices/{invoiceId} per invoice (level 2) — assert both URL shapes requested; the walk advances last_walked_job_id after each job and stops when Date.now() >= deadline (assert watermark advanced to the last processed jobId before the budget break).
  </behavior>
  <action>
    Write pure Deno unit tests (jsr:@std/assert@1, mirroring sweep.test.ts structure — one Deno.test per behavior). Inject a hand-rolled mock Supabase client (a small object recording .from/.select/.eq/.is/.lt/.update/.upsert/.delete calls and returning canned {data}) AND a mock fetch (returning canned API pages, then an empty page) — NO live DB, NO network. These tests MUST FAIL initially because Task 1's lib bodies throw "not implemented" and resources/{contacts,jobs,job-walk}.ts do not exist yet (Plan 03 creates them); that is the RED state. Import the real signatures from Task 1's modules and the eventual resources/ module paths.
    For contacts.test.ts / jobs.test.ts / job-walk.test.ts, since resources/contacts.ts, resources/jobs.ts, resources/job-walk.ts do not yet exist (Plan 03 creates them), write each test importing the eventual module path and assert on a mock-fetch-injected sync; the test file fails to import until Plan 03 lands the module — that is the intended RED.
    scripts/acculynx-reconcile-check.sql: a standalone `SELECT account_key, resource, api_count, brain_count, delta_pct FROM public.v_acculynx_reconciliation WHERE delta_pct IS NULL OR delta_pct > 5 ORDER BY delta_pct DESC NULLS FIRST;` plus a header comment explaining it flags any (account,resource) outside the <5% tolerance (research A5), and a note that this script is CONTENT/grep-verified in this plan but its FUNCTIONAL gate (running it against the live v_acculynx_reconciliation) is Plan 04 Task 3 (Wave 3) — the view it queries is created by Plan 01 migration 170, which is parallel Wave 1, so it cannot be run live here. No analog exists in scripts/ (PATTERNS "No Analog Found") — keep it a single SELECT.
    Do not implement the lib or resource bodies here — implementation is Plan 03 (GREEN).
  </action>
  <verify>
    <automated>deno test supabase/functions/acculynx-sync/lib/ --allow-env; LIB_EXIT=$?; deno test supabase/functions/acculynx-sync/resources/ --allow-env --allow-net=localhost; RES_EXIT=$?; test $LIB_EXIT -ne 0 && test $RES_EXIT -ne 0 && echo "RED confirmed (lib + resources tests both exit non-zero)"</automated>
  </verify>
  <acceptance_criteria>
    - 6 test files exist: lib/accounts.test.ts, lib/watermark.test.ts, lib/diff.test.ts, resources/contacts.test.ts, resources/jobs.test.ts, resources/job-walk.test.ts
    - `deno test supabase/functions/acculynx-sync/lib/ --allow-env; test $? -ne 0` — lib RED proven by NON-zero exit (not by grepping runner text)
    - `deno test supabase/functions/acculynx-sync/resources/ --allow-env --allow-net=localhost; test $? -ne 0` — resources RED (contacts/jobs/job-walk) proven by NON-zero exit, so resources/ is actually checked, not just lib/
    - `grep -i "delete" supabase/functions/acculynx-sync/lib/diff.test.ts` shows an assertion that .delete is NOT called
    - `grep -i "production" supabase/functions/acculynx-sync/lib/accounts.test.ts` returns a match
    - `grep "pageStartIndex" supabase/functions/acculynx-sync/resources/contacts.test.ts` AND `grep "recordStartIndex" supabase/functions/acculynx-sync/resources/jobs.test.ts` each return a match (pagination-split assertions present)
    - `grep -iE "account_key" supabase/functions/acculynx-sync/resources/contacts.test.ts` AND `...jobs.test.ts` each show a stamping assertion
    - `grep -i "v_acculynx_reconciliation" scripts/acculynx-reconcile-check.sql` returns a match; the SQL header notes its functional gate is Plan 04 Task 3 (content-only check here)
  </acceptance_criteria>
  <done>6 failing Wave 0 test files encode the REQ-03/REQ-04 contracts + hard-rule invariants + behavioral pagination/stamping/budget assertions for contacts & jobs; reconciliation smoke SQL exists (content-verified; functional gate deferred to Plan 04). RED state proven by non-zero exit per directory for Plan 03 to turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| acculynx-sync → AccuLynx API | Per-account Bearer key crosses to the vendor; one key must never fetch another account's data |
| Deno.env (secrets) → function memory | Secret values resolved at runtime; must never be logged or persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | Cross-account key bleed: a shared module-level apiKey leaks account A's key into account B's request | mitigate | apiKey promoted to explicit parameter everywhere (no module-level key); accounts.test.ts asserts production-only fan-out; jobs/contacts.test.ts assert per-row account_key+market stamping; key-bleed assertion lands in Plan 03 integration |
| T-02-05 | Information Disclosure | Secret value logged or stored | mitigate | resolveKey resolves Deno.env.get(env_secret_name); only the secret NAME is ever referenced; skip-on-missing warns the NAME, never the value (hard rule 2); redactSample reused for any stored sample |
| T-02-06 | Tampering | diff path uses .delete() and destroys client atoms | mitigate | diff.ts signature uses .update() only; diff.test.ts asserts .delete is never called (hard rule 1) |
| T-02-11 | Tampering | Wrong pagination param / missing stamp ships behind a grep-on-content gate | mitigate | resources/contacts.test.ts + resources/jobs.test.ts behaviorally assert pageStartIndex vs recordStartIndex and per-row account_key+market via mock fetch; RED proven by non-zero exit, GREEN gated in Plan 03 Task 2 |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; only jsr:@supabase/supabase-js@2 and jsr:@std/assert@1 already used in-repo (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- acculynx-sync source present in repo (downloaded or recreated), all four core files deno check green.
- 6 Wave 0 test files fail (RED) — bodies/modules unimplemented; RED proven by non-zero exit per directory.
- diff signature/test enforce mark-not-delete; accounts query enforces production-only fan-out.
- contacts/jobs tests behaviorally assert the pagination split + per-row account_key/market stamping + budget-stop.
- reconciliation smoke SQL queries v_acculynx_reconciliation (content-verified; functional gate Plan 04 Task 3).
</verification>

<success_criteria>
- The deployed function is no longer untracked: acculynx-sync source lives in supabase/functions/acculynx-sync/.
- All research-identified Wave 0 gaps are closed as failing tests/scaffolds, INCLUDING behavioral pagination/stamping/budget contracts for contacts & jobs.
- The hard-rule invariants (production-only fan-out, secret-name-only, mark-not-delete, no key bleed) are encoded as executable assertions ready for Plan 03 to satisfy.
</success_criteria>

<output>
Create `.planning/phases/02-multi-location-full-ingestion/02-02-SUMMARY.md` when done.
Note whether the v10 source was located (downloaded) or recreated, and record any download CLI error.
</output>
</output>
