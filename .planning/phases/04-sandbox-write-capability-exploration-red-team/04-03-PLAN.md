---
phase: 04-sandbox-write-capability-exploration-red-team
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - scripts/acculynx-write-sweep-reconcile.sql
autonomous: false
requirements: [REQ-06]
must_haves:
  truths:
    - "Every one of the 38 write endpoints has at least one acculynx_write_probe row from a real sandbox call"
    - "The ~8 deep-lane endpoints each have probe rows across their applicable red-team dimensions, stopped at diminishing returns"
    - "Each endpoint carries an evidence-based acculynx_write_catalog verdict (writable / write-only / unsupported / fragile-with-guardrail / blocked-by-dependency)"
    - "Every probe row is tagged source_account_key='sandbox' and carries the run_tag"
    - "The reconcile gate returns zero rows across all assertions"
  artifacts:
    - path: "scripts/acculynx-write-sweep-reconcile.sql"
      provides: "the gate that proves 38/38 coverage + sandbox-only + 2xx shape completeness"
      contains: "acculynx_write_probe"
  key_links:
    - from: "deployed acculynx-write-sweep"
      to: "acculynx_write_probe / acculynx_write_catalog"
      via: "invocation writes evidence rows"
      pattern: "acculynx_write_probe"
    - from: "reconcile SQL"
      to: "acculynx_write_checklist"
      via: "coverage assertion (every checklist op has a probe row)"
      pattern: "acculynx_write_checklist"
---

<objective>
Run the deployed `acculynx-write-sweep` against the AccuLynx sandbox account, red-teaming the ~8 deep-lane endpoints across all applicable dimensions to diminishing returns (2 consecutive no-new-signal probes) and smoke-testing the remaining ~30, until every one of the 38 endpoints has evidence rows and an evidence-based verdict. Then run the reconciliation gate and confirm zero rows across all assertions. This plan produces the evidence (REQ-06 success criteria 1 and 2).

Purpose: The tables + harness exist; this plan generates the actual behavioral evidence that the matrix (Plan 04) is generated from.
Output: Populated `acculynx_write_probe` / `acculynx_write_catalog` rows in prod; a passing reconcile gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-CONTEXT.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-RESEARCH.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-01-SUMMARY.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke the sweep against the sandbox; iterate to 38/38 coverage</name>
  <files>scripts/acculynx-write-sweep-reconcile.sql</files>
  <read_first>
    - supabase/functions/acculynx-write-sweep/index.ts (the deployed harness — its invocation contract + JSON summary shape)
    - 04-RESEARCH.md §"Common Pitfalls" (Pitfall 3 unassigned-lead visibility; Pitfall 4 bare-lead shallow graph → blocked-by-dependency; Pitfall 6 per-key rate limit → run in isolation, no concurrent sandbox-key consumers)
    - 04-RESEARCH.md §"Open Questions" (financials provisioning delay; PUT /jobs/{jobId}/insurance shape discovery; search-POST verdict handling)
    - CLAUDE.md hard rule 5 (sandbox-only) + "verify against the live DB"
  </read_first>
  <action>
    Invoke the deployed `acculynx-write-sweep` Edge Function (POST) against the sandbox account, in ISOLATION (no concurrent sandbox-key consumers — e.g. do not run `seed-sandbox-from-wichita.mjs` at the same time; Pitfall 6). Inspect the returned JSON summary (`batch_id`, `calls_made`, `verdicts`). Query `acculynx_write_probe` for the latest batch and check coverage against `acculynx_write_checklist`. If any endpoint is missing a probe row or a genuinely-untestable endpoint surfaced (bare-lead shallow graph, missing seed ID), re-invoke or record the evidence-backed `blocked-by-dependency` verdict with notes — NEVER fabricate a synthetic pass (RESEARCH Anti-Patterns). Handle the known live discoveries: the financials-provisioning retry, the `PUT /jobs/{jobId}/insurance` real body shape, and the two search-POSTs' verdict (fold into `writable`/`read-shaped` per the Plan-01 schema choice). Continue until all 38 checklist operations have ≥1 probe row for the latest batch.
  </action>
  <verify>
    <automated>echo "Run via Supabase MCP execute_sql against rnhmvcpsvtqjlffpsayu — coverage of latest batch: with latest as (select probe_batch_id from acculynx_write_probe order by probed_at desc limit 1) select count(*) from acculynx_write_checklist c where not exists (select 1 from acculynx_write_probe p, latest l where p.probe_batch_id=l.probe_batch_id and p.probe_name=c.operation_id); -- expect 0"</automated>
  </verify>
  <acceptance_criteria>
    - The latest `acculynx_write_probe` batch covers all 38 checklist operation_ids (coverage query returns 0 unreconciled).
    - Every probe row in the batch has `source_account_key='sandbox'` and a non-null `run_tag`.
    - Any `blocked-by-dependency` verdict carries evidence in notes/guardrail_notes (no bare verdict).
    - No production account key was used (structurally guaranteed by the hard gate; confirmed by zero non-sandbox rows).
  </acceptance_criteria>
  <done>Every one of the 38 write endpoints has at least one real sandbox probe row in the latest batch, all sandbox-tagged.</done>
</task>

<task type="auto">
  <name>Task 2: Deep-lane red-team to diminishing returns + evidence-based verdicts</name>
  <files>scripts/acculynx-write-sweep-reconcile.sql</files>
  <read_first>
    - 04-CONTEXT.md D-05 (5 dimensions on the ~8 deep endpoints; stop rule = 2 consecutive no-new-signal probes)
    - 04-RESEARCH.md §"Red-Team Dimension Recipes" + §"The 4 DELETE Endpoints" (the create→delete→delete-again idempotency chain; 412 ordering targets; idempotency anchor on external-references)
    - 04-RESEARCH.md §"Common Pitfalls" (Pitfall 5 write-only messages have no read-back → write-only verdict, not writable)
    - 04-VALIDATION.md §"Manual-Only Verifications" (red-team verdict interpretation is a human-reviewed judgement over real responses)
  </read_first>
  <action>
    For each of the ~8 deep-lane endpoints (contact/job create, PUT custom-fields, POST worksheet/items, the payments/* POSTs, POST documents+photos, POST messages, representatives/*, POST external-references), confirm the harness exercised each applicable red-team dimension and stopped at the D-05 rule. Inspect the probe rows per endpoint to confirm: bad-input probes surfaced the documented error shapes (invalid enum → 404 type-conversion; empty contactTypeIds → 400; wrong address shape → 4xx); the external-references idempotency anchor was double-probed (duplicate {jobId,source,projectId}); the 4 DELETE endpoints ran create→delete→delete-again (second delete → 404); write-only endpoints (messages) are classified `write-only` (no read-back), not `writable`. Where the deep-lane probes reveal a repeatable guardrail, record it in the catalog's `guardrail_notes`. If any deep endpoint's dimensions are incomplete, re-invoke the sweep (or targeted probes) until diminishing returns. This task's output is the evidence-based verdict + guardrail note per deep endpoint in `acculynx_write_catalog`.
  </action>
  <verify>
    <automated>echo "Run via Supabase MCP execute_sql — deep-lane dimension coverage: select w.probe_name, count(distinct w.red_team_dimension) dims from acculynx_write_probe w join acculynx_write_checklist c on c.operation_id=w.probe_name where c.tier='deep' and w.red_team_dimension is not null group by w.probe_name; -- expect each deep endpoint to show multiple dimensions"</automated>
  </verify>
  <acceptance_criteria>
    - Each deep-lane endpoint has probe rows spanning its applicable red-team dimensions (bad_input at minimum, plus ordering/idempotency/authz where applicable).
    - The 4 DELETE endpoints each have a create → delete → second-delete (404) idempotency chain recorded.
    - `POST /jobs/{jobId}/messages` catalog verdict is `write-only` (no read-back path), not `writable`.
    - Every deep-lane catalog row carries a `guardrail_notes` value.
    - The external-references idempotency anchor has a documented duplicate-call result.
  </acceptance_criteria>
  <done>The ~8 deep-lane endpoints are red-teamed to diminishing returns with evidence-based verdicts and guardrail notes recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the reconcile gate + human review of red-team verdicts</name>
  <files>scripts/acculynx-write-sweep-reconcile.sql</files>
  <read_first>
    - scripts/acculynx-write-sweep-reconcile.sql (the gate written in Plan 01)
    - 04-VALIDATION.md §"Sampling Rate" (reconcile returns zero rows across all assertions before /gsd-verify-work) + §"Manual-Only Verifications"
  </read_first>
  <what-built>A fully populated evidence set: 38/38 endpoints probed against the sandbox, deep lanes red-teamed to diminishing returns, verdicts + guardrail notes recorded in acculynx_write_catalog.</what-built>
  <action>
    Run `scripts/acculynx-write-sweep-reconcile.sql` against prod `rnhmvcpsvtqjlffpsayu` via the Supabase MCP `execute_sql` and confirm it returns ZERO rows across all assertions (no unreconciled endpoint, no 2xx row with null result_summary, no non-sandbox row, no blocked-by-dependency without evidence). Then present the deep-lane verdicts + guardrail notes for human review — the operator confirms the interpretation of real vendor behavior (which endpoints are writable vs write-only vs fragile vs blocked-by-dependency) is sound, since a verdict on a live API's error/side-effect behavior is a judgement, not a pure function.
  </action>
  <how-to-verify>
    1. Reconcile gate returns zero rows across all assertions (paste the result).
    2. Review the per-deep-endpoint verdict + guardrail_notes; confirm each verdict is backed by the probe evidence (e.g. messages = write-only because no GET exists; any blocked-by-dependency carries a real reason).
    3. Confirm the stale `POST /jobs/{id}/measurements` phantom is NOT present as a real endpoint (it isn't in the 38 — verify it never got a synthetic row).
  </how-to-verify>
  <resume-signal>Type "verified" once the reconcile gate is zero-rows and the deep-lane verdicts are confirmed sound, or list the verdicts to revisit.</resume-signal>
  <done>Reconcile gate passes (zero rows) and the human confirms the red-team verdicts are evidence-backed and sound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sweep invocation → AccuLynx sandbox | Live mutating calls; must never touch a production account |
| Sweep → prod Postgres | Evidence rows written; PII already redacted upstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-sandbox | Tampering / Elevation of Privilege | sweep invocation | mitigate | The deployed function's hard gate (Plan 02) is the only path; reconcile assertion (3) `non_sandbox_row` returns zero, proving every write hit the sandbox only |
| T-04-fakepass | Repudiation / integrity of evidence | verdict recording | mitigate | Anti-pattern guard: any `blocked-by-dependency` verdict must carry evidence in notes (Task 1 acceptance + reconcile optional 4th assertion); Task 3 human review confirms no synthetic passes |
| T-04-contention | Denial of Service (false 429 signal) | idempotency probes | mitigate | Run the sweep in isolation (Pitfall 6) so 429s are real red-team evidence, not shared-budget contention |
| T-04-SC | Tampering | package installs | accept | No new packages this phase |
</threat_model>

<verification>
- Coverage query: latest batch covers all 38 checklist operations (0 unreconciled).
- Deep-lane dimension coverage query shows multiple dimensions per deep endpoint.
- `scripts/acculynx-write-sweep-reconcile.sql` returns zero rows across all assertions.
- Zero non-sandbox rows; every row carries run_tag.
</verification>

<success_criteria>
- REQ-06 SC1: every POST/PUT/DELETE exercised against sandbox entities with real behavior recorded.
- REQ-06 SC2: each deep-lane path red-teamed to diminishing returns.
- Reconcile gate green (zero rows).
- Human-confirmed evidence-based verdicts + guardrail notes.
</success_criteria>

<output>
Create `.planning/phases/04-sandbox-write-capability-exploration-red-team/04-03-SUMMARY.md` when done.
</output>
