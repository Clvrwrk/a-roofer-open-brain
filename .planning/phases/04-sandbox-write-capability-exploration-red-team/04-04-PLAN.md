---
phase: 04-sandbox-write-capability-exploration-red-team
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - docs/37-acculynx-write-capability-matrix.md
  - docs/knowledge-base/acculynx/api/write-capability.md
  - docs/knowledge-base/acculynx/ingestion/write-sweep.md
autonomous: true
requirements: [REQ-06]
must_haves:
  truths:
    - "docs/37 is regenerated from acculynx_write_catalog evidence rows, not hand-maintained"
    - "The phantom POST /jobs/{id}/measurements endpoint is corrected (dropped) in the regenerated matrix"
    - "Each writable path has a documented guardrail recipe (it-just-works conditions + failure modes)"
    - "write-capability.md carries the evidence-based verdict per endpoint superseding the 2026-06-30 seed findings"
    - "A write-sweep design doc exists mirroring read-sweep.md"
  artifacts:
    - path: "docs/37-acculynx-write-capability-matrix.md"
      provides: "the superseding evidence-based write matrix"
      contains: "writable"
    - path: "docs/knowledge-base/acculynx/api/write-capability.md"
      provides: "knowledge-base write-capability matrix regenerated from evidence"
    - path: "docs/knowledge-base/acculynx/ingestion/write-sweep.md"
      provides: "write-sweep harness design/behavior doc mirroring read-sweep.md"
  key_links:
    - from: "docs/37"
      to: "acculynx_write_catalog"
      via: "matrix generated by SQL query over evidence rows"
      pattern: "acculynx_write_catalog"
    - from: "docs/37"
      to: "measurements correction"
      via: "phantom endpoint removed / marked unsupported"
      pattern: "measurements"
---

<objective>
Regenerate the write-capability matrix from the `acculynx_write_catalog` evidence rows (D-03) — updating `docs/37-acculynx-write-capability-matrix.md` and `docs/knowledge-base/acculynx/api/write-capability.md` with evidence-based verdicts and a guardrail recipe per writable path, and correcting the stale `POST /jobs/{id}/measurements` phantom endpoint (it does not exist in the 124-op surface). Write a new `write-sweep.md` design doc mirroring `read-sweep.md`. This closes REQ-06 success criteria 3 and 4.

Purpose: The evidence tables are the source of truth (D-03); the docs are generated FROM them, ending the drift docs/37 exists to fix.
Output: Three docs regenerated/created from evidence; the phantom endpoint corrected.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-CONTEXT.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-RESEARCH.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-03-SUMMARY.md
@docs/37-acculynx-write-capability-matrix.md
@docs/knowledge-base/acculynx/api/write-capability.md
@docs/knowledge-base/acculynx/ingestion/read-sweep.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the evidence matrix and regenerate docs/37 + write-capability.md</name>
  <files>docs/37-acculynx-write-capability-matrix.md, docs/knowledge-base/acculynx/api/write-capability.md</files>
  <read_first>
    - docs/37-acculynx-write-capability-matrix.md (the doc being superseded — read its structure/section headings; note §4.10 handoff targets and the "meaningful write lane")
    - docs/knowledge-base/acculynx/api/write-capability.md (the 2026-06-30 seed findings to fold in as prior evidence; the 19/15/4 surface breakdown)
    - 04-CONTEXT.md D-03 (matrix is GENERATED from the tables, not hand-maintained)
    - 04-RESEARCH.md §"State of the Art" (the measurements-endpoint correction: no such endpoint exists; closest analog is POST /jobs/{jobId}/photos-videos)
  </read_first>
  <action>
    Query `acculynx_write_catalog` (via Supabase MCP `execute_sql` against `rnhmvcpsvtqjlffpsayu`) for every endpoint's `endpoint_pattern`, `method`, `verdict`, `tier`, `side_effect`, `guardrail_notes`, and `red_team_dimensions_covered`, ordered by `category` then `method`. Emit a markdown matrix table from that result set. Regenerate `docs/37-acculynx-write-capability-matrix.md` and `docs/knowledge-base/acculynx/api/write-capability.md` from this generated table — replacing hand-maintained verdicts with the evidence-based ones. Explicitly CORRECT the stale content: remove/mark-unsupported the phantom `POST /jobs/{id}/measurements` (+ `/measurements/files`) category with a note that no such endpoint exists in the 124-op surface and `POST /jobs/{jobId}/photos-videos` is the closest existing analog. Add a header note in each doc stating it is generated from `acculynx_write_catalog` (as of the sweep batch date) and lists the SQL used, so the next regeneration is reproducible (D-03). Confirm the 38 endpoints map to 19 POST / 15 PUT / 4 DELETE and every one carries a verdict.
  </action>
  <verify>
    <automated>grep -ciE 'writable|write-only|unsupported|fragile-with-guardrail|blocked-by-dependency' docs/37-acculynx-write-capability-matrix.md # expect > 0; then confirm phantom corrected: grep -ci 'no such endpoint\|does not exist\|phantom\|unsupported' docs/37-acculynx-write-capability-matrix.md # expect > 0 near a measurements mention</automated>
  </verify>
  <acceptance_criteria>
    - `docs/37-acculynx-write-capability-matrix.md` contains an evidence-based verdict column using the D-02 enum values.
    - The `measurements` endpoint is corrected: it appears only as unsupported/non-existent with the photos-videos analog note (not as a live WRITE endpoint).
    - Both docs carry a header note that they are generated from `acculynx_write_catalog` with the batch date + the generating SQL.
    - The matrix accounts for all 38 endpoints (19 POST / 15 PUT / 4 DELETE), each with a verdict.
  </acceptance_criteria>
  <done>docs/37 and write-capability.md are regenerated from the evidence tables with corrected verdicts and the phantom endpoint removed.</done>
</task>

<task type="auto">
  <name>Task 2: Guardrail recipes per writable path + write-sweep design doc</name>
  <files>docs/knowledge-base/acculynx/api/write-capability.md, docs/knowledge-base/acculynx/ingestion/write-sweep.md</files>
  <read_first>
    - docs/knowledge-base/acculynx/ingestion/read-sweep.md (the design-doc analog — mirror its section headings/prose structure for write-sweep.md)
    - 04-RESEARCH.md §"Common Pitfalls" (the guardrail source material: address asymmetry, priority enum, unassigned-lead visibility, bare-lead shallow graph, write-only messages)
    - 04-CONTEXT.md success criteria 4 (each writable path has a documented guardrail recipe: it-just-works conditions + failure modes)
    - 04-03-SUMMARY.md (the recorded guardrail_notes per deep endpoint)
  </read_first>
  <action>
    For each `writable` / `fragile-with-guardrail` endpoint in the evidence, write a guardrail recipe section in `write-capability.md`: the "it just works" preconditions (required body fields, correct address shape, valid enum values, required parent IDs) and the known failure modes (the exact error shape + status when a precondition is violated), sourced from the `guardrail_notes` / probe error shapes recorded in Plan 03. Then create `docs/knowledge-base/acculynx/ingestion/write-sweep.md` mirroring `read-sweep.md`'s structure: purpose, hard-gate design, the dependency walk (contact→job→financials), the tiered deep/smoke red-team + stop rule, the evidence tables + reconcile gate, and the deploy path (`supabase functions deploy`, not Coolify). Cross-link the three docs. Ensure the knowledge-base `acculynx-api` skill's reference set can reach these (a pointer note if the skill indexes them).
  </action>
  <verify>
    <automated>test -f docs/knowledge-base/acculynx/ingestion/write-sweep.md && grep -ci 'guardrail\|it just works\|precondition\|failure mode' docs/knowledge-base/acculynx/api/write-capability.md # expect > 0</automated>
  </verify>
  <acceptance_criteria>
    - `docs/knowledge-base/acculynx/ingestion/write-sweep.md` exists and covers hard gate, dependency walk, tiered red-team + stop rule, evidence tables, reconcile gate, deploy path.
    - Every `writable`/`fragile-with-guardrail` endpoint in write-capability.md has a guardrail recipe (preconditions + failure modes).
    - The three docs cross-link (matrix ↔ write-capability ↔ write-sweep design).
  </acceptance_criteria>
  <done>Each writable path has a documented guardrail recipe and a write-sweep design doc mirrors read-sweep.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence tables → generated docs | The docs must faithfully reflect the evidence rows, not reintroduce hand-edited drift |
| Docs → repo (committed) | No secret or raw client PII may enter the generated markdown |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-drift | Repudiation / integrity | matrix generation | mitigate | D-03: docs generated FROM `acculynx_write_catalog` with the generating SQL + batch date recorded in the header, making regeneration reproducible and hand-edit drift detectable |
| T-04-pii | Information Disclosure | generated docs | mitigate | Source rows are already redacted (Plan 02/03 `redactSample`); the generation query selects verdict/guardrail metadata, not raw payload PII |
| T-04-secret | Information Disclosure | committed docs | mitigate | Reconcile companion grep (Plan 01) scans docs for leaked key patterns; no secret value is ever in the evidence rows |
| T-04-SC | Tampering | package installs | accept | No new packages; docs-only plan |
</threat_model>

<verification>
- docs/37 contains D-02 verdict enum values and the corrected (non-existent) measurements note.
- Both docs carry the "generated from acculynx_write_catalog" header + SQL + batch date.
- write-sweep.md exists with the full design sections.
- Every writable path has a guardrail recipe.
</verification>

<success_criteria>
- REQ-06 SC3: a live write-capability matrix supersedes docs/37 with evidence-based verdicts, generated from the tables.
- REQ-06 SC4: each writable path has a documented guardrail recipe.
- The stale phantom measurements endpoint is corrected.
</success_criteria>

<output>
Create `.planning/phases/04-sandbox-write-capability-exploration-red-team/04-04-SUMMARY.md` when done.
</output>
