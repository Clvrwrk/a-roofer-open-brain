---
phase: 04-sandbox-write-capability-exploration-red-team
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/cleverwork-roofer/182-acculynx-write-catalog-ddl.sql
  - schemas/cleverwork-roofer/183-acculynx-write-checklist-seed.sql
  - scripts/acculynx-write-sweep-reconcile.sql
  - supabase/functions/acculynx-write-sweep/sweep.ts
  - supabase/functions/acculynx-write-sweep/sweep.test.ts
autonomous: false
requirements: [REQ-06]
must_haves:
  truths:
    - "acculynx_write_catalog and acculynx_write_probe tables exist in prod DB rnhmvcpsvtqjlffpsayu"
    - "acculynx_write_checklist holds 38 seed rows (19 POST / 15 PUT / 4 DELETE)"
    - "assertSandbox throws for every non-sandbox secret name and accepts only PE_CC_SANDBOX_ACCULYNX_API_KEY"
    - "shouldStopProbing returns true after 2 consecutive no-new-signal probes"
    - "buildContactAddress emits object state/country; buildJobAddress emits string state/country"
  artifacts:
    - path: "schemas/cleverwork-roofer/182-acculynx-write-catalog-ddl.sql"
      provides: "additive/idempotent DDL for acculynx_write_catalog + acculynx_write_probe"
      contains: "create table if not exists public.acculynx_write_catalog"
    - path: "schemas/cleverwork-roofer/183-acculynx-write-checklist-seed.sql"
      provides: "acculynx_write_checklist table + 38-row seed"
      contains: "create table if not exists public.acculynx_write_checklist"
    - path: "scripts/acculynx-write-sweep-reconcile.sql"
      provides: "coverage/sandbox-only reconciliation gate"
      contains: "acculynx_write_probe"
    - path: "supabase/functions/acculynx-write-sweep/sweep.ts"
      provides: "assertSandbox, redactSample, pathParams, shouldStopProbing, buildContactAddress, buildJobAddress"
      exports: ["assertSandbox", "redactSample", "shouldStopProbing", "buildContactAddress", "buildJobAddress", "SANDBOX_SECRET_NAME"]
    - path: "supabase/functions/acculynx-write-sweep/sweep.test.ts"
      provides: "failing-first Deno unit tests for the pure sweep core"
  key_links:
    - from: "sweep.test.ts"
      to: "sweep.ts"
      via: "import from ./sweep.ts"
      pattern: "from \"./sweep.ts\""
    - from: "183 seed rows"
      to: "acculynx_write_checklist"
      via: "insert ... on conflict (operation_id) do nothing"
      pattern: "insert into public.acculynx_write_checklist"
---

<objective>
Wave 0 foundation for the write-capability sweep: create the evidence tables (DDL 182), the 38-endpoint checklist seed (183), the reconciliation gate SQL, and the pure `sweep.ts` core (hard gate, redaction, stop-rule, address-shape builders) with FAILING-first Deno tests. Then APPLY migrations 182/183 to prod Supabase so the tables exist live before any sweep run inserts into them.

Purpose: Every downstream plan (index implementation, sweep execution, matrix generation) reads these tables and imports these pure functions. This plan defines the contracts first (interface-first ordering) so the executor in Wave 2 receives them, not a scavenger hunt.
Output: 5 files created + migrations 182/183 applied and verified in `information_schema`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-CONTEXT.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-RESEARCH.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-PATTERNS.md
@.planning/phases/04-sandbox-write-capability-exploration-red-team/04-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DDL (182) — acculynx_write_catalog + acculynx_write_probe (additive/idempotent)</name>
  <files>schemas/cleverwork-roofer/182-acculynx-write-catalog-ddl.sql</files>
  <read_first>
    - schemas/cleverwork-roofer/166-acculynx-catalog-probe-ddl.sql (the column-for-column analog — read in full; mirror its header convention, RLS+grant footer, index pattern)
    - 04-PATTERNS.md §"182-acculynx-write-catalog-ddl.sql" (exact extended column set per D-02)
    - 04-CONTEXT.md D-02/D-03 (verdict enum, run_tag, created_entity_id, side_effect, red_team_dimension)
    - CLAUDE.md hard rule 1 (additive/idempotent — CREATE ... IF NOT EXISTS only, never DROP/TRUNCATE)
  </read_first>
  <action>
    Create migration 182 defining two NEW tables mirroring 166's read-side shape, extended per D-02. `acculynx_write_catalog`: base columns `endpoint_pattern`, `method`, `last_probe_status`, `last_probed_at`, `notes`, `created_at`/`updated_at`; plus write-specific `verdict text` with a CHECK constraint over `writable`/`write-only`/`unsupported`/`fragile-with-guardrail`/`blocked-by-dependency` (add `read-shaped` per RESEARCH Open Question 3 for the two search-POSTs — your discretion, but if omitted, document the choice in a column comment and use `notes` for search-POST nuance), `tier text` CHECK in `deep`/`smoke`, `red_team_dimensions_covered text[]`, `side_effect text`, `guardrail_notes text`, `category text` repurposed as a grouping label (contacts/jobs/financials/payments/documents/messages/representatives/subscriptions), `source_account_key text not null default 'sandbox'`. `acculynx_write_probe`: keep `id`, `probe_batch_id`, `probe_name`, `api_endpoint`, `method`, `http_status`, `response_ms`, `result_summary jsonb not null default '{}'`, `payload_sample jsonb`, `error`, `probed_at`; drop `reported_count`/`items_on_page`; add `request_body_sample jsonb` (redacted), `red_team_dimension text` CHECK in `bad_input`/`partial_failure`/`idempotency`/`ordering_dependency`/`authz_scope` (nullable for happy-path smoke), `side_effect_observed text`, `created_entity_id text` (D-04 tag+leave traceability), `run_tag text` (D-04 run marker), `source_account_key text not null default 'sandbox'`. Enable RLS on both, grant `select` to `authenticated, service_role` only (deny-by-default, matches 166/177). Create indexes on `probe_batch_id` and `source_account_key`. Unique key on catalog `(endpoint_pattern, method)` to support the Wave 2 upsert. All CREATE statements use `if not exists`; no DROP/ALTER-retype/data touch.
  </action>
  <verify>
    <automated>grep -v '^--' schemas/cleverwork-roofer/182-acculynx-write-catalog-ddl.sql | grep -c 'create table if not exists' # expect >= 2; then: grep -vE '^\s*--' schemas/cleverwork-roofer/182-acculynx-write-catalog-ddl.sql | grep -ciE 'drop table|truncate|drop column' # expect 0</automated>
  </verify>
  <acceptance_criteria>
    - File contains `create table if not exists public.acculynx_write_catalog` and `create table if not exists public.acculynx_write_probe`.
    - No `DROP TABLE`, `DROP COLUMN`, or `TRUNCATE` tokens anywhere (grep count 0, comment lines stripped).
    - `acculynx_write_catalog` has a `verdict` CHECK constraint listing writable/write-only/unsupported/fragile-with-guardrail/blocked-by-dependency.
    - Both tables have `enable row level security` and `grant select ... to authenticated, service_role`.
    - Catalog has a unique constraint or index on `(endpoint_pattern, method)`.
  </acceptance_criteria>
  <done>Migration 182 file exists, is additive/idempotent, defines both write tables with the extended column set + RLS + grants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checklist seed (183) — acculynx_write_checklist table + 38-row seed</name>
  <files>schemas/cleverwork-roofer/183-acculynx-write-checklist-seed.sql</files>
  <read_first>
    - schemas/cleverwork-roofer/167-acculynx-get-checklist.sql (the seed analog — read in full; copy the `$$...$$` dollar-quoting + `on conflict (operation_id) do nothing` idempotency guard + RLS/grant footer)
    - 04-RESEARCH.md §"Full 38-Endpoint Enumeration" (the 13 deep + 25 smoke rows — each row maps to one seed tuple; source of the operation_id/path/method/required-body/dependency-chain/success-codes per endpoint)
    - 04-PATTERNS.md §"183-acculynx-write-checklist-seed.sql" (table column mapping: tier deep/smoke, method, required_body_fields, dependency_chain, red_team_dimensions, probeability)
  </read_first>
  <action>
    Create migration 183 defining a NEW `acculynx_write_checklist` table (distinct from `acculynx_write_catalog` — the checklist is the INPUT target list; the catalog is the evidence OUTPUT, per the RESEARCH architecture diagram) mirroring `acculynx_get_checklist`'s shape: `operation_id text primary key`, `path`, `base_url`, `tier text` CHECK in `deep`/`smoke` (per D-05, replacing A/B/C), `method text` CHECK in `POST`/`PUT`/`DELETE`, `path_params jsonb default '[]'`, `required_body_fields jsonb`, `dependency_chain text` (e.g. `contactId -> jobId -> financialsId`), `red_team_dimensions jsonb` (full 5-element list for deep rows, shorter for smoke), `probeability text` CHECK in `probeable`/`tier_gated`/`blocked-by-dependency` (pre-flag `PUT /jobs/{jobId}/insurance` and adjuster-on-bare-lead per RESEARCH Open Question 2 / Pitfall 4), `swept boolean not null default false`, `created_at`. Then INSERT all 38 rows from the RESEARCH enumeration: 13 deep-lane rows (POST /contacts, POST /jobs, PUT custom-fields, POST worksheet/items, the three payments/* POSTs, POST documents, POST photos-videos, POST messages, the three representatives/* POSTs, POST external-references) with the full 5-dimension `red_team_dimensions`; and 25 smoke rows (the remaining PUTs, contact custom-fields, logs, both search-POSTs, address, adjuster, appointment PUT+DELETE, insurance PUTs, job-categories, lead-source, priority, trade-types, work-type, message replies, the two representative DELETEs, the four subscription endpoints, test-event) with `["bad_input"]` (happy-path implied). Use `$$...$$` dollar-quoting for every literal and `on conflict (operation_id) do nothing`. Confirm the count is exactly 19 POST + 15 PUT + 4 DELETE = 38. Enable RLS, grant `select` to `authenticated, service_role`.
  </action>
  <verify>
    <automated>grep -c 'https://api.acculynx.com/api/v2' schemas/cleverwork-roofer/183-acculynx-write-checklist-seed.sql # expect 38 (one per seed tuple); then: grep -c 'create table if not exists public.acculynx_write_checklist' schemas/cleverwork-roofer/183-acculynx-write-checklist-seed.sql # expect 1</automated>
  </verify>
  <acceptance_criteria>
    - File defines `create table if not exists public.acculynx_write_checklist` with a `tier` CHECK in (deep, smoke) and a `method` CHECK in (POST, PUT, DELETE).
    - Exactly 38 seed value-tuples present (one base_url token per row).
    - Insert uses `on conflict (operation_id) do nothing`.
    - Both search-POSTs (`postJobsSearch`, `postContactsSearch` or equivalent operation_ids) present as smoke rows.
    - The 4 DELETE endpoints present: initial-appointment, representatives/ar-owner, representatives/sales-owner, subscriptions/{id}.
    - File has RLS enable + grant footer.
  </acceptance_criteria>
  <done>Migration 183 seeds exactly 38 write endpoints into acculynx_write_checklist, idempotently, matching the 19/15/4 method breakdown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pure sweep core (sweep.ts) + FAILING-first Deno tests (sweep.test.ts)</name>
  <files>supabase/functions/acculynx-write-sweep/sweep.ts, supabase/functions/acculynx-write-sweep/sweep.test.ts</files>
  <read_first>
    - supabase/functions/acculynx-read-sweep/sweep.ts (copy assertSandbox verbatim renaming only the error string; copy redactSample and pathParams verbatim)
    - supabase/functions/acculynx-read-sweep/sweep.test.ts (mirror the assertSandbox + redactSample test structure and the 9-account forbidden-key fixture list)
    - 04-PATTERNS.md §"sweep.ts" and §"sweep.test.ts" (shouldStopProbing shape + ProbeSignal interface; buildContactAddress/buildJobAddress asymmetry)
    - scripts/seed-sandbox-from-wichita.mjs lines 38-53 and 106-124 (verified contact-address-as-objects vs job-address-as-strings builders — the source of the two builder shapes)
    - 04-RESEARCH.md §"Known Landmines to Encode as Fixtures" (state/country object vs string, contactTypeIds required, priority enum)
  </read_first>
  <behavior>
    - assertSandbox("PE_CC_KANSAS_CITY_ACCULYNX_API_KEY") throws Error matching /sandbox-only/; same for all 8 production key names and "" and "ACCULYNX_API_KEY".
    - assertSandbox("PE_CC_SANDBOX_ACCULYNX_API_KEY") does NOT throw.
    - redactSample masks firstName/lastName/email/phone/street1 to `[redacted:...]` tokens; preserves non-PII keys (id, count, checkNumber, amount) and structure.
    - shouldStopProbing([]) === false; shouldStopProbing([one]) === false; two identical-signal probes → true; two differing-signal probes → false.
    - buildContactAddress({state:"KS",...}).state is an object ({id/name/abbreviation}); buildJobAddress({state:"KS",...}).state is the string "KS".
  </behavior>
  <action>
    Write `sweep.test.ts` FIRST (tests must fail against a not-yet-implemented sweep.ts), then implement `sweep.ts` to green. `sweep.ts` exports: `SANDBOX_SECRET_NAME = "PE_CC_SANDBOX_ACCULYNX_API_KEY"` and `assertSandbox(secretName)` (verbatim from read-sweep, error string renamed to `acculynx-write-sweep`); `redactSample(value)` and `pathParams(path)` (verbatim from read-sweep — `pathParams` is needed to resolve `{jobId}`/`{financialsId}`/`{contactId}`/`{customFieldId}`/`{subscriptionId}`/`{messageId}`); a NEW pure `shouldStopProbing(history: ProbeSignal[])` implementing D-05's rule (return true when the last two probes share status + errorShape + guardrail, i.e. 2 consecutive no-new-signal); and NEW pure `buildContactAddress(a)` / `buildJobAddress(a)` builders encoding the object-vs-string state/country asymmetry (two DISTINCT functions — never share one builder, per RESEARCH Pitfall 1). Do NOT port `paginationParam` (writes don't paginate). Keep the module doc-comment convention ("NO secret value ever appears here — only the sandbox secret NAME constant"). `sweep.test.ts` mirrors read-sweep's test file: the assertSandbox forbidden-key loop (all 8 production key names + "" + "ACCULYNX_API_KEY"), the assertSandbox-accepts-sandbox case, the redactSample masking test (extend the fixture with a write-body field like `checkNumber`/`amount` to confirm non-PII survives), a table-driven `shouldStopProbing` test (0/1/2-identical/2-differing), and a builder-asymmetry test (contact.state typeof object, job.state typeof string). Import from `jsr:@std/assert@1`.
  </action>
  <verify>
    <automated>deno test supabase/functions/acculynx-write-sweep/sweep.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `deno test supabase/functions/acculynx-write-sweep/sweep.test.ts` exits 0 with all tests passing.
    - `grep -c 'SANDBOX_SECRET_NAME = "PE_CC_SANDBOX_ACCULYNX_API_KEY"' supabase/functions/acculynx-write-sweep/sweep.ts` == 1.
    - `grep -c 'export function shouldStopProbing' supabase/functions/acculynx-write-sweep/sweep.ts` == 1.
    - `grep -cE 'export function buildContactAddress|export function buildJobAddress' supabase/functions/acculynx-write-sweep/sweep.ts` == 2.
    - No `paginationParam` export in sweep.ts (writes don't paginate).
    - No literal API key value (only the secret NAME string) appears in either file.
  </acceptance_criteria>
  <done>sweep.ts exports the hard gate, redaction, stop-rule, and both address builders; sweep.test.ts is green covering all five behaviors above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Reconciliation gate SQL (mirror of read-sweep-reconcile)</name>
  <files>scripts/acculynx-write-sweep-reconcile.sql</files>
  <read_first>
    - scripts/acculynx-read-sweep-reconcile.sql (the entire 37-line file — copy the latest-batch CTE + three-assertion union all structure + secret-leak grep companion comment)
    - 04-PATTERNS.md §"acculynx-write-sweep-reconcile.sql" (retarget table names; generalize 200→2xx; optional 4th blocked-by-dependency evidence assertion)
  </read_first>
  <action>
    Create `scripts/acculynx-write-sweep-reconcile.sql` as a direct structural mirror of the read-sweep reconcile. Latest-batch CTE selects the most recent `probe_batch_id` from `acculynx_write_probe`. Three assertions via `union all`, all must return ZERO rows: (1) coverage — every `operation_id` in `acculynx_write_checklist` must appear as a `probe_name` in the batch (`unreconciled_op`); (2) shape-completeness — no 2xx row may have a null `result_summary` (`where http_status between 200 and 299 and result_summary is null`); (3) sandbox-only — no batch row may have `source_account_key is distinct from 'sandbox'`. Add an OPTIONAL 4th assertion per RESEARCH Anti-Patterns: any catalog row with verdict `blocked-by-dependency` must carry evidence in `notes` or `guardrail_notes` (not a bare verdict) — `blocked_dep_missing_evidence`. Keep the secret-leak grep companion comment verbatim (retarget the doc path token to the write-capability doc), retitle header to "Phase 4 write-sweep gate".
  </action>
  <verify>
    <automated>grep -c 'acculynx_write_probe' scripts/acculynx-write-sweep-reconcile.sql # expect >= 2; then: grep -c 'acculynx_write_checklist' scripts/acculynx-write-sweep-reconcile.sql # expect >= 1</automated>
  </verify>
  <acceptance_criteria>
    - File references `acculynx_write_probe` and `acculynx_write_checklist` (not the read-side tables).
    - Contains three `union all`-joined assertions (unreconciled_op, 2xx-null-summary, non_sandbox_row).
    - The 2xx assertion uses a range (`between 200 and 299`), not a bare `= 200`.
    - Header comment says "Phase 4" / "write-sweep gate".
  </acceptance_criteria>
  <done>Reconcile SQL mirrors the read gate, retargeted to the write tables, with 2xx-generalized shape check and sandbox-only assertion.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 5: [BLOCKING] Apply migrations 182 + 183 to prod Supabase and verify tables exist live</name>
  <files>schemas/cleverwork-roofer/182-acculynx-write-catalog-ddl.sql, schemas/cleverwork-roofer/183-acculynx-write-checklist-seed.sql</files>
  <read_first>
    - CLAUDE.md hard rule 1 (additive/idempotent) + "Live ⇄ Dev alignment" (shared prod DB rnhmvcpsvtqjlffpsayu; migrations applied there are live for both dev and live)
    - 04-RESEARCH.md §"Environment Availability" (prod project rnhmvcpsvtqjlffpsayu confirmed available)
    - The planning-context schema_apply_requirement (apply via Supabase MCP apply_migration/execute_sql — NOT `supabase db push`; verify via information_schema, not just that the .sql file exists)
  </read_first>
  <action>
    Apply migrations 182 and 183 to prod Supabase project `rnhmvcpsvtqjlffpsayu` using the Supabase MCP `apply_migration` (or `execute_sql`) tool — NOT `supabase db push`, per this repo's convention. Apply 182 first (tables), then 183 (checklist table + 38-row seed). This MUST happen after the SQL files are written (Tasks 1-2) but BEFORE any sweep run inserts into these tables (Wave 3) and before the reconcile gate runs. Because DDL is idempotent (`if not exists` / `on conflict do nothing`), re-application is safe. This is a checkpoint because applying to the shared prod DB is a state-changing operation on the client brain that the operator should confirm — but the agent performs the apply itself via MCP; the human confirms it landed.
  </action>
  <verify>
    <automated>echo "Live-DB existence check — run via Supabase MCP execute_sql against rnhmvcpsvtqjlffpsayu: select table_name from information_schema.tables where table_schema='public' and table_name in ('acculynx_write_catalog','acculynx_write_probe','acculynx_write_checklist'); -- expect 3 rows. Then: select count(*) from public.acculynx_write_checklist; -- expect 38."</automated>
  </verify>
  <acceptance_criteria>
    - `information_schema.tables` in prod `rnhmvcpsvtqjlffpsayu` returns all three of `acculynx_write_catalog`, `acculynx_write_probe`, `acculynx_write_checklist` (3 rows).
    - `select count(*) from public.acculynx_write_checklist` returns 38 in the live DB.
    - No destructive statement executed (idempotent apply only).
  </acceptance_criteria>
  <resume-signal>Type "applied" once the information_schema check returns 3 tables and the checklist count is 38, or report the error.</resume-signal>
  <done>Tables acculynx_write_catalog, acculynx_write_probe, acculynx_write_checklist exist in prod with 38 checklist rows — verified via information_schema, not just the .sql file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Edge Function → AccuLynx API | Sandbox key crosses here; misresolution to a prod key would write to a live client account |
| Edge Function → prod Postgres | Probe/catalog rows written here; PII in payload samples must be redacted before crossing |
| Migration file → prod DB | DDL applied to the shared client brain; a destructive statement would violate hard rule 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-sandbox | Tampering / Elevation of Privilege | sweep.ts assertSandbox | mitigate | Code-level `assertSandbox()` copied verbatim from the proven read-sweep gate; unit test (Task 3) asserts it throws for all 8 production key names + "" + generic name, accepts only PE_CC_SANDBOX_ACCULYNX_API_KEY |
| T-04-pii | Information Disclosure | sweep.ts redactSample | mitigate | `redactSample()` reused verbatim; Task 3 test asserts firstName/lastName/email/phone/street1 masked before any payload/request sample is stored |
| T-04-secret | Information Disclosure | sweep.ts / migration files | mitigate | Only the secret NAME string appears in code; acceptance criteria (Task 3) forbids a literal key value; reconcile companion grep (Task 4) scans schemas/scripts for leaked key patterns |
| T-04-ddl | Tampering | migration 182/183 | mitigate | Additive/idempotent only; Task 1 verify greps for DROP/TRUNCATE and expects 0; live apply (Task 5) is `if not exists` / `on conflict do nothing` |
| T-04-SC | Tampering | package installs | accept | No new packages this phase — only reused jsr:@supabase/supabase-js@2 already running in prod (RESEARCH Package Legitimacy Audit is a no-op); no install task, so no legitimacy checkpoint needed |
</threat_model>

<verification>
- `deno test supabase/functions/acculynx-write-sweep/sweep.test.ts` exits 0.
- Migrations 182/183 grep clean for destructive statements.
- Live DB: 3 write tables exist in `information_schema`; `acculynx_write_checklist` count = 38.
- Reconcile SQL references the write tables.
</verification>

<success_criteria>
- Tables `acculynx_write_catalog`, `acculynx_write_probe`, `acculynx_write_checklist` exist in prod with 38 checklist rows (live-DB verified).
- `sweep.ts` pure core is green: hard gate, redaction, stop-rule, address builders all tested.
- Reconcile gate SQL exists, retargeted to the write tables.
- All artifacts additive/idempotent per hard rule 1.
</success_criteria>

<output>
Create `.planning/phases/04-sandbox-write-capability-exploration-red-team/04-01-SUMMARY.md` when done.
</output>
