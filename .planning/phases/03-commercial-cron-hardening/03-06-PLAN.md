---
phase: 03-commercial-cron-hardening
plan: 06
type: execute
wave: 5
depends_on: ["03-02", "03-03", "03-04", "03-05"]
files_modified:
  - docs/knowledge-base/acculynx/ingestion/runbook.md
autonomous: false
requirements: [REQ-07]
user_setup:
  - service: acculynx
    why: "Enable production fan-out to the 6 remaining location accounts (florida, colorado, georgia, texas, insurance_program, multi_family_commercial) - requires setting their Edge Function API-key secrets, a human-approved scope step (D-08)"
    env_vars:
      - name: PE_CC_FLORIDA_ACCULYNX_API_KEY
        source: "AccuLynx account API key; exact env_secret_name per the acculynx_accounts registry row for each account"
      - name: PE_CC_COLORADO_ACCULYNX_API_KEY
        source: "AccuLynx account API key (env_secret_name from acculynx_accounts)"
      - name: PE_CC_GEORGIA_ACCULYNX_API_KEY
        source: "AccuLynx account API key (env_secret_name from acculynx_accounts)"
      - name: PE_CC_TEXAS_ACCULYNX_API_KEY
        source: "AccuLynx account API key (env_secret_name from acculynx_accounts)"
      - name: PE_CC_INSURANCE_PROGRAM_ACCULYNX_API_KEY
        source: "AccuLynx account API key (env_secret_name from acculynx_accounts)"
      - name: PE_CC_MULTI_FAMILY_COMMERCIAL_ACCULYNX_API_KEY
        source: "AccuLynx account API key (env_secret_name from acculynx_accounts)"

must_haves:
  truths:
    - "The expansion gate is proven OPEN before any secret is set: KC + Wichita reconcile within tolerance (delta_pct <= 2) on ALL synced resources AND jobs last_api_count stores the true total (D-07/D-08)"
    - "One canary account is enabled first, reconciles within tolerance, before the remaining five are enabled (D-09)"
    - "No secret is set until an explicit human approval of the scope step (D-08)"
    - "After all six are enabled, the serial hourly loop stays within the 110s budget and 30 req/s IP limit (D-01), resuming via watermarks across runs"
  artifacts:
    - path: "docs/knowledge-base/acculynx/ingestion/runbook.md"
      provides: "Runbook updated with the expansion procedure + canary-then-batch record"
      contains: "canary"
  key_links:
    - from: "v_acculynx_reconciliation (KC+Wichita delta_pct <= 2, all resources)"
      to: "the D-08 human approval to set the 6 secrets"
      via: "gate query must pass before approval"
      pattern: "delta_pct"
    - from: "acculynx_accounts registry env_secret_name"
      to: "Edge Function secrets set for the 6 accounts"
      via: "each account's registered secret name is what gets set"
      pattern: "env_secret_name"
---

<objective>
The human-gated 6-account expansion (D-08 gate + D-09 canary-then-batch). This is the LAST wave: it does
NOT proceed until the reconciliation gate is provably open, then brings the remaining six production
accounts online one canary at a time. Setting the secrets is the physical act that enables fan-out to an
account (the edge function can only sync accounts whose key is in Deno.env).

Purpose: completes REQ-07's "all accounts" scope and the Phase-2 carry-forward 6-account expansion under
the mandated human-approved, tolerance-gated, canary-first discipline. Rushing this (enabling all 6
before KC+Wichita prove tolerance, or before last_api_count is real) would fan out an unproven pipeline
across 8 accounts hourly - exactly what the gate prevents.
Output: 6 accounts syncing hourly within tolerance; runbook records the expansion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-commercial-cron-hardening/03-CONTEXT.md
@.planning/phases/03-commercial-cron-hardening/03-RESEARCH.md
@.planning/phases/03-commercial-cron-hardening/03-PATTERNS.md
@.planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md
@schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql
@schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql
@supabase/functions/acculynx-sync/lib/accounts.ts
@scripts/acculynx-reconcile-check.sql
@docs/knowledge-base/acculynx/ingestion/runbook.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Prove the expansion gate is OPEN (D-08) before any secret is set</name>
  <what-built>
    The hardened hourly pipeline (plans 02-05) has been running; KC+Wichita backfill has been draining. The
    D-08 gate must be proven OPEN with live queries before any of the 6 secrets is touched. This is a
    read-only proof, no changes.
  </what-built>
  <action>Run the read-only D-08 gate queries against prod via the Supabase MCP and present the OPEN/CLOSED result to the user per <how-to-verify>. Set NO secret in this task. Blocking human-gated.</action>
  <read_first>
    - .planning/phases/03-commercial-cron-hardening/03-CONTEXT.md D-07/D-08/D-09 (gate definition + canary-then-batch)
    - schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql (v_acculynx_reconciliation.delta_pct)
    - scripts/acculynx-reconcile-check.sql (the reconcile-check query shape; here the threshold is 2, not 5)
    - .planning/phases/03-commercial-cron-hardening/03-02-SUMMARY.md (confirmation jobs last_api_count is real, not 1)
  </read_first>
  <how-to-verify>
    Run against `rnhmvcpsvtqjlffpsayu` via Supabase MCP, read-only:
    1. `select account_key, resource_type, delta_pct from public.v_acculynx_reconciliation where account_key in
       ('kansas_city','wichita') order by account_key, resource_type;` - EVERY synced resource for both accounts
       must show `delta_pct <= 2` (D-07). If any resource is still backfilling (>2%), the gate is CLOSED - do
       NOT proceed; the hourly cron must drain further first.
    2. `select account_key, resource_type, last_api_count from public.acculynx_sync_watermark where
       resource_type='jobs' and account_key in ('kansas_city','wichita');` - both must show a real total, NOT 1
       (D-08 prerequisite). If still 1, the gate is CLOSED - the plan-02 last_api_count follow-up must land first.
    3. Present the gate result to the user. Setting the 6 secrets is a human-approved scope step - do NOT set
       any secret in this task. This checkpoint only proves the gate and requests approval to proceed to Task 2.
  </how-to-verify>
  <acceptance_criteria>
    - Every (kansas_city|wichita, resource) row in `v_acculynx_reconciliation` shows `delta_pct <= 2` (recorded).
    - jobs `last_api_count` for kansas_city and wichita is a real total, not `1` (recorded).
    - The gate result (OPEN/CLOSED) is presented to the user; NO secret has been set in this task.
    - If CLOSED, the checkpoint reports which resource(s) block the gate and stops (does not proceed to Task 2).
  </acceptance_criteria>
  <resume-signal>Type "approved - expand" to authorize the scope step (Task 2), or "hold" if the gate is closed / you want to wait for more backfill.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Canary-then-batch enable the 6 accounts (D-09), verify each reconciles + budget holds</name>
  <what-built>
    Gate proven open in Task 1 and human-approved. Now set Edge Function secrets to physically enable fan-out,
    one canary first (D-09), confirm it reconciles within tolerance and the serial loop stays within the 110s
    budget / 30 req/s IP limit, then enable the remaining five.
  </what-built>
  <action>After gate approval, set the canary account's Edge secret first, verify its run, then set the remaining five; verify per-account reconciliation, zero bleed, and 110s budget per <how-to-verify>. Blocking human-gated scope step (D-08/D-09).</action>
  <read_first>
    - .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md (the exact env_secret_name registered per account in acculynx_accounts)
    - schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql (the 9-key registry - environment='production' rows the fan-out loads)
    - supabase/functions/acculynx-sync/lib/accounts.ts (loadProductionAccounts + resolveKey - a key absent from Deno.env means that account is skipped; setting it enables it)
    - .planning/phases/03-commercial-cron-hardening/03-RESEARCH.md Pitfall 4 (110s budget once 8 accounts + job-walk are all hourly)
    - docs/knowledge-base/acculynx/ingestion/runbook.md (append the expansion record here)
  </read_first>
  <how-to-verify>
    Human-required (Claude cannot read AccuLynx dashboard keys): the user provides / sets the 6 Edge Function
    secrets using the exact `env_secret_name` values from acculynx_accounts. Claude drives via CLI/API where it
    can and pauses only for the actual key values. Explain-then-ship each step.
    CANARY (D-09): set ONLY the first account's secret (recommend the smallest-volume account per
    03-LIVE-STATE.md). Wait for / trigger one hourly run. Verify: (a) `select account_key, resource_type,
    delta_pct from v_acculynx_reconciliation where account_key='<canary>';` trends toward or within 2% as it
    backfills; (b) no cross-account bleed - `select distinct account_key from acculynx_jobs;` shows the canary
    stamped correctly; (c) the run stays within budget - check crm_sync_log api_response_ms < 110000 and
    v_acculynx_cron_outcomes shows success (not timeout). If the serial loop exceeds the 110s budget with the
    canary added, STOP - the bounded-backfill-slice (D-02) pacing must absorb it across runs, not fail; adjust
    slice size before enabling more.
    BATCH: once the canary is proven, set the remaining five secrets. Confirm all 6 appear in
    v_acculynx_reconciliation and each trends within tolerance over subsequent hourly runs; confirm no bleed and
    budget still holds. Append a "6-account expansion (canary-then-batch)" record to the runbook noting which
    account was the canary, the per-account first-run outcome, and any slice-size adjustment.
  </how-to-verify>
  <acceptance_criteria>
    - The canary account's secret was set FIRST and its run verified (delta trending within/toward 2%, correct account_key stamping, no bleed, api_response_ms < 110000) before any other secret was set.
    - All six secrets are set using the registered `env_secret_name` values; no key value echoed to logs or docs.
    - `select distinct account_key from acculynx_jobs;` shows the newly-enabled accounts stamped correctly with zero cross-account bleed.
    - Each of the 6 accounts appears in `v_acculynx_reconciliation` and the hourly serial loop stays within the 110s budget (crm_sync_log / v_acculynx_cron_outcomes show success, not timeout).
    - The runbook records the canary choice, first-run outcomes, and any backfill-slice adjustment.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the canary is proven and all six accounts are enabled and reconciling within budget, or describe the failure (budget overrun / bleed / delta stuck).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AccuLynx dashboard API keys (human-held) -> Edge Function secrets | Six new production keys cross into Deno.env; never into code, logs, or committed files (hard rule 2) |
| 6 newly-enabled accounts -> shared acculynx_* tables | Each row must be stamped with the correct account_key/market; zero cross-account bleed (enforced by 178 NOT NULL + per-account watermark) |
| serial hourly loop -> shared 30 req/s IP limit | Adding 6 accounts must not breach the shared IP rate limit or the 110s budget (D-01) |

## STRIDE Threat Register (maps to RESEARCH security domain)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Information Disclosure | 6 new API keys during the expansion | mitigate | Keys set only as Edge secrets via dashboard/CLI; resolveKey reads Deno.env only; no value echoed to logs/docs; runbook records env_secret_name NAMES only (hard rule 2) |
| T-03-16 | Tampering / cross-account bleed | 6 accounts writing to shared tables | mitigate | 178 NOT NULL account_key/market + per-account watermark (mig 171) + Task 2 `distinct account_key` bleed check; canary-first (D-09) limits blast radius |
| T-03-17 | Denial of Service (rate-limit / budget overrun) | serial hourly loop across 8 accounts + job-walk | mitigate | Canary-first proves budget before batch; D-02 bounded-backfill-slice + per-page watermark resume absorbs overflow across runs; D-01 serial loop (no concurrent fan-out) respects the 30 req/s IP limit (RESEARCH Pitfall 4) |
| T-03-18 | Elevation of Privilege (premature scope) | enabling accounts before the pipeline is proven | mitigate | D-08 gate (Task 1) proves KC+Wichita within tolerance + last_api_count real BEFORE any secret is set; human approval required to proceed |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Task 1: D-08 gate proven open live (all KC+Wichita resources delta_pct <= 2, jobs last_api_count real) - no secret set.
- Task 2: canary enabled + verified before batch; all 6 stamped correctly, zero bleed, within 110s budget; runbook updated.
</verification>

<success_criteria>
- SC1 (full scope): the hourly scheduler runs for ALL production accounts within per-key rate limits.
- Carry-forward: fan-out expanded to the remaining 6 accounts under the human-approved, tolerance-gated, canary-then-batch discipline.
</success_criteria>

<output>
Create `.planning/phases/03-commercial-cron-hardening/03-06-SUMMARY.md` when done.
</output>
