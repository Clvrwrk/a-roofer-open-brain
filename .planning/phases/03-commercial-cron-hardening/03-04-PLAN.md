---
phase: 03-commercial-cron-hardening
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - schemas/cleverwork-roofer/177-acculynx-rls-deny-by-default.sql
  - schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql
  - schemas/cleverwork-roofer/179-acculynx-rot-guard-views.sql
  - schemas/cleverwork-roofer/180-acculynx-legacy-null-row-triage.sql
autonomous: false
requirements: [REQ-07]
user_setup: []

must_haves:
  truths:
    - "anon and authenticated cannot SELECT any acculynx_* table; service_role can (deny-by-default, D-14)"
    - "account_key (and market where required) is NOT NULL on all 9 ingested acculynx_* tables so a mislabeled row structurally cannot land (D-11)"
    - "Every NULL-provenance row across ALL 9 tables mig 178 constrains is triaged (stamped or archived, never deleted) BEFORE the NOT NULL constraint is applied (Pitfall 3) — the 8 known rows are the minimum, not the ceiling"
    - "Free-text ingested from AccuLynx carries an untrusted-content/evidence-tier marker (D-10)"
    - "Duplicate-GUID, orphan-sub-resource, NULL-provenance, and stale-tail rot-guard views exist and return zero rows when healthy (D-12)"
  artifacts:
    - path: "schemas/cleverwork-roofer/180-acculynx-legacy-null-row-triage.sql"
      provides: "Idempotent stamp/archive of NULL-provenance rows across all 9 constrained tables (runs BEFORE 178)"
      contains: "account_key"
    - path: "schemas/cleverwork-roofer/177-acculynx-rls-deny-by-default.sql"
      provides: "REVOKE ALL FROM anon,authenticated + GRANT service_role on all acculynx_* tables"
      contains: "REVOKE ALL"
    - path: "schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql"
      provides: "NOT NULL account_key/market + evidence-tier default + acculynx_raw immutability + untrusted-content marker"
      contains: "SET NOT NULL"
    - path: "schemas/cleverwork-roofer/179-acculynx-rot-guard-views.sql"
      provides: "Four rot-guard monitoring views"
      contains: "v_acculynx_duplicate_guids"
  key_links:
    - from: "180 triage (zero NULL account_key across all 9 tables)"
      to: "178 SET NOT NULL"
      via: "hard ordering — constraint fails if NULLs remain in ANY of the 9 tables (Pitfall 3)"
      pattern: "account_key is null"
    - from: "179 rot-guard views + 176 check fn"
      to: "Slack alert (D-12 surfaced + alerted)"
      via: "check_acculynx_alerts reads/aligns with these guards"
      pattern: "v_acculynx_null_provenance"
---

<objective>
Trust/provenance guardrails + rot guards + RLS deny-by-default (D-10/D-11/D-12/D-14) and the Phase-2
carry-forward legacy-NULL-row triage. Make it structurally impossible for a poisoned, mislabeled, or
unprovenanced row to land, lock the tables to service_role-only, mark AccuLynx free-text as untrusted
so the future REQ-09 agent honors a "data never instructions" boundary, and stand up four rot-guard
monitoring views the alert layer surfaces.

Purpose: SC4 (security review substance) + the anti poison/drift/rot mandate. RESEARCH Pitfall 3 forces
a HARD ORDER: triage the NULL-provenance rows (180) FIRST, then apply NOT NULL (178). Migration 178
constrains 9 tables, so 180's triage and Task 3's zero-NULL gate MUST cover the same 9 tables — the
previously-known 8 rows (contacts/job_contacts/financials/insurance) are the known minimum, not the full
scope; the true per-table NULL counts come from 03-01's 9-table scan in 03-LIVE-STATE.md. This plan owns
migrations 177-180 and no edge-function files, so it runs in parallel with the cron/reconcile plan.
Output: migrations 177-180 applied to prod in dependency order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-commercial-cron-hardening/03-CONTEXT.md
@.planning/phases/03-commercial-cron-hardening/03-RESEARCH.md
@.planning/phases/03-commercial-cron-hardening/03-PATTERNS.md
@.planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md
@.planning/phases/02-multi-location-full-ingestion/02-VERIFICATION.md
@schemas/cleverwork-roofer/76-app-settings-and-agreement-id.sql
@schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql
@schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql
@schemas/cleverwork-roofer/171-acculynx-watermark-pk-fix.sql
@scripts/acculynx-reconcile-check.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 180 (legacy NULL-row triage across all 9 tables) then 178 (trust invariants) in strict order</name>
  <files>schemas/cleverwork-roofer/180-acculynx-legacy-null-row-triage.sql, schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql</files>
  <read_first>
    - .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md (OQ3b — confirmed live NULL-row counts for ALL 9 tables mig 178 will constrain; this is the authoritative scope, not the 8-row Phase-2 claim)
    - .planning/phases/02-multi-location-full-ingestion/02-VERIFICATION.md (the 8 known rows: 3 contacts, 3 job_contacts, 1 financial, 1 insurance — the known MINIMUM)
    - schemas/cleverwork-roofer/171-acculynx-watermark-pk-fix.sql (surgical idempotent live-data-fix precedent + hard-rule-1 footer)
    - schemas/cleverwork-roofer/169-acculynx-resource-tables-ddl.sql lines 13-15 + 232-237 (nullable account_key/market columns + the "follow-up constraint will enforce NOT NULL" deferral this migration fulfills)
    - .planning/phases/03-commercial-cron-hardening/03-PATTERNS.md lines 211-247 (178 assignment: sequencing note, ALTER COLUMN SET NOT NULL guarded by NULL-count check) and lines 284-301 (Pitfall 3)
    - .planning/phases/03-commercial-cron-hardening/03-RESEARCH.md §Security Domain (D-10 untrusted-content marker; check schemas/ob1-base/ for an existing trust_tier/data_source convention to reuse)
  </read_first>
  <action>
    Migration 180 FIRST (data fix, no schema DROP): triage EVERY NULL-provenance row across ALL 9 tables that
    migration 178 will constrain — `acculynx_jobs, acculynx_contacts, acculynx_job_contacts, acculynx_estimates,
    acculynx_invoices, acculynx_invoice_lines, acculynx_job_financials, acculynx_job_insurance,
    acculynx_job_milestone_history`. For each table, `UPDATE ... WHERE account_key IS NULL` to stamp a
    provenance value OR archive via the existing update-only archive idiom (`archived_at`/`archive_reason`,
    NEVER `.delete()` — hard rule 1). Use the confirmed provenance from 03-LIVE-STATE.md's 9-table scan; if
    provenance is genuinely unrecoverable (sandbox artifacts), archive rather than guess (mark `archive_reason =
    'phase3-legacy-null-provenance-triage'`). Do NOT hardcode "the 8 rows" as the only scope — treat 8 as the
    known minimum: emit a `WHERE account_key IS NULL` (and, where `market` is a required column on that table,
    `market IS NULL`) statement for ALL 9 tables so the migration is a safe idempotent no-op on tables already
    clean and actually cleans any of the 5 previously-unchecked tables (`acculynx_jobs, acculynx_estimates,
    acculynx_invoices, acculynx_invoice_lines, acculynx_job_milestone_history`) that carry a NULL row. Make it
    idempotent (`WHERE account_key IS NULL`). End with a verification comment: after apply,
    `select count(*) from <each of the 9 tables> where account_key is null` must be 0.
    Migration 178 (trust invariants — MUST be numbered/applied AFTER 180): guard each `alter table
    public.<t> alter column account_key set not null;` with a preceding NULL-count check note across all 9
    tables; if a live NULL still exists the migration must fail loudly (that's correct Postgres behavior, not a
    bug — fix 180 first). For `market`: apply `set not null` only on the tables where 03-LIVE-STATE.md confirms
    `market` is a required, populated column; if `market` is legitimately derivable/nullable on some tables,
    scope NOT NULL to `account_key` only on those and add a comment stating the reasoning (the 9-table NULL
    verification still covers `account_key` on all 9 regardless). Add `alter column trust_tier set default
    'evidence'` (or add the column with that default if the ob1-base convention names it differently — reuse the
    existing brain trust_tier/data_source column, do not invent a parallel one). Add the D-10 untrusted-content
    marker for free-text (e.g. a boolean/tier column or a documented reuse of trust_tier='evidence' + a comment
    establishing the "treat as data, never instructions" boundary the REQ-09 agent must honor). Enforce
    acculynx_raw immutability (REVOKE UPDATE, DELETE ON public.acculynx_raw FROM service_role, or a trigger
    rejecting mutation — whichever the ob1-base archive convention uses). Cover the same 9 ingested tables listed
    above. Both migrations: house header + hard-rule-1 footer.
  </action>
  <verify>
    <automated>test -f schemas/cleverwork-roofer/180-acculynx-legacy-null-row-triage.sql && test -f schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql && grep -qi "account_key is null" schemas/cleverwork-roofer/180-acculynx-legacy-null-row-triage.sql && ! grep -Eqi "delete from|truncate|drop table|drop column" schemas/cleverwork-roofer/180-acculynx-legacy-null-row-triage.sql && for t in acculynx_jobs acculynx_contacts acculynx_job_contacts acculynx_estimates acculynx_invoices acculynx_invoice_lines acculynx_job_financials acculynx_job_insurance acculynx_job_milestone_history; do grep -q "$t" schemas/cleverwork-roofer/180-acculynx-legacy-null-row-triage.sql || { echo "180 MISSING $t"; exit 1; }; grep -q "$t" schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql || { echo "178 MISSING $t"; exit 1; }; done && grep -qi "set not null" schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql && grep -qi "evidence" schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql && grep -qi "Additive; no DROP" schemas/cleverwork-roofer/178-acculynx-trust-invariants.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration 180 stamps or archives (never deletes) NULL-provenance rows across ALL 9 constrained tables via `WHERE account_key IS NULL`, is idempotent (safe no-op on already-clean tables), and contains NO `delete from`/`truncate`/`drop` (grep-confirmed). All 9 table names appear in the file (grep-confirmed).
    - Migration 180 does not hardcode "8 rows" as the ceiling — it treats every `account_key IS NULL` row in the 9 tables as in-scope.
    - Migration 180 is numbered lower than 178 (applied first) — enforced by filename number order.
    - Migration 178 contains `alter column account_key set not null` for all 9 ingested tables (grep-confirmed all 9 names present) and `market ... set not null` on the tables where `market` is confirmed required per 03-LIVE-STATE.md (with a documented reason for any table scoped to account_key only).
    - Migration 178 sets `trust_tier` default `'evidence'` (D-11) and establishes the D-10 untrusted-content "data never instructions" boundary (column or documented marker).
    - Migration 178 enforces acculynx_raw immutability (REVOKE UPDATE/DELETE from service_role OR a reject trigger).
    - Both files end with the hard-rule-1 footer comment.
  </acceptance_criteria>
  <done>NULL-provenance rows across all 9 tables are triaged first; provenance/trust invariants are structural on all 9; free-text is marked untrusted — a poisoned or mislabeled row structurally cannot land in any constrained table.</done>
</task>

<task type="auto">
  <name>Task 2: Write migration 177 (RLS deny-by-default) + 179 (four rot-guard views)</name>
  <files>schemas/cleverwork-roofer/177-acculynx-rls-deny-by-default.sql, schemas/cleverwork-roofer/179-acculynx-rot-guard-views.sql</files>
  <read_first>
    - .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md (OQ2 result: which acculynx_* tables already have RLS enabled / which grant SELECT to authenticated — determines enable-and-revoke vs revoke-only)
    - schemas/cleverwork-roofer/76-app-settings-and-agreement-id.sql lines 32-34 (exact deny-by-default block to copy)
    - schemas/cleverwork-roofer/170-acculynx-reconciliation-view.sql (CREATE OR REPLACE VIEW + comment-on-view + grant tail for the rot-guard views)
    - scripts/acculynx-reconcile-check.sql (WHERE account_key IS NULL clause to turn into the NULL-provenance view)
    - .planning/phases/03-commercial-cron-hardening/03-PATTERNS.md lines 172-247 (177 CRITICAL "must REVOKE not just ENABLE" + the 14-table list) and lines 234-247 (179 four-view shapes) and lines 455-465 (shared RLS pattern note)
  </read_first>
  <action>
    Migration 177 (RLS): for EACH acculynx_* table — the 12 from mig 169 (acculynx_jobs, acculynx_contacts,
    acculynx_job_contacts, acculynx_contact_emails, acculynx_contact_phones, acculynx_estimates,
    acculynx_invoices, acculynx_invoice_lines, acculynx_job_financials, acculynx_job_insurance,
    acculynx_insurance_carriers, acculynx_job_milestone_history) PLUS acculynx_accounts and
    acculynx_sync_watermark — emit the 76-pattern block: `ALTER TABLE public.<t> ENABLE ROW LEVEL SECURITY;`
    (idempotent — safe if already enabled per 03-LIVE-STATE.md) then `REVOKE ALL ON public.<t> FROM anon,
    authenticated;` (the missing half — existing migs only GRANTed SELECT to authenticated) then `GRANT ALL ON
    public.<t> TO service_role;`. Do NOT touch acculynx_cron_dispatch here (plan 02 mig 173 already applied
    deny-by-default to it). Header comment must cite that dashboard reads are safe because supabase.server.ts
    always uses SUPABASE_SERVICE_ROLE_KEY (service_role bypasses RLS). Do NOT use the 127 CREATE POLICY pattern
    (D-14 wants service-role-only, not authenticated read).
    Migration 179 (rot guards): four `CREATE OR REPLACE VIEW` + comment-on-view + `grant select ... to
    authenticated, service_role`: (1) `v_acculynx_duplicate_guids` — per-table GROUP BY id HAVING count(*)>1,
    UNION ALL across the resource tables using each table's AccuLynx GUID column; (2)
    `v_acculynx_orphan_subresources` — sub-resource rows (job_contacts, financials, insurance,
    milestone_history, invoices) whose parent job/contact id is not present in acculynx_jobs/acculynx_contacts;
    (3) `v_acculynx_null_provenance` — rows WHERE account_key IS NULL across the ingested tables (same class as
    the 8 legacy rows; should be empty after 180); (4) `v_acculynx_stale_tail` — rows not re-seen across N full
    sweeps that markNotSeen did not archive (last_seen_at older than N sweeps AND archived_at IS NULL). Each
    returns zero rows when healthy. Both migrations: house header + hard-rule-1 footer.
  </action>
  <verify>
    <automated>test -f schemas/cleverwork-roofer/177-acculynx-rls-deny-by-default.sql && test -f schemas/cleverwork-roofer/179-acculynx-rot-guard-views.sql && grep -c "REVOKE ALL" schemas/cleverwork-roofer/177-acculynx-rls-deny-by-default.sql | awk '$1>=10{exit 0} {exit 1}' && grep -q "acculynx_jobs" schemas/cleverwork-roofer/177-acculynx-rls-deny-by-default.sql && for v in v_acculynx_duplicate_guids v_acculynx_orphan_subresources v_acculynx_null_provenance v_acculynx_stale_tail; do grep -q "$v" schemas/cleverwork-roofer/179-acculynx-rot-guard-views.sql || { echo "MISSING $v"; exit 1; }; done && grep -qi "Additive; no DROP" schemas/cleverwork-roofer/179-acculynx-rot-guard-views.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Migration 177 emits `REVOKE ALL ON public.<t> FROM anon, authenticated` for at least the 14 acculynx_* tables (grep count ≥ 10, all named tables present).
    - Migration 177 does NOT touch `acculynx_cron_dispatch` (owned by plan 02) and does NOT use `CREATE POLICY` (deny-by-default, not policy-based).
    - Migration 179 defines all four views: `v_acculynx_duplicate_guids`, `v_acculynx_orphan_subresources`, `v_acculynx_null_provenance`, `v_acculynx_stale_tail`, each with a `grant select ... to authenticated, service_role` tail.
    - Both migrations end with the hard-rule-1 footer comment.
  </acceptance_criteria>
  <done>All acculynx_* tables are deny-by-default service-role-only; four rot-guard views exist and are healthy-when-empty.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply 180→177→179→178 to prod via Supabase MCP in order, then verify RLS + zero-NULL (all 9 tables) + rot-guards</name>
  <what-built>
    Migrations 177-180 written. Not live until applied via Supabase MCP. STRICT APPLY ORDER: 180 (triage) →
    177 (RLS) → 179 (views) → 178 (NOT NULL constraints LAST, after 180 zeroed the NULLs). Applying 178 before
    180 will fail with "column account_key contains null values" (Pitfall 3) — that's correct, not a bug. 178
    constrains 9 tables, so the pre-apply zero-NULL gate below MUST assert zero NULLs across all 9, not just 4.
  </what-built>
  <action>Explain-then-ship, then apply migrations in STRICT order 180, 177, 179, 178 to prod via the Supabase MCP `apply_migration` (180 must zero the NULLs across all 9 tables before 178's NOT NULL constraint), then run the RLS + all-9-table zero-NULL + rot-guard verification in <how-to-verify>. Blocking human-gated.</action>
  <read_first>
    - .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md (baseline NULL counts for all 9 tables + RLS status to confirm the change took)
    - scripts/acculynx-reconcile-check.sql (the WHERE account_key IS NULL check pattern)
    - CONVENTIONS.md §13 (explain-then-ship)
  </read_first>
  <how-to-verify>
    Explain-then-ship: state the change (lock tables to service-role-only; enforce provenance NOT NULL on all 9
    ingested tables; add rot guards), impact (anon/authenticated lose direct SELECT — dashboard unaffected, uses
    service_role), rollback (RLS revoke is reversible via GRANT; NOT NULL reversible via DROP NOT NULL; no data
    destroyed — 180 archives, never deletes). Apply via Supabase MCP in order 180, 177, 179, 178. After apply:
    1. Zero-NULL gate (must pass before 178 was applied) — assert across ALL 9 tables mig 178 constrains:
       `select count(*) from <table> where account_key is null;` for EACH of
       `acculynx_jobs, acculynx_contacts, acculynx_job_contacts, acculynx_estimates, acculynx_invoices,
       acculynx_invoice_lines, acculynx_job_financials, acculynx_job_insurance, acculynx_job_milestone_history`
       → all 0. (For any table where 178 also constrains `market NOT NULL`, additionally assert
       `count(*) where market is null` = 0 on that table.)
    2. RLS gate (D-14): attempt a SELECT on an acculynx_* table with the ANON key (expect denied/empty) and
       with the SERVICE_ROLE key (expect rows). Record both results in the security posture doc (plan 05, D-13).
    3. Constraint gate: confirm 178 applied without error (proves 180 zeroed the NULLs across all 9 tables).
    4. Rot-guard gate: `select count(*) from v_acculynx_null_provenance;` → 0; seed one known-bad row
       (e.g., a duplicate GUID) via service-role, confirm `v_acculynx_duplicate_guids` surfaces it, then remove
       the seed (update-only/archive, never delete).
    Record the anon-vs-service-role RLS result for the plan-05 security posture doc.
  </how-to-verify>
  <acceptance_criteria>
    - Migrations applied to `rnhmvcpsvtqjlffpsayu` in order 180 → 177 → 179 → 178 (178 succeeded, proving zero NULLs first).
    - `select count(*) ... where account_key is null` returns 0 for ALL 9 constrained tables (`acculynx_jobs, acculynx_contacts, acculynx_job_contacts, acculynx_estimates, acculynx_invoices, acculynx_invoice_lines, acculynx_job_financials, acculynx_job_insurance, acculynx_job_milestone_history`) — plus `market IS NULL` = 0 on any table 178 constrains `market`.
    - Anon-key SELECT on an acculynx_* table is denied/empty; service-role-key SELECT returns rows — both recorded for the security posture doc.
    - `v_acculynx_null_provenance` returns 0 rows; a seeded duplicate GUID is surfaced by `v_acculynx_duplicate_guids` (then the seed archived, not deleted).
  </acceptance_criteria>
  <resume-signal>Type "approved" once all four gates pass (zero-NULL across all 9 tables) and the anon-vs-service-role RLS result is recorded, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authenticated (public web roles) → acculynx_* tables (PII: contact names, phones, addresses) | Must be fully denied; only service_role crosses |
| AccuLynx API free-text (untrusted external input) → brain atoms later read by an LLM agent | Must be labeled untrusted-content/evidence-tier so it is never treated as instructions (D-10) |
| ingest writes → acculynx_raw archive | Archive must be immutable; no post-hoc mutation of source-of-truth raw |

## STRIDE Threat Register (maps to RESEARCH §Security Domain)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | RLS misconfig exposing acculynx_* PII to anon/public | mitigate | Migration 177 REVOKE ALL FROM anon,authenticated on all acculynx_* (D-14); verified live anon-SELECT-denied in Task 3; dashboard safe (service_role bypasses RLS) |
| T-03-09 | Tampering / cross-account bleed | mislabeled or unprovenanced row landing | mitigate | Migration 178 NOT NULL account_key/market on all 9 ingested tables (structural backstop, D-11); 180 triages every NULL-provenance row across those 9 tables first (Pitfall 3 ordering) — 8-row claim is the minimum, 03-01's 9-table scan is authoritative scope |
| T-03-10 | Elevation of Privilege (untrusted data as instructions) | AccuLynx free-text reaching REQ-09 agent | mitigate | D-10 untrusted-content/evidence-tier marker + documented "data never instructions" boundary in 178; agent-side enforcement deferred to REQ-09 (out of scope) but the label exists now |
| T-03-11 | Tampering (data rot) | duplicate/orphan/null-provenance/stale-tail rows silently degrading the brain | mitigate | Migration 179 four rot-guard views (D-12); check_acculynx_alerts (plan 03) surfaces + alerts on them |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- 177-180 written to house idempotency standard; 180 has no delete/truncate/drop and covers all 9 tables; 178 has SET NOT NULL + evidence default across all 9.
- Applied in strict order 180→177→179→178 via Supabase MCP under the blocking gate.
- Live: zero NULL provenance across all 9 tables, anon denied / service_role allowed, rot-guard views healthy, seeded bad row surfaced.
</verification>

<success_criteria>
- SC4 substance: RLS deny-by-default confirmed live; provenance/trust invariants structural on all 9 ingested tables; free-text marked untrusted; four rot guards live.
- Carry-forward: every NULL-provenance row across the 9 tables triaged (stamped/archived, never deleted) — the 8 known legacy rows plus any found in the 5 previously-unchecked tables.
</success_criteria>

<output>
Create `.planning/phases/03-commercial-cron-hardening/03-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
