---
phase: 03-commercial-cron-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-acculynx-cron.sql
  - .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md
autonomous: false
requirements: [REQ-07]
user_setup: []

must_haves:
  truths:
    - "The exact current pg_cron acculynx jobname, schedule, and command (does it pass multiAccount:true or the legacy resources array?) are known and recorded"
    - "The current relrowsecurity + anon/authenticated grants on every acculynx_* table are known and recorded"
    - "The current acculynx_sync_watermark.last_api_count values for (kansas_city, jobs) and (wichita, jobs) are known and recorded"
    - "The NULL account_key/market counts for ALL 9 tables migration 178 will constrain are known and recorded (not just the 4 previously-known tables)"
    - "The Slack alert channel + webhook/bot identity for D-05 alerts is confirmed"
    - "A committed, re-runnable cron-schedule assertion script exists"
  artifacts:
    - path: ".planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md"
      provides: "Recorded answers to RESEARCH Open Questions 1-4 (live-DB ground truth)"
      contains: "multiAccount"
    - path: "scripts/verify-acculynx-cron.sql"
      provides: "Re-runnable cron schedule + stuck-pending assertion"
      contains: "cron.job"
  key_links:
    - from: "03-LIVE-STATE.md"
      to: "03-02-PLAN.md (cron cutover migration 172)"
      via: "confirmed real jobname to cron.unschedule + confirmed cutover-needed-or-not"
      pattern: "jobname"
    - from: "03-LIVE-STATE.md"
      to: "03-03-PLAN.md (RLS migration 177)"
      via: "confirmed current RLS/grant status determines enable-vs-audit"
      pattern: "relrowsecurity"
    - from: "03-LIVE-STATE.md (9-table NULL counts)"
      to: "03-04-PLAN.md (mig 180 triage + mig 178 NOT NULL)"
      via: "confirmed per-table NULL account_key counts scope the triage + zero-NULL gate across all 9 tables"
      pattern: "account_key is null"
---

<objective>
Wave 0 live-DB verification. Resolve RESEARCH.md Open Questions 1-4 against the shared prod DB
(`rnhmvcpsvtqjlffpsayu`) via the Supabase MCP BEFORE any cutover/RLS/watermark migration is written,
because build/type checks pass without reflecting live schema (false-positive verification state).
Also create the committed, re-runnable cron-schedule assertion script that later waves gate on.

Purpose: The researcher had no live-DB tool. Every downstream migration (cron jobname to unschedule,
whether the last_api_count bug is real after cutover, whether RLS is un-done or already-enabled,
which Slack channel receives alerts, and the true NULL-provenance scope across all 9 tables mig 178
constrains) depends on facts only the live DB can answer. Getting these wrong writes a migration that
targets a non-existent jobname, duplicates an already-applied change, or fails mid-Wave-4 when mig 178's
NOT NULL hits a NULL row in one of the 5 tables the 4-table triage never covered.
Output: `03-LIVE-STATE.md` (recorded ground truth) + `scripts/verify-acculynx-cron.sql`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-commercial-cron-hardening/03-CONTEXT.md
@.planning/phases/03-commercial-cron-hardening/03-RESEARCH.md
@.planning/phases/03-commercial-cron-hardening/03-PATTERNS.md
@.planning/phases/02-multi-location-full-ingestion/02-VERIFICATION.md
@scripts/acculynx-reconcile-check.sql
@.claude/skills/slack-agents/SKILL.md
@.claude/skills/sentry/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Query live DB for the four open questions and record ground truth</name>
  <files>.planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md</files>
  <read_first>
    - .planning/phases/03-commercial-cron-hardening/03-RESEARCH.md §Open Questions (1-4) and §Assumptions Log (A1, A2, A3) — the exact SQL to run and what "wrong" means for each
    - .planning/phases/03-commercial-cron-hardening/03-PATTERNS.md lines 30-49 (mig 172 "CRITICAL — must resolve via live query") and lines 183-193 (RLS live-status query)
    - .planning/phases/02-multi-location-full-ingestion/02-VERIFICATION.md (the "last_api_count stores 1" and 8-NULL-row claims to confirm/refute live)
    - .planning/phases/03-commercial-cron-hardening/03-04-PLAN.md (mig 178 constrains 9 tables — OQ3b below MUST cover all 9, not just the 4 previously-known)
  </read_first>
  <action>
    Using the Supabase MCP against project `rnhmvcpsvtqjlffpsayu` (execute_sql / list_tables), run and
    record the results of each query into a new file `03-LIVE-STATE.md`:
    (OQ1 cron) `select jobid, jobname, schedule, command from cron.job order by jobname;` AND
    `select prosrc from pg_proc where proname = 'trigger_acculynx_sync';` — record the exact acculynx
    jobname(s), the schedule string, and whether the command passes `{"multiAccount":true}` or a legacy
    resources array like `["users","jobs"]`.
    (OQ2 RLS) `select relname, relrowsecurity from pg_class where relname like 'acculynx_%' and relkind='r';`
    AND grant inspection `select table_name, grantee, privilege_type from information_schema.role_table_grants
    where table_name like 'acculynx_%' and grantee in ('anon','authenticated','service_role') order by table_name, grantee;`
    — record per-table RLS-enabled state and which of anon/authenticated currently hold SELECT.
    (OQ3 watermark) `select account_key, resource_type, last_api_count, last_modified_date, last_sync_at
    from acculynx_sync_watermark order by account_key, resource_type;` — record the actual last_api_count
    for (kansas_city,jobs) and (wichita,jobs) and note whether it is `1`, NULL, or a real total.
    (OQ3b NULL rows — ALL 9 tables migration 178 will constrain) For EACH of the 9 tables
    `acculynx_jobs, acculynx_contacts, acculynx_job_contacts, acculynx_estimates, acculynx_invoices,
    acculynx_invoice_lines, acculynx_job_financials, acculynx_job_insurance, acculynx_job_milestone_history`
    run `select count(*) from <table> where account_key is null;` AND (where the `market` column exists on that
    table) `select count(*) from <table> where market is null;`. Record the per-table count in a table.
    The 4 previously-known tables (contacts=3, job_contacts=3, job_financials=1, job_insurance=1 → 8 total)
    are the KNOWN MINIMUM, not the ceiling — confirm the true count and confirm whether the other 5 tables
    (`acculynx_jobs, acculynx_estimates, acculynx_invoices, acculynx_invoice_lines,
    acculynx_job_milestone_history`) have any NULL `account_key` rows. If `market` is legitimately nullable
    on some tables, note which tables have `market IS NULL` rows so 03-04 can decide whether to scope NOT NULL
    to `account_key` only on those tables.
    (OQ3c pg_net TTL) `show pg_net.ttl;` and `select extversion from pg_extension where extname in ('pg_cron','pg_net');`.
    (OQ4 Slack) From `.claude/skills/slack-agents/SKILL.md` + `.claude/skills/sentry/SKILL.md`, record the
    chosen alert channel (RESEARCH recommends `#cc-proexteriors`, already the Sentry-integration channel)
    and the exact bot identity / webhook env var name that later plans will use — do NOT paste any token value.
    In 03-LIVE-STATE.md add an explicit "Downstream impact" section stating, per finding: whether mig 172
    cutover is still required, whether the last_api_count fix needs a jobs.ts code change or is fixed by the
    cutover alone, whether mig 177 is "enable+revoke" or "audit+revoke-only", and — from the 9-table NULL scan —
    the exact set of tables mig 180 must triage and the exact set mig 178 may safely constrain NOT NULL on.
    Never record a raw key/token.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md && grep -qi "multiAccount" .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md && grep -qi "relrowsecurity\|RLS" .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md && grep -qi "last_api_count" .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md && for t in acculynx_jobs acculynx_contacts acculynx_job_contacts acculynx_estimates acculynx_invoices acculynx_invoice_lines acculynx_job_financials acculynx_job_insurance acculynx_job_milestone_history; do grep -q "$t" .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md || { echo "MISSING NULL-scan row for $t"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `03-LIVE-STATE.md` exists and contains a recorded answer (with the actual SQL output) for each of OQ1, OQ2, OQ3, OQ3b, OQ3c, OQ4.
    - OQ1 section names the EXACT current acculynx cron jobname(s) (the string later `cron.unschedule` will target) and states whether the command currently passes `multiAccount:true`.
    - OQ3 section states the literal `last_api_count` value for (kansas_city,jobs) and (wichita,jobs).
    - OQ3b section records a `count(*) where account_key is null` result for ALL 9 tables mig 178 constrains (`acculynx_jobs, acculynx_contacts, acculynx_job_contacts, acculynx_estimates, acculynx_invoices, acculynx_invoice_lines, acculynx_job_financials, acculynx_job_insurance, acculynx_job_milestone_history`), plus `market IS NULL` counts where that column exists — grep-confirmed all 9 table names appear.
    - A "Downstream impact" section states, in plain terms, the decision for each of: cron cutover needed?, last_api_count needs code change?, RLS enable-or-audit?, and the exact table sets mig 180 must triage / mig 178 may constrain.
    - grep confirms NO raw secret/token/key value string appears in the file (only env var NAMES / channel names).
  </acceptance_criteria>
  <done>The four RESEARCH Open Questions are answered from the live DB and recorded; the full 9-table NULL-provenance scope is known; downstream migration authors have the real jobname, RLS status, watermark values, and true triage/NOT-NULL scope.</done>
</task>

<task type="auto">
  <name>Task 2: Write the committed re-runnable cron verification script</name>
  <files>scripts/verify-acculynx-cron.sql</files>
  <read_first>
    - scripts/acculynx-reconcile-check.sql (the full-file template: "Purpose / Run / Expected output when healthy" header, "zero rows = healthy" query design)
    - .planning/phases/03-commercial-cron-hardening/03-PATTERNS.md lines 375-408 (verify-acculynx-cron.sql assignment + the two query sections to include)
    - .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md (Task 1 output — use the confirmed real jobname in the expected-output comment)
  </read_first>
  <action>
    Create `scripts/verify-acculynx-cron.sql` following the exact header convention of
    `acculynx-reconcile-check.sql` (Purpose / Run: `psql $DATABASE_URL -f scripts/verify-acculynx-cron.sql` /
    Expected output when healthy). Section 1: `select jobid, jobname, schedule, command from cron.job where
    jobname ilike '%acculynx%' order by jobname;` — comment the expected healthy state as exactly one hourly
    sync row with schedule `0 * * * *` and `multiAccount` in the command, zero rows for the old daily jobname
    (use the real old jobname confirmed in 03-LIVE-STATE.md). Section 2 (stuck-pending / D-05d guard):
    `select request_id, dispatched_at, status_code, reconciled_at from public.acculynx_cron_dispatch where
    reconciled_at is null and dispatched_at < now() - interval '30 minutes' order by dispatched_at;` — comment
    that zero rows = healthy, non-empty = unreconciled dispatch past grace window (this table is created in
    plan 03-02 mig 173; the script is committed now, becomes fully runnable after that migration applies).
    End the file with a comment noting which migrations must be applied for each section to return data.
    No fenced code executed at author time — this is a static committed SQL artifact.
  </action>
  <verify>
    <automated>test -f scripts/verify-acculynx-cron.sql && grep -q "cron.job" scripts/verify-acculynx-cron.sql && grep -qi "0 \* \* \* \*" scripts/verify-acculynx-cron.sql && grep -qi "Expected output when healthy" scripts/verify-acculynx-cron.sql && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-acculynx-cron.sql` exists with a Purpose / Run / Expected-output-when-healthy header matching `acculynx-reconcile-check.sql`.
    - Contains a `cron.job` query filtering `jobname ilike '%acculynx%'` and asserts (in comment) exactly one hourly row `0 * * * *`.
    - Contains a second section querying `acculynx_cron_dispatch` for unreconciled rows past a 30-min grace window.
    - File is pure SQL + comments (no host-shell heredoc), committed to the repo.
  </acceptance_criteria>
  <done>A re-runnable, committed SQL gate proves the hourly-cron and reconciliation state; later waves and the runbook reference it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner/executor → live prod DB (`rnhmvcpsvtqjlffpsayu`) | Read-only introspection queries cross into the shared client brain; must not mutate anything this wave |
| skills files (Slack/Sentry) → recorded plan doc | Token/secret NAMES only may cross; raw values must never be written to a committed .md |

## STRIDE Threat Register (maps to RESEARCH §Security Domain)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | 03-LIVE-STATE.md / verify script | mitigate | Record only env var NAMES + channel names; grep gate in Task 1 acceptance forbids any raw key/token string in the committed file (hard rule 2) |
| T-03-02 | Tampering | live prod DB during introspection | mitigate | Wave 0 is read-only: only SELECT / SHOW / catalog reads via Supabase MCP; no execute_sql that writes, no apply_migration this wave (hard rule 1) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: N/A — no new packages); nothing to gate |
</threat_model>

<verification>
- `03-LIVE-STATE.md` answers OQ1-4 with real SQL output and a Downstream-impact section.
- OQ3b records a NULL account_key count for all 9 tables mig 178 constrains (not just the 4 known).
- `scripts/verify-acculynx-cron.sql` committed, runnable, "zero rows = healthy" design.
- No raw secret appears in either committed artifact.
</verification>

<success_criteria>
- The real acculynx cron jobname, RLS status, and (kansas_city/wichita, jobs) last_api_count are recorded from the live DB.
- The true NULL-provenance scope across all 9 tables is recorded so mig 180 triage + mig 178 NOT NULL cannot fail on an unscanned table.
- Downstream migration authors (plans 02/03/04) can proceed without guessing any live-DB fact.
</success_criteria>

<output>
Create `.planning/phases/03-commercial-cron-hardening/03-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
