---
phase: 03-commercial-cron-hardening
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - docs/knowledge-base/acculynx/ingestion/runbook.md
  - docs/knowledge-base/acculynx/ingestion/sync-pipeline.md
  - docs/knowledge-base/acculynx/security/posture.md
autonomous: false
requirements: [REQ-07]
user_setup: []

must_haves:
  truths:
    - "A security posture doc records secret handling, RLS deny-by-default (with the live anon-vs-service-role proof), least-privilege, and the untrusted-content/injection boundary (D-13)"
    - "/gsd-secure-phase has been run and its findings are reflected/resolved in the posture doc (D-13)"
    - "A comprehensive recovery runbook covers stuck/pending pg_net, watermark rewind/reset, edge fn rollback v19->v12, rate-limit lockout, secret rotation, and re-running a torn backfill (D-15)"
    - "sync-pipeline.md reflects the hourly cadence + fixed reconciliation (no longer describes the daily-08:15 / perpetual-pending state)"
  artifacts:
    - path: "docs/knowledge-base/acculynx/security/posture.md"
      provides: "Durable security posture artifact (D-13)"
      contains: "RLS"
    - path: "docs/knowledge-base/acculynx/ingestion/runbook.md"
      provides: "D-15 recovery runbook (6 scenarios + alert-verification procedure)"
      contains: "Rollback"
    - path: "docs/knowledge-base/acculynx/ingestion/sync-pipeline.md"
      provides: "Updated architecture reference (hourly + reconciled)"
      contains: "hourly"
  key_links:
    - from: "03-04 Task 3 anon-vs-service-role result + 03-03 Task 3 alert proof"
      to: "posture.md + runbook.md"
      via: "live verification results become the durable documented evidence"
      pattern: "service_role"
    - from: "runbook.md"
      to: "scripts/verify-acculynx-cron.sql + supabase functions deploy path"
      via: "runbook references the committed check script + confirmed deploy/rollback command"
      pattern: "verify-acculynx-cron"
---

<objective>
Security review (D-13) + recovery runbook (D-15) + architecture doc update — the durable artifacts that
make the hardening auditable and operable without babysitting. Runs LAST because it documents the live,
verified state produced by plans 02-04 (hourly cron, reconciliation, alerting, RLS, trust invariants,
rot guards).

Purpose: SC3 (runbook documents recovery) + SC4 (security review confirms secret handling, RLS,
least-privilege; no secret exposed). This is where the phase becomes provably "commercial-grade":
someone other than the author can recover a torn backfill or a stuck pg_net loop from the runbook, and
an auditor can read the security posture and confirm deny-by-default + no-secret-in-logs.
Output: posture.md, runbook.md, updated sync-pipeline.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-commercial-cron-hardening/03-CONTEXT.md
@.planning/phases/03-commercial-cron-hardening/03-RESEARCH.md
@.planning/phases/03-commercial-cron-hardening/03-PATTERNS.md
@.planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md
@docs/knowledge-base/acculynx/ingestion/sync-pipeline.md
@docs/knowledge-base/runbooks/release-day.md
@scripts/verify-acculynx-cron.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: [BLOCKING] Run /gsd-secure-phase and write the security posture doc (D-13)</name>
  <what-built>
    Plans 02-04 delivered RLS deny-by-default, secret-safe alerting, trust invariants, rot guards - all
    verified live. D-13 requires BOTH running /gsd-secure-phase AND producing a written security posture doc
    as the durable artifact. The scan may surface findings that must be resolved before the phase closes.
  </what-built>
  <action>Run `/gsd-secure-phase` for phase 3, resolve or accept every finding, then author `docs/knowledge-base/acculynx/security/posture.md` per <how-to-verify>. Blocking human-gated (security review is the durable D-13 artifact).</action>
  <read_first>
    - .planning/phases/03-commercial-cron-hardening/03-RESEARCH.md security domain section (ASVS V4/V5 categories + the 5 STRIDE threat patterns to attest against)
    - .planning/phases/03-commercial-cron-hardening/03-04-SUMMARY.md (the live anon-vs-service-role RLS result from plan 04 Task 3)
    - .planning/phases/03-commercial-cron-hardening/03-03-SUMMARY.md (the no-secret-in-alert proof)
    - supabase/functions/acculynx-sync/lib/accounts.ts (resolveKey secret-handling discipline to attest)
    - the threat_model blocks in 03-01 through 03-04 PLAN.md (T-03-01..T-03-11 dispositions to confirm mitigated)
  </read_first>
  <how-to-verify>
    Run `/gsd-secure-phase` for phase 3. Resolve or explicitly accept (with rationale) every finding. Then
    write `docs/knowledge-base/acculynx/security/posture.md` (knowledge-base frontmatter style) covering:
    (1) Secret handling - Deno.env-only resolution, only env_secret_name ever logged, alert redaction guard,
    no literal webhook/DSN in any migration; cite the grep gates from plans 02/03.
    (2) RLS / access control (ASVS V4) - deny-by-default on all acculynx_* tables; include the LIVE anon-SELECT
    -denied vs service-role-SELECT-allowed result recorded in plan 04 Task 3.
    (3) Least-privilege - service_role writes (edge fn) + service_role reads (dashboard); note the
    single-service-role-for-both as a documented future-hardening candidate (RESEARCH V4 note), not a D-14 gap.
    (4) Untrusted-content / injection boundary (ASVS V5 / D-10) - free-text marked evidence-tier untrusted; the
    "data never instructions" boundary REQ-09 must honor.
    (5) STRIDE attestation table - T-03-01..T-03-11, each with its live-verified disposition.
    (6) /gsd-secure-phase result summary - findings + resolutions.
    No secret value anywhere in the doc.
  </how-to-verify>
  <acceptance_criteria>
    - `/gsd-secure-phase` for phase 3 has been run; all findings resolved or explicitly accepted with rationale.
    - `docs/knowledge-base/acculynx/security/posture.md` exists covering all six sections above.
    - The posture doc contains the LIVE anon-vs-service-role RLS proof from plan 04 (not a claim - the actual result).
    - The STRIDE table attests each of T-03-01..T-03-11 as mitigated/accepted with live evidence.
    - grep confirms NO raw secret/token/webhook-URL string in posture.md.
  </acceptance_criteria>
  <resume-signal>Type "approved" once /gsd-secure-phase is clean (or findings accepted) and posture.md is complete, or describe unresolved findings.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write the recovery runbook (D-15) and update sync-pipeline.md</name>
  <files>docs/knowledge-base/acculynx/ingestion/runbook.md, docs/knowledge-base/acculynx/ingestion/sync-pipeline.md</files>
  <read_first>
    - docs/knowledge-base/runbooks/release-day.md lines 1-34 (frontmatter + Preconditions/Steps/Rollback/Owners shape to copy)
    - docs/knowledge-base/acculynx/ingestion/sync-pipeline.md (full file - the architecture diagram + "Known gaps" section to update in place; preserve frontmatter/citation style)
    - .planning/phases/03-commercial-cron-hardening/03-PATTERNS.md lines 412-451 (runbook + sync-pipeline update assignments, exact deploy/rollback command)
    - .planning/phases/03-commercial-cron-hardening/03-03-SUMMARY.md (the seed->fire->observe alert-verification steps recorded in plan 03 Task 3)
    - scripts/verify-acculynx-cron.sql (reference from the runbook as the health-check gate)
    - .planning/phases/03-commercial-cron-hardening/03-LIVE-STATE.md (confirmed pg_net TTL, jobnames, deploy path)
  </read_first>
  <action>
    Create `docs/knowledge-base/acculynx/ingestion/runbook.md` with release-day.md frontmatter shape
    (`type: runbook`, tags `[acculynx, ingestion, pg_cron, edge-function, watermark, runbook]`, timestamp) and a
    `##`-header subsection per D-15 scenario: (a) stuck/pending pg_net - run reconcile_acculynx_cron_outcomes()
    manually, check acculynx_cron_dispatch, re-dispatch; (b) watermark rewind/reset - UPDATE
    acculynx_sync_watermark last_page_index/last_modified_date (never delete the row); (c) edge function
    rollback v19->v12 - `supabase functions deploy acculynx-sync --project-ref rnhmvcpsvtqjlffpsayu` semantics +
    how to redeploy the prior version; (d) rate-limit lockout - 429/Retry-After behavior, how to pause the
    hourly cron (cron.unschedule then re-schedule); (e) secret rotation - rotate an Edge secret via the
    dashboard, no code change, re-verify; (f) re-running a torn backfill - resume from watermark, bounded slice
    per hourly run. Add a `## Health check` section pointing at `scripts/verify-acculynx-cron.sql` and the
    alert-verification seed->fire->observe procedure from plan 03. Add `## Rollback` and `## Owners`. No secret
    values.
    Update `sync-pipeline.md` IN PLACE: architecture diagram `pg_cron (daily 08:15 UTC)` -> `pg_cron (hourly,
    0 * * * *)`, `trigger_acculynx_sync('["users","jobs"]')` -> `trigger_acculynx_sync('{"multiAccount":true}')`;
    convert the "Known gaps (2026-06-30)" section to "Resolved in Phase 3" describing the reconciliation
    (owned dispatch table + */10 reconcile cron) and alerting; bump the timestamp; preserve frontmatter/citation style.
  </action>
  <verify>
    <automated>test -f docs/knowledge-base/acculynx/ingestion/runbook.md && for kw in "pg_net" "watermark" "v19" "v12" "rate.limit" "rotat" "backfill" "Rollback"; do grep -qiE "$kw" docs/knowledge-base/acculynx/ingestion/runbook.md || { echo "runbook missing: $kw"; exit 1; }; done && grep -qiE "hourly|0 \* \* \* \*" docs/knowledge-base/acculynx/ingestion/sync-pipeline.md && grep -qi "multiAccount" docs/knowledge-base/acculynx/ingestion/sync-pipeline.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/knowledge-base/acculynx/ingestion/runbook.md` exists with release-day.md frontmatter/section shape.
    - Runbook covers all six D-15 scenarios (grep: pg_net, watermark, v19, v12, rate-limit, rotation, backfill) plus a Rollback and Owners section.
    - Runbook has a Health check section referencing `scripts/verify-acculynx-cron.sql` and the alert seed->fire->observe procedure.
    - `sync-pipeline.md` architecture now says hourly `0 * * * *` and `multiAccount:true`; the old "Known gaps" perpetual-pending section is replaced with the Phase-3 resolution.
    - No raw secret value in either file.
  </acceptance_criteria>
  <done>The runbook makes recovery repeatable by anyone; sync-pipeline.md matches the hardened live reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live-verified security state (plans 02-04) -> written posture/runbook docs | Only NON-secret evidence crosses; env var NAMES and results, never values |
| operator following the runbook -> live prod DB / edge function | Recovery steps must be additive/reversible (watermark UPDATE not DELETE; rollback redeploy) - never destructive (hard rule 1) |

## STRIDE Threat Register (maps to RESEARCH security domain)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information Disclosure | posture.md / runbook.md accidentally embedding a secret | mitigate | grep gate in both tasks forbids raw secret/token/webhook-URL strings; docs cite env var NAMES only (hard rule 2) |
| T-03-13 | Tampering (operator error during recovery) | runbook recovery steps | mitigate | Runbook uses update-only / reversible steps (watermark UPDATE, cron re-schedule, version redeploy) - explicitly never DELETE/TRUNCATE/DROP (hard rule 1) |
| T-03-14 | Repudiation / audit gap | undocumented security posture | mitigate | D-13 posture doc + /gsd-secure-phase run make the hardened state auditable with live evidence |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- /gsd-secure-phase run; posture.md complete with live anon-vs-service-role proof + T-03-01..T-03-14 attestation; no secret in doc.
- runbook.md covers all six D-15 scenarios + health-check + rollback + owners.
- sync-pipeline.md updated to hourly + multiAccount:true + reconciliation-resolved.
</verification>

<success_criteria>
- SC3: recovery runbook documents clean resume + all recovery scenarios.
- SC4: security review (scan + posture doc) confirms secret handling, RLS, least-privilege sound; no secret exposed.
</success_criteria>

<output>
Create `.planning/phases/03-commercial-cron-hardening/03-05-SUMMARY.md` when done.
</output>
