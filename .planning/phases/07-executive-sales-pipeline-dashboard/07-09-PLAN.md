---
phase: 07-executive-sales-pipeline-dashboard
plan: 09
type: execute
wave: 8
depends_on: [07-06, 07-07, 07-08]
gap_closure: true
autonomous: false
requirements: [REQ-10]
files_modified:
  - app/command-center/src/lib/executive-pipeline.ts
  - docs/knowledge-base/acculynx/ingestion/runbook.md

must_haves:
  truths:
    - "The rebuilt acculynx-sync edge function is deployed to prod and a backfill rotation is kicked off so all ~6,434 jobs get their first-sight full pull over a paced per-account rotation (D-18); crm_pipeline row counts and financials/rep coverage grow run over run for all 8 accounts."
    - "KS-11 renders on the LIVE dashboard assigned to Bob Smolek with ~$30,368 value and ~$17,532 AR outstanding (the red AR chart segment appears) — the whole-phase proof."
    - "The 07-04 Task 3 human-verify checkpoint is RE-RUN (not re-asserted) against the live dashboard and passes: all 8 locations current within the hourly SLA, honest freshness badges, real/growing margin coverage, no duplicate job with conflicting values."
  artifacts:
    - "Deployed acculynx-sync (rebuilt) + a running backfill rotation"
    - "runbook.md backfill-rotation procedure"
    - "the re-run human-verify checkpoint result"
  key_links:
    - "deployed edge function -> hourly multiAccount cron -> crm_pipeline current for all 8 accounts"
    - "backfill rotation -> first-sight full pull per job -> financials/reps -> crm_pipeline coverage"
    - "live crm_pipeline (KS-11) -> dashboard -> Bob Smolek / $30,368 / $17,532 AR segment"
---

<objective>
Land the whole gap closure on production and prove it on the live dashboard. With 07-05 (mappers +
counted errors + watermark fix), 07-06 (crm_pipeline restoration + financials/reps + rotation), and
07-08 (webhook triggers) merged, this plan deploys the rebuilt acculynx-sync edge function, kicks off
the D-18 backfill rotation so every one of the ~6,434 jobs gets its first-sight full pull over a
paced per-account rotation, and then RE-RUNS the exact 07-04 Task 3 human-verify checkpoint against
the live dashboard — the checkpoint that originally caught the failure. The KS-11 ground-truth anchor
is the acceptance proof: KS-11 must render assigned to Bob Smolek with ~$30,368 value and ~$17,532 AR
outstanding (the red AR segment appears). Only after the live checkpoint passes is Phase 7 closed.

Purpose: Converts the code fixes into live, verified data honesty (REQ-10's core promise: "proves the
full multi-location data is present"). Depends on 07-06/07/08 — the sync must be rebuilt, the loader
deduped, and the trigger layer in place before the live re-verify.
Output: a deployed sync, a running backfill, an updated runbook, and a passed live checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-CONTEXT.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-VERIFICATION.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-04-SUMMARY.md

# Deploy conventions + the accountFilter/rotation backfill lever
@supabase/functions/acculynx-sync/index.ts
@docs/knowledge-base/acculynx/ingestion/runbook.md
@.claude/skills/coolify/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy the rebuilt acculynx-sync + kick off the D-18 backfill rotation</name>
  <read_first>
    - supabase/functions/acculynx-sync/index.ts (post-07-06 — the rotation + syncCrmPipeline wiring being deployed)
    - docs/knowledge-base/acculynx/ingestion/runbook.md lines 195-264 (the trigger_acculynx_sync accountFilter lever, the 8 enabled accounts, the setting-secrets caveat, backfill-completes-over-runs pacing)
    - project instructions: Edge Functions deploy via `supabase functions deploy`, NOT Coolify
  </read_first>
  <files>docs/knowledge-base/acculynx/ingestion/runbook.md</files>
  <action>
    Deploy the rebuilt sync: `supabase functions deploy acculynx-sync --project-ref
    rnhmvcpsvtqjlffpsayu` (Edge Functions deploy via supabase, never Coolify). State the
    explain-then-ship note (what changes: crm_pipeline now written on the hourly cron, financials/reps
    mapped, budget rotation, capture-first raw archive; impact: dashboard data becomes live/honest;
    rollback: redeploy the prior function source per runbook Scenario C). Kick off the D-18 backfill
    rotation: trigger the multiAccount sync so the first-sight full pull begins draining across
    accounts — either the default rotating fan-out (`select
    public.trigger_acculynx_sync('{"multiAccount":true}'::jsonb)`) or paced per-account invocations
    using accountFilter (`{"multiAccount":true,"accountFilter":["wichita"]}`, etc.) starting with the
    previously-starved accounts. Watch `v_acculynx_cron_outcomes` for success and confirm crm_pipeline
    row counts climbing and acculynx_job_walk_errors staying at/near zero (a spike means the mappers
    are wrong — investigate before continuing). Add a "Backfill rotation (Phase 7 gap closure)"
    section to runbook.md documenting the kickoff commands, the expected multi-run pacing, and how to
    watch crm_pipeline coverage + acculynx_job_walk_errors.
  </action>
  <verify>
    <automated>grep -qi 'backfill rotation' docs/knowledge-base/acculynx/ingestion/runbook.md && grep -qi 'accountFilter' docs/knowledge-base/acculynx/ingestion/runbook.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase functions deploy acculynx-sync` completed (function version bumped).
    - A multiAccount run was triggered and `v_acculynx_cron_outcomes` shows success for the run.
    - Live-DB probe: `select count(*) from public.crm_pipeline where data_source='api_sync' and updated_at > now() - interval '2 hours'` returns a growing count (crm_pipeline no longer frozen).
    - `select count(*) from public.acculynx_job_walk_errors where resolved_at is null and occurred_at > now() - interval '2 hours'` is at or near zero (mappers writing cleanly).
    - runbook.md has a Backfill rotation section with the kickoff commands + watch queries.
  </acceptance_criteria>
  <done>The rebuilt sync is deployed; a D-18 backfill rotation is running; crm_pipeline grows for all 8 accounts with near-zero job-walk errors; runbook documents the procedure.</done>
</task>

<task type="auto">
  <name>Task 2: Live-DB assertion of the KS-11 ground-truth anchor before the human checkpoint</name>
  <read_first>
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-VERIFICATION.md (the KS-11 evidence: job GUID 0c732e56-5b34-4ae6-9e84-3f57a38f3633, approvedJobValue 30368.48, balanceDue 17532.48, company rep user.id 779da1e7 -> Bob Smolek)
    - app/command-center/src/lib/executive-pipeline.ts (the loader that must now surface these values post-dedup + post-crm_pipeline-restore)
  </read_first>
  <files>app/command-center/src/lib/executive-pipeline.ts</files>
  <action>
    Assert the KS-11 anchor against the LIVE prod DB (verify against the live DB, not migration files
    — CLAUDE.md lesson). Run live-DB probes: (a) `select acculynx_job_id, contract_amount,
    balance_due, primary_salesperson, data_source from public.crm_pipeline where acculynx_job_id =
    '0c732e56-5b34-4ae6-9e84-3f57a38f3633'` — expect exactly ONE row after dedup (api_sync),
    contract_amount ~30368.48, balance_due ~17532.48, primary_salesperson 'Bob Smolek'; (b) confirm
    no duplicate KS-11 row survives (dedup working). If the values are not yet present, let the
    backfill rotation reach the wichita/kansas account and re-probe (this is expected multi-run
    pacing, not a fault). Only a code change is warranted here if a probe reveals a mapping bug
    (e.g. the amount lands in the wrong column) — in that case fix the specific mapper/loader line
    and note it; otherwise this task is a live-DB verification with no code change. Record the probe
    results for the human checkpoint.
  </action>
  <verify>
    <automated>echo "Live-DB KS-11 probe is manual/procedural (prod DB access); results recorded in SUMMARY. This gate asserts the probe was RUN and its output captured." && test -f .planning/phases/07-executive-sales-pipeline-dashboard/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The live-DB probe returns exactly one KS-11 crm_pipeline row (no duplicate) with contract_amount ~30368.48, balance_due ~17532.48, primary_salesperson 'Bob Smolek'.
    - If not yet populated, the SUMMARY records that the backfill has not yet reached the account and the re-probe plan — the human checkpoint is not entered until the anchor is live.
    - Any mapping bug found is fixed at the specific line and re-probed; no scope creep.
  </acceptance_criteria>
  <done>The KS-11 anchor is confirmed live in crm_pipeline (single deduped row, correct value/AR/rep) — the data proof is in place before the human sees the dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-run the 07-04 Task 3 live-dashboard checkpoint (phase close)</name>
  <what-built>The full gap closure is live: the rebuilt sync writes crm_pipeline for all 8 accounts hourly, financials/reps are mapped, duplicates are deduped, the backfill rotation is draining, and the KS-11 anchor is confirmed in the live DB. This RE-RUNS the exact 07-04 Task 3 checkpoint that originally caught the failure — it must be re-run against the live site, not re-asserted.</what-built>
  <how-to-verify>
    Visit https://cc.proexteriorsus.net/executive/pipeline (authenticated) and confirm all of:
    1. All 8 locations are present and filterable by location/market; each shows data current within the hourly SLA.
    2. Freshness badges are HONEST — no location shows "ready" while its crm_pipeline rows are actually stale/absent.
    3. Margin/AR coverage captions reflect real, growing coverage (not 0%); the numbers move as the backfill drains.
    4. KS-11 (the wichita job) renders assigned to Bob Smolek with ~$30,368 value and ~$17,532 AR outstanding — the red AR chart segment appears.
    5. No job appears twice with conflicting values (dedup working).
    6. The AccuLynx drill-down link works; mobile layout is responsive.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 7, or describe the issue (which location / which KPI / KS-11 mismatch) so a follow-up gap plan can be scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| edge function deploy -> prod | Deploying the rebuilt sync changes live ingestion behavior for all 8 accounts. |
| backfill rotation -> AccuLynx API | A large first-sight pull runs against rate-limited external APIs. |
| live crm_pipeline -> executive dashboard | The verified data now drives real executive decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09-01 | Denial of Service | backfill rotation hammering the AccuLynx API | high | mitigate | Rotation is paced (PACE_MS + per-account budget slice from 07-06); backfill completes over many hourly runs, not one burst — the intended pacing per runbook; watch v_acculynx_cron_outcomes for 429s. |
| T-07-09-02 | Tampering | a mapper bug silently writing wrong values live | high | mitigate | acculynx_job_walk_errors count is watched during rollout (near-zero required); the KS-11 live-DB probe verifies the exact value/column mapping before the human sees the dashboard; a spike halts the rollout. |
| T-07-09-03 | Repudiation | claiming "live" without deploy proof | medium | mitigate | Deploy verified by function-version bump + v_acculynx_cron_outcomes success + a live-DB crm_pipeline growth probe (not a SUMMARY claim) — matches the CLAUDE.md verify-against-live-DB rule. |
| T-07-09-04 | Information Disclosure | dashboard exposing PII | low | accept | Access unchanged — the existing WorkOS gate governs; no new surface added by this plan. |
| T-07-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs — deploy + backfill only; no legitimacy gate needed. |
</threat_model>

<verification>
- acculynx-sync deployed (version bump); a multiAccount run shows success in v_acculynx_cron_outcomes.
- Live-DB probes: crm_pipeline api_sync rows growing; acculynx_job_walk_errors near zero; KS-11 single deduped row with 30368.48 / 17532.48 / 'Bob Smolek'.
- The 07-04 Task 3 human-verify checkpoint is re-run on the live site and approved.
- runbook.md documents the backfill rotation.
</verification>

<success_criteria>
- All 6 VERIFICATION gaps are closed and PROVEN live (not asserted): crm_pipeline current for 8 accounts, sub-resources populated, financials/reps mapped, no starvation, deduped, docs accurate.
- REQ-10's core promise holds: the live dashboard proves the full multi-location data is present.
- KS-11 renders correctly on the live dashboard; the human-verify checkpoint passes; Phase 7 closes.
</success_criteria>

<artifacts>
## Artifacts this plan produces

**Files modified:**
- `docs/knowledge-base/acculynx/ingestion/runbook.md` (Backfill rotation — Phase 7 gap closure section)
- `app/command-center/src/lib/executive-pipeline.ts` (only if a live probe reveals a specific mapping bug)

**Deployed / operational:**
- Rebuilt `acculynx-sync` Edge Function deployed to prod (via supabase functions deploy)
- A running D-18 backfill rotation (first-sight full pull across all 8 accounts)

**Verified live (not a new artifact — the phase proof):**
- crm_pipeline current for all 8 accounts; KS-11 = Bob Smolek / ~$30,368 / ~$17,532 AR on the live dashboard
- The re-run 07-04 Task 3 human-verify checkpoint result
</artifacts>

<output>
Create `.planning/phases/07-executive-sales-pipeline-dashboard/07-09-SUMMARY.md` when done
</output>
