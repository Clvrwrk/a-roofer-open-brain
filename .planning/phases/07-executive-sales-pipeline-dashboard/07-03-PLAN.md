---
phase: 07-executive-sales-pipeline-dashboard
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - app/command-center/src/lib/nav.ts
  - app/command-center/src/lib/prewarm.server.ts
  - app/command-center/src/layouts/AppShell.astro
  - app/command-center/src/pages/healthz.ts
  - app/command-center/src/pages/sw.js.ts
  - app/command-center/src/pages/executive/[slug].astro
  - app/command-center/src/pages/weekly-snapshot.astro
  - app/command-center/src/scripts/weekly-snapshot.ts
  - app/command-center/src/lib/weekly-snapshot.ts
  - app/command-center/src/lib/weekly-snapshot-routes.ts
  - app/command-center/src/pages/weekly-snapshot/[slice].astro
  - app/command-center/src/pages/weekly-snapshot/records/[record].astro
  - app/command-center/src/pages/weekly-snapshot/rep/[rep].astro
autonomous: true
requirements: [REQ-10]

must_haves:
  truths:
    - "The /weekly-snapshot route and all its sub-routes/loaders/scripts no longer exist — the route 404s and nothing imports the deleted modules (D-02)."
    - "The Executive nav entry reads 'Sales Pipeline' -> /executive/pipeline; the /executive (Overview) live-work surface is untouched (D-01)."
    - "The Astro build and the full vitest suite are green after deletion — zero dangling imports."
  artifacts:
    - "app/command-center/src/lib/nav.ts (Executive item relabeled/re-hrefed)"
    - "app/command-center/src/lib/prewarm.server.ts (executive_pipeline prewarm or entry removed)"
  key_links:
    - "nav.ts Executive items -> /executive/pipeline"
    - "prewarm.server.ts -> loadExecutivePipelineDashboard (or entry removed)"
    - "healthz.ts requiredRoutes + sw.js.ts precache -> /executive/pipeline"
---

<objective>
Retire the weekly snapshot (D-02) and cut the Executive tab over to the new dashboard (D-01). Delete the page + loader + client script + the four extra weekly-snapshot files surfaced by grep (routes helper + three sub-pages), and update every remaining reference (`nav.ts`, `prewarm.server.ts`, `AppShell.astro`, `healthz.ts`, `sw.js.ts`, the `[slug].astro` stub). The Astro build and full vitest suite going green is the proof that zero dangling imports remain.

Purpose: REQ-10 requires the dashboard to REPLACE the weekly snapshot, not sit beside it. This plan makes the replacement clean — no redirect, no legacy page, the old route 404s.
Output: weekly-snapshot fully removed; Executive nav points at /executive/pipeline; green build + suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-CONTEXT.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-PATTERNS.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-02-SUMMARY.md
@app/command-center/src/lib/nav.ts
@app/command-center/src/lib/prewarm.server.ts
</context>

<artifacts_this_phase_produces>
This plan is primarily DELETIONS + edits; the new symbols it introduces:

- **prewarm entry:** `executive_pipeline` (replacing `weekly_snapshot`) calling `loadExecutivePipelineDashboard()` — OR the prewarm entry is removed entirely (executor's discretion per PATTERNS.md, but the stale import MUST go)
- **nav item:** `{ label: "Sales Pipeline", href: "/executive/pipeline", status: "built" }` (replacing the Weekly Snapshot item)

Files DELETED (7): `weekly-snapshot.astro`, `scripts/weekly-snapshot.ts`, `lib/weekly-snapshot.ts`, `lib/weekly-snapshot-routes.ts`, `weekly-snapshot/[slice].astro`, `weekly-snapshot/records/[record].astro`, `weekly-snapshot/rep/[rep].astro`. The `executive/[slug].astro` stub is deleted OR gutted to no longer default to "pipeline" (Task detail).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 7 weekly-snapshot files + retire the [slug].astro stub</name>
  <read_first>
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-PATTERNS.md (Deletion-scope correction: the 3 named + 4 grep-found files; the AppShell/prewarm edit notes)
    - app/command-center/src/pages/executive/[slug].astro (the stub whose default slug was "pipeline" — now superseded by the real pipeline.astro)
    - app/command-center/src/lib/weekly-snapshot-routes.ts (confirm it is only consumed by the three sub-pages being deleted)
  </read_first>
  <action>
    Delete these seven files (D-02, widened per PATTERNS.md grep):
    - `src/pages/weekly-snapshot.astro`
    - `src/scripts/weekly-snapshot.ts`
    - `src/lib/weekly-snapshot.ts`
    - `src/lib/weekly-snapshot-routes.ts`
    - `src/pages/weekly-snapshot/[slice].astro`
    - `src/pages/weekly-snapshot/records/[record].astro`
    - `src/pages/weekly-snapshot/rep/[rep].astro`
    (After deleting the three sub-pages, remove the now-empty `src/pages/weekly-snapshot/` directory tree.)

    Retire the `src/pages/executive/[slug].astro` stub: since the real `pipeline.astro` now owns `/executive/pipeline` (Astro prefers the static route over the `[slug]` dynamic route, but leaving a stub that also matches "pipeline" is ambiguous), DELETE `[slug].astro` unless a grep shows another executive sub-slug still depends on it. If another slug is in use, instead edit it to no longer treat "pipeline" as its default/only slug. Record which path was taken in the SUMMARY.

    Do NOT yet touch nav.ts/prewarm/AppShell/healthz/sw — Task 2 handles the reference rewiring. (Order matters: delete first will make the build red on dangling imports; Task 2 makes it green. Commit Task 1 and Task 2 together only after Task 2's build passes, OR commit Task 1 knowing the tree is transiently red — executor keeps them in one wave.)
  </action>
  <verify>
    <automated>cd app/command-center && test ! -f src/lib/weekly-snapshot.ts && test ! -f src/pages/weekly-snapshot.astro && test ! -d src/pages/weekly-snapshot && echo DELETED_OK</automated>
    <automated>cd app/command-center && test ! -f src/lib/weekly-snapshot-routes.ts && test ! -f src/scripts/weekly-snapshot.ts && echo AUX_DELETED_OK</automated>
  </verify>
  <acceptance_criteria>
    - All 7 weekly-snapshot files are gone and the `src/pages/weekly-snapshot/` directory no longer exists (source assertion).
    - The `executive/[slug].astro` stub is deleted or no longer claims "pipeline" as its default slug (source assertion; path taken recorded in SUMMARY).
    - No file OTHER than the reference-holders handled in Task 2 imports a deleted module (verified by Task 2's grep + build).
  </acceptance_criteria>
  <done>The weekly-snapshot page, loader, client script, routes helper, three sub-pages, and the executive stub are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire the five reference files + prove zero dangling imports (build + suite green)</name>
  <read_first>
    - app/command-center/src/lib/nav.ts (Executive section items array ~89-97)
    - app/command-center/src/lib/prewarm.server.ts (line ~12 import loadWeeklySnapshot; line ~45 { name: "weekly_snapshot", run: ... })
    - app/command-center/src/layouts/AppShell.astro (line ~163 "/weekly-snapshot" in the route list; activePage union line ~11 — confirm "executive" token exists)
    - app/command-center/src/pages/healthz.ts (line ~57 requiredRoutes includes "/weekly-snapshot")
    - app/command-center/src/pages/sw.js.ts (line ~13 precache list includes "/weekly-snapshot")
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-PATTERNS.md (nav.ts required edit; prewarm replacement pattern)
  </read_first>
  <action>
    Rewire every remaining reference to the deleted route/modules:
    - `src/lib/nav.ts`: replace the Executive `{ label: "Weekly Snapshot", href: "/weekly-snapshot", status: "built" }` item with `{ label: "Sales Pipeline", href: "/executive/pipeline", status: "built" }` (D-01). Leave the "Overview" -> `/executive` item untouched (DepartmentHome live-work surface stays).
    - `src/lib/prewarm.server.ts`: replace the `import { loadWeeklySnapshot } from "@lib/weekly-snapshot"` and the `{ name: "weekly_snapshot", run: () => loadWeeklySnapshot() }` entry with an `executive_pipeline` entry importing `loadExecutivePipelineDashboard` from `@lib/executive-pipeline` — OR remove the prewarm entry entirely if the new loader doesn't warrant prewarming (executor's discretion per PATTERNS.md; either way the stale import MUST be gone).
    - `src/layouts/AppShell.astro`: update the `/weekly-snapshot` reference (line ~163, prefetch/sw route list) to `/executive/pipeline`.
    - `src/pages/healthz.ts`: in `requiredRoutes` (line ~57) replace `"/weekly-snapshot"` with `"/executive/pipeline"` so the health probe checks the live route.
    - `src/pages/sw.js.ts`: in the precache list (line ~13) replace `"/weekly-snapshot"` with `"/executive/pipeline"`.

    Then run `grep -rn "weekly-snapshot\|weekly_snapshot\|loadWeeklySnapshot" src/` — the ONLY acceptable remaining hits are inside the SUMMARY/planning docs, not `src/`. If any `src/` hit remains, resolve it. Run `npm run build` (must succeed — dangling imports fail the Astro build) and `npx vitest run` (full suite green).
  </action>
  <verify>
    <automated>cd app/command-center && grep -rn "weekly-snapshot\|weekly_snapshot\|loadWeeklySnapshot" src/ | grep -c . | grep -qx 0 && echo NO_DANGLING_REFS</automated>
    <automated>cd app/command-center && grep -q '/executive/pipeline' src/lib/nav.ts && echo NAV_OK</automated>
    <automated>cd app/command-center && npm run build 2>&1 | tail -8</automated>
    <automated>cd app/command-center && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "weekly-snapshot\|weekly_snapshot\|loadWeeklySnapshot" src/` returns ZERO hits (source assertion — the NO_DANGLING_REFS gate).
    - `nav.ts` Executive section has the "Sales Pipeline" -> `/executive/pipeline` item and still has the untouched "Overview" -> `/executive` item (source assertion).
    - `prewarm.server.ts` no longer imports `@lib/weekly-snapshot` (source assertion).
    - `healthz.ts` requiredRoutes and `sw.js.ts` precache reference `/executive/pipeline`, not `/weekly-snapshot` (source assertion).
    - `npm run build` succeeds (build assertion — the definitive zero-dangling-import proof).
    - `npx vitest run` full suite green (regression assertion).
  </acceptance_criteria>
  <done>Nav points at the new dashboard; prewarm/AppShell/healthz/sw updated; zero weekly-snapshot references remain in src/; build and full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deploy build -> production | A dangling import or a stale precache/health route ships to prod if the deletion is incomplete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Denial of Service (availability) | Dangling imports after D-02 deletion | high | mitigate | `npm run build` + full `npx vitest run` must be green before this plan is done; the NO_DANGLING_REFS grep gate over `src/` proves no module still imports a deleted file. The Astro build fails hard on a dangling import, so a red build blocks the deploy in Plan 04. |
| T-07-08 | Information Disclosure | Stale sw.js precache / healthz requiredRoutes | low | accept | Updating sw.js precache + healthz requiredRoutes to `/executive/pipeline` avoids a service worker caching a 404 and a false-negative health check; low severity because a stale precache entry degrades gracefully (miss -> network) rather than exposing data. |
</threat_model>

<verification>
- `cd app/command-center && grep -rn "weekly-snapshot\|weekly_snapshot\|loadWeeklySnapshot" src/` -> zero hits.
- `cd app/command-center && npm run build` -> succeeds (no dangling imports).
- `cd app/command-center && npx vitest run` -> full suite green.
- `nav.ts` shows "Sales Pipeline" -> /executive/pipeline; "Overview" -> /executive untouched.
</verification>

<success_criteria>
The weekly snapshot is fully retired (7 files + stub gone), every reference is rewired to /executive/pipeline, the src/ tree has zero weekly-snapshot references, and both the Astro build and the full vitest suite are green — proving the cutover left no dangling imports.
</success_criteria>

<output>
Create `.planning/phases/07-executive-sales-pipeline-dashboard/07-03-SUMMARY.md` when done
</output>
