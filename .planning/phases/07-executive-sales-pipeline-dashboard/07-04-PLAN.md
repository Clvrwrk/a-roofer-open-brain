---
phase: 07-executive-sales-pipeline-dashboard
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03"]
files_modified:
  - docs/knowledge-base/acculynx/dashboard-spec.md
  - docs/knowledge-base/acculynx/index.md
autonomous: false
user_setup: []

requirements: [REQ-10]

must_haves:
  truths:
    - "The dashboard spec artifact (KPIs, join map, margin-coverage contract, chart-library pick, freshness architecture) lives in the OKF bundle and is linked from its index (D-07, Phase 6 SC3)."
    - "The dashboard is deployed and verified LIVE at cc.proexteriorsus.net/executive/pipeline — buildCommit on /healthz flipped to the pushed SHA (SC4)."
    - "The retired /weekly-snapshot route 404s in production."
  artifacts:
    - "docs/knowledge-base/acculynx/dashboard-spec.md"
  key_links:
    - "contrib branch -> main (converge) -> Coolify build -> /healthz buildCommit flip"
    - "OKF index.md -> dashboard-spec.md"
---

<objective>
Close the phase: (1) land the researched dashboard spec into the OKF AccuLynx bundle (D-07; the "dashboard spec" artifact Phase 6 SC3 already names), documenting the final KPI set, the `crm_pipeline -> acculynx_jobs` account_key join, the margin-fallback + coverage contract, the chart-library pick, the freshness architecture, and the region/program-vs-geography decision; then (2) deploy to production and verify LIVE per the deploy gate (SC4) — converge to main, push, poll `/healthz` `buildCommit`, and confirm the new route serves while `/weekly-snapshot` 404s.

Purpose: SC1's research becomes a durable spec the agent/team cite; SC4 proves the dashboard is actually live, not just merged. This is the phase's exit gate.
Output: `dashboard-spec.md` in the OKF bundle (linked from index); a live, verified dashboard at cc.proexteriorsus.net/executive/pipeline.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-CONTEXT.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-RESEARCH.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-UI-SPEC.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-01-SUMMARY.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-02-SUMMARY.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-03-SUMMARY.md
@docs/knowledge-base/acculynx/index.md
@.claude/skills/coolify/SKILL.md
</context>

<artifacts_this_phase_produces>
New artifacts this plan creates:

- **File:** `docs/knowledge-base/acculynx/dashboard-spec.md` (OKF dashboard spec, D-07)
- **Edit:** `docs/knowledge-base/acculynx/index.md` (add a link to dashboard-spec.md)
- **Runtime state (not a repo file):** the Coolify deploy of the `command-center` app at the pushed SHA
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the OKF dashboard spec + link it from the bundle index (D-07)</name>
  <read_first>
    - docs/knowledge-base/acculynx/index.md (OKF bundle root — the section/link style to match; where the dashboard-spec link belongs)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-RESEARCH.md (Dashboard Spec substance: KPI formulas, Architecture Patterns, join chain, chart-library rationale, freshness architecture, Open Questions 1-3 resolutions)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-UI-SPEC.md (final layout/section order, copy contracts, tone thresholds)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-01-SUMMARY.md and 07-02-SUMMARY.md (the AS-BUILT KPI set, the observed live margin-coverage numbers, the final window tokens)
  </read_first>
  <action>
    Write `docs/knowledge-base/acculynx/dashboard-spec.md` capturing the AS-BUILT dashboard (not a re-plan). Sections:
    - **KPIs (D-05 anchors + any research-added):** pipeline value by stage (funnel), jobs sold + close rate (snapshot-proxy, NOT cohort — with the mandatory qualifier), new leads volume, rep leaderboard + AR rollup, and the profitability-per-job breakdowns (region/office/commercial-residential/team/rep) with margin-%-first presentation.
    - **Data join map:** `crm_pipeline.acculynx_job_id -> acculynx_jobs.id` for `account_key` (the 8-location dimension) + `job_category_name` (commercial/residential/property-management/uncategorized). Explicit warning that `crm_pipeline.market` is a county slug, NOT the location dimension (Pitfall 2). Milestone stage logic uses lowercase `crm_pipeline.current_milestone` (Pitfall 5).
    - **Margin + coverage contract (D-06):** primary = AccuLynx job-financials GP; fallback = contract_amount − invoiced cost via `v_invoice_acculynx_match` + `abc_invoice_lines`; margin-%-first; mandatory N-of-M coverage caption; the observed live coverage numbers at go-live (from Plan 01's SUMMARY). Never default missing cost to 0.
    - **Chart library:** chart.js@4.5.1, MIT, tree-shaken registration, funnel-via-horizontal-stacked-bar rationale (D-08).
    - **Freshness architecture (D-12):** `acculynx_sync_watermark` + `v_acculynx_cron_outcomes` -> per-location badges; the ready/review/critical tone thresholds.
    - **Resolved research Open Questions:** OQ1 = margin computed in TS, no SQL view (v1); OQ2 = window set This Week / Last 7 Days / MTD / QTD, default Last 7 Days; OQ3 = all 8 accounts are peer location entries, program-vs-geography noted for a future split.
    Then add a link to `dashboard-spec.md` from `docs/knowledge-base/acculynx/index.md` in the existing bundle-index link style. Keep content clean/professional (rule 10); no secrets (rule 2).
  </action>
  <verify>
    <automated>test -f docs/knowledge-base/acculynx/dashboard-spec.md && echo SPEC_OK</automated>
    <automated>grep -q "dashboard-spec" docs/knowledge-base/acculynx/index.md && echo LINKED_OK</automated>
    <automated>grep -qi "account_key" docs/knowledge-base/acculynx/dashboard-spec.md && grep -qi "coverage" docs/knowledge-base/acculynx/dashboard-spec.md && echo CONTENT_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/knowledge-base/acculynx/dashboard-spec.md` exists and documents the AS-BUILT KPIs, the account_key join map, the margin+coverage contract with go-live coverage numbers, the chart pick, and the freshness architecture (source assertion).
    - `docs/knowledge-base/acculynx/index.md` links to the spec (source assertion — Phase 6 SC3 / D-07 satisfied).
    - The spec explicitly warns market≠account_key and records the three Open Question resolutions (source assertion).
  </acceptance_criteria>
  <done>The OKF bundle carries a linked, AS-BUILT dashboard spec covering KPIs, joins, margin/coverage, chart pick, freshness, and the resolved open questions.</done>
</task>

<task type="auto">
  <name>Task 2: Converge to main + deploy + verify live (SC4, explain-then-ship)</name>
  <read_first>
    - .claude/skills/coolify/SKILL.md (the deploy + healthz-verify play — the deploy contract for this repo)
    - CLAUDE.md (Live ⇄ Dev alignment: confirm the canonical/deploying branch via `git fetch origin` before assuming main; converge contrib -> main -> push -> watch buildCommit)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-03-SUMMARY.md (confirm build + full suite were green post-deletion)
  </read_first>
  <action>
    Follow the explain-then-ship discipline (CLAUDE.md Live ⇄ Dev alignment) and the `/coolify` skill exactly.

    1. `git fetch origin` and confirm which branch Coolify's `command-center` app builds (do NOT assume `main` — per CLAUDE.md the live branch has been non-main before). Record it.
    2. Pre-flight (all green BEFORE pushing): `cd app/command-center && npm run build` succeeds; `npx vitest run` full suite green; `git status --short` shows only intended changes.
    3. STATE the change set before pushing (in the SUMMARY and the deploy note): what is changing (new /executive/pipeline dashboard + chart.js dep; weekly-snapshot removed), user-visible impact (Executive tab "Weekly Snapshot" becomes "Sales Pipeline"; old route now 404s), and the rollback (revert the phase merge commit; the deleted files are recoverable from git history — no destructive DB change, no migration, additive-only per rule 1).
    4. Converge the contrib branch into the confirmed canonical branch and push to origin (agent deploys — CLAUDE.md corrected rule).
    5. Poll the Coolify deploy to completion via the skill: watch `GET https://cc.proexteriorsus.net/healthz` `buildCommit` flip to the pushed SHA (≈30-90s, up to ~300s cold). Use `scripts/coolify-redeploy.sh` if a manual redeploy trigger is needed.
    6. Live-verify: `curl -s https://cc.proexteriorsus.net/healthz | jq .buildCommit` equals the pushed SHA; `curl -sI https://cc.proexteriorsus.net/weekly-snapshot` returns 404 (route retired); the `/executive/pipeline` route is reachable (behind WorkOS — a 200 or the auth-redirect, NOT a 404/500).
    No secrets in code or logs (rule 2). No new env var is required by this phase (Supabase creds reused).
  </action>
  <verify>
    <automated>curl -s https://cc.proexteriorsus.net/healthz | jq -r .buildCommit</automated>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" https://cc.proexteriorsus.net/weekly-snapshot</automated>
    <automated>curl -s -o /dev/null -w "%{http_code}\n" https://cc.proexteriorsus.net/executive/pipeline</automated>
  </verify>
  <acceptance_criteria>
    - The deploying branch was confirmed via `git fetch origin` (not assumed) and recorded (CLI/source assertion in SUMMARY).
    - `/healthz` `buildCommit` equals the pushed SHA (CLI assertion — SC4 buildCommit flip).
    - `curl -sI https://cc.proexteriorsus.net/weekly-snapshot` returns HTTP 404 (CLI assertion — D-02 route retired in prod).
    - `curl -sI https://cc.proexteriorsus.net/executive/pipeline` returns 200 or a WorkOS auth redirect (3xx), NOT 404/500 (CLI assertion — new route live).
    - The explain-then-ship note (change / impact / rollback) is recorded in the SUMMARY before the push (source assertion).
  </acceptance_criteria>
  <done>The dashboard is live at cc.proexteriorsus.net/executive/pipeline with buildCommit flipped to the pushed SHA; /weekly-snapshot 404s in prod; the change was shipped explain-then-ship on the confirmed canonical branch.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human gate — verify the live dashboard and close Phase 7</name>
  <what-built>The Executive Sales Pipeline dashboard deployed LIVE at https://cc.proexteriorsus.net/executive/pipeline, replacing the weekly snapshot.</what-built>
  <how-to-verify>
    1. Visit https://cc.proexteriorsus.net/executive/pipeline (authenticate via WorkOS as usual).
    2. Confirm the Executive nav shows "Sales Pipeline" (not "Weekly Snapshot"), and https://cc.proexteriorsus.net/weekly-snapshot now 404s.
    3. Confirm all 8 locations are selectable in the filter bar and the KPIs/charts update when you filter (D-13); the time-window selector changes window-based KPIs (D-03).
    4. Confirm per-location freshness badges show "data as of {time}" and match the latest `v_acculynx_cron_outcomes` run (D-12 — honest, not cosmetic).
    5. Confirm the profitability breakdowns lead with margin % and show the "N of M jobs have cost data" coverage caption (D-06 honesty).
    6. Click through a drill-down to a job's "Open in AccuLynx" link (D-09).
    7. Load it on your phone — cards stack, charts resize (D-10).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 7, or describe any issue (wrong data, missing location, stale badge not flagged, caption missing, mobile layout broken).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contrib branch -> main -> production | A deploy ships whatever is on the canonical branch; converging the wrong branch or an un-built tree ships broken code. |
| Public internet -> cc.proexteriorsus.net | The dashboard surface is internet-reachable (WorkOS-gated); the deploy must not expose the JSON endpoint unauthenticated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-09 | Tampering / availability | Deploy of the wrong branch or an un-built tree | high | mitigate | Explain-then-ship: `git fetch origin` to confirm the canonical branch (not assume main), pre-flight `npm run build` + full `npx vitest run` green before push, then poll `/healthz` buildCommit to confirm the exact pushed SHA is live. Rollback = revert the merge commit (additive-only, no DB change). |
| T-07-10 | Information Disclosure | Financial dashboard exposed post-deploy | high | mitigate | The `.astro` page and `/api/executive/pipeline.json` remain behind the existing WorkOS middleware gate (validated in Plan 02); the live-verify step confirms `/executive/pipeline` returns 200-or-auth-redirect (gated), and no new PUBLIC_PREFIXES entry was added. No new env var / secret introduced. |
</threat_model>

<verification>
- `curl -s https://cc.proexteriorsus.net/healthz | jq .buildCommit` equals the pushed SHA.
- `curl -sI https://cc.proexteriorsus.net/weekly-snapshot` -> 404.
- `curl -sI https://cc.proexteriorsus.net/executive/pipeline` -> 200 or auth redirect.
- `docs/knowledge-base/acculynx/dashboard-spec.md` exists and is linked from index.md.
- Human checkpoint: live dashboard verified on the real site + phone.
</verification>

<success_criteria>
The dashboard is live and verified at cc.proexteriorsus.net/executive/pipeline (buildCommit flipped, SC4), the weekly snapshot is gone (404 in prod, D-02), the OKF bundle carries a linked AS-BUILT dashboard spec (D-07 / Phase 6 SC3), and a human has confirmed the live dashboard reflects all 8 locations with honest freshness + margin-coverage.
</success_criteria>

<output>
Create `.planning/phases/07-executive-sales-pipeline-dashboard/07-04-SUMMARY.md` when done
</output>
