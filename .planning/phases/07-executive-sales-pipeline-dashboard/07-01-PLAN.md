---
phase: 07-executive-sales-pipeline-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/command-center/package.json
  - app/command-center/package-lock.json
  - app/command-center/src/lib/executive-pipeline.ts
  - app/command-center/src/lib/executive-pipeline.test.ts
autonomous: true
requirements: [REQ-10]

must_haves:
  truths:
    - "The dashboard loader computes pipeline funnel, close-rate proxy, margin-with-coverage, region/office derivation, and freshness badges from live Supabase data without crashing SSR on a missing env."
    - "Margin % is reported with an explicit N-of-M coverage count per slice, never silently defaulting missing cost data to 0."
    - "Region/office/commercial-residential dimensions come from acculynx_jobs.account_key/job_category_name joined via acculynx_job_id — NOT from crm_pipeline.market."
    - "Every full-table query paginates via a selectAll .range() loop (no unbounded limit)."
  artifacts:
    - "app/command-center/src/lib/executive-pipeline.ts (loader + exported pure core)"
    - "app/command-center/src/lib/executive-pipeline.test.ts (vitest unit coverage)"
    - "chart.js@4.5.1 in app/command-center/package.json dependencies"
  key_links:
    - "crm_pipeline.acculynx_job_id -> acculynx_jobs.id (account_key + job_category_name join)"
    - "v_invoice_acculynx_match + abc_invoice_lines -> per-job cost fallback for margin"
    - "acculynx_sync_watermark + v_acculynx_cron_outcomes -> per-location freshness badges"
---

<objective>
Build the data layer for the Executive Sales Pipeline dashboard: install the one new chart dependency (chart.js@4.5.1, D-08), and author `lib/executive-pipeline.ts` — a loader whose pure aggregation core (funnel grouping, close-rate proxy, margin+coverage, region/office mapping, window filtering, freshness badges) is fully unit-tested per VALIDATION.md's Wave 0 contract.

Purpose: The loader is the dependency for the page (Plan 02), the JSON endpoint (Plan 02), and the deletion cutover (Plan 03). Extracting a Supabase-free pure core makes REQ-10's honest-data guarantees (margin coverage, market≠account_key, pagination) testable before any UI exists.
Output: chart.js dependency added; `executive-pipeline.ts` loader with an exported pure core; `executive-pipeline.test.ts` covering the six REQ-10 behaviors.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-CONTEXT.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-RESEARCH.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-PATTERNS.md
@.planning/phases/07-executive-sales-pipeline-dashboard/07-VALIDATION.md
@app/command-center/src/lib/weekly-snapshot.ts
@app/command-center/src/lib/acculynx-pending-write.test.ts
</context>

<artifacts_this_phase_produces>
This section lists every NEW symbol/path/dependency this plan creates so downstream drift verification excludes them from "unknown symbol" checks:

- **npm dependency:** `chart.js@4.5.1` (added to `app/command-center/package.json` + lockfile)
- **File:** `app/command-center/src/lib/executive-pipeline.ts`
- **File:** `app/command-center/src/lib/executive-pipeline.test.ts`
- **Exported function:** `loadExecutivePipelineDashboard(filters?)` — SSR loader
- **Exported pure functions (Supabase-free core, unit-tested):** `groupPipelineFunnel`, `computeCloseRate`, `computeMarginByDimension`, `deriveRegionOffice`, `filterByWindow`, `computeFreshnessBadges` (planner-authoritative names; executor may adjust internal helper names but MUST keep these exported and test-referenced)
- **Exported types/interfaces:** `ExecutivePipelineDashboard`, `DashboardFilters`, `MarginCoverage`, `FreshnessBadge`, `AcculynxJobRow`, `FunnelStage`, `LeaderboardRow`, `MarginByDimensionRow`
- **Window tokens (D-03 planner choice):** `this_week` | `last_7_days` | `mtd` | `qtd`, default `last_7_days`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install chart.js@4.5.1 + author the Wave 0 failing test suite for the pure core</name>
  <read_first>
    - app/command-center/package.json (confirm zero existing chart deps; see the exact dependencies block to edit)
    - app/command-center/src/lib/acculynx-pending-write.test.ts (vitest conventions: imports, fixture-builder functions, describe/it structure)
    - app/command-center/src/lib/weekly-snapshot.ts (PipelineRow interface lines ~65-90; AbcInvoiceRow ~92-101; soldMilestones set ~557-564; inWindow helper ~192 — reuse these shapes as test fixtures)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-RESEARCH.md (Phase Requirements -> Test Map: the six -t grep filters; Pitfalls 2/4/5)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
    Run `cd app/command-center && npm install chart.js@4.5.1` (D-08 — the single new dependency; legitimacy pre-cleared in 07-RESEARCH.md Package Legitimacy Audit: MIT, no postinstall). Confirm `package.json` dependencies now lists `"chart.js": "4.5.1"` and the lockfile updated.

    Author `src/lib/executive-pipeline.test.ts` following `acculynx-pending-write.test.ts`'s vitest conventions. Import the (not-yet-implemented) pure functions from `@lib/executive-pipeline`: `groupPipelineFunnel`, `computeCloseRate`, `computeMarginByDimension`, `deriveRegionOffice`, `filterByWindow`, `computeFreshnessBadges`. Build a `makePipelineRow(overrides)` fixture from the `PipelineRow` shape and a `makeJobRow(overrides)` fixture from `AcculynxJobRow` (fields: `id`, `account_key`, `job_category_name`).

    Name `describe` blocks so their titles contain the exact substrings the test map greps with `-t`: `"funnel"`, `"close rate"`, `"margin"`, `"region"`, `"pagination"`, `"freshness"`. Assert:
    - funnel: `groupPipelineFunnel` groups rows by NORMALIZED lowercase `crm_pipeline.current_milestone` (never Title-Case `acculynx_jobs.current_milestone`) — a row with `current_milestone: "approved"` lands in the approved stage; casing/vocab from `acculynx_jobs` is not used for stage logic (Pitfall 5).
    - close rate: `computeCloseRate` uses the snapshot-window proxy (soldMilestones set count in window ÷ lead count in window), NOT a milestone-history transition; returns a ratio plus the caption qualifier data (period-snapshot, not cohort) (Pitfall 4).
    - margin: `computeMarginByDimension` prefers a job-financials GP value when present, falls back to `contract_amount − invoiced cost`, and for a slice of 130 jobs where only 18 have cost data returns `MarginCoverage { jobsWithCostData: 18, totalJobsInSlice: 130, coveragePct: ~14 }` — a job with NO cost data is EXCLUDED from the margin average and COUNTED in the coverage denominator, never treated as 0 cost / 100% margin (Pattern 4 / Pitfall 1).
    - region: `deriveRegionOffice` maps a pipeline row to its location via `acculynx_job_id -> acculynx_jobs.account_key` (one of the 8 keys colorado/florida/georgia/kansas_city/texas/wichita/insurance_program/multi_family_commercial); asserting it does NOT return `crm_pipeline.market` county-slug values like `sedgwick_ks` (Pitfall 2). A row whose `job_category_name` is null derives commercial/residential = "uncategorized", not dropped.
    - pagination: a small pure `paginateRange(total, pageSize)`-style helper (or the boundary math the loader uses) returns the correct `[from,to]` windows so a 6434-row table is fully covered across pages (regression guard for the 1000-row silent-truncation pitfall — Pitfall 3).
    - freshness: `computeFreshnessBadges` flags a location `ready` when `last_sync_at` is within the hourly SLA, `review` between 1x-2x SLA, `critical` beyond 2x SLA (UI-SPEC.md thresholds).

    All six describe blocks MUST fail on first run (functions not implemented yet) — this is the RED step. Do NOT implement the loader in this task.
  </action>
  <verify>
    <automated>cd app/command-center && grep -q '"chart.js": "4.5.1"' package.json && echo CHARTJS_OK</automated>
    <automated>cd app/command-center && npx vitest run src/lib/executive-pipeline.test.ts 2>&1 | grep -qiE 'fail|error|cannot find' && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies contains `"chart.js": "4.5.1"` (source assertion) and `package-lock.json` records it.
    - `src/lib/executive-pipeline.test.ts` exists with six describe blocks whose titles contain: funnel, close rate, margin, region, pagination, freshness (source assertion).
    - `npx vitest run src/lib/executive-pipeline.test.ts` FAILS (RED) because `@lib/executive-pipeline` exports do not exist yet (test-command assertion).
    - The margin test explicitly asserts a no-cost-data job is excluded from the average AND counted in the coverage denominator (source assertion — the honesty guarantee).
    - The region test explicitly asserts the location dimension is an account_key, not a `crm_pipeline.market` county slug (source assertion).
  </acceptance_criteria>
  <done>chart.js@4.5.1 installed; failing (RED) test suite committed asserting the six REQ-10 pure-core behaviors including the two honesty guarantees (margin coverage, market≠account_key).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the loader + pure core until the suite is GREEN (live-DB verified)</name>
  <read_first>
    - app/command-center/src/lib/weekly-snapshot.ts (selectAll .range() helper lines ~260-281 — copy verbatim; createServerSupabaseClient usage + unconfigured guard lines ~302-320; inWindow ~192; soldMilestones ~557-564)
    - app/command-center/src/lib/executive-pipeline.test.ts (the RED suite from Task 1 — the contract to satisfy)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-PATTERNS.md (executive-pipeline.ts pattern assignments; Shared Patterns: selectAll, degraded rendering, createServerSupabaseClient)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-RESEARCH.md (System Architecture Diagram — the exact table set; Pitfalls 1-5; Open Question 1 recommendation = compute margin in TS, no view)
    - .planning/phases/07-executive-sales-pipeline-dashboard/07-UI-SPEC.md (Copywriting Contract — the margin-coverage + close-rate caption strings the loader must emit as data)
  </read_first>
  <action>
    Implement `src/lib/executive-pipeline.ts` so the Task 1 suite goes GREEN.

    Structure: a Supabase-free PURE CORE (the six exported functions from artifacts_this_phase_produces, operating on already-fetched plain-object rows) plus a thin `loadExecutivePipelineDashboard(filters?)` that fetches via `createServerSupabaseClient(getRuntimeEnv())`, applies the unconfigured-guard fallback shape (return a degraded payload with `errors: string[]`, never throw / never 500 SSR — per weekly-snapshot.ts's `loadLiveData` guard), then calls the pure core.

    Data fetch (ALL via a `selectAll` `.range()` loop copied verbatim from weekly-snapshot.ts — Pitfall 3):
    - `crm_pipeline` (milestone, salesperson, contract_amount, primary_estimate_amount, balance_due, lead_date/approved_date/milestone_date/created_at/updated_at, lead sources, acculynx_job_id) — the PipelineRow shape.
    - `acculynx_jobs` (id, account_key, job_category_name) — join in-memory by `acculynx_job_id` for region/office + commercial/residential (D-13). NEVER use `crm_pipeline.market` as the location dimension.
    - `acculynx_job_financials` (GP fields) — primary margin source; PER Pitfall 1 treat as near-empty live, so try-row-then-fallback.
    - `v_invoice_acculynx_match` + `abc_invoice_lines` — fallback cost path (contract_amount − invoiced cost).
    - `acculynx_sync_watermark` + `v_acculynx_cron_outcomes` — per-location freshness (D-12).
    - `acculynx_accounts` — registry labels for the 8 location keys.

    Pure-core rules (make the tests pass): milestone stage logic uses lowercase `crm_pipeline.current_milestone` only (Pitfall 5); close rate is the snapshot proxy with the mandatory caption qualifier as DATA (Pitfall 4); margin is margin-%-first (D-06) with `MarginCoverage {jobsWithCostData,totalJobsInSlice,coveragePct}` per slice and the exact UI-SPEC caption string emitted as a field, never a bare percentage; margin computed in TypeScript (Open Question 1 — NO new SQL view for v1); window tokens `this_week|last_7_days|mtd|qtd` default `last_7_days` (D-03 planner choice, state explicitly); point-in-time KPIs (pipeline value, AR) ignore the window (D-03); treat all 8 accounts as peer location entries and note the program-vs-geography distinction in a code comment (Open Question 3 planner choice).

    Before finalizing, live-DB verify the current coverage numbers per RESEARCH.md's "valid until 7 days" note: query `acculynx_job_financials` row count/account_key, and `v_invoice_acculynx_match` matched-vs-total, via the Supabase MCP or a curl against `rnhmvcpsvtqjlffpsayu`. If job-financials now has real production rows, the primary path is exercised; if still ~0, the fallback is the live path. Record the observed numbers in the SUMMARY. Do NOT hardcode coverage — always compute it.

    No new SQL migration/view in this plan (additive rule 1 respected by adding nothing). No secrets in code (rule 2) — all creds via `getRuntimeEnv`.
  </action>
  <verify>
    <automated>cd app/command-center && npx vitest run src/lib/executive-pipeline.test.ts 2>&1 | tail -5</automated>
    <automated>cd app/command-center && grep -Eq 'from *[0-9]|\.range\(' src/lib/executive-pipeline.ts && echo SELECTALL_OK</automated>
    <automated>cd app/command-center && grep -v '^ *//' src/lib/executive-pipeline.ts | grep -c 'crm_pipeline.market' | grep -qx 0 && echo NO_MARKET_DIM_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/executive-pipeline.test.ts` PASSES all six describe blocks (test-command assertion — GREEN step).
    - `executive-pipeline.ts` exports `loadExecutivePipelineDashboard` plus the six pure functions and the interfaces in artifacts_this_phase_produces (source assertion).
    - The file uses a `.range()` pagination loop for every full-table query (source assertion via grep).
    - Non-comment code does NOT use `crm_pipeline.market` as the location/region dimension (source assertion via the NO_MARKET_DIM_OK grep — the account_key join is the dimension).
    - Margin output carries a per-slice `MarginCoverage` object; a zero-cost-data slice yields the "No cost data available for these jobs yet" state, not "0% margin" (source/behavior assertion in the passing margin test).
    - The full CC suite still passes: `cd app/command-center && npx vitest run` is green (regression assertion).
    - SUMMARY records the live-DB-observed `acculynx_job_financials` and `v_invoice_acculynx_match` coverage numbers at implementation time.
  </acceptance_criteria>
  <done>Loader GREEN against the Wave 0 contract; pure core enforces the honest-margin and market≠account_key guarantees; live coverage numbers re-verified and recorded; full CC vitest suite green; zero new SQL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase prod (rnhmvcpsvtqjlffpsayu) -> SSR loader | Loader reads live job/financial/watermark data server-side via service-role creds from RuntimeEnv; must never surface a raw error/stack to the client. |
| npm registry -> build | One new dependency (chart.js) enters the supply chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Information Disclosure | Loader error handling | medium | mitigate | Unconfigured/error guard returns a degraded payload with `errors: string[]`; never throws a raw Supabase error/stack to SSR output (weekly-snapshot.ts `loadLiveData` precedent). |
| T-07-02 | Tampering (integrity) | Margin coverage computation | high | mitigate | Margin % NEVER defaults missing cost data to 0; every slice carries an explicit N-of-M `MarginCoverage`; zero-coverage slices render "No cost data" not "0% margin". Unit-tested (honesty guarantee for the PE-owner audience). |
| T-07-SC | Tampering | npm install (chart.js) | high | mitigate | chart.js@4.5.1 pre-cleared in RESEARCH Package Legitimacy Audit (MIT, canonical repo chartjs/Chart.js, no postinstall script, verified via `npm view`). Pinned exact version; verdict OK/Approved — no [ASSUMED]/[SUS]/[SLOP] package, so no blocking-human checkpoint required. |
</threat_model>

<verification>
- `cd app/command-center && npx vitest run src/lib/executive-pipeline.test.ts` — six REQ-10 behaviors green.
- `cd app/command-center && npx vitest run` — full CC suite green (no regression).
- `grep '"chart.js": "4.5.1"' app/command-center/package.json` — dependency present and pinned.
- Non-comment grep confirms `crm_pipeline.market` is not the location dimension.
</verification>

<success_criteria>
The loader computes all D-05 KPIs (funnel, close rate, margin-with-coverage, leaderboard, freshness) from live data via paginated queries, with an exported pure core that unit-tests the honest-margin and market≠account_key guarantees; chart.js@4.5.1 is installed; no new SQL and no secrets in code.
</success_criteria>

<output>
Create `.planning/phases/07-executive-sales-pipeline-dashboard/07-01-SUMMARY.md` when done
</output>
