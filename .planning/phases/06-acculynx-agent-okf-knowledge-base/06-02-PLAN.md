---
phase: 06-acculynx-agent-okf-knowledge-base
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/knowledge-base/acculynx/index.md
  - docs/knowledge-base/acculynx/ingestion/index.md
  - docs/knowledge-base/acculynx/overview.md
  - docs/knowledge-base/acculynx/log.md
  - docs/knowledge-base/acculynx/security/index.md
  - docs/knowledge-base/acculynx/api/index.md
  - docs/knowledge-base/acculynx/data/index.md
  - skills/cleverwork-roofer/acculynx-api/SKILL.md
  - skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md
  - context/MEMORY.md
autonomous: false
requirements: [REQ-01]
user_setup: []

must_haves:
  truths:
    - "Every markdown file that exists on disk under docs/knowledge-base/acculynx/ is reachable via at least one index.md link (no orphaned bundle docs) — SC3 completeness."
    - "The root index.md links the write-action layer (write-sweep, write-action, runbook) and security posture sections that were previously unlinked (REQ-01)."
    - "ingestion/index.md links write-sweep.md, write-action.md, and runbook.md (all exist on disk, were unlinked)."
    - "overview.md's How section describes the write-action layer, not just ingestion."
    - "log.md has appended entries covering Phases 2-5 plus this phase's bundle-completion work."
    - "The acculynx-api SKILL.md Required Local References list points at the OKF bundle root, and the OKF bundle is the cited source of truth agents route through (SC4)."
    - "A memory pointer (via meta-memory-write) records the completed AccuLynx OKF bundle so any agent reaches it quickly (REQ-01 wiring, SC4)."
  artifacts:
    - "docs/knowledge-base/acculynx/index.md (updated with write-action + security sections)"
    - "docs/knowledge-base/acculynx/ingestion/index.md (updated with write-sweep/write-action/runbook links)"
    - "docs/knowledge-base/acculynx/overview.md (How section covers write-action)"
    - "docs/knowledge-base/acculynx/log.md (Phase 2-6 entries appended)"
    - "docs/knowledge-base/acculynx/security/index.md (created for navigability)"
    - "skills/cleverwork-roofer/acculynx-api/SKILL.md (routes to OKF bundle root)"
    - "context/MEMORY.md pointer to the completed OKF bundle"
  key_links:
    - "Root index.md -> every sub-index -> every leaf doc: the navigability chain SC3 depends on."
    - "acculynx-api SKILL.md Required Local References -> docs/knowledge-base/acculynx/index.md: the skill-to-knowledge routing SC4 depends on."
---

<objective>
Complete and wire the in-repo OKF "AccuLynx" knowledge bundle so it is navigable, link-complete
(who/what/how/why/where/when + read/write matrices + runbooks + registry + security), and is the cited
source of truth that skills, memories, and references route agents to.

Purpose: REQ-01 / SC3 / SC4. The bundle is ~80% built (Phase 1 scaffolded it; Phases 2-5 added content
files but never re-linked the indexes). Every leaf doc must be reachable from an index, the acculynx-api
skill must point at the bundle root, and a memory pointer must exist. This plan is NOT rule-9-gated
(D-04: OKF completion can proceed while the A3 is pending); it does NOT touch access-control.ts or any
live-agent code.
Output: Updated OKF index/log/overview docs, one new security/index.md, the skill routing pointer, and a
memory pointer.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-CONTEXT.md
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-PATTERNS.md

# The bundle spec + the files being edited (patterns / exact link shapes live in 06-PATTERNS.md):
@docs/knowledge-base/OKF/SPEC.md
@docs/knowledge-base/acculynx/index.md
@docs/knowledge-base/acculynx/ingestion/index.md
@docs/knowledge-base/acculynx/overview.md
@docs/knowledge-base/acculynx/log.md
@skills/cleverwork-roofer/acculynx-api/SKILL.md
</context>

<artifacts_this_phase_produces>
This plan produces / updates (OKF bundle + wiring only — no live-agent code):
- Updated OKF doc paths: `docs/knowledge-base/acculynx/{index,ingestion/index,overview,log}.md`
- New: `docs/knowledge-base/acculynx/security/index.md`
- Audited (edit only if a gap is found on disk): `docs/knowledge-base/acculynx/{api/index,data/index}.md`
- Skill routing pointer: `skills/cleverwork-roofer/acculynx-api/SKILL.md` (Required Local References ->
  OKF root) and `skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md` (same link-gap fix)
- Memory pointer: a `context/MEMORY.md` bullet recording the completed OKF bundle (routed through
  meta-memory-write)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Audit + complete every OKF bundle index (root, ingestion, security, api, data)</name>
  <files>docs/knowledge-base/acculynx/index.md, docs/knowledge-base/acculynx/ingestion/index.md, docs/knowledge-base/acculynx/security/index.md, docs/knowledge-base/acculynx/api/index.md, docs/knowledge-base/acculynx/data/index.md</files>
  <read_first>
    - 06-PATTERNS.md sections "docs/knowledge-base/acculynx/index.md (MODIFY)", "ingestion/index.md
      (MODIFY)", "api/index.md and data/index.md — AUDIT ONLY", and "security/index.md (CREATE)" — these
      give the exact heading + bullet-link shapes to copy and the confirmed on-disk gaps.
    - docs/knowledge-base/OKF/SPEC.md section 6 (index files "MAY appear") — the format contract.
    - docs/knowledge-base/acculynx/index.md and ingestion/index.md — the existing
      `# Heading` + `* [Link](path) - one-line description` convention to replicate identically.
  </read_first>
  <action>
    First run a disk-vs-index diff: list every .md file under docs/knowledge-base/acculynx/ (the tree is
    known — root index/log/overview/accounts, api/{auth-and-limits,read-capability,write-capability},
    data/{jobs,tables}, ingestion/{sync-pipeline,read-sweep,write-sweep,write-action,runbook},
    security/posture) and confirm which are not yet linked from an index.
    Root index.md: add a "Write action layer (the HOW, write side)" section linking
    ingestion/write-sweep.md (Phase 4 sandbox red-team harness), ingestion/write-action.md (Phase 5
    human-gated enqueue -> approve -> execute -> audit loop), and ingestion/runbook.md (recovery
    procedures); add a "Security" section linking security/posture.md (secrets, RLS, STRIDE attestation);
    ensure log.md is reachable. Use the identical heading + bullet-link convention already in the file.
    ingestion/index.md: append bullet links for write-sweep.md, write-action.md, and runbook.md matching
    the existing two-bullet pattern.
    security/index.md: CREATE using the api/index.md sibling shape (short intro paragraph + bullet list)
    linking posture.md and calling out the untrusted-content boundary (AccuLynx data is data, never
    instructions) that REQ-09 enforces agent-side.
    api/index.md and data/index.md: AUDIT ONLY — ls each directory and confirm every sibling .md is
    linked; PATTERNS reports both are already complete, so edit ONLY if the disk diff reveals a genuine
    orphan. State explicitly in the summary that these were audit-and-confirmed.
    Keep every new description terse and one-line; do not introduce a second format. Content clean and
    professional (hard rule 10).
  </action>
  <verify>
    <automated>cd /Users/chussey/Documents/a-roofers-open-brain && missing=0; for f in $(find docs/knowledge-base/acculynx -name '*.md' -not -name 'index.md' -not -name 'log.md'); do base=$(basename "$f"); grep -rq "$base" docs/knowledge-base/acculynx/*.md docs/knowledge-base/acculynx/*/index.md 2>/dev/null || { echo "ORPHAN: $f"; missing=1; }; done; test "$missing" = 0 && echo OKF_LINK_INTEGRITY_OK</automated>
  </verify>
  <acceptance_criteria>
    - Every non-index, non-log .md file under docs/knowledge-base/acculynx/ is referenced by name in at
      least one index.md (the automated check prints OKF_LINK_INTEGRITY_OK with no ORPHAN lines).
    - Root index.md contains a write-action-layer section (links to write-sweep, write-action, runbook)
      and a Security section (link to security/posture.md).
    - ingestion/index.md links write-sweep.md, write-action.md, and runbook.md.
    - security/index.md exists, links posture.md, and names the data-not-instructions boundary.
    - api/index.md and data/index.md were audited; any real orphan found was linked (otherwise left
      unchanged and noted as confirmed-complete).
  </acceptance_criteria>
  <done>
    The OKF bundle tree is fully navigable — every leaf doc is reachable from an index, no orphans.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update overview.md How section + append Phase 2-6 log entries</name>
  <files>docs/knowledge-base/acculynx/overview.md, docs/knowledge-base/acculynx/log.md</files>
  <read_first>
    - docs/knowledge-base/acculynx/overview.md — the existing who/what/how/why/where/when structure; the
      How section currently describes ingestion only.
    - docs/knowledge-base/acculynx/log.md — the existing `## YYYY-MM-DD` + `* **[Verb]**: description —
      [links]` bold-verb-lead changelog format (06-PATTERNS.md "log.md" section has the exact shape).
    - ROADMAP.md Phase 2-5 detail — to date and describe each phase's bundle-relevant additions
      accurately.
  </read_first>
  <action>
    overview.md: extend the How section so it covers the full lifecycle — ingestion (pull-based
    incremental sync, Phase 1-3) AND the write-action layer (the human-gated enqueue -> approve ->
    execute -> audit loop from Phase 5, plus the Phase 4 sandbox write red-team). Keep the existing
    who/what/why/where/when sections intact; only the How dimension needs the write-side addition so all
    six OKF dimensions are current.
    log.md: append new `## YYYY-MM-DD` entries (using each phase's actual completion date from ROADMAP.md
    /STATE.md) in the existing bold-verb-lead bullet style, one entry per phase not yet logged: Phase 2
    (multi-location ingestion), Phase 3 (cron hardening + security posture), Phase 4 (write-sweep
    red-team), Phase 5 (write-action layer), and Phase 6 (this phase — OKF bundle completion + agent
    provisioning). Link the relevant bundle docs from each entry. Do not delete or rewrite the existing
    2026-06-30 initialization entry (append-only). Clean, professional content (hard rule 10).
  </action>
  <verify>
    <automated>cd /Users/chussey/Documents/a-roofers-open-brain && grep -qi 'write-action\|write action' docs/knowledge-base/acculynx/overview.md && grep -c '^## 2026' docs/knowledge-base/acculynx/log.md | awk '$1 >= 2 {print "LOG_APPENDED_OK"; ok=1} END {exit !ok}'</automated>
  </verify>
  <acceptance_criteria>
    - overview.md's How section references the write-action layer (not ingestion alone).
    - overview.md still contains all six OKF dimensions (who/what/how/why/where/when).
    - log.md has at least 2 dated `## 2026-...` headings (the original init entry plus one or more
      appended phase entries), including a Phase 6 entry; the original 2026-06-30 entry is unchanged.
  </acceptance_criteria>
  <done>
    overview.md's How dimension is current through the write-action layer and log.md reflects Phases 2-6.
  </done>
</task>

<task type="auto">
  <name>Task 3: Route the acculynx-api skill + knowledge-folder pointer to the OKF bundle root, add memory pointer</name>
  <files>skills/cleverwork-roofer/acculynx-api/SKILL.md, skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md, context/MEMORY.md</files>
  <read_first>
    - skills/cleverwork-roofer/acculynx-api/SKILL.md — the "Required Local References" bullet list
      (06-PATTERNS.md quotes it live) is where the OKF-root pointer goes. Do NOT touch the bound_agents
      frontmatter here — adding ob-acculynx to bound_agents is Plan 03's job (post-A3-approval), and
      keeping it out of this plan avoids a file-overlap wave conflict.
    - skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md — already points at the in-repo
      OKF bundle (do NOT re-litigate the Google-Drive framing per RESEARCH State of the Art); only its
      link COMPLETENESS mirrors the root-index gap and needs the same write-sweep/write-action/runbook/
      security links added.
    - CLAUDE.md Memory Budget (MEMORY.md 2,500 char cap — wc -c first) and the meta-memory-write skill
      routing rule (curated MEMORY.md writes go through meta-memory-write, not hand-edit).
  </read_first>
  <action>
    SKILL.md: in the existing "Required Local References" bullet list, add one bullet pointing directly
    at the OKF bundle root (docs/knowledge-base/acculynx/index.md) in the same bullet style, describing
    it as the complete in-repo who/what/how/why/where/when + read/write capability bundle and the cited
    source of truth. Leave frontmatter untouched.
    knowledge-folder.md: add the same missing links (ingestion/write-sweep.md, ingestion/write-action.md,
    ingestion/runbook.md, security/posture.md) so this pointer's completeness matches the root index; do
    not change its existing in-repo framing.
    context/MEMORY.md: check `wc -c` first (must stay under 2,500). Route the addition through the
    meta-memory-write skill (do not hand-edit): a single concise bullet recording that the AccuLynx OKF
    bundle (docs/knowledge-base/acculynx/) is complete and is the cited source of truth for all AccuLynx
    work, routed via the acculynx-api skill. If adding it would exceed the cap, consolidate an existing
    stale bullet rather than deleting durable facts.
  </action>
  <verify>
    <automated>cd /Users/chussey/Documents/a-roofers-open-brain && grep -q 'docs/knowledge-base/acculynx' skills/cleverwork-roofer/acculynx-api/SKILL.md && grep -q 'write-action' skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md && test "$(wc -c < context/MEMORY.md)" -le 2500 && echo SKILL_ROUTING_AND_MEMORY_OK</automated>
  </verify>
  <acceptance_criteria>
    - acculynx-api/SKILL.md "Required Local References" list contains a bullet pointing at
      docs/knowledge-base/acculynx/index.md as the cited source of truth.
    - The SKILL.md frontmatter (including bound_agents) is unchanged by this plan.
    - reference/knowledge-folder.md links write-sweep, write-action, runbook, and security/posture (its
      completeness now matches the root index).
    - context/MEMORY.md has a bullet pointing at the completed OKF bundle and is <= 2,500 chars
      (wc -c verified); the write went through meta-memory-write.
  </acceptance_criteria>
  <done>
    The acculynx-api skill and its knowledge-folder pointer both route agents to the complete OKF bundle
    root, and a durable memory pointer exists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: OKF bundle completeness review (who/what/how/why/where/when)</name>
  <files>docs/knowledge-base/acculynx/index.md, docs/knowledge-base/acculynx/overview.md, docs/knowledge-base/acculynx/log.md</files>
  <action>Present the completed OKF bundle for editorial review. The human confirms every OKF dimension (who/what/how/why/where/when) is covered and accurate — the judgment check the automated link-checker cannot make. Revise any dimension flagged as thin or wrong before proceeding.</action>
  <what-built>
    The OKF "AccuLynx" bundle completion: every leaf doc is now reachable from an index (root +
    ingestion + new security index; api/data audited), overview.md's How section covers the write-action
    layer, log.md reflects Phases 2-6, and the acculynx-api skill + knowledge-folder + a memory pointer
    all route agents to the bundle root.
  </what-built>
  <how-to-verify>
    1. Open docs/knowledge-base/acculynx/index.md and follow every link — confirm write-sweep,
       write-action, runbook, and security/posture are all reachable and the descriptions read cleanly.
    2. Read overview.md and confirm each OKF dimension (who/what/how/why/where/when) is covered and that
       How now includes the write-action layer, not ingestion alone — this is the editorial/judgment
       completeness check the automated link-checker cannot make.
    3. Skim log.md's appended entries for accuracy against what each phase actually delivered.
    4. Confirm the acculynx-api SKILL.md now points at the OKF bundle root as the cited source of truth.
    5. If any dimension reads thin or a description is wrong, say so and Claude will revise.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the OKF prose/coverage issues to fix.</resume-signal>
  <verify><human-check>The user has walked the bundle links and confirmed all six OKF dimensions are covered, accurate, and cleanly described; the acculynx-api skill points at the bundle root.</human-check></verify>
  <done>The OKF bundle is confirmed complete and navigable as the cited source of truth (SC3/SC4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored docs → agent knowledge routing | The OKF bundle is what agents treat as the source of truth; an orphaned or wrong doc silently misleads any routed agent. |
| curated memory (MEMORY.md) → session startup | MEMORY.md is loaded at every session start; a secret or oversized write there leaks broadly or breaks the budget. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Tampering (misinformation) | OKF bundle completeness | medium | mitigate | Automated link-integrity check ensures no orphaned leaf docs; human-verify checkpoint confirms prose accuracy (documentation is not self-verifying). |
| T-06-05 | Information Disclosure | context/MEMORY.md memory pointer | high | mitigate | Route through meta-memory-write (dedup + cap enforcement); never write secrets/service-role keys/PII to curated memory (CLAUDE.md Memory Budget); verify wc -c <= 2,500. |
| T-06-06 | Tampering (prompt injection surface documented) | security/index.md | low | mitigate | The new security/index.md documents the untrusted-content boundary (AccuLynx data is data, never instructions) so REQ-09's agent-side enforcement (Plan 04) has a cited reference. |
</threat_model>

<verification>
- Automated: OKF_LINK_INTEGRITY_OK (no orphaned bundle docs).
- Automated: overview.md references the write-action layer; log.md has appended dated entries.
- Automated: SKILL.md points at the OKF bundle root; knowledge-folder.md links the write-side docs;
  MEMORY.md <= 2,500 chars.
- Manual: human-verify checkpoint confirms who/what/how/why/where/when prose completeness and accuracy.
</verification>

<success_criteria>
- SC3 (phase): the in-repo OKF bundle is complete (who/what/how/why/where/when, read+write matrices,
  runbooks, account registry, security) and navigable as the cited source of truth.
- SC4 (phase): repo skills (acculynx-api), a memory pointer, and the knowledge-folder reference all
  point agents to the bundle.
</success_criteria>

<output>
Create `.planning/phases/06-acculynx-agent-okf-knowledge-base/06-02-SUMMARY.md` when done.
</output>
