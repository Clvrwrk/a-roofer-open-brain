---
phase: 06-acculynx-agent-okf-knowledge-base
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - app/command-center/src/lib/access-control.ts
  - app/command-center/src/lib/access-control.test.ts
  - skills/cleverwork-roofer/acculynx-api/SKILL.md
autonomous: false
requirements: [REQ-09]
user_setup:
  - service: slack
    why: "ob-acculynx needs its own Slack bot identity to post write-pending + ingestion-health notifications under its own name (D-02). A Slack config token cannot mint a bot token — a human must Install the app to the Workspace."
    dashboard_config:
      - task: "Create the ob-acculynx Slack app, Install to Workspace, copy the Bot User OAuth Token (xoxb-...)"
        location: "api.slack.com -> Your Apps -> (new app) -> Install to Workspace -> OAuth & Permissions"
    env_vars:
      - name: OB_ACCULYNX_BOT_TOKEN
        source: "Slack app -> OAuth & Permissions -> Bot User OAuth Token; set in Coolify env (never committed)"
  - service: coolify
    why: "The ob-acculynx service token and Slack bot token are prod secrets — set in Coolify env, never in the repo (hard rule 2)."
    env_vars:
      - name: AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX
        source: "sha256 of a freshly generated ob-acculynx bearer token (hashed form, per workos-agent-auth); set in Coolify env"
      - name: OB_ACCULYNX_BOT_TOKEN
        source: "Bot User OAuth Token from the Slack app; set in Coolify env"

must_haves:
  truths:
    - "ob-acculynx resolves in SERVICE_AGENT_IDENTITIES with departmentAccess: \"all\" (spans sales/accounting/operations — required, NOT ['accounting','operations'])."
    - "A vitest assertion proves ob-acculynx exists in the roster with departmentAccess: \"all\" (Wave 0 test scaffold created because no access-control.test.ts existed)."
    - "The access-control.ts edit lands ONLY after the Wave 1 A3 reads Status: approved (D-04 hard gate)."
    - "ob-acculynx is added to the acculynx-api SKILL.md bound_agents frontmatter array."
    - "A live smoke test proves an ob-acculynx bearer token resolves to an actor that can enqueue a write for a sales-department lane, an accounting-department lane, and an operations-department lane (departmentAccess:\"all\" proof, REQ-09/SC2)."
    - "No secret value is committed anywhere — service token + bot token live only in Coolify env (hard rule 2)."
  artifacts:
    - "app/command-center/src/lib/access-control.ts (ob-acculynx roster entry, entry #16)"
    - "app/command-center/src/lib/access-control.test.ts (new — Wave 0 vitest scaffold asserting the roster entry)"
    - "skills/cleverwork-roofer/acculynx-api/SKILL.md (bound_agents += ob-acculynx)"
  key_links:
    - "SERVICE_AGENT_IDENTITIES ob-acculynx entry + AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX env var -> resolveServiceActorFromToken resolves the agent (zero other code changes needed)."
    - "ob-acculynx actor (departmentAccess:\"all\") -> /api/agent/acculynx-write-action/enqueue accepts lanes across all three departments."
---

<objective>
Register the ob-acculynx live roster identity and wire its provisioning: add the entry to
SERVICE_AGENT_IDENTITIES with departmentAccess: "all", create the Wave 0 vitest scaffold that asserts
it, add ob-acculynx to the acculynx-api skill's bound_agents, then run the human-required provisioning
(service token + Slack bot token in Coolify) and a cross-department enqueue smoke test.

Purpose: REQ-09 / SC2 — "The AccuLynx Agent exists (role, bound skills/tools, read + gated-write
capability) and can answer/act across the full data and capability surface." This plan delivers the
LIVE-RUNTIME half of the hybrid agent (D-01a). It is gated on Wave 1: no roster edit ships before the A3
is approved (D-04, Pitfall 4). NO Supabase migration is added — ob-acculynx is a TS array entry, not a
DB table (schema-gate confirmed clean).
Output: the roster entry + its test, the skill binding, and a live cross-department enqueue proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-CONTEXT.md
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-PATTERNS.md
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-RESEARCH.md

# The exact roster shape + the generic resolution/enqueue code it plugs into:
@app/command-center/src/lib/access-control.ts
@app/command-center/src/pages/api/agent/acculynx-write-action/enqueue.ts

# Provisioning skills (read the SKILL.md for each before the provisioning tasks):
@.claude/skills/workos-agent-auth/SKILL.md
@.claude/skills/slack-agents/SKILL.md
@.claude/skills/coolify/SKILL.md

# GATE: this plan depends on the Wave 1 A3 approval:
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces (LIVE-RUNTIME agent half — post-A3-approval only):
- `app/command-center/src/lib/access-control.ts` — new `ob-acculynx` entry in SERVICE_AGENT_IDENTITIES
  (id "ob-acculynx", displayName "AccuLynx", handle "@ob-acculynx", departmentAccess "all", roles).
- `app/command-center/src/lib/access-control.test.ts` — new vitest file (Wave 0 scaffold) asserting the
  ob-acculynx roster entry and its departmentAccess.
- `skills/cleverwork-roofer/acculynx-api/SKILL.md` — `ob-acculynx` appended to `bound_agents`.
- Provisioned (in Coolify env, NOT the repo): `AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX`,
  `OB_ACCULYNX_BOT_TOKEN`.
</artifacts_this_phase_produces>

<a3_gate_precondition>
Before ANY task in this plan runs, confirm the Wave 1 A3 (proposals/2026-07-01-acculynx-agent.md) reads
`Status: approved` with a filled Section 8 decision block (Approver + date). If it does NOT, STOP — this
plan is blocked by D-04. No commit touching access-control.ts / SKILL.md / any provisioning may be
timestamped before the A3's Status: approved line (Pitfall 4).
</a3_gate_precondition>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ob-acculynx to SERVICE_AGENT_IDENTITIES + create the Wave 0 access-control test</name>
  <files>app/command-center/src/lib/access-control.ts, app/command-center/src/lib/access-control.test.ts</files>
  <read_first>
    - app/command-center/src/lib/access-control.ts — the ServiceAgentIdentity interface (id, displayName,
      handle, departmentAccess, roles) and the ob-conductor entry (departmentAccess: "all") as the exact
      analog; SERVICE_AGENT_IDENTITIES ends after the session-analyst entry (append before the closing
      `];`).
    - 06-PATTERNS.md "access-control.ts — add ob-acculynx" section — the exact object shape to copy and
      the note that serviceAgentToActor / SERVICE_AGENT_PERMISSIONS / the enqueue route are already
      generic (no other code change needed).
    - 06-RESEARCH.md Pitfall 1 + Alternatives Considered — WHY departmentAccess MUST be "all" (LANE_DEPARTMENT
      spans sales + accounting + operations; ["accounting","operations"] would 403 sales lanes).
    - app/command-center/vitest.config.ts — the test runner config (per 06-VALIDATION.md).
  </read_first>
  <behavior>
    - Test 1: SERVICE_AGENT_IDENTITIES contains exactly one entry with id "ob-acculynx".
    - Test 2: that entry's departmentAccess equals the string "all" (not an array).
    - Test 3: departmentAllowed(actor, "sales"), (actor, "accounting"), and (actor, "operations") all
      return true for an ob-acculynx-derived actor (or the equivalent public predicate) — proving the
      "all" grant covers every write-lane department.
  </behavior>
  <action>
    First (RED): create app/command-center/src/lib/access-control.test.ts — no access-control.test.ts
    exists yet (Wave 0 scaffold, per 06-VALIDATION.md). Import from ./access-control and write the three
    assertions above. Use the existing exported symbols (SERVICE_AGENT_IDENTITIES, and the
    departmentAccess predicate at access-control.ts around line 558, `actor.departmentAccess === "all" ||
    actor.departmentAccess.includes(department)`) — resolve an actor via serviceAgentToActor or the
    exported resolver rather than reaching into internals. Run vitest; it MUST fail (no ob-acculynx yet).
    Then (GREEN): add the ob-acculynx object to SERVICE_AGENT_IDENTITIES, appended after the
    session-analyst entry and before the closing `];`, matching the ob-conductor shape exactly: id
    "ob-acculynx", displayName "AccuLynx", handle "@ob-acculynx", departmentAccess "all", roles a
    two-element string array in the established style (roles are descriptive metadata only, unused in
    access decisions — planner's cosmetic call). departmentAccess MUST be the string "all", NOT
    ["accounting","operations"] (Pitfall 1). No other code edit is required. Re-run vitest; all three
    tests MUST pass.
  </action>
  <verify>
    <automated>cd /Users/chussey/Documents/a-roofers-open-brain/app/command-center && npx vitest run src/lib/access-control.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - access-control.test.ts exists and its three assertions pass under vitest.
    - SERVICE_AGENT_IDENTITIES has exactly one ob-acculynx entry with departmentAccess === "all".
    - The entry shape matches ServiceAgentIdentity (id, displayName, handle, departmentAccess, roles).
    - No changes were needed to serviceAgentToActor, SERVICE_AGENT_PERMISSIONS, or the enqueue route.
  </acceptance_criteria>
  <done>
    ob-acculynx is a registered roster identity with an "all" department grant, proven by a green vitest.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ob-acculynx to the acculynx-api skill bound_agents</name>
  <files>skills/cleverwork-roofer/acculynx-api/SKILL.md</files>
  <read_first>
    - skills/cleverwork-roofer/acculynx-api/SKILL.md frontmatter — the bound_agents YAML array
      (currently: capture, conductor, auditor, ob-sales, ob-ops, ob-accounting). NOTE: the Required
      Local References OKF-root pointer was already added by Plan 02; this task touches ONLY the
      bound_agents frontmatter array (kept separate from Plan 02 to avoid a same-wave file conflict —
      Plan 02 is Wave 1, this is Wave 2, so the two SKILL.md edits are sequenced across waves).
  </read_first>
  <action>
    Add `ob-acculynx` as a new entry in the bound_agents frontmatter array of
    skills/cleverwork-roofer/acculynx-api/SKILL.md, preserving the existing entries and YAML list
    indentation. This binds the live roster identity to the skill it routes through (D-01: one brain,
    two entry points, both through the acculynx-api skill). Do not alter any other frontmatter key or the
    Required Local References list (already handled in Plan 02).
  </action>
  <verify>
    <automated>cd /Users/chussey/Documents/a-roofers-open-brain && grep -A12 '^bound_agents:' skills/cleverwork-roofer/acculynx-api/SKILL.md | grep -q 'ob-acculynx' && echo BOUND_AGENTS_OK</automated>
  </verify>
  <acceptance_criteria>
    - The bound_agents frontmatter array contains ob-acculynx alongside the pre-existing entries.
    - No other frontmatter key was modified.
  </acceptance_criteria>
  <done>
    The acculynx-api skill lists ob-acculynx among its bound agents.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Provision ob-acculynx secrets (Slack bot + service token in Coolify)</name>
  <files>app/command-center/src/lib/access-control.ts</files>
  <action>Guide the user through the human-only provisioning: create/install the ob-acculynx Slack app (config tokens cannot mint bot tokens — T-06-09), generate the ob-acculynx bearer token + its sha256, and set OB_ACCULYNX_BOT_TOKEN + AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX in Coolify (never the repo, hard rule 2), then redeploy. Never echo token values.</action>
  <what-built>
    The ob-acculynx roster identity + test + skill binding are in the repo. The remaining provisioning is
    human-only: a Slack config token CANNOT mint a bot token, and prod secrets go in Coolify, not the
    repo (hard rule 2). These steps mint the two ob-acculynx secrets.
  </what-built>
  <how-to-verify>
    1. SLACK BOT (per the slack-agents skill): at api.slack.com -> Your Apps, create the ob-acculynx app
       (or reuse the shared @openbrain fallback if you decide a dedicated bot is not needed yet — D-02
       prefers its own identity). Install to Workspace. Copy the Bot User OAuth Token (xoxb-...).
    2. SERVICE TOKEN (per the workos-agent-auth skill): generate a fresh ob-acculynx bearer token and
       compute its sha256 (hashed form preferred — no plaintext at rest).
    3. COOLIFY ENV (per the coolify skill): set OB_ACCULYNX_BOT_TOKEN and
       AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX in the Command Center Coolify env. Do NOT commit either
       value. Redeploy so the new env is live.
    4. After redeploy, confirm the deploy landed (buildCommit flipped) and that
       GET /healthz shows agentServiceTokenCount incremented by one (the new hashed token is registered).
    5. Provide the plaintext ob-acculynx bearer token to Claude ONLY via a local .env / secure channel
       (never pasted into a committed file) so the next task can run the smoke test — or run the smoke
       test yourself.
  </how-to-verify>
  <resume-signal>Type "provisioned" once OB_ACCULYNX_BOT_TOKEN + AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX are set in Coolify, redeployed, and /healthz agentServiceTokenCount has incremented; or "deferred: bot" if you are reusing the @openbrain fallback bot for now.</resume-signal>
  <verify><human-check>GET /healthz shows agentServiceTokenCount incremented by one after redeploy (the hashed ob-acculynx token is registered); the two secrets exist in Coolify env and in no committed file.</human-check></verify>
  <done>ob-acculynx's service token (and, unless deferred, its Slack bot token) are live in Coolify env, redeployed, with no secret committed to the repo.</done>
</task>

<task type="auto">
  <name>Task 4: Cross-department enqueue smoke test (departmentAccess "all" proof)</name>
  <files>app/command-center/src/lib/access-control.ts</files>
  <read_first>
    - 06-RESEARCH.md Code Examples — the "Enqueuing a proven-safe AccuLynx write as ob-acculynx" curl
      shape and the LANE_DEPARTMENT mapping (sales: postContact/postJob/putJobLeadSource/
      deleteJobSalesOwner; accounting: postJobPaymentReceived/postWorksheetItem; operations: the rest).
    - app/command-center/src/pages/api/agent/acculynx-write-action/enqueue.ts — the accepted body shape
      (lane, accountKey, targetEnv "sandbox", payload) and its department-scoped gate.
    - .claude/skills/workos-agent-auth/SKILL.md — the bearer-auth curl cookbook.
  </read_first>
  <action>
    Using the provisioned ob-acculynx bearer token against the SANDBOX target env (targetEnv:
    "sandbox" — never prod, D-09 boundary), enqueue one proven-safe write per department to prove the
    departmentAccess "all" grant works end-to-end: one sales-department lane (e.g. postContact), one
    accounting-department lane (e.g. postJobPaymentReceived or postWorksheetItem), and one
    operations-department lane (e.g. postJobMessage). Each POST to
    /api/agent/acculynx-write-action/enqueue must return an accepted/pending-review response (HTTP 2xx),
    NOT a 403 (a 403 would mean the grant is too narrow — the Pitfall 1 failure mode). Do NOT approve or
    execute any of these — the agent enqueues only (D-03: never approves); leaving them as pending_review
    is the correct terminal state for this smoke test. Record the three responses in the SUMMARY as live
    evidence (this repo verifies Command-Center integration surfaces via live smoke tests, not unit
    tests — RESEARCH Validation Architecture). This task edits no source file; access-control.ts is
    listed as the touched artifact only because the smoke test proves its ob-acculynx grant.
  </action>
  <verify>
    <human-check>Three enqueue POSTs (one sales lane, one accounting lane, one operations lane) each returned a 2xx accepted/pending_review response with the ob-acculynx bearer token against targetEnv sandbox; none returned 403. Response bodies recorded in the SUMMARY. (Live smoke test — no unit-test equivalent exists for this integration surface per RESEARCH.)</human-check>
  </verify>
  <acceptance_criteria>
    - An ob-acculynx bearer call to /healthz (or /api/agent/work-queue) resolves the actor (no 401).
    - Three enqueue POSTs across sales/accounting/operations departments each return 2xx
      accepted/pending_review (no 403) against targetEnv sandbox.
    - No write was approved or executed (all three remain pending_review — agent never approves, D-03).
    - The three response bodies are recorded in the SUMMARY as live evidence.
  </acceptance_criteria>
  <done>
    ob-acculynx can read/answer and enqueue proven-safe writes across all three departments, proving the
    live-runtime agent works within the D-03 authority envelope.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bearer token → server actor | resolveServiceActorFromToken maps the ob-acculynx bearer token to a CommandCenterActor server-side; a leaked token grants departmentAccess:"all" enqueue authority. |
| agent → write-action (enqueue only) | ob-acculynx may enqueue but the approval decision is a separate human-only permission; the agent must never receive approval authority. |
| repo → prod secrets | The service token + bot token must exist only in Coolify env, never in a committed file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Elevation of Privilege | ob-acculynx departmentAccess:"all" token leak | high | mitigate | Use the hashed env-var form AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX (no plaintext at rest, per workos-agent-auth); documented rotation on suspected leak; token set in Coolify env only. |
| T-06-08 | Elevation of Privilege | Agent gaining approval authority | high | mitigate | ob-acculynx is a service_agent; approval.decide / approval.decide_prod_write are human-only permissions never granted to service agents (D-03: never approves). Smoke test leaves all writes at pending_review, proving enqueue-only. |
| T-06-09 | Information Disclosure | Service token / bot token committed to repo or echoed in transcript | high | mitigate | Hard rule 2: values live only in Coolify env; env var NAMES only in docs/plan; never echo token values (established skill discipline). Smoke test uses the token from local .env / secure channel, not a committed file. |
| T-06-10 | Tampering | Enqueue smoke test accidentally targeting prod | high | mitigate | Smoke test hard-codes targetEnv:"sandbox"; the Phase 5 D-09 two-barrier gate independently blocks any prod write without approval. |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this phase (RESEARCH: no new runtime dependencies, no package.json change). No install task exists; nothing to gate. |
</threat_model>

<verification>
- Automated: `npx vitest run src/lib/access-control.test.ts` passes (ob-acculynx present, departmentAccess
  "all", all three departments allowed).
- Automated: bound_agents contains ob-acculynx.
- Manual: /healthz agentServiceTokenCount incremented after Coolify provisioning; three cross-department
  enqueue POSTs return 2xx (no 403) against sandbox; no write approved/executed.
- Full suite gate before verify: `cd app/command-center && npx vitest run && npx astro check`.
</verification>

<success_criteria>
- SC2 (phase): the AccuLynx Agent exists as a live roster identity that can read/answer and enqueue
  gated writes across the full department surface (sales/accounting/operations).
- REQ-09 (live-runtime half): ob-acculynx registered, provisioned, and proven end-to-end in sandbox.
- D-04 honored: no roster/skill/provisioning change landed before the A3 Status: approved.
</success_criteria>

<output>
Create `.planning/phases/06-acculynx-agent-okf-knowledge-base/06-03-SUMMARY.md` when done.
</output>
