---
phase: 06-acculynx-agent-okf-knowledge-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proposals/2026-07-01-acculynx-agent.md
autonomous: false
requirements: [REQ-09]
user_setup: []

must_haves:
  truths:
    - "An A3 proposal for the AccuLynx Agent exists in proposals/ following the 9-section _a3-template.md structure (SC1, D-04)."
    - "The A3 justifies the agent per CLAUDE.md hard rule 9 (10x ROI gate), using the mission-grade-infrastructure exemption framing precedent from 2026-06-09-acculynx-api.md."
    - "The A3 has been explicitly approved by the user (Status: approved) before ANY agent code or config ships (D-04 hard gate)."
    - "The A3 risk section names the broad departmentAccess:\"all\" grant and the untrusted-content (data-not-instructions, REQ-09) boundary as explicit risks."
  artifacts:
    - "proposals/2026-07-01-acculynx-agent.md (the rule-9 A3, Status flips to approved at the human checkpoint)"
  key_links:
    - "A3 approval gates the agent-build wave (Wave 2: Plans 03/04) — the roster-identity edit and subagent file MUST NOT land before Status: approved."
---

<objective>
Author the rule-9 A3 (`proposals/2026-07-01-acculynx-agent.md`) that justifies the dedicated AccuLynx
Agent and obtain the user's explicit approval. This is the D-04 HARD SEQUENCING GATE: no agent code
(roster identity, subagent file, Slack/Coolify provisioning) ships live until this A3 reads
`Status: approved`.

Purpose: CLAUDE.md hard rule 9 (10x ROI gate) requires an approved A3 (`proposals/`) before any new
agent skill/agent is built. SC1 for this phase is exactly "An approved A3 (proposals/) justifies the
AccuLynx Agent." This plan satisfies SC1 and unblocks the agent-build wave (Wave 2: Plans 03/04).
Output: A single A3 governance markdown file, drafted then human-approved.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-CONTEXT.md
@.planning/phases/06-acculynx-agent-okf-knowledge-base/06-RESEARCH.md

# The A3 structure and the same-domain precedent to copy framing from:
@proposals/_a3-template.md
@proposals/2026-06-09-acculynx-api.md
</context>

<artifacts_this_phase_produces>
This plan produces:
- `proposals/2026-07-01-acculynx-agent.md` — the rule-9 A3 (config/governance doc). Status transitions
  pending → approved at the human checkpoint. This file's approval is the gate for the agent-build wave (Plans 03/04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Draft the AccuLynx Agent A3 proposal</name>
  <files>proposals/2026-07-01-acculynx-agent.md</files>
  <read_first>
    - proposals/_a3-template.md — the required 9-section structure (header block + sections 1-9).
    - proposals/2026-06-09-acculynx-api.md — the same-domain precedent A3 (the acculynx-api skill),
      approved same-day, exempted from the 10x ROI math as mission-grade infrastructure. Copy its
      exemption framing and its section-8 decision-block format.
    - .planning/phases/06-acculynx-agent-okf-knowledge-base/06-CONTEXT.md — D-01 (hybrid substrate),
      D-02 (departmentAccess "all" + service token + Slack bot), D-03 (authority envelope: read/answer +
      enqueue-never-approve + ingestion monitor/trigger), D-04 (A3 first, hard gate).
    - CLAUDE.md hard rules 2, 5, 9, 12 — the approval bars the A3 must speak to.
  </read_first>
  <action>
    Create proposals/2026-07-01-acculynx-agent.md following the exact 9-section header + heading
    structure from proposals/_a3-template.md. Fill the header block: Proposed by "Chris"; Date
    2026-07-01; Status pending; Affected clients "template-wide"; A3 file
    proposals/2026-07-01-acculynx-agent.md.
    Section 1 (The problem, measured): AccuLynx work (read/answer, gated writes, ingestion monitoring)
    is currently done ad hoc by whichever agent picks it up, with no single owner routed through the
    OKF bundle. Section 2 (Root cause, 5 Whys brief): no dedicated agent identity owns the AccuLynx
    surface. Section 3 (Proposed solution): the hybrid AccuLynx Agent per D-01 — an ob-acculynx roster
    identity for the live runtime plus a Claude Code subagent for dev sessions, both routed through the
    acculynx-api skill + OKF bundle. Describe the authority envelope per D-03: read/answer across the
    full data + capability surface; enqueue proven-safe writes through the Phase 5 action layer
    (POST /api/agent/acculynx-write-action/enqueue) but NEVER approve (approval stays human-only per
    Phase 5 D-07/D-09); ingestion ownership = monitor cron health + trigger backfills, does NOT rewrite
    the cron. Section 4 (New state): one discoverable owner, one shared knowledge source. Section 5
    (The math): reuse the exemption-from-ROI-math framing from 2026-06-09-acculynx-api.md — this A3 has
    an even stronger exemption case because it operationalizes ~$0-marginal-cost read/write capability
    that Phases 1-5 already built and proved live, rather than proposing new capability; mark ROI
    multiplier "Exempt" and cite "mission-grade infrastructure for the primary PM adapter." Section 6
    (Risks): explicitly name (a) the broad departmentAccess "all" grant as a privilege-escalation
    surface if the token leaks (mitigation: hashed service-token env var form, rotation procedure), and
    (b) the untrusted-content boundary — AccuLynx-sourced free text must be treated as data, never as
    instructions (REQ-09 agent-side enforcement, per security/posture.md section 4). Section 7
    (Alternative considered): reusing a generic Claude Code session with the acculynx-api skill invoked
    manually — rejected per D-01 (user wants a named, discoverable agent). Section 8 (Decision): copy
    the checkbox decision block from the precedent (Approve / Kill / Defer) with Approver "Chris" and a
    blank approval date to be filled at approval time. Section 9 (Post-build tracking): note the tracked
    outcome is ob-acculynx live + subagent invokable + OKF bundle cited. Keep content clean and
    professional (hard rule 10). Do not commit or reference any secret values (hard rule 2) — env var
    names only.
  </action>
  <verify>
    <automated>test -f proposals/2026-07-01-acculynx-agent.md && grep -qE '^## 1\.' proposals/2026-07-01-acculynx-agent.md && grep -qE '^## 8\. Decision' proposals/2026-07-01-acculynx-agent.md && grep -qi 'exempt' proposals/2026-07-01-acculynx-agent.md && grep -qi 'departmentAccess' proposals/2026-07-01-acculynx-agent.md && echo A3_STRUCTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - proposals/2026-07-01-acculynx-agent.md exists with all 9 numbered section headings present.
    - Header block includes Status (pending at draft time), Date 2026-07-01, and the A3 file path.
    - Section 5 uses the exemption-from-10x-ROI-math framing (ROI multiplier "Exempt", mission-grade
      infrastructure rationale).
    - Section 6 names BOTH the departmentAccess "all" grant risk AND the data-not-instructions
      (REQ-09) untrusted-content boundary risk.
    - No secret values appear anywhere in the file (env var names only).
  </acceptance_criteria>
  <done>
    A complete, on-template A3 exists and is ready for the user to review and approve.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: A3 approval gate (rule-9, D-04 hard gate)</name>
  <files>proposals/2026-07-01-acculynx-agent.md</files>
  <action>Present the drafted A3 to the user for review. On the user's explicit "approved" signal, edit Section 8 to check the Approve box + fill Approver name and approval date, and set the header Status to "approved". Never flip Status autonomously (T-06-01). If the user requests changes, revise and re-present.</action>
  <what-built>
    A drafted A3 proposal at proposals/2026-07-01-acculynx-agent.md justifying the dedicated AccuLynx
    Agent, following the 9-section _a3-template.md structure and reusing the mission-grade-infrastructure
    ROI exemption framing from the same-domain 2026-06-09-acculynx-api.md precedent.
  </what-built>
  <how-to-verify>
    1. Open proposals/2026-07-01-acculynx-agent.md and read all 9 sections.
    2. Confirm the authority envelope (Section 3) matches D-03: read/answer, enqueue-never-approve,
       ingestion monitor/trigger (not cron rewrite).
    3. Confirm Section 5's ROI exemption framing is acceptable (or provide the exact ROI numbers you
       want instead — Claude's Discretion per CONTEXT.md).
    4. Confirm Section 6 names the departmentAccess "all" grant and the data-not-instructions boundary.
    5. If you APPROVE: in Section 8, check the "Approve" box, set Approver to your name and the approval
       date; and set the header Status to "approved". (Claude will make these exact edits on your
       signal.) NO agent code, roster edit, or Slack/Coolify provisioning ships before this Status:
       approved line is set — this is the D-04 hard gate.
    6. If you want changes, describe them and Claude will revise the A3 before re-presenting.
  </how-to-verify>
  <resume-signal>Type "approved" (Claude will set Status: approved + fill the Section 8 decision block), or describe the changes you want.</resume-signal>
  <verify><human-check>The user has reviewed the A3 and explicitly approved it; Section 8 shows the Approve box checked with an Approver name + approval date; the header Status reads "approved".</human-check></verify>
  <done>proposals/2026-07-01-acculynx-agent.md reads Status: approved with a filled Section 8 decision block — the agent-build wave (Plans 03/04) is unblocked (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → repo (governance) | The A3's Status: approved line is the authorization boundary; a self-declared approval (agent flipping Status without the human) would bypass rule 9. |
| authored doc → future agent config | The A3 authorizes a broad departmentAccess:"all" grant and a service token; if it under-specifies the risk, downstream provisioning inherits an unreviewed grant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | A3 self-approval (agent flips Status without human) | high | mitigate | Approval is a blocking-human checkpoint; the agent sets Status: approved ONLY on the user's explicit "approved" signal, never autonomously. D-04 hard gate. |
| T-06-02 | Repudiation | A3 lacks a named approver / date | medium | mitigate | Section 8 decision block captures Approver name + approval date, per the precedent format — an auditable governance record. |
| T-06-03 | Information Disclosure | Secret values (service token, bot token) pasted into the A3 | high | mitigate | Hard rule 2: A3 references env var NAMES only (e.g., AGENT_SERVICE_TOKEN_SHA256_OB_ACCULYNX), never values; verify grep finds no token literals. |
</threat_model>

<verification>
- proposals/2026-07-01-acculynx-agent.md exists with all 9 section headings.
- The file contains the ROI exemption framing and names both required risks.
- Status: approved is set ONLY after the human checkpoint (verified by the presence of an Approver name
  and approval date in Section 8).
- No secret literals anywhere in the file.
</verification>

<success_criteria>
- SC1 (phase): An approved A3 in proposals/ justifies the AccuLynx Agent per CLAUDE.md rule 9.
- D-04 gate satisfied: the A3 exists and is approved before the agent-build wave (Plans 03/04) begins.
</success_criteria>

<output>
Create `.planning/phases/06-acculynx-agent-okf-knowledge-base/06-01-SUMMARY.md` when done.
</output>
