---
phase: 01-foundation-account-registry-read-capability-discovery
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - docs/65-acculynx-read-capability-matrix.md
  - scripts/acculynx-read-sweep-reconcile.sql
  - skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md
  - skills/cleverwork-roofer/acculynx-api/SKILL.md
autonomous: false
requirements: [REQ-05, REQ-01]

must_haves:
  truths:
    - "A reconciliation query proves every one of the 86 checklist GETs is represented in the latest sweep batch (with any verdict: 200/empty/404/unprobeable/tier_gated)"
    - "A read-capability matrix doc exists with ~86 rows, reconciled against openapi-index.json, noting where live sandbox behavior differs from published reference"
    - "A Google Drive 'AccuLynx' knowledge folder skeleton exists with WHO/WHAT/HOW/WHY/WHERE/WHEN sections plus matrices/ and runbooks/"
    - "A repo pointer file routes agents to the Drive folder and the acculynx-api SKILL links to it"
    - "No AccuLynx secret value appears anywhere in schemas/, docs/, scripts/, or the knowledge folder"
  artifacts:
    - path: "docs/65-acculynx-read-capability-matrix.md"
      provides: "Read-capability matrix, one row per documented GET, evidence-linked to probe_batch_id"
      min_lines: 90
      contains: "operationId"
    - path: "scripts/acculynx-read-sweep-reconcile.sql"
      provides: "Distinct-probed-endpoints vs 86-op checklist reconciliation + secret-leak grep companion"
      contains: "acculynx_get_checklist"
    - path: "skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md"
      provides: "Repo pointer to the Drive AccuLynx folder (URL/ID + index), no secrets"
      contains: "AccuLynx"
  key_links:
    - from: "docs/65-acculynx-read-capability-matrix.md"
      to: "acculynx_api_probe.probe_batch_id"
      via: "each row cites its evidence batch"
      pattern: "probe_batch_id"
    - from: "skills/cleverwork-roofer/acculynx-api/SKILL.md"
      to: "skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md"
      via: "Required Local References link"
      pattern: "knowledge-folder"
---

<objective>
Close Phase 1: prove read-surface completeness via a reconciliation query, publish the read-capability matrix (REQ-05 criterion 3), and scaffold the Google Drive "AccuLynx" knowledge folder with a repo pointer (REQ-01 / criterion 4). The Drive folder is created through the Google Workspace OAuth bridge / Workspace persona (no Google MCP is wired) and verified by a human checkpoint.

Purpose: Convert the sweep evidence into a human- and agent-navigable capability matrix reconciled against the published spec, and stand up the knowledge-folder skeleton that Phase 6 will complete.

Output: docs/65 matrix, a committed reconciliation SQL script, the Drive folder skeleton, and the repo pointer file wired into the acculynx-api skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-account-registry-read-capability-discovery/01-RESEARCH.md
@skills/cleverwork-roofer/acculynx-api/SKILL.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconciliation script + read-capability matrix doc (docs/65)</name>
  <read_first>
    - .planning/phases/01-foundation-account-registry-read-capability-discovery/01-RESEARCH.md (§Capability Matrix Doc Format — exact column set + status vocabulary; §Validation Architecture reconciliation query)
    - docs/37-acculynx-write-capability-matrix.md (the write-matrix this read-matrix pairs with — match its house style)
    - .planning/phases/01-foundation-account-registry-read-capability-discovery/01-VALIDATION.md (reconciliation row in the per-task map)
  </read_first>
  <files>scripts/acculynx-read-sweep-reconcile.sql, docs/65-acculynx-read-capability-matrix.md</files>
  <action>
    Write scripts/acculynx-read-sweep-reconcile.sql with three committed assertions: (1) reconciliation — `SELECT operation_id FROM acculynx_get_checklist EXCEPT SELECT DISTINCT <matching key> FROM acculynx_api_probe WHERE probe_batch_id = (latest)` must return zero rows, proving every checklist GET is represented in the sweep batch under SOME verdict; (2) shape-completeness — zero 200 rows with NULL result_summary in the latest batch; (3) sandbox-only — zero rows in the batch tagged a non-sandbox source_account_key. Include a comment block with the secret-leak grep companion command (`grep -rIE 'ACCULYNX.*=.{20,}' schemas/ docs/ scripts/ skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md`).
    Generate docs/65-acculynx-read-capability-matrix.md from the latest probe batch joined to the checklist: one row per documented GET with columns operationId, Path, Tier, Sandbox status (200/404/400/401/empty/unprobeable/tier_gated per RESEARCH status vocabulary), real top-level keys, item keys (collections), pagination param, includes honored, reported count, and Quirks/delta-from-docs. Each row cites the evidence probe_batch_id. Explicitly note the documented quirks: milestone-history path is /jobs/{jobId}/milestone-history (not /milestones/history), milestone filter case-sensitivity, write-only paths have no read path, Reports GETs are unprobeable without a seed id. The doc must have roughly 86 endpoint rows.
  </action>
  <verify>
    <automated>`test -f docs/65-acculynx-read-capability-matrix.md && grep -c '| get' docs/65-acculynx-read-capability-matrix.md` returns a count of roughly 86; AND scripts/acculynx-read-sweep-reconcile.sql run via the Supabase MCP returns zero rows for the EXCEPT reconciliation and zero for both the shape-completeness and sandbox-only assertions</automated>
  </verify>
  <acceptance_criteria>
    - scripts/acculynx-read-sweep-reconcile.sql exists; the EXCEPT reconciliation returns zero rows (every checklist GET represented in the batch)
    - Zero 200 rows with NULL result_summary; zero non-sandbox-tagged rows in the batch
    - docs/65-acculynx-read-capability-matrix.md exists with roughly 86 endpoint rows and the full column set
    - Documented quirks (milestone-history path, case-sensitive milestone filter, write-only no-read, unprobeable Reports) are noted in the matrix
    - No secret value present in either file (grep companion clean)
  </acceptance_criteria>
  <done>The sweep evidence is reconciled against the 86-op checklist and published as docs/65, with a committed reconciliation script.</done>
</task>

<task type="auto">
  <name>Task 2: Repo pointer file + wire it into the acculynx-api skill</name>
  <read_first>
    - .planning/phases/01-foundation-account-registry-read-capability-discovery/01-RESEARCH.md (§Google Drive Knowledge Folder — recommended skeleton + repo-pointer guidance)
    - skills/cleverwork-roofer/acculynx-api/SKILL.md (§Required Local References — where to add the pointer line)
  </read_first>
  <files>skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md, skills/cleverwork-roofer/acculynx-api/SKILL.md</files>
  <action>
    Create skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md as the repo→Drive pointer: a one-line index of the AccuLynx knowledge folder structure (WHO/WHAT/HOW/WHY/WHERE/WHEN + matrices/ + runbooks/ + account-registry.md) and a placeholder line `Drive folder: <URL/ID set by checkpoint in Task 3>` to be filled once the human supplies the created-folder URL. Include NO secrets and NO real key values (hard rule 2). Cross-reference docs/65 (read matrix) and docs/37 (write matrix) and the acculynx_accounts registry as the source-of-truth tables.
    Edit SKILL.md: add a "Knowledge Folder" bullet under "## Required Local References" pointing to reference/knowledge-folder.md so any bound agent reaches the folder. Keep the edit minimal and within the existing section structure.
  </action>
  <verify>
    <automated>`test -f skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md && grep -q 'knowledge-folder' skills/cleverwork-roofer/acculynx-api/SKILL.md && echo OK` prints OK</automated>
  </verify>
  <acceptance_criteria>
    - knowledge-folder.md exists, indexes the WHO/WHAT/HOW/WHY/WHERE/WHEN + matrices/ + runbooks/ skeleton, and has a Drive-URL placeholder
    - SKILL.md Required Local References links to knowledge-folder.md
    - No secret value present in either file
  </acceptance_criteria>
  <done>The repo points agents to the (about-to-be-created) Drive folder via a wired skill reference.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create + verify the Google Drive AccuLynx knowledge folder skeleton</name>
  <what-built>
    Tasks 1–2 produced the read-capability matrix (docs/65), the reconciliation script, and the repo pointer file with a Drive-URL placeholder. This checkpoint creates the actual Drive folder skeleton — which routes through the Google Workspace OAuth bridge / a Workspace-enabled persona because NO Google Workspace MCP is wired in this repo (RESEARCH §Google Drive Knowledge Folder / A5).
  </what-built>
  <how-to-verify>
    1. Create (via the Google Workspace OAuth bridge / Workspace persona, or manually if the bridge is unavailable) a Drive folder named "AccuLynx" with the skeleton from RESEARCH §Google Drive Knowledge Folder: 00-README.md, WHO.md, WHAT.md, HOW.md, WHY.md, WHERE.md, WHEN.md, a matrices/ subfolder (read-capability-matrix.md mirroring docs/65 + write-capability-matrix.md placeholder), account-registry.md (human-readable mirror of acculynx_accounts, NO secrets), and an empty runbooks/ subfolder.
    2. Open the folder URL and confirm all WHO/WHAT/HOW/WHY/WHERE/WHEN files + matrices/ + runbooks/ are present.
    3. Paste the created folder URL/ID into skills/cleverwork-roofer/acculynx-api/reference/knowledge-folder.md (replace the placeholder) and confirm the file resolves to the live folder.
    4. Confirm no secret/key value appears in any folder file (account-registry.md lists env_secret_name NAMES only).
  </how-to-verify>
  <resume-signal>Type "approved" with the folder URL, or describe what is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe evidence → docs/65 + Drive matrices | Sampled response shapes cross into committed/published docs; must carry no homeowner PII or secrets |
| repo → Google Drive (OAuth bridge) | Folder creation crosses into a Workspace persona's Drive; the account-registry mirror must be name-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-secret-leak | Information Disclosure | docs/65, knowledge-folder.md, Drive account-registry.md | mitigate | All three carry env_secret_name NAMES only; committed secret-leak grep companion in the reconcile script; checkpoint step 4 confirms no value in any folder file |
| T-1-pii | Information Disclosure | docs/65 matrix sample columns | mitigate | Matrix stores key SHAPES (top_keys/item_keys), not homeowner values; sourced from already-redacted payload_sample (hard rule 2) |
| T-1-prod-probe | Tampering | reconciliation sandbox-only assertion | mitigate | scripts/acculynx-read-sweep-reconcile.sql asserts zero non-sandbox rows before the matrix is accepted as evidence |
| T-{phase}-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (docs + SQL + Drive only) |
</threat_model>

<verification>
- scripts/acculynx-read-sweep-reconcile.sql: EXCEPT reconciliation = 0 rows; shape-completeness = 0; sandbox-only = 0.
- docs/65-acculynx-read-capability-matrix.md exists with roughly 86 rows reconciled against openapi-index.json.
- The Drive "AccuLynx" folder skeleton opens and contains WHO/WHAT/HOW/WHY/WHERE/WHEN + matrices/ + runbooks/ (human checkpoint).
- knowledge-folder.md carries the live folder URL and SKILL.md links to it.
- Secret-leak grep returns nothing across schemas/, docs/, scripts/, and the pointer file.
</verification>

<success_criteria>
Phase success criteria 3 and 4 are met: a read-capability matrix doc exists reconciled against the published reference noting live deltas (REQ-05), and a Google Drive "AccuLynx" knowledge folder skeleton (who/what/how/why/where/when) exists with a repo reference pointing agents to it (REQ-01).
</success_criteria>

<output>
Create `.planning/phases/01-foundation-account-registry-read-capability-discovery/01-03-SUMMARY.md` when done.
</output>
