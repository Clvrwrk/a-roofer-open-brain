---
phase: 01-foundation-account-registry-read-capability-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql
  - schemas/cleverwork-roofer/166-acculynx-catalog-probe-ddl.sql
  - schemas/cleverwork-roofer/167-acculynx-get-checklist.sql
  - config/.env.example
autonomous: true
requirements: [REQ-02, REQ-05]
user_setup:
  - service: supabase
    why: "Set the sandbox AccuLynx API key as a Supabase project secret so the Phase-1 sweep can resolve it at runtime. Only the sandbox secret is required this phase."
    env_vars:
      - name: PE_CC_SANDBOX_ACCULYNX_API_KEY
        source: ".env (already present locally, mode-600) → set as a Supabase project secret via `supabase secrets set` or dashboard. NEVER commit the value."
    dashboard_config:
      - task: "Confirm the hyphenated production key name MULTI-FAMILY_COMMERCIAL will be set as PE_CC_MULTI_FAMILY_COMMERCIAL_ACCULYNX_API_KEY (hyphen→underscore) — record the canonical name in the registry; production secrets are NOT required until Phase 2."
        location: "Supabase Dashboard → Project Settings → Edge Functions → Secrets"

must_haves:
  truths:
    - "acculynx_accounts holds 9 rows mapping every API key (8 production + 1 sandbox) to account_key, env_secret_name (NAME only), label, market, state, and environment"
    - "The repo is source-of-truth for the live acculynx_api_catalog and acculynx_api_probe tables via idempotent CREATE TABLE IF NOT EXISTS DDL"
    - "The 86 documented GET operations exist as a queryable checklist (acculynx_get_checklist) carrying tier, pagination param, path params, includes"
    - "No AccuLynx secret value appears anywhere in schemas/ or .planning/"
  artifacts:
    - path: "schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql"
      provides: "acculynx_accounts table + 9 seed rows (env-name-only)"
      contains: "CREATE TABLE IF NOT EXISTS public.acculynx_accounts"
    - path: "schemas/cleverwork-roofer/166-acculynx-catalog-probe-ddl.sql"
      provides: "Idempotent DDL matching live acculynx_api_catalog (15 cols) + acculynx_api_probe (13 cols)"
      contains: "CREATE TABLE IF NOT EXISTS public.acculynx_api_catalog"
    - path: "schemas/cleverwork-roofer/167-acculynx-get-checklist.sql"
      provides: "acculynx_get_checklist table + 86 seed rows from openapi-index.json"
      contains: "CREATE TABLE IF NOT EXISTS public.acculynx_get_checklist"
  key_links:
    - from: "acculynx_accounts.env_secret_name"
      to: "Supabase project secret (Deno.env.get)"
      via: "name reference, never value"
      pattern: "PE_CC_.*_ACCULYNX_API_KEY"
    - from: "acculynx_get_checklist"
      to: "skills/cleverwork-roofer/acculynx-api/reference/openapi-index.json"
      via: "operationId + path + parameters"
      pattern: "operationId"
---

<objective>
Stand up the relational foundation for Phase 1: the account registry (REQ-02), the repo-as-source-of-truth DDL for the already-live probe/catalog tables, and the 86-GET checklist seed table that drives the sweep (REQ-05). Set the sandbox secret so the sweep in plan 01-02 can resolve a key.

Purpose: Phase 2 fans out across all 8 production keys by reading acculynx_accounts; Phase 1 exercises only the sandbox row. The checklist table converts the OpenAPI spec into a deterministic, queryable sweep target so the sweep is spec-driven (not a re-run of the prior 198-row guessing history).

Output: Migrations 165/166/167 applied to prod Supabase via the Supabase MCP, the sandbox secret set, and config/.env.example updated with the 9 placeholder key names.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-account-registry-read-capability-discovery/01-RESEARCH.md
@CLAUDE.md
@CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create acculynx_accounts registry migration (165) + 9 seed rows</name>
  <read_first>
    - .planning/phases/01-foundation-account-registry-read-capability-discovery/01-RESEARCH.md (§Account Registry Design — full DDL + seed-row list + hyphen-rename note)
    - CLAUDE.md (hard rules 1 additive/idempotent, 2 no secrets)
    - schemas/cleverwork-roofer/164-invoice-register-export.sql (numbering + idempotent migration style to match)
  </read_first>
  <files>schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql</files>
  <action>
    Create migration 165 with `CREATE TABLE IF NOT EXISTS public.acculynx_accounts` using the exact column set from RESEARCH §Account Registry Design: id (bigint GENERATED ALWAYS AS IDENTITY PK), account_key (text NOT NULL UNIQUE), env_secret_name (text NOT NULL UNIQUE), label (text NOT NULL), program (text), market (text), state (text), environment (text NOT NULL CHECK environment IN ('production','sandbox')), is_active (boolean NOT NULL DEFAULT true), acculynx_company_id (uuid), notes (text), created_at/updated_at (timestamptz NOT NULL DEFAULT now()). Add the COMMENT ON COLUMN env_secret_name asserting it stores the secret NAME only, never the value.
    Seed 9 rows via `INSERT ... ON CONFLICT (account_key) DO NOTHING` (idempotent): 8 production rows for account_keys florida, colorado, georgia, kansas_city, texas, wichita, insurance_program, multi_family_commercial (environment='production'), and 1 sandbox row (account_key='sandbox', environment='sandbox'). Set env_secret_name to the canonical Supabase secret names: PE_CC_FLORIDA_ACCULYNX_API_KEY, PE_CC_COLORADO_ACCULYNX_API_KEY, PE_CC_GEORGIA_ACCULYNX_API_KEY, PE_CC_KANSAS_CITY_ACCULYNX_API_KEY, PE_CC_TEXAS_ACCULYNX_API_KEY, PE_CC_WICHITA_ACCULYNX_API_KEY, PE_CC_INSURANCE_PROGRAM_ACCULYNX_API_KEY, PE_CC_MULTI_FAMILY_COMMERCIAL_ACCULYNX_API_KEY (hyphen renamed to underscore per RESEARCH A2), PE_CC_SANDBOX_ACCULYNX_API_KEY. Store program for insurance_program ('Insurance Program') and multi_family_commercial ('Multi-Family Commercial'); leave state NULL for program rows and sandbox. NEVER write any secret VALUE — only the NAME string.
    Enable RLS on the table and grant the service-role write / authenticated read pattern used by sibling migrations (read 164 for the exact GRANT/RLS idiom in this repo). Apply the migration to prod Supabase via the Supabase MCP apply_migration (there is no `supabase db push` flow here).
  </action>
  <verify>
    <automated>Via Supabase MCP: `select count(*) as n, array_agg(distinct environment) as envs from public.acculynx_accounts` returns n=9 and envs containing both 'production' and 'sandbox'</automated>
  </verify>
  <acceptance_criteria>
    - acculynx_accounts exists with exactly 9 rows; distinct environment = {production, sandbox}
    - env_secret_name is UNIQUE and matches the 9 canonical PE_CC_*_ACCULYNX_API_KEY names (multi-family uses underscores)
    - The environment CHECK constraint rejects any value not in ('production','sandbox')
    - No secret value string is present anywhere in the migration file
    - Re-applying the migration is a no-op (CREATE TABLE IF NOT EXISTS + ON CONFLICT DO NOTHING)
  </acceptance_criteria>
  <done>acculynx_accounts is live with 9 env-name-only rows and the file is committed under schemas/cleverwork-roofer/165-*.</done>
</task>

<task type="auto">
  <name>Task 2: Capture live catalog/probe DDL into idempotent migration (166)</name>
  <read_first>
    - .planning/phases/01-foundation-account-registry-read-capability-discovery/01-RESEARCH.md (§Probe Harness Design — exact column lists for acculynx_api_catalog 15 cols and acculynx_api_probe 13 cols)
    - schemas/cleverwork-roofer/165-acculynx-accounts-registry.sql (the file you just created — for style consistency)
  </read_first>
  <files>schemas/cleverwork-roofer/166-acculynx-catalog-probe-ddl.sql</files>
  <action>
    Before writing, introspect the LIVE tables via the Supabase MCP (list_tables or a column query against information_schema) to confirm the exact column names/types of acculynx_api_catalog and acculynx_api_probe — do NOT trust the research column list blindly; match the live schema exactly (RESEARCH A6 / pitfall: live tables have no repo DDL).
    Write migration 166 with `CREATE TABLE IF NOT EXISTS public.acculynx_api_catalog (...)` reproducing the 15 live columns (id, endpoint_pattern, method, category, subcategory, requires_param, is_collection, response_keys jsonb, target_table, sync_enabled, last_probe_status, last_probed_at, notes, created_at, updated_at) and `CREATE TABLE IF NOT EXISTS public.acculynx_api_probe (...)` reproducing the 13 live columns (id, probe_name, probe_batch_id, api_endpoint, method, http_status, response_ms, reported_count, items_on_page, result_summary jsonb, payload_sample, error, probed_at). Because the tables already exist with data, this DDL MUST be purely idempotent — no DROP, no column retype, no data touch (hard rule 1). Add `ALTER TABLE ... ADD COLUMN IF NOT EXISTS source_account_key text` to BOTH tables (or just acculynx_api_probe) so every sweep row can be tagged with its source account — this is the threat mitigation for accidental-prod-probe detection (T-1-prod-probe) and is consumed by plan 01-02's sweep and 01-03's reconciliation.
    Apply via the Supabase MCP apply_migration. Confirm it applies cleanly against the existing populated tables (idempotent — adds only the source_account_key column if missing, otherwise no-op).
  </action>
  <verify>
    <automated>Via Supabase MCP: `select column_name from information_schema.columns where table_name='acculynx_api_probe' and column_name='source_account_key'` returns one row; and `select count(*) from public.acculynx_api_probe` is unchanged (~198, no data loss)</automated>
  </verify>
  <acceptance_criteria>
    - Migration 166 contains CREATE TABLE IF NOT EXISTS for both acculynx_api_catalog and acculynx_api_probe matching the live column sets
    - source_account_key column exists on acculynx_api_probe after apply
    - Existing probe/catalog row counts are unchanged (no destructive operation ran)
    - Re-applying is a no-op
  </acceptance_criteria>
  <done>The repo now carries idempotent DDL for both live probe tables plus the source_account_key tagging column; committed under 166-*.</done>
</task>

<task type="auto">
  <name>Task 3: Seed the 86-GET checklist table (167) from openapi-index.json + update .env.example</name>
  <read_first>
    - skills/cleverwork-roofer/acculynx-api/reference/openapi-index.json (the 124 operations; filter method==GET → 86)
    - .planning/phases/01-foundation-account-registry-read-capability-discovery/01-RESEARCH.md (§The Definitive GET Probe Checklist — tier A/B/C assignment + pagination split recordStartIndex vs pageStartIndex)
    - config/.env.example (current ACCULYNX placeholders to extend)
  </read_first>
  <files>schemas/cleverwork-roofer/167-acculynx-get-checklist.sql, config/.env.example</files>
  <action>
    Generate the checklist seed from the spec, not by hand: filter openapi-index.json operations to method=GET (must be exactly 86), and for each derive operationId, path, base_url (api/v2 for 83, webhooks/v2 for /subscriptions, /subscriptions/{subscriptionId}, /topics), the tier (A=no path param, B=single {param}, C=two+ params or reports — use RESEARCH §Checklist tables as the authoritative tier map), path_params (array of {param} names in the path), pagination_param (select recordStartIndex if present in parameters else pageStartIndex else null — per RESEARCH §Pagination param split), includes_supported (boolean from whether an 'includes' query param exists), and a seed_source note for Tier B/C (e.g. 'jobs list', 'getFinancialsForJob'). Mark the 5 Reports GETs and the 3 webhook GETs with a probeability flag (reports = 'unprobeable_no_seed', webhooks = 'tier_gated') per RESEARCH §Reports caveat and Pitfall 7.
    Write migration 167: `CREATE TABLE IF NOT EXISTS public.acculynx_get_checklist (operation_id text PRIMARY KEY, path text NOT NULL, base_url text NOT NULL, tier text NOT NULL CHECK (tier IN ('A','B','C')), path_params jsonb NOT NULL DEFAULT '[]'::jsonb, pagination_param text, includes_supported boolean NOT NULL DEFAULT false, seed_source text, probeability text NOT NULL DEFAULT 'probeable', swept boolean NOT NULL DEFAULT false, created_at timestamptz NOT NULL DEFAULT now())`, then 86 idempotent `INSERT ... ON CONFLICT (operation_id) DO NOTHING` rows. The driver in 01-02 reads this table; the `swept` flag supports watermark-resume.
    Extend config/.env.example: replace the single placeholder ACCULYNX_API_KEY block with the 9 canonical PE_CC_*_ACCULYNX_API_KEY names each set to `__set_me__` (NAMES only, no values), so the repo documents the multi-key secret contract (hard rule 2 / RESEARCH §Secure Multi-Key Storage). Keep the existing webhook-secret placeholder.
    Apply 167 via the Supabase MCP apply_migration.
  </action>
  <verify>
    <automated>Via Supabase MCP: `select count(*) as n, count(*) filter (where pagination_param='recordStartIndex') as record_idx, count(*) filter (where pagination_param='pageStartIndex') as page_idx from public.acculynx_get_checklist` returns n=86, record_idx≈21, page_idx≈10; and `grep -c PE_CC_ config/.env.example` ≥ 9</automated>
  </verify>
  <acceptance_criteria>
    - acculynx_get_checklist has exactly 86 rows; tier distribution roughly A≈26 / B≈38 / C≈ the remainder per RESEARCH
    - pagination_param split matches the spec (≈21 recordStartIndex, ≈10 pageStartIndex, rest null)
    - The 3 webhook GETs carry base_url containing /webhooks/v2; the 5 Reports GETs are flagged probeability='unprobeable_no_seed'
    - config/.env.example lists all 9 PE_CC_*_ACCULYNX_API_KEY names as placeholders with no real values
  </acceptance_criteria>
  <done>The 86-GET checklist is queryable in the DB, the env.example documents the 9-key contract, and migration 167 is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo/migration → prod Supabase DB | DDL + seed data crosses into the shared live brain; must be additive/idempotent |
| local .env → Supabase project secret | The 9 key VALUES cross from a mode-600 .env into the encrypted secret store; only NAMES ever land in DB/repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-secret-leak | Information Disclosure | 165 registry rows, 167 .env.example, all three migrations | mitigate | env_secret_name stores NAME only + COMMENT asserting it; .env.example uses `__set_me__` placeholders; secret-leak grep gate in plan 01-03; .env confirmed gitignored |
| T-01-data-loss | Tampering | 166 DDL against populated live tables | mitigate | CREATE TABLE IF NOT EXISTS + ADD COLUMN IF NOT EXISTS only; verify row counts unchanged after apply; no DROP/TRUNCATE (hard rule 1) |
| T-01-bad-env-check | Tampering | 165 environment column | mitigate | CHECK (environment IN ('production','sandbox')) rejects misclassification that could later route a prod key into a sandbox-only path |
| T-1-prod-probe | Tampering | source_account_key column (added in 166) | mitigate | Provides the per-row account tag the sweep (01-02) and reconciliation (01-03) assert against to prove no prod call occurred |
| T-{phase}-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this phase (RESEARCH §Package Legitimacy Audit: N/A) — nothing to verify |
</threat_model>

<verification>
- All three migrations apply idempotently to prod Supabase via the Supabase MCP with no row loss on existing tables.
- `select count(*) from acculynx_accounts` = 9; `select count(*) from acculynx_get_checklist` = 86.
- The sandbox secret PE_CC_SANDBOX_ACCULYNX_API_KEY is set in Supabase project secrets (user_setup; resolved at sweep time in 01-02).
- `grep -rIE 'ACCULYNX.*=.{20,}' schemas/ config/.env.example` returns no real key (placeholders only).
</verification>

<success_criteria>
Phase success criterion 1 (REQ-02) is met: the registry maps all 9 keys → location/program, market, state, environment, storing keys securely by name only. The checklist + probe-table DDL groundwork (REQ-05) is in place for the sweep in plan 01-02.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-account-registry-read-capability-discovery/01-01-SUMMARY.md` when done.
</output>
