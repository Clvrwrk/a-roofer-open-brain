# 2026-06-04

## Session 1

**Goal**: Move the repo from maintenance layer into app skeleton, product data surface, auth discovery, and memory-system planning.

**Deliverables**:

- Merged Command Center maintenance/GSD/app skeleton.
- Added Supabase product/pricing surface and WorkOS/auth.md discovery routes.
- Archived raw uncommitted imports under `archive/local-uncommitted-2026-06-04/`.
- Added MemSearch/Open Brain memory skeleton under `context/`.

**Decisions**:

- Keep raw imports untouched until a sanitize/extract manifest exists.
- Use server-side Supabase access for product data; do not expose service-role keys.
- Treat MemSearch markdown files as source-of-truth and vector/Milvus state as rebuildable cache.

**Open threads**:

- User will add `.env`.
- Need choose shared memory backend and install/enable MemSearch Codex hooks when approved.
- Need build sanitizer/extractor for archived website, pricing/accounting, and property-enrichment material.

## Session 2

**Goal**: Verify root `.env`, install MemSearch Codex plugin, warm the embedding model, and index Open Brain memory.

**Deliverables**:

- Added Command Center `vite.envDir` so the app reads repo-root `.env` from the sub-app.
- Ran MemSearch Codex plugin installer; installed `memory-recall` and `memory-config` skills plus Codex hooks.
- Warmed ONNX MemSearch path and indexed `open_brain_memory`.
- Verified Command Center health sees Supabase and product surface is live.

**Decisions**:

- Use local Milvus Lite for first indexing pass; do not write Zilliz credentials into tracked config.
- Treat Zilliz shared backend as a later switch when real `ZILLIZ_URI`/`ZILLIZ_TOKEN` values are present.

**Open threads**:

- WorkOS env values are still needed before real human auth can be enabled.
- Public command center origin/env aliases should be set for deployment, even though the app defaults to `https://cc.proexteriorsus.net`.

## Session 3

**Goal**: Fold Hetzner/Coolify/OpenClaw/Sentry deployment requirements into the repo.

**Deliverables**:

- Added Hetzner single-host plan for Coolify, Hermes, OpenClaw, and MemSearch indexing.
- Added Sentry MCP/CLI observability plan.
- Installed Sentry CLI binary at `/Users/chussey/.local/bin/sentry`; sandbox could not complete state initialization check.
- Added Sentry, Hetzner, Milvus/Zilliz env names to `config/.env.example`.

**Decisions**:

- CPX41 is acceptable for first deployment if browser/OpenClaw workloads stay limited and Milvus stays local Lite or remote Zilliz.
- Prefer project-constrained Sentry MCP URL once org/project slugs are known.

**Open threads**:

- Need server SSH access, DNS control, Coolify plan, Sentry org/project slugs, and WorkOS credentials.
