# 2026-06-04

## Session 1

**Goal**: Move the repo from maintenance layer into app skeleton, product data surface, auth discovery, and memory-system planning.

**Deliverables**:

- Merged Command Center maintenance/GSD/app skeleton.
- Added Supabase product/pricing surface and WorkOS/auth.md discovery routes.
- Archived raw uncommitted imports under `archive/local-uncommitted-2026-06-04/`.
- Added MemSearch/Open Brain memory skeleton under `context/`.

**Decisions**:

- Keep raw imports untouched until a sanitize/extract manifest exists.
- Use server-side Supabase access for product data; do not expose service-role keys.
- Treat MemSearch markdown files as source-of-truth and vector/Milvus state as rebuildable cache.

**Open threads**:

- User will add `.env`.
- Need choose shared memory backend and install/enable MemSearch Codex hooks when approved.
- Need build sanitizer/extractor for archived website, pricing/accounting, and property-enrichment material.

## Session 2

**Goal**: Verify root `.env`, install MemSearch Codex plugin, warm the embedding model, and index Open Brain memory.

**Deliverables**:

- Added Command Center `vite.envDir` so the app reads repo-root `.env` from the sub-app.
- Ran MemSearch Codex plugin installer; installed `memory-recall` and `memory-config` skills plus Codex hooks.
- Warmed ONNX MemSearch path and indexed `open_brain_memory`.
- Verified Command Center health sees Supabase and product surface is live.

**Decisions**:

- Use local Milvus Lite for first indexing pass; do not write Zilliz credentials into tracked config.
- Treat Zilliz shared backend as a later switch when real `ZILLIZ_URI`/`ZILLIZ_TOKEN` values are present.

**Open threads**:

- WorkOS env values are still needed before real human auth can be enabled.
- Public command center origin/env aliases should be set for deployment, even though the app defaults to `https://cc.proexteriorsus.net`.

## Session 3

**Goal**: Fold Hetzner/Coolify/OpenClaw/Sentry deployment requirements into the repo.

**Deliverables**:

- Added Hetzner single-host plan for Coolify, Hermes, OpenClaw, and MemSearch indexing.
- Added Sentry MCP/CLI observability plan.
- Installed Sentry CLI binary at `/Users/chussey/.local/bin/sentry`; sandbox could not complete state initialization check.
- Added Sentry, Hetzner, Milvus/Zilliz env names to `config/.env.example`.

**Decisions**:

- CPX41 is acceptable for first deployment if browser/OpenClaw workloads stay limited and Milvus stays local Lite or remote Zilliz.
- Prefer project-constrained Sentry MCP URL once org/project slugs are known.

**Open threads**:

- Need server SSH access, DNS control, Coolify plan, Sentry org/project slugs, and WorkOS credentials.

## Session 4

**Goal**: Clarify env handling, access needs, Codex account/API modes, and ABC sandbox setup.

**Deliverables**:

- Added a connection/access checklist for `.env`, Hetzner, Coolify/DNS, Supabase, WorkOS, Sentry, ABC Supply, MemSearch/Zilliz, and provider APIs.
- Added ABC Supply sandbox read-only test plan.
- Added ABC Supply env names to `config/.env.example`.

**Decisions**:

- Do not copy `.env` to `.env.example`; mirror names only with placeholders.
- Use namespaced ABC vars in code even if the provider portal labels the secrets `ClientID` and `Client_Secret`.
- Keep interactive Codex on account auth; use API keys or Codex access tokens for automation depending on governance needs.

**Open threads**:

- User should add ABC alias vars in `.env` or approve a compatibility shim before sandbox tests.
- Need ABC sandbox scopes/track and safe sample identifiers before live read-only calls.

## Session 5

**Goal**: Run ABC Supply sandbox read-only endpoint smoke using local sandbox credentials.

**Deliverables**:

- Added `integrations/bridges/abc-supply/sandbox-smoke.mjs`, a redacted Node smoke harness that
  loads repo-root `.env`, requests an ABC sandbox client-credentials token, starts with account
  search, derives safe IDs, and probes read/read-like endpoints without logging secrets or raw
  account/PDF/price payloads.
- Added `.gitignore` entry for `integrations/bridges/abc-supply/.sandbox-runs/`.
- Updated ABC sandbox docs with the accepted read-only scope set and latest smoke result.

**Results**:

- OAuth passed with `location.read product.read account.read pricing.read order.read allOrder.read notification.read invoice.read invoice.history.read`; access token lifetime was 30 minutes.
- Final smoke passed 35 read/read-like probes, skipped 1 dependent webhook-by-ID probe, and surfaced 2 expected/non-blocking issues: docs list `/api/accounts/v1/.../contacts`, which returned 401 while `/api/account/v1/.../contacts` passed; `GET /api/notification/v2/webhooks` returned 404 because no webhook exists.
- Storefront-only account search exposed sandbox account records; state-filtered searches for common states returned zero.

**Decisions**:

- Keep order placement, favorite writes, webhook register/update/delete, and invoice PDF download behind a human review gate.
- Do not commit `.sandbox-runs/` output; it is generated and may contain masked identifiers/shape summaries.

## Session 6

**Goal**: Expand ABC Supply sandbox validation around Supabase-populating data surfaces and start spaced rate-limit validation.

**Deliverables**:

- Updated `integrations/bridges/abc-supply/sandbox-smoke.mjs` to start with ABC's recommended
  `accountType = Ship-To` search, collect Ship-To branch access, capture product/pricing UOM field
  paths, price the selected item against up to three Ship-To-access branches, and emit a redacted
  `supabaseCoverage` block for locations, catalog, availability, pricing, UOM, price dates, orders,
  invoices, and notifications.
- Updated `integrations/bridges/abc-supply/SANDBOX-TEST-PLAN.md` with the Supabase table-fit notes
  and the latest baseline run.
- Created Codex heartbeat automation `abc-sandbox-2-hour-validation` to run the same smoke three
  more times at two-hour intervals, making four total validation runs including the baseline.

**Results**:

- Baseline run `2026-06-04T22:15:03Z` wrote ignored local output
  `.sandbox-runs/2026-06-04T22-15-03-979Z.json`.
- OAuth passed with the read-only discovery scope set and 30-minute token lifetime.
- ABC's recommended Ship-To account search returned `49` sandbox Ship-To accounts across `10`
  pages.
- Passed `26` read/read-like probes, skipped `1` dependent webhook-by-ID probe, and had `2`
  expected/non-blocking misses: documented `/api/accounts/v1/.../contacts` returned 401 while the
  `/api/account/v1/.../contacts` fallback passed; `GET /api/notification/v2/webhooks` returned 404
  because no webhook is registered.
- Per-branch pricing passed for `3` Ship-To-access branches with line status `OK`; no price values
  were logged.
- Request stats: `29` total HTTP requests, zero 429/rate-limit responses, observed rate `0.65`
  req/sec, average endpoint latency `1565` ms.

**Decisions**:

- Treat ABC availability as branch-offered-item coverage, not inventory.
- Derive UOM mapping from product detail and pricing line fields unless ABC exposes a later dedicated
  conversion endpoint.
- Keep price date columns optional for this provider until a response returns explicit date/effective
  fields.
