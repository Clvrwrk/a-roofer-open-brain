# 2026-08-25

## Session 01

**Goal**: Verify the invoice audit loop has run daily since 08/10 — which it has, but verifying it exposed four defects in the pricing join and the QB export path.

**Deliverables**:
- `schemas/cleverwork-roofer/277-item-aware-version-cascade.sql` — item-aware version supersession on all three ABC arms
- `schemas/cleverwork-roofer/278-inv-processed-weekly-view.sql` — `v_inv_processed_weekly`, the weekly QB export set
- `schemas/cleverwork-roofer/279-vendor-arm-parity.sql` — supersession ported to SRS/QXO; evergreen unified across all four arms
- `schemas/cleverwork-roofer/280-negatives-are-credit-memos.sql` — positive-total gate + `v_credit_memo_tbd` (cross-vendor CM queue)
- `scripts/build-inv-processed-weekly.mjs` — the missing docs/81 Phase 6 Tuesday producer; per-vendor files, prep-only by default
- Rules propagated to CONVENTIONS §10b, CLAUDE.md, AGENTS.md, .cursor/rules; docs/81 d2/d14 marked superseded
- Linear PEC-253…PEC-259

**Decisions**:
- **The loop IS running**: 16/16 days since 08/10 (ingest + Alex triage at 03:30 ET, matview every 15 min, zero failures). `morning_abc_sync` — the Slack hold-notice agent pass — remains paused; that is the only gap.
- **Evergreen applies per ITEM.** Expiry was never the cause of the No-Price flood; all 13 agreements were already tagged evergreen. The bug was version supersession being item-blind — a shorter new price list silently repealed the prices it omitted. 414 lines regained a benchmark; 0 changed, 0 lost.
- **Vendor evals may differ only where the vendor process differs.** Legitimate: QXO has no agreements ever; SRS = Level 4 sheet → Richardson; PDF/OCR is ABC-only. Everything else is vendor-agnostic and is now written down.
- **A negative total is a credit memo** regardless of the vendor flag — 5 unflagged negatives were leaking into the QB payables export.
- **The QB bank export is one file per vendor** (separate QB bank registers). Supersedes docs/81 d2/d14.
- **The $1.67M backlog was a records gap, not a money gap.** Everything was hand-keyed as QBO *Purchases* (not Bills — `qbo_bills` for ABC stops in Oct 2023). Invoice number lives in `private_note` in five different formats. 620 verified and reconciliation-stamped; the CSV was never loaded.
- `2009557754-001` stays cancelled — Lucinda's 08-20 call upheld; `do_not_auto_revive` stamped into its packet.
- No-Price threshold stays at `purchases_ytd >= 2`.

**Open threads**:
- PEC-257 — 7 reopened August lines ($142.25) need disposition; blocks 6 invoices from export
- PEC-258 — 9 credit memos with no original invoice ($26,601.90)
- Weekly batch unstamped: 3 files / 13 invoices / $14,610.49
- 309 pre-August lines likewise re-benchmarked; 38 show $575.92 overcharge — needs a ruling on sweeping them
- **Commit-message IDs pec-244…248 are void** (collide with live Pax issues); real mapping in PEC-259
