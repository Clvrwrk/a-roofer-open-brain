
## Session — Invoice Audit 4+4 redesign, Sent CM workspace, territory drops (06:15 CT)

**mig 249**: Wichita Falls (ABC + QXO 249) + Austin (ABC 39, 465) → `out_of_boundary`.
Wichita Falls came in via the closed Euless isochrone with no human decision; Austin were
Chris's 2026-06-10 overrides, now reversed. Both Austin invoices are no-price shop supplies
(S.S.TOOLS, TRUCK 102) so **no dollar moves**. Now **0 covered branches sit outside an
active isochrone** — `office_for_point()` and `pricing_status` agree everywhere.

**Invoice Audit toolbar 10 → 4 controls**: search · office · **segmented To audit/Due now/All**
· **More filters ▾**. The 3 checkboxes still exist HIDDEN and remain the source of truth for
`applyFilter()`, so tree logic + deep links are untouched; the switch drives them. The
Due-Now card button *clicks the switch* because setting `.checked` in code does not fire
`change`.

**Pills 7 → 4 cards**: Claim it · Chase it · Pay it (`56 → 32 → 5` as a pipeline) · Fix it.

**TWO regressions I introduced and caught only in the browser** (build + 309 tests were green):
1. KPI refresh did `card.querySelector('.iv-process-btn')` = *first button in card*. That was
   Export in the 7-pill layout, but **Filter ↓** after merging → the 60s poll replaced Filter
   with an Export link. Fixed with explicit `[data-kpi-btn="qb"]`.
2. Refresh replaces `innerHTML` of `[data-kpi-sub="awaiting"]`; I had nested
   `[data-kpi-val="awaiting"]` inside it → wiped on first poll. Un-nested.
**Lesson: the KPI refresh contract is positional — never move a button between cards without
re-reading `refreshKpiPills`.**

**Tree had NO empty state** — default view rendered blank because "To audit" is legitimately
0 (every open invoice is fully audited). Added one that names the way out.

**New `/accounting/credit-memos/sent`** — exact replica of Weekly CM scoped to `status='sent'`,
Request→Sent language, + sent/follow-up dates, overdue in red, "Mark received" instead of
Approve, no drafts section. `weekly-csv` gained `?status=sent` (default stays `approved`).
**Key fact: NO outbound email body is stored** (packet has provenance keys only), so the page
REBUILDS the email from today's lines and says so in a banner — docs/93 revised some sent
figures, so a line can differ from what the vendor received.

Open: exact email replay needs the rendered HTML stored at send time (upstream fix).

### CM workspace header enhancements + mig 250 (06:40 CT)

Chris's markup on Weekly CM, applied to **both** Weekly and Sent via one shared helper
(`lib/cm-request-header.ts`) so they cannot drift:
1. **Vendor in the subject**: `Subject: [SRS Distribution] Credit memo request - …`
2. **Claim header context line**: PE office · vendor · vendor branch (name + id) · Job ↗
   (client) · Invoiced date. Job is a live AccuLynx link when an id exists, text when only a
   number does. Missing pieces are NAMED ("office not resolved"), never blanked.

**mig 250 — `v_vendor_invoice_acculynx_match`.** First render printed **"no job on the PO"**
on every SRS claim whose PO plainly read KS-189/188/184/194/198. Not missing data — a WRONG
label: `v_invoice_acculynx_match` is built `FROM abc_invoices` and never covered
`vendor_invoices` at all. New view applies mig 237's canonical PO job token to
vendor_invoices → **32/33 resolve**. Header loader reads both matchers, ABC wins collisions
(3 tiers vs 1).

Result: Sent page 44/44 headers enriched, **0** false "no job" labels, 43 live job links.

**Lesson (2nd time this session): the build was green and the wrong label was a plausible
sentence. Only rendering the page caught it.** Same class as the KPI-refresh positional bug.

Perf note: `v_invoice_audit_invoice` filtered by `invoice_number IN (...)` pushes down to an
index scan — 55ms for a page's worth. Safe to query directly; do NOT join it wholesale.

### SRS agreement PDF link path (06:45 CT)

Ask: active agreement-PDF link on the credit-memo workspace for SRS.

**The SRS PDFs do not exist anywhere reachable.** `agreements` bucket = 7 objects, ALL ABC.
All 3 active SRS agreements name a file, none stored, none on this machine:
`Wichita_Quote0049828559.pdf` · `Englewood_co_Revised…(1).pdf` ·
`Richardson_MELESSA_PRICE_SHEET- LEVEL 4 (1).pdf`. Told Chris; gave the upload command.

**Fixed the two bugs that would have kept the link broken EVEN AFTER upload:**
1. `scripts/upload-agreement-pdf.mjs` binds `source_pdf_url` to a **bucket-relative path**
   (`agreements/x.pdf`); the endpoint redirected to it verbatim → relative `Location`
   resolves against the app host → 404. Endpoint now splits bucket/key and **signs** it;
   absolute URLs still pass through.
2. The evidence panel used `source_pdf_url` directly as an href — same breakage. Now routes
   through `/api/price-agreement/pdf/<id>` like the ABC path always did.

**Truthfulness regression caught in the same pass**: another session backfilled the SRS
re-audit writer, so discrepancy rows now carry a **uuid** `agreement_id` → the panel flipped
from its derived path to its RECORDED path, which only enriched ABC **integer** ids. For a
uuid it left version_label/source_file/ceo_verified null → `isQuote` defaulted false →
Document-type check said "Priced from an agreement" for an **accepted QUOTE**. docs/93 says
a quote must never be hidden. Recorded branch now enriches uuids from `price_agreements`.
Restored: "Quote 0049828559 is accepted as the governing price agreement for Wichita, KS."

**Standing lesson, 3rd time this session**: green build + plausible sentence ≠ correct.
Rendering the page is what caught all three.

### Mark-sent buttons + SRS writer verification (06:55 CT)

**SRS re-audit writer: ALREADY FIXED, no work needed.** Verified all classifications:
srs discrepancy 11/11 have agreement_id + office_id + office_name; abc discrepancy 128/128.
Only `no_price` rows lack an agreement — correct by definition. The real gap was the panel
DISPLAYING a uuid citation, fixed in `dbe0e1f`. Confirmed across all 5 approved SRS claims:
`source: recorded`, `isQuote: true`, `ceoVerified: true`, Document type + Citation
provenance both pass.

**"Mark sent" buttons** — `mark-sent` already existed in the disposition API (sets
status=sent, sent_by/at, follow_up_due_at +14d, auto-fires the Process stamp) but had no
entry point outside the single-invoice detail page.
- **Weekly CM**: per-vendor button in the vendor summary (one email per vendor = the natural
  unit) AND per-invoice beside `detail →`. `stopPropagation` so the `<summary>` does not
  collapse.
- **Invoice Audit Card 1**: one button; reads `/api/credit-memos/pending`, takes the
  `approved` set, and **names every vendor in the confirm** because the set can span vendors
  and you may only have emailed one. Explicit `[data-kpi-btn="marksent"]` hook for the 60s
  poll — NOT positional (per this session's repeated lesson).

Nothing is emailed (SOUL). Each write is vendor-scoped by the existing endpoint.
Verified by stubbing `window.confirm` to DECLINE — correct text on both surfaces, summary
does not collapse, zero prod rows touched.

### Wrapup (07:00 CT)

Session commits `8d38263..52c67fa` (4), all deployed, live buildCommit = 52c67fa, tree clean.
Migrations 249 (Wichita Falls + Austin out_of_boundary) and 250 (vendor-side AccuLynx job
matcher). Renamed my `docs/99-…` → **docs/100** — the previous session had already taken 99.

Money state unchanged by this session (correct — the work was UI, territory and matching,
none of which touches pricing): at_risk $3,679.58 total / $2,692.08 actionable,
credit_memo_amount $997.05, 5 approved / 44 sent.

Handoff: `docs/handoffs/current.md`; prior archived to
`archive/2026-08-21-0005-uiux-phases-money-truth.md`.

**Next task**: upload the 3 SRS agreement PDFs (link path is built and waiting). The 2
Colorado sheets need an export from the PE accounting mailbox — not reachable from
chussey@cleverwork.io (commit ed792ce).
