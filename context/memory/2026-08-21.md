
## Session — Invoice Audit 4+4 redesign, Sent CM workspace, territory drops (06:15 CT)

**mig 249**: Wichita Falls (ABC + QXO 249) + Austin (ABC 39, 465) → `out_of_boundary`.
Wichita Falls came in via the closed Euless isochrone with no human decision; Austin were
Chris's 2026-06-10 overrides, now reversed. Both Austin invoices are no-price shop supplies
(S.S.TOOLS, TRUCK 102) so **no dollar moves**. Now **0 covered branches sit outside an
active isochrone** — `office_for_point()` and `pricing_status` agree everywhere.

**Invoice Audit toolbar 10 → 4 controls**: search · office · **segmented To audit/Due now/All**
· **More filters ▾**. The 3 checkboxes still exist HIDDEN and remain the source of truth for
`applyFilter()`, so tree logic + deep links are untouched; the switch drives them. The
Due-Now card button *clicks the switch* because setting `.checked` in code does not fire
`change`.

**Pills 7 → 4 cards**: Claim it · Chase it · Pay it (`56 → 32 → 5` as a pipeline) · Fix it.

**TWO regressions I introduced and caught only in the browser** (build + 309 tests were green):
1. KPI refresh did `card.querySelector('.iv-process-btn')` = *first button in card*. That was
   Export in the 7-pill layout, but **Filter ↓** after merging → the 60s poll replaced Filter
   with an Export link. Fixed with explicit `[data-kpi-btn="qb"]`.
2. Refresh replaces `innerHTML` of `[data-kpi-sub="awaiting"]`; I had nested
   `[data-kpi-val="awaiting"]` inside it → wiped on first poll. Un-nested.
**Lesson: the KPI refresh contract is positional — never move a button between cards without
re-reading `refreshKpiPills`.**

**Tree had NO empty state** — default view rendered blank because "To audit" is legitimately
0 (every open invoice is fully audited). Added one that names the way out.

**New `/accounting/credit-memos/sent`** — exact replica of Weekly CM scoped to `status='sent'`,
Request→Sent language, + sent/follow-up dates, overdue in red, "Mark received" instead of
Approve, no drafts section. `weekly-csv` gained `?status=sent` (default stays `approved`).
**Key fact: NO outbound email body is stored** (packet has provenance keys only), so the page
REBUILDS the email from today's lines and says so in a banner — docs/93 revised some sent
figures, so a line can differ from what the vendor received.

Open: exact email replay needs the rendered HTML stored at send time (upstream fix).

### CM workspace header enhancements + mig 250 (06:40 CT)

Chris's markup on Weekly CM, applied to **both** Weekly and Sent via one shared helper
(`lib/cm-request-header.ts`) so they cannot drift:
1. **Vendor in the subject**: `Subject: [SRS Distribution] Credit memo request - …`
2. **Claim header context line**: PE office · vendor · vendor branch (name + id) · Job ↗
   (client) · Invoiced date. Job is a live AccuLynx link when an id exists, text when only a
   number does. Missing pieces are NAMED ("office not resolved"), never blanked.

**mig 250 — `v_vendor_invoice_acculynx_match`.** First render printed **"no job on the PO"**
on every SRS claim whose PO plainly read KS-189/188/184/194/198. Not missing data — a WRONG
label: `v_invoice_acculynx_match` is built `FROM abc_invoices` and never covered
`vendor_invoices` at all. New view applies mig 237's canonical PO job token to
vendor_invoices → **32/33 resolve**. Header loader reads both matchers, ABC wins collisions
(3 tiers vs 1).

Result: Sent page 44/44 headers enriched, **0** false "no job" labels, 43 live job links.

**Lesson (2nd time this session): the build was green and the wrong label was a plausible
sentence. Only rendering the page caught it.** Same class as the KPI-refresh positional bug.

Perf note: `v_invoice_audit_invoice` filtered by `invoice_number IN (...)` pushes down to an
index scan — 55ms for a page's worth. Safe to query directly; do NOT join it wholesale.

### SRS agreement PDF link path (06:45 CT)

Ask: active agreement-PDF link on the credit-memo workspace for SRS.

**The SRS PDFs do not exist anywhere reachable.** `agreements` bucket = 7 objects, ALL ABC.
All 3 active SRS agreements name a file, none stored, none on this machine:
`Wichita_Quote0049828559.pdf` · `Englewood_co_Revised…(1).pdf` ·
`Richardson_MELESSA_PRICE_SHEET- LEVEL 4 (1).pdf`. Told Chris; gave the upload command.

**Fixed the two bugs that would have kept the link broken EVEN AFTER upload:**
1. `scripts/upload-agreement-pdf.mjs` binds `source_pdf_url` to a **bucket-relative path**
   (`agreements/x.pdf`); the endpoint redirected to it verbatim → relative `Location`
   resolves against the app host → 404. Endpoint now splits bucket/key and **signs** it;
   absolute URLs still pass through.
2. The evidence panel used `source_pdf_url` directly as an href — same breakage. Now routes
   through `/api/price-agreement/pdf/<id>` like the ABC path always did.

**Truthfulness regression caught in the same pass**: another session backfilled the SRS
re-audit writer, so discrepancy rows now carry a **uuid** `agreement_id` → the panel flipped
from its derived path to its RECORDED path, which only enriched ABC **integer** ids. For a
uuid it left version_label/source_file/ceo_verified null → `isQuote` defaulted false →
Document-type check said "Priced from an agreement" for an **accepted QUOTE**. docs/93 says
a quote must never be hidden. Recorded branch now enriches uuids from `price_agreements`.
Restored: "Quote 0049828559 is accepted as the governing price agreement for Wichita, KS."

**Standing lesson, 3rd time this session**: green build + plausible sentence ≠ correct.
Rendering the page is what caught all three.

### Mark-sent buttons + SRS writer verification (06:55 CT)

**SRS re-audit writer: ALREADY FIXED, no work needed.** Verified all classifications:
srs discrepancy 11/11 have agreement_id + office_id + office_name; abc discrepancy 128/128.
Only `no_price` rows lack an agreement — correct by definition. The real gap was the panel
DISPLAYING a uuid citation, fixed in `dbe0e1f`. Confirmed across all 5 approved SRS claims:
`source: recorded`, `isQuote: true`, `ceoVerified: true`, Document type + Citation
provenance both pass.

**"Mark sent" buttons** — `mark-sent` already existed in the disposition API (sets
status=sent, sent_by/at, follow_up_due_at +14d, auto-fires the Process stamp) but had no
entry point outside the single-invoice detail page.
- **Weekly CM**: per-vendor button in the vendor summary (one email per vendor = the natural
  unit) AND per-invoice beside `detail →`. `stopPropagation` so the `<summary>` does not
  collapse.
- **Invoice Audit Card 1**: one button; reads `/api/credit-memos/pending`, takes the
  `approved` set, and **names every vendor in the confirm** because the set can span vendors
  and you may only have emailed one. Explicit `[data-kpi-btn="marksent"]` hook for the 60s
  poll — NOT positional (per this session's repeated lesson).

Nothing is emailed (SOUL). Each write is vendor-scoped by the existing endpoint.
Verified by stubbing `window.confirm` to DECLINE — correct text on both surfaces, summary
does not collapse, zero prod rows touched.

### Wrapup (07:00 CT)

Session commits `8d38263..52c67fa` (4), all deployed, live buildCommit = 52c67fa, tree clean.
Migrations 249 (Wichita Falls + Austin out_of_boundary) and 250 (vendor-side AccuLynx job
matcher). Renamed my `docs/99-…` → **docs/100** — the previous session had already taken 99.

Money state unchanged by this session (correct — the work was UI, territory and matching,
none of which touches pricing): at_risk $3,679.58 total / $2,692.08 actionable,
credit_memo_amount $997.05, 5 approved / 44 sent.

Handoff: `docs/handoffs/current.md`; prior archived to
`archive/2026-08-21-0005-uiux-phases-money-truth.md`.

**Next task**: upload the 3 SRS agreement PDFs (link path is built and waiting). The 2
Colorado sheets need an export from the PE accounting mailbox — not reachable from
chussey@cleverwork.io (commit ed792ce).

## Session 2 — SRS agreements, ceo_verified retirement, invoice ingest

**Goal**: store + bind the SRS agreement PDFs (the next task from Session 1), then whatever
Chris put in front of me. Three rounds: 4 agreement PDFs, then a policy change, then 5
invoice PDFs.

**Deliverables**
- Migrations **251** (SRS Colorado price list, 101 items, Denver office), **252** (retire the
  `ceo_verified` gate), **253** (SRS branch-code aliases), **254** (`po_number` → AccuLynx job
  number, 9 rows), **255** (canonicalize `po_number` on write, fails open).
- `integrations/bridges/ingest-vendor-invoice-csv.mjs` — new `--pdf` arm on the SAME script,
  reusing its writers. Strict reconciliation gate.
- Code: `lib/credit-memo.ts`, `lib/abc-price-gaps.ts`, `lib/vendor-territories.ts`,
  `price-agreement/builder.astro`, `api/price-agreement/review/promote.ts` — gate removed.
- `docs/101-…` + 2 addenda. Commits `a6b900c`, `fbb32d1`, wrapup. Both deployed and verified.
- Linear **PEC-224/225/226**; closed **PEC-222** and **PEC-211**.

**Decisions**
- **The 4th Colorado PDF got its own agreement record, not a bind to Englewood.** Provenance
  from the PDF author (Blake Wells = the `B WELLS` on both quotes) + TOP SHIELD catalog.
  Anchored to `AMSDE` because agreements resolve at the OFFICE level — proved it: invoice
  `0049707508-001` on `SBP-DENVER` prices off the `AMSDE`-bound quote.
- **Englewood NOT archived.** Date windows decide; 11 re-audit lines cite it.
- **9 `$0.00`/`CALL` items deliberately not loaded** — they would fabricate discrepancies.
- **`ceo_verified` columns kept, not dropped** (rule 1; they are real provenance).
  docs/93 untouched — the panel still says the document is a quote.
- **`po_number` rewritten but the printed value preserved** at `raw.po_number_as_printed`.
  The 255 trigger **fails open** — an unrecognised PO stays as printed, because "no job yet"
  is information and blanking it destroys the only clue.
- `0050471744-001` → KS-208 on exact address evidence; `0050708886-001` → TX-455 on Chris's
  say-so, recorded as a human decision so nobody later reads it as machine inference.

**The finding that matters (PEC-226)**
The vendor price path matches on EXACT item-number or description equality — no trigram arm
(ABC has one at 0.45). SRS **quotes** carry item numbers so they work (55 and 66 line hits);
the two **price sheets** are description-only and match **zero**. The Melissa Level 4 sheet
has been inert since 2026-02-16 — 97 prices, 0 matches, unnoticed. Live SRS: 73 priced lines
/$16,990.55 vs **216 unpriced / $63,642.53**; **Richardson is 0-for-28**.

**Two bugs my own code nearly shipped, both caught by rendering/reconciling, not by tests**
1. The new Document-type string hardcoded SRS wording on a path ABC quotes also use.
2. The PDF line regex required a digit before the decimal; SRS prints `.01 /BD`, so a line
   vanished and the invoice missed SUB-TOTAL by exactly its $0.89.

**Not mine**: 5 CM requests moved approved→sent at 14:21Z mid-session (SRS, ~1s apart,
Card 1 bulk Mark-sent). My dev server logged zero POSTs — a human on the live site.

**Open threads**: PEC-226 (item-number backfill vs trigram arm — needs Chris's call); 9
Colorado `$0.00` items need prices from Blake Wells; prefer the portal CSV over PDFs when
available (PDF truncates PO NUMBER to 16 chars).

## Session 12:20 — PEC-226 + WIP/AR Expense Realized audit

**Goal**: Complete PEC-226 (SRS description→item-number backfill); audit the AR/WIP
Expenses Realized discrepancy and the nightly Hetzner/QBO/AccuLynx sync; make KPI pills
clickable filters; expand the board to all signed contracts.

**Deliverables**:
- `schemas/…/256-srs-description-item-number-backfill.sql` — token helpers,
  `price_agreement_item_candidates` review queue, `refresh_price_agreement_item_candidates()`,
  `v_price_agreement_item_review`. 8 exact-token bindings auto-applied, 500 candidates queued.
- `schemas/…/257-qbo-job-cost-bare-customer-ref.sql` — the real bug. Plus
  `v_qbo_job_cost_unattributed` guard view.
- `schemas/…/258-wip-ar-signed-contract-population.sql` — union gate + `population_reason`.
- `app/command-center/src/lib/friday-wip.ts` — KPI keys tagged during accumulation.
- `app/command-center/src/pages/accounting/friday-wip.astro` — 12 filter pills, signed-contract
  dot, corrected schedule footer.
- `docs/102-wip-ar-expense-realized-and-population.md`.

**Decisions**:
- PEC-226 auto-applies ONLY exact token-set equality + agreeing UOM + unique target. The
  brand gate (first core token must match) was essential — without it the pool proposed
  'IKO HIP & RIDGE' → a TAMKO item. Colour families go to review because one sheet row maps
  to many item numbers and `raw_item_number` is a single column.
- 257 leaves the colon branch byte-identical. Only the missing bare-name branch was added.
- 258 UNIONs the gate rather than replacing it, so AR KPIs are unchanged; `population_reason`
  lets the AR meeting filter back to its own population.
- KPI keys are tagged server-side in the same pass that sums them, so a pill's filter and its
  number cannot drift.

**Verified**: build green, 309 tests pass, browser-verified against prod (347 rows, all 12
pills filter to the right counts, totals reconcile). Deployed — live `buildCommit` 3666cc3.

**Open threads**:
- PEC-226 review queue: 24 high-signal candidates await Chris (IKO CAMBRIDGE AR colour family
  ~$38k; TAMKO HIP AND RIDGE has 4 sheet rows at 4 prices → same item, genuine ambiguity).
- ⚠ CPA accrual CSV moved 122 → 347 rows ($12.69M billed, $8.90M costs). Outside-facing.
- `est_total_costs` populated on 0/347 rows → Expense Outstanding renders `—` everywhere.
- QBO run_key still says `thursday` though it runs daily.
- Stale worktree `.claude/worktrees/jolly-bell-f2a86a`.

## Session 13:40 — YTD accrual, GM budgeting, colour variants, categorization

**Goal**: Chris's four follow-ups after the WIP/AR audit.

**Deliverables**: migrations 259 (YTD accrual), 260 (per-office GM% →
est_total_costs / expense_outstanding + wip_office_margin override),
261 (colour-variant rule for all vendors, colour vocabulary from the PE product
file), 262 (price-line → product binding + categorize worksurface).
New: `/accounting/price-agreement/categorize`, `/api/accounting/friday-wip/margin`,
`/api/accounting/product-match/decide`, `src/lib/product-categorize.ts`. docs/103.

**Decisions**:
- ABC's 0.45 trigram rule was NOT copied verbatim: measured on SRS it priced
  STEEL FLASHING SHINGLES off STEEL A ROOF EDGE for +1,630% variance. Installed
  colour-key equality instead (product head minus qualifiers minus colour terms).
  ABC keeps trigram pending Chris's go-ahead — 2,263 priced lines at stake.
- GM% guards: contract_amount already includes change orders; office rate needs
  5 jobs AND $250k billed (Georgia was 56.6% off ONE job); clamped 0..75.
- category_key is a GENERATED column, so categorization = binding to products
  and inheriting taxonomy, not writing a category.

**Verified**: build green, 309 tests, browser round-trips against prod for the
margin override (Florida 45% → reverted) and the product bind (line 138 →
reverted). Both left zero residue.

**Open threads**: ABC trigram → colour rule (needs Chris); PEC-226's 24
candidates; product_taxonomy has mangled `<gt/>` entities; 427 lines in the
categorization queue.


## Session 15:30 — zero-balance diagnosis + three fixes (mig 263)

**Goal**: Explain why 230 of 346 WIP/AR jobs showed $0 Balance Due; then fix it.

**Findings**: two unrelated causes. (a) 225 jobs contracted/invoiced/collected
in full but never advanced to Closed in AccuLynx — the mig-258 signed-contract
gate has no "still owes money" condition and the drop rule only fires on
'closed'. Job 10 has sat at 'invoiced' 1,576 days, fully paid. (b) 5 jobs whose
open invoices are absent from their own job-level Balance Due — $195,098.53,
worst is job 5 with invoice 5-4 unpaid at $136,892.90.

**Deliverables**: migration 263 — draft invoices excluded from AR (MC-76's
$36,000), `attention_flag`/`attention_note` + `diagnose_wip_ar_attention()`.
Board: two gold pills (Stale 225, Contradiction 5), stale hidden by default and
excluded from money KPIs, "Our Best Guess" popout hung off the CLIENT NAME
(job number is already the AccuLynx link). docs/103 §5.

**Decisions**:
- Stale rows flagged, not deleted — they're the AccuLynx cleanup list for the
  Friday meeting.
- Contradiction rows stay in the money KPIs; that AR is real.
- Notes are derived on every rebuild, so a fixed job clears its own flag.
- refresh_wip_ar_master amended via anchored DO-block replacement rather than
  retyped: 2 lines changed out of ~200.

**Board moved**: 346 → 120 jobs; Billed AR $1,087,819 → $1,056,535; Expense
realized $8,900,089 → $2,549,960 ($6.35M belonged to finished jobs, still
reachable via the stale pill).

**Open threads**: unchanged — ABC trigram → colour rule; PEC-226's 24
candidates; product_taxonomy `<gt/>` entities; 427 categorization lines.

## Session 17:30 — WIP/AR navigation overhaul, shipped

**Goal**: Rework Friday WIP/AR navigation after Chris's screen-recording of the
scroll problems; mock locally, then publish on approval.

**Deliverables** (all front-end; no migrations): column groups Money /
Est. Cash Flow (hybrid of two-view + progressive disclosure), collapsed-by-
default offices at 10 jobs each, derived Balance to Pay, live 3-week cash map,
currency field, overdue tracer, sticky-header fixes. Rule codified as
"Long-list disclosure" in standards/design/v1.md, CONVENTIONS.md §11a,
CLAUDE.md, AGENTS.md, .cursor/rules.

**Measured before → after** (1920×900): page 4,465 → 1,021px; chrome before
row 1 711 → 578px; rows visible 3 → 11; row height 67 → 29px; horizontal
overflow 585 → 0.

**Bugs found and fixed** (the "data above the frozen header" artifact was four
faults stacked):
1. `overflow-x: auto` + `overflow-y: visible` → Y computes to `auto`, so the
   pane became an unbounded scroll container and sticky anchored to it.
2. Second header row's sticky `top` hardcoded 26px; group row measures 31px.
3. `th:nth-child(-n+2)` also matches the column-group row (spacer=1, MONEY=2),
   freezing MONEY left and demoting it to z-index 2.
4. Group header `colspan` fixed at 2, so an expanded group left right-hand
   columns uncovered. colspan can't be set in CSS — recompute on toggle.
Also: `Balance to Pay` was rendering static billed_ar (KS-90 showed $0 against
$146,576 due with $75,000 to collect); now derived Balance Due − To Collect.

**Decisions**:
- Pane is ALWAYS exactly 10 rows tall, measured. The wheel only changes owner
  as the direct result of a click — that is what makes the internal scroll safe.
- View state moved sessionStorage → localStorage (weekly planning surface).
- ⚠ Cash map basis changed from billed AR to Balance Due, to match the
  worksheet it is now driven by. Caption/labels updated. Doctrine change, not a
  defect fix — flagged to Chris.
- `Collected?` column removed (QBO polls payments nightly).

**Shipped**: main 6c328cd, live `buildCommit` 6c328cd, tree clean.

**Open threads**: apply Long-list disclosure to Invoice Audit, Price List
Review, Categorize Price Lines, Credit Memos, Agreement Builder. Plus carried:
ABC trigram → colour rule (needs Chris); PEC-226's 24 candidates;
product_taxonomy `<gt/>` entities; 427 categorization lines.

## Session — PR #9 upkeep across the day (PEC-221)

**Goal**: Keep PR #9 (`claude/project-handoff-5ua2fw`) merged with a fast-moving main, verify
the coverage findings survive every parallel change, and make the branch visible in the handoff.

**Deliverables**
- Migrations renumbered seven times as parallel sessions claimed numbers: now **263-266**;
  doc now **`docs/104`**. Applied Supabase labels still read `245_`/`246_`/`248_`/`249_` —
  cosmetic, the DB keys on timestamp.
- `docs/handoffs/current.md`: re-added the "Open branch not on main" subsection and this
  branch's blockers after two wrapups wiped them; merged both blocker lists rather than
  picking a side; corrected the stale architecture invariant, the PR status, the migration
  watermark, and the completed PEC-226 next-task.
- `docs/104`: Colorado addendum, the territory-is-not-enough correction, the PEC-226 outcome,
  and the note that mig 253's aliases do not fix the ring defect.
- Command Center: honoured `no_book` rulings and stopped labelling invoice totals
  "un-audited" (both from the Greptile review); unresolved spend now renders independently of
  the gap count. 315 tests pass.

**Decisions**
- **Re-run the views after every parallel merge** rather than trusting commit messages. This
  caught real drift twice: mig 249 moved unresolved spend to $27,566.56 / 26 inv while its
  message said nothing moved, and mig 251's Colorado book landed inert.
- **Marked PR #9 ready for review.** The code is complete and validated; the outstanding
  Denver × SRS item is a business decision the PR documents rather than implements, so it is
  not a merge prerequisite. Seven renumberings in ~30 hours made the churn the larger cost.
- **Predicted PEC-226's outcome and it held**: Denver × SRS stayed at `priced_items = 0`
  because the backfill only addresses the line-level half.
- **Did not decide the branch identity** — mig 240's rule is that a guess cannot become a fact.

**Open threads**
- **Needs Chris**: confirm `AMSDE` == `SBP-SOUTHDENVER` so the two Denver books (101 + 22
  items) can be repointed, **or** approve repointing the agreement join to `vendor_branch_id`
  with mig 244's equivalence proof. `v_agreement_unreachable` = 6 agreements / 237 items.
- The $63k SRS gap needs the trigram arm, the branch-identity fix, or both; exact-token
  matching on description-only sheets matched 8 items in total.
- PR #9 is ready for review, 0 behind main, not merged and not deployed.

**Verified facts recorded along the way** (each measured against prod, not inferred)
- Territory alone does not reach the ring: `v_office_vendor_branch` needs
  `vb.geom IS NOT NULL` first, then an exact branch-number **text** match. `AMSDE` and `SSMEL`
  both carry correct territory and return `in_ring = false`.
- The 2026-08-14 Colorado list (101 items) attached to `AMSDE`, so it is ring-unreachable too;
  two independent blockers sit on that book and fixing either alone leaves it inert.
- Mig 253's SRS aliases do not help: `AMSDE` is not in the seed, and `vendor_branch_alias`
  resolves an *invoice's* branch code at ingest, not the *agreement* → branch join.
- PEC-226 matched 4 of 101 Colorado and 4 of 97 Melissa items; SRS unpriced lines 216 → 211.

## Session 5 — PR #9 review response, and a defect found in my own migration

**Goal**: Work PR #9's review queue to a clean state, and verify each finding against prod
rather than taking it at face value.

**Deliverables**
- `a1222de` — the coverage comments now cite `office_vendor_agreement_status` (mig 265) as
  the source of record instead of naming an individual. Better on its own merit: a reader
  asking "why isn't QXO in the chase queue" gets a table to query.
- `212dc77` — **mig 264 defect I introduced.** The `geocode_status` CASE keyed on
  `vb.city IS NULL`, which (a) demoted already-geocoded rows `ok` → `pending`, re-queueing a
  good geocode — branches **21 (Raleigh)** and **684 (Norman)**, both stamped
  `2026-08-20 10:59:17`, the migration's own run — and (b) missed address-only recovery.
  Now gated on `vb.geom IS NULL`. Verified 0 rows affected on current data, so a proven
  no-op; **not re-applied**.
- `f291a90` — four more: deterministic `raw_best` tiebreak on `i.id` (ties had no defined
  order, so two runs could pick different city/state — not idempotent); `NULLIF` empty-string
  normalisation matching mig 200 (0 of 1,091 payloads affected, defensive); `selectAll` now
  passes a per-view unique sort key (offset paging with no ORDER BY can duplicate or skip a
  row); accepted-ruling pill reads "Accepted ruling ({status})" since `isAccepted` covers
  `not_pursued` too.
- PR #9 description rewritten — it still named migrations `250-253` and `docs/100`, filenames
  that stopped existing after seven renumberings.

**Decisions**
- **Declined the "remove individual attribution" finding on the operational records.**
  CodeRabbit had already withdrawn the same premise on the code thread ("the broader PII
  premise does not apply as stated"). `Needs Chris` names *who must decide something not yet
  decided* — a role label makes the handoff less actionable, and the schema agrees:
  `office_vendor_agreement_status` carries `decided_by text`.
- **Did not repair the 4 geocoded rows sitting at `pending`.** Two are mine; branches 39 and
  465 were touched at 13:04 by a process this session does not own, where `pending` may be a
  deliberate re-geocode request. Cancelling queued work on a guess violates mig 240's rule.
- Left the historical "309 passing tests" line in the handoff intact — 309 was true when that
  session wrote it, and the line's whole point is that a green suite of known size still
  missed two defects. Only the live verification command was refreshed to 315.

**Open threads**
- Unchanged and still the one thing needing a human: confirm `AMSDE` == `SBP-SOUTHDENVER`,
  or approve repointing the agreement join to `vendor_branch_id` with mig 244's proof.
- 4 rows violate the `geom IS NOT NULL ⇒ geocode_status = 'ok'` invariant (1,752 rows hold
  it). Documented in `docs/104` with timestamps; needs a human to confirm the 13:04 intent.
- PR #9: Greptile 5/5 and Cursor Security both green on `a1222de`; head is now `f291a90`.

**Lesson**
- A bot's merge-risk line can be stamped against a stale head — this one said `up to b4940`
  while the branch was two commits further on. Check which commit a finding measures before
  acting on it, and check the finding itself against the live DB: two of six were latent
  (0 rows), one was real and already fired in prod, one was wrong about the fix direction.

### Session 5 amendment — `646ccab`

Two defects in my own `f291a90`, found by chasing a follow-up review finding:

- The deterministic tiebreak referenced **`i.id`; `abc_invoices` has no such column** (its key
  is `invoice_number`). Mig 264 would have **thrown on any rerun**. It surfaced only because I
  dry-ran the statement against prod instead of trusting that it read correctly.
- The geocode CASE still missed **state-only** recovery — `(city IS NULL OR address IS NULL)`
  is not the question the WHERE asks. The CASE is now textually identical to the WHERE plus
  the geom guard, so anything qualifying a row for update is exactly what queues the geocode.

**Lesson (durable): an applied migration is never re-compiled, so "looks right" is not
evidence.** Dry-run any edited migration against prod as a SELECT before committing — it
costs one query and it caught a hard error a careful read had missed twice.

**Second lesson: a bot's "✅ Addressed in commit X" stamp is a guess, not a verification.**
CodeRabbit marked the state-only finding addressed by `f291a90` — the very commit that
introduced it. Re-check the claim against the file rather than accepting the stamp.
