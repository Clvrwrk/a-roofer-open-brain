
## Session — Invoice Audit 4+4 redesign, Sent CM workspace, territory drops (06:15 CT)

**mig 249**: Wichita Falls (ABC + QXO 249) + Austin (ABC 39, 465) → `out_of_boundary`.
Wichita Falls came in via the closed Euless isochrone with no human decision; Austin were
Chris's 2026-06-10 overrides, now reversed. Both Austin invoices are no-price shop supplies
(S.S.TOOLS, TRUCK 102) so **no dollar moves**. Now **0 covered branches sit outside an
active isochrone** — `office_for_point()` and `pricing_status` agree everywhere.

**Invoice Audit toolbar 10 → 4 controls**: search · office · **segmented To audit/Due now/All**
· **More filters ▾**. The 3 checkboxes still exist HIDDEN and remain the source of truth for
`applyFilter()`, so tree logic + deep links are untouched; the switch drives them. The
Due-Now card button *clicks the switch* because setting `.checked` in code does not fire
`change`.

**Pills 7 → 4 cards**: Claim it · Chase it · Pay it (`56 → 32 → 5` as a pipeline) · Fix it.

**TWO regressions I introduced and caught only in the browser** (build + 309 tests were green):
1. KPI refresh did `card.querySelector('.iv-process-btn')` = *first button in card*. That was
   Export in the 7-pill layout, but **Filter ↓** after merging → the 60s poll replaced Filter
   with an Export link. Fixed with explicit `[data-kpi-btn="qb"]`.
2. Refresh replaces `innerHTML` of `[data-kpi-sub="awaiting"]`; I had nested
   `[data-kpi-val="awaiting"]` inside it → wiped on first poll. Un-nested.
**Lesson: the KPI refresh contract is positional — never move a button between cards without
re-reading `refreshKpiPills`.**

**Tree had NO empty state** — default view rendered blank because "To audit" is legitimately
0 (every open invoice is fully audited). Added one that names the way out.

**New `/accounting/credit-memos/sent`** — exact replica of Weekly CM scoped to `status='sent'`,
Request→Sent language, + sent/follow-up dates, overdue in red, "Mark received" instead of
Approve, no drafts section. `weekly-csv` gained `?status=sent` (default stays `approved`).
**Key fact: NO outbound email body is stored** (packet has provenance keys only), so the page
REBUILDS the email from today's lines and says so in a banner — docs/93 revised some sent
figures, so a line can differ from what the vendor received.

Open: exact email replay needs the rendered HTML stored at send time (upstream fix).

### CM workspace header enhancements + mig 250 (06:40 CT)

Chris's markup on Weekly CM, applied to **both** Weekly and Sent via one shared helper
(`lib/cm-request-header.ts`) so they cannot drift:
1. **Vendor in the subject**: `Subject: [SRS Distribution] Credit memo request - …`
2. **Claim header context line**: PE office · vendor · vendor branch (name + id) · Job ↗
   (client) · Invoiced date. Job is a live AccuLynx link when an id exists, text when only a
   number does. Missing pieces are NAMED ("office not resolved"), never blanked.

**mig 250 — `v_vendor_invoice_acculynx_match`.** First render printed **"no job on the PO"**
on every SRS claim whose PO plainly read KS-189/188/184/194/198. Not missing data — a WRONG
label: `v_invoice_acculynx_match` is built `FROM abc_invoices` and never covered
`vendor_invoices` at all. New view applies mig 237's canonical PO job token to
vendor_invoices → **32/33 resolve**. Header loader reads both matchers, ABC wins collisions
(3 tiers vs 1).

Result: Sent page 44/44 headers enriched, **0** false "no job" labels, 43 live job links.

**Lesson (2nd time this session): the build was green and the wrong label was a plausible
sentence. Only rendering the page caught it.** Same class as the KPI-refresh positional bug.

Perf note: `v_invoice_audit_invoice` filtered by `invoice_number IN (...)` pushes down to an
index scan — 55ms for a page's worth. Safe to query directly; do NOT join it wholesale.

### SRS agreement PDF link path (06:45 CT)

Ask: active agreement-PDF link on the credit-memo workspace for SRS.

**The SRS PDFs do not exist anywhere reachable.** `agreements` bucket = 7 objects, ALL ABC.
All 3 active SRS agreements name a file, none stored, none on this machine:
`Wichita_Quote0049828559.pdf` · `Englewood_co_Revised…(1).pdf` ·
`Richardson_MELESSA_PRICE_SHEET- LEVEL 4 (1).pdf`. Told Chris; gave the upload command.

**Fixed the two bugs that would have kept the link broken EVEN AFTER upload:**
1. `scripts/upload-agreement-pdf.mjs` binds `source_pdf_url` to a **bucket-relative path**
   (`agreements/x.pdf`); the endpoint redirected to it verbatim → relative `Location`
   resolves against the app host → 404. Endpoint now splits bucket/key and **signs** it;
   absolute URLs still pass through.
2. The evidence panel used `source_pdf_url` directly as an href — same breakage. Now routes
   through `/api/price-agreement/pdf/<id>` like the ABC path always did.

**Truthfulness regression caught in the same pass**: another session backfilled the SRS
re-audit writer, so discrepancy rows now carry a **uuid** `agreement_id` → the panel flipped
from its derived path to its RECORDED path, which only enriched ABC **integer** ids. For a
uuid it left version_label/source_file/ceo_verified null → `isQuote` defaulted false →
Document-type check said "Priced from an agreement" for an **accepted QUOTE**. docs/93 says
a quote must never be hidden. Recorded branch now enriches uuids from `price_agreements`.
Restored: "Quote 0049828559 is accepted as the governing price agreement for Wichita, KS."

**Standing lesson, 3rd time this session**: green build + plausible sentence ≠ correct.
Rendering the page is what caught all three.

### Mark-sent buttons + SRS writer verification (06:55 CT)

**SRS re-audit writer: ALREADY FIXED, no work needed.** Verified all classifications:
srs discrepancy 11/11 have agreement_id + office_id + office_name; abc discrepancy 128/128.
Only `no_price` rows lack an agreement — correct by definition. The real gap was the panel
DISPLAYING a uuid citation, fixed in `dbe0e1f`. Confirmed across all 5 approved SRS claims:
`source: recorded`, `isQuote: true`, `ceoVerified: true`, Document type + Citation
provenance both pass.

**"Mark sent" buttons** — `mark-sent` already existed in the disposition API (sets
status=sent, sent_by/at, follow_up_due_at +14d, auto-fires the Process stamp) but had no
entry point outside the single-invoice detail page.
- **Weekly CM**: per-vendor button in the vendor summary (one email per vendor = the natural
  unit) AND per-invoice beside `detail →`. `stopPropagation` so the `<summary>` does not
  collapse.
- **Invoice Audit Card 1**: one button; reads `/api/credit-memos/pending`, takes the
  `approved` set, and **names every vendor in the confirm** because the set can span vendors
  and you may only have emailed one. Explicit `[data-kpi-btn="marksent"]` hook for the 60s
  poll — NOT positional (per this session's repeated lesson).

Nothing is emailed (SOUL). Each write is vendor-scoped by the existing endpoint.
Verified by stubbing `window.confirm` to DECLINE — correct text on both surfaces, summary
does not collapse, zero prod rows touched.

### Wrapup (07:00 CT)

Session commits `8d38263..52c67fa` (4), all deployed, live buildCommit = 52c67fa, tree clean.
Migrations 249 (Wichita Falls + Austin out_of_boundary) and 250 (vendor-side AccuLynx job
matcher). Renamed my `docs/99-…` → **docs/100** — the previous session had already taken 99.

Money state unchanged by this session (correct — the work was UI, territory and matching,
none of which touches pricing): at_risk $3,679.58 total / $2,692.08 actionable,
credit_memo_amount $997.05, 5 approved / 44 sent.

Handoff: `docs/handoffs/current.md`; prior archived to
`archive/2026-08-21-0005-uiux-phases-money-truth.md`.

**Next task**: upload the 3 SRS agreement PDFs (link path is built and waiting). The 2
Colorado sheets need an export from the PE accounting mailbox — not reachable from
chussey@cleverwork.io (commit ed792ce).

## Session 2 — SRS agreements, ceo_verified retirement, invoice ingest

**Goal**: store + bind the SRS agreement PDFs (the next task from Session 1), then whatever
Chris put in front of me. Three rounds: 4 agreement PDFs, then a policy change, then 5
invoice PDFs.

**Deliverables**
- Migrations **251** (SRS Colorado price list, 101 items, Denver office), **252** (retire the
  `ceo_verified` gate), **253** (SRS branch-code aliases), **254** (`po_number` → AccuLynx job
  number, 9 rows), **255** (canonicalize `po_number` on write, fails open).
- `integrations/bridges/ingest-vendor-invoice-csv.mjs` — new `--pdf` arm on the SAME script,
  reusing its writers. Strict reconciliation gate.
- Code: `lib/credit-memo.ts`, `lib/abc-price-gaps.ts`, `lib/vendor-territories.ts`,
  `price-agreement/builder.astro`, `api/price-agreement/review/promote.ts` — gate removed.
- `docs/101-…` + 2 addenda. Commits `a6b900c`, `fbb32d1`, wrapup. Both deployed and verified.
- Linear **PEC-224/225/226**; closed **PEC-222** and **PEC-211**.

**Decisions**
- **The 4th Colorado PDF got its own agreement record, not a bind to Englewood.** Provenance
  from the PDF author (Blake Wells = the `B WELLS` on both quotes) + TOP SHIELD catalog.
  Anchored to `AMSDE` because agreements resolve at the OFFICE level — proved it: invoice
  `0049707508-001` on `SBP-DENVER` prices off the `AMSDE`-bound quote.
- **Englewood NOT archived.** Date windows decide; 11 re-audit lines cite it.
- **9 `$0.00`/`CALL` items deliberately not loaded** — they would fabricate discrepancies.
- **`ceo_verified` columns kept, not dropped** (rule 1; they are real provenance).
  docs/93 untouched — the panel still says the document is a quote.
- **`po_number` rewritten but the printed value preserved** at `raw.po_number_as_printed`.
  The 255 trigger **fails open** — an unrecognised PO stays as printed, because "no job yet"
  is information and blanking it destroys the only clue.
- `0050471744-001` → KS-208 on exact address evidence; `0050708886-001` → TX-455 on Chris's
  say-so, recorded as a human decision so nobody later reads it as machine inference.

**The finding that matters (PEC-226)**
The vendor price path matches on EXACT item-number or description equality — no trigram arm
(ABC has one at 0.45). SRS **quotes** carry item numbers so they work (55 and 66 line hits);
the two **price sheets** are description-only and match **zero**. The Melissa Level 4 sheet
has been inert since 2026-02-16 — 97 prices, 0 matches, unnoticed. Live SRS: 73 priced lines
/$16,990.55 vs **216 unpriced / $63,642.53**; **Richardson is 0-for-28**.

**Two bugs my own code nearly shipped, both caught by rendering/reconciling, not by tests**
1. The new Document-type string hardcoded SRS wording on a path ABC quotes also use.
2. The PDF line regex required a digit before the decimal; SRS prints `.01 /BD`, so a line
   vanished and the invoice missed SUB-TOTAL by exactly its $0.89.

**Not mine**: 5 CM requests moved approved→sent at 14:21Z mid-session (SRS, ~1s apart,
Card 1 bulk Mark-sent). My dev server logged zero POSTs — a human on the live site.

**Open threads**: PEC-226 (item-number backfill vs trigram arm — needs Chris's call); 9
Colorado `$0.00` items need prices from Blake Wells; prefer the portal CSV over PDFs when
available (PDF truncates PO NUMBER to 16 chars).

## Session — PR #9 upkeep: handoff visibility + re-verify after mig 249 (PEC-221)

Branch `claude/project-handoff-5ua2fw` (draft PR #9), kept merged with main all day.

- **Handoff repair**: main's 2026-08-20 wrapup (`8d38263`) rewrote `docs/handoffs/current.md`
  without mentioning this branch. Re-added an "Open branch not on main" subsection under Git
  State and **blocker 4** (Denver x SRS branch identity), plus a sub-bullet under their
  blocker 3 noting the expiry of `0049345641` is not the whole story — the office ring cannot
  reach it at all. A next session would otherwise not have known migs 250-253 are applied to
  prod on an unmerged branch.
- **Migration numbering settled**: parallel sessions took 245-248, then **249**
  (drop-wichita-falls-austin-out-of-boundary). This branch's files sit at **250-253** and the
  sequence is now contiguous. Applied Supabase labels still read `245_`/`246_`/`248_`/`249_`
  — cosmetic; the DB keys on timestamp.
- **Re-verified my figures against prod after their mig 249 changed territory assignments.**
  Their commit said "no dollar figure moves" — true for at_risk, NOT for the coverage views:
  - unresolved (no-office) spend **$26,848.33 / 24 inv -> $27,566.56 / 26 inv**
  - Richardson x ABC **-$718.23** (the two Austin invoices), Wichita x ABC **+$108.04**
  - Delta reconciles exactly: money moved from a covered office into the honest "no office"
    bucket. Fail-closed working as intended. Corrected the stale figure in docs/100.
  - **Unchanged and still correct**: Denver x SRS $17,437.63 / `priced_items = 0` /
    needs_ruling / agreement_not_reaching; all three SRS agreements still ring-unreachable
    (`v_agreement_unreachable`); Atlanta x ABC $5,226.90 pending; QXO no_book.

**Lesson**: another session's "no dollar figure moves" is scoped to the metric THEY were
watching. Re-run your own views after anyone touches shared dimensions (here, branch
territory) rather than trusting the claim.

**Still open (needs Chris)**: confirm `AMSDE` == `SBP-SOUTHDENVER` so the Denver book can be
repointed, or approve repointing the agreement join to `vendor_branch_id` with mig 244's
equivalence proof. PR #9 remains a draft; nothing merged or deployed.

### Later that day — merged 5 more commits; the fresh Colorado book landed and still does not price

- **Third renumbering.** Parallel sessions took 250, 251, 252 and moved their doc to
  `docs/100`. This branch's files are now **253-256**, doc **`docs/102`**. Numbering is
  contiguous again. Applied Supabase labels remain `245_`/`246_`/`248_`/`249_` — cosmetic.
- **Handoff sections were wiped** by their 2026-08-21 wrapup (`07e3466`), as predicted in the
  check-in note. Re-added: the "Open branch not on main" subsection survived auto-merge (its
  numbers corrected); the blocker list conflicted and was resolved by MERGING both lists —
  their four new items plus this branch's Denver x SRS blocker and Colorado sub-bullet.
- **The finding that matters**: mig 251 stored the **SRS Colorado price list, 2026-08-14,
  101 items** — the book handoff blocker 1 was waiting on. It attached to **`AMSDE`**, the
  ring-unreachable row. **Denver x SRS still reports `priced_items = 0`** with TWO live books
  now (101 + 22). Ring-unreachable SRS items **136 -> 237**.
  - Their migration note already flags one reason: description-only, no SRS item numbers, so
    the **line-level** path cannot resolve it.
  - The **ring** path fails independently: it cannot reach `AMSDE` at all.
  - **Fixing either alone leaves the book inert.** Recorded in docs/102 and the handoff so
    nobody schedules half the work and wonders why Colorado is still no-price.
- Re-verified after the merge: Denver x SRS $17,437.63 unchanged, `needs_ruling` and
  `agreement_not_reaching` still true; Atlanta x ABC $5,226.90 pending; Wichita x SRS now 27
  invoices / $66,966.32 (their new data), priced_items still 17.

**Standing lesson, now twice-confirmed**: re-run your own views after any parallel session
touches a shared dimension. Their commit messages are scoped to the metric they were watching.

### Fourth renumbering + verified mig 253 does not fix the ring defect

- Parallel session took **253** (SRS branch-code aliases). This branch moved to **254-257**.
  Numbering contiguous; doc stays `docs/102`.
- **Checked whether their aliases fix the Denver x SRS defect. They do not**, and it is worth
  knowing why so nobody assumes otherwise: `AMSDE` is not in their seed (`DJWIC`, `SSMEL`,
  `SSCOP`, `AMDEN`, `SHCOL`), and `vendor_branch_alias` resolves an INVOICE's branch code at
  ingest — a different mechanism from the AGREEMENT -> branch join in
  `v_office_vendor_branch`, which reads branch_number as text and never consults the alias
  table. Recorded in docs/102.
- Re-measured after merge: Denver x SRS $17,437.63, priced_items 0, both flags true;
  `v_agreement_unreachable` 6 agreements / 237 items. All unchanged.

### Fifth renumbering + a stated invariant that is not true

- Parallel session took **254, 255**. This branch moved to **256-259**; doc stays `docs/102`.
- **Found a load-bearing error in the handoff's architecture note**, which PEC-226 is planned
  on: *"anchor a new agreement to any branch in the right territory and it covers all of
  them."* Not sufficient — `v_office_vendor_branch` requires `vb.geom IS NOT NULL` BEFORE
  territory is consulted, then matches the agreement by branch-number text. Verified: `AMSDE`
  and `SSMEL` both carry the correct `pricing_territory_office_id` and both return
  `in_ring = false`.
- **Practical consequence, recorded in the handoff and docs/102**: PEC-226 backfills
  `raw_item_number` (the line-level half). Melissa should start pricing after it, because an
  archived duplicate agreement reaches the ring via the geocoded twin `SBP-MELISSA`.
  **Colorado has no twin and will stay at `priced_items = 0`.** Whoever runs PEC-226 should
  expect that split, not read it as a failed backfill.
- Blocker lists merged again rather than picking a side; their 7 items plus this branch's two.

