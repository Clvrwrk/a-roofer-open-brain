# 2026-06-25 Session Log

## What we did

### Agent Onboarding Automation — All 4 One-Time Setups Complete

Completed the full automation infrastructure so future agent deployments require zero human steps:

1. **Kasm API key** — Generated `hermes-automation` API key in Kasm admin (`/api/admin/` path, NOT `/api/v2/`). Required DB permission grant (`group_permissions`, `permission_id=200`, Administrators group) — not obvious from docs. Key in `.env` as `KASM_API_KEY`.

2. **Slack config token** — Got workspace-level config token from `api.slack.com/authentication/config-tokens`. Important discovery: this is `xoxe.xoxp-` (account-level), NOT `xapp-` (app-level Socket Mode token). Both access token + refresh token saved. Manifest API validated.

3. **gcloud auth** — Installed gcloud CLI via Homebrew. Ran BOTH required auth commands: `gcloud auth login` (CLI tools) AND `gcloud auth application-default login` (Python/SDK). Both are required separately. Org policy `iam.disableServiceAccountKeyCreation` is permanently lifted (was disabled for Maya, never re-enabled). Project `custom-frame-500419-s3`, org `686389385048`.

4. **GHL Firebase token** — Got full 546-char token via Chrome extension (`GHL - Claude CLI/chrome-extension/`). Confirmed GHL Pro Exteriors has 0 workflows (clean slate). Firebase API key is baked into `ghl_internal_client.py` source — gets redacted by scanner, must be extracted via Path().read_text() and patched into module before import.

**verify-automation-setups.py**: 4/4 passing.

### Forensic Review — New Skills Built

After thorough forensic review of all session learnings:

- **`credential-handling-patterns`** — Token safety, length verification, write_file truncation pitfall, safe merge pattern, expected lengths for all platform tokens
- **`kasm-workspaces`** updated — `/api/admin/` path, permissions grant, entrypoint fix
- **`slack-agent-bot`** updated — config token vs app token distinction, refresh token required
- **`ghl-workflow-builder`** updated — dual .env loading, Firebase key extraction pattern
- **`open-brain-agent-onboarding`** updated — gcloud ADC pair requirement, org policy status
- **verify-automation-setups.py** — added to skill as reusable pre-deploy check script
- **config/.env.example** — detailed WHERE/pitfall comments for all 4 automation credentials

### Agent Profile System — Built from Scratch

Read all source documents (persona docs, access-control.ts, ROLE.md files, credentials spreadsheet, cadence config) and built:

- **`agents/profiles/_schema.yaml`** — 58 fields, 44 required, 10 validation rules. Platform defaults. Critical invariants enforced: `approval_decide: false`, `external_send_authorized: false`, email domain `@cc.proexteriorsus.net`, Rowan external-only network.

- **7 agent profiles** — maya-chen, alex-rivers, casey-morgan, jordan-price, sam-torres, rowan-vale, lena-brooks. All 7/7 pass schema validation.

- **`agent-profile-builder`** skill — creates/validates profiles, includes `validate-all-profiles.py` script

- **`deploy-agent`** skill — 14-phase fully automated deployment, idempotent, Slack-only output

- **Committed** `76f79a5` to `main`.

### Design Decisions Made

- Profile YAML = canonical source of truth for infrastructure. Spreadsheet = credentials (runtime only, never in YAML)
- Deploy is fully silent — Slack summary when done, no console output
- Idempotent — safe to re-run, skips what's already done
- Rowan Vale is the only agent with `external-only` network policy (no internal brain access, no Supabase token)
- Slack admin token (`SLACK_ADMIN_BOT_TOKEN`) needed for zero-click app install — last remaining manual step

## State at handoff

### Infrastructure
- All 4 automation setups: ✅ complete and verified
- Maya Chen: ✅ fully onboarded (all 14 phases except 12+14)
- 7 agent profiles: ✅ valid, committed to repo

### Pending
- Maya Phase 12 (cron) and Phase 14 (guardrails) — not yet done
- Alex Rivers: ready to deploy via `deploy-agent alex-rivers`
- `scripts/deploy-agent.py` — the actual deploy script — not yet written (skill describes it, script to be built)
- `SLACK_ADMIN_BOT_TOKEN` — one-time admin OAuth grant needed for fully automated Slack app installs

### Repo state
- Branch: `main`
- Latest commit: `76f79a5` (profiles)
- `context/MEMORY.md`, `docs/handoffs/current.md`, `integrations/bridges/gohighlevel/README.md` — modified, need commit
- Untracked: StormWatch/ZoomInfo work from another session (separate work stream, do not touch)

## Next session start

1. Run `verify-automation-setups.py` — confirm 4/4 still passing
2. Run `validate-all-profiles.py` — confirm 7/7 still valid
3. Build `scripts/deploy-agent.py` (the actual execution script)
4. Deploy Alex Rivers: `deploy-agent alex-rivers`
5. Then: Maya Phase 12 (cron) + Phase 14 (guardrails)
