## Session — Live-review fix round: 2 more silent-truncation bugs + full-fleet crm_pipeline backfill

**Goal**: User live review (F-grade items): zero-value rows, KS-11 rep, colorado empty, name numbers, trailing-7 redesign.
**Deliverables**: Dashboard round (buildCommit 7ee28c4): zero-value jobs hidden w/ caption, formatJobDisplayName numeric-name fix, 7-pill trailing row (transitions + AR totals + aging + collected; collected := approvedJobValue − balanceDue), durable-reps-from-raw (sync v41). THEN two live incidents root-caused + fixed by orchestrator: (1) crm-pipeline financials `.in(1286 ids)` blew PostgREST URL limit → 400 every pass on big accounts (fix: account_key equality, v42, commit 5a518f2); (2) unpaginated loads truncated at the 1000-row cap (jobs/financials/raw-reps → pageAll .range(), v43, commit 6323ced; playbook pitfall #3 struck AGAIN — now fixed at all sync query sites). Driver loops backfilled the fleet.
**State**: KS-11 COMPLETE (Bob Smolek / 30368.48 / 17532.48 in crm_pipeline + live dashboard). Accounts done: wichita 1286/1286, colorado 1844/1844, kansas_city 166/166 (100% reps), georgia 435/435 (100% reps), florida/insurance/mfc. PENDING: texas loop (~03:40Z, 2301 jobs); reps keep filling on subsequent passes; hourly cron maintains steady-state + webhooks 8/8 live.
**Open threads**: verify texas after loop; consider /gsd-complete-milestone; watch check_acculynx_alerts (21 stale watermarks alert resolves as rotation completes).

### Incident tally addendum (early AM)
Incidents #3-#5 found+fixed same night: (#3) index.ts job-ID walk list unpaginated → colorado/texas/wichita tails NEVER walked (v44, f5641fe); (#4=same family) — full audit: no other unpaginated account reads; (#5) PostgREST bulk-upsert column-UNION nulled omitted primary_salesperson keys (rep WIPE 947→800 observed live) + global newest-4000 reps window diluted by other accounts → fix: partitioned upserts (rep/no-rep) + endpoint-targeted chunked .in() rep lookup (v45, f517634). RESULT: wichita reps 1,229/1,286 (> pre-wipe peak; 57 truly repless in AccuLynx), mfc 362/362. colorado/texas tails walking (loops → ~05:15/05:55Z), then their v45 pass resolves reps identically. Lesson reinforced: EVERY unpaginated PostgREST read/bulk-write is a silent-truncation risk — playbook #3 now enforced across the sync.

### FINAL BOARD (backfill complete, ~05:5xZ)
All 8 accounts: **6,451/6,451 crm_pipeline rows · 6,394/6,451 reps (99.1% — 57 wichita jobs genuinely unassigned in AccuLynx) · 1,006 jobs with real contract value.** Per-account reps: colorado 1844/1844, texas 2301/2301, georgia/kc/mfc/florida/insurance 100%, wichita 1229/1286. KS-11 live: Bob Smolek / 30368.48 / 17532.48. Steady-state from here: hourly cron (D-18 fair-share) + 8/8 webhooks; alert condition (e) guards walk errors; stale-watermark alert clears as rotation cadence normalizes. Sync at v45; dashboard at 7ee28c4-era build + data now behind it.
- 11:09Z ABC production sync (env=production): PARTIAL/BLOCKED — full pull not runnable in Cowork sandbox (bwrap --die-with-parent kills background jobs; 45s/call < ~25min catalog). Token 200. Tables confirmed CURRENT & unchanged: abc_product_catalog=331390, abc_vendor_branches=720, abc_regions=40 (delta 0 vs 2026-06-26). Fix: run full pull on Hetzner host or switch job to incremental mode.

## Memory Health

CAP COMPLIANCE failed: context/MEMORY.md at 2,447 / 2,500 chars (within 5% of cap). Consolidate via meta-memory-write skill rules. Other checks green.

### Round 3 shipped (filter semantics) — via GitHub MCP (gh token expired)
Deployed 310b984: unified window model (WTD/MTD-default/QTD/YTD, per-stage dates: lead/prospect/approved/invoiced/closed; closed-or-invoiced with NO balance due outside window = excluded everywhere, WITH balance due = always visible); unassigned rules (no-value leads dropped; unassigned → [account] rep bucket; GUID-ish names → [rep-unknown]); trailing-7 immune to window, obeys loc/type/rep; account bars single-justification + bold-$ (count); drill-downs Value + Outstanding AR, window-filtered; freshness badge now keys per-account jobs watermark (root cause: min over legacy NULL + job_walk rows → everything read stale); Executive nav = single "Pipeline Review", /executive redirects. 241/241 vitest. NOTE: local git push auth broken (gh keyring token invalid; no SSH key) — shipped as 4 MCP push_files commits (166d8bd/7e55c6e/aa37360/310b984), trees verified byte-identical, local reset onto remote. User to re-auth: gh auth login -h github.com. Interpretation flag pending user review: open stages (lead/prospect/approved) window by their OWN stage dates.

## Session — WRAPUP (learnings hardened into the knowledge base)
**Learnings persisted per user directive**: docs/42 playbooks 9-11 (.in() URL 400; exactly-N cap signature; bulk-upsert column-UNION null-wipe); runbook "silent-truncation signatures" table + driver-loop recipe (p_resources arg); coolify skill "git push auth fails" section (gh re-auth, GitHub MCP push_files fallback + byte-identical verify + local reset, never scan .env); dashboard-spec.md as-built rounds 1-3 addendum (authoritative = UI-SPEC amendments + pure core); repo MEMORY.md playbook line + pickup pointer refreshed (2,419 chars); agent auto-memory: postgrest-silent-truncation + github-push-mcp-fallback.
**Open threads (next session)**: (a) human: `gh auth login -h github.com`; (b) user confirm open-stage windowing interpretation; (c) `/gsd-complete-milestone`; (d) deferred registry unchanged (Phase 5 prod payment; ob-acculynx Slack provisioning; ABC full-pull host limitation).

## Session — AR-truth fixes (audit round 4, user-approved fixes 1-4) — SHIPPED
**Goal**: fix the four audit findings (AR card vs pill $5.2K/$817.7K split; AR exception too narrow; close-rate headline unweighted; leads-count gap vs AccuLynx).
**Deliverables**: executive-pipeline.ts (+test, +astro page, +client script) on contrib/cleverwork/ar-truth-fixes → merged to main 3f1293e → deployed (healthz buildCommit confirmed ~2min post-push). 249/249 vitest, build green. dashboard-spec.md AR-truth as-built addendum.
**Decisions**: (1) ONE AR truth — every AR signal reads per-job open-invoice AR from acculynx_invoices (openInvoiceArByJobId); crm_pipeline.balance_due RETIRED as AR signal (disagrees job-by-job; $2.4M on cancelled). KPI card ≡ trailing-7 pill by construction ($817,694.96 live). (2) Window AR exception covers ANY job with an open invoice (live: 100% of open AR sat on approved/completed — old invoiced/closed-only exception hid all of it); zero-value drill rows with open AR no longer hidden. (3) Monies Collected = invoice ledger (total_price − balance_due per invoice, floored; nothing billed = $0 collected) — replaces contract−balance proxy. (4) Close Rate headline = job-count-weighted snapshot (closeRateSnapshotOverall, 67.5% live) not Res/Com mean (50%). (5) Leads gap CONFIRMED structural: AccuLynx public API v2 has NO leads-list endpoint (only /leads/{id}/history; matrix docs/65 concurs) → unconverted Leads-module records can't sync; New Leads captioned "jobs in Lead milestone".
**Also**: gh re-authed with user-supplied PAT (keyring, push working again).
**Open threads**: user visual check of /executive/pipeline; prior deferred registry unchanged.
