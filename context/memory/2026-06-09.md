# 2026-06-09 Memory

## Session 1

**Goal**: Review WorkOS state, interview Chris, produce WorkOS AuthKit spec + build plan for cc.proexteriorsus.net.
**Deliverables**: `docs/34-workos-human-auth-spec.md` (new).
**Decisions**:
- AuthKit launch methods: email+password, Google OAuth, magic code; MFA humans-only (dashboard MFA stays Optional because WorkOS TOTP requirement is environment-wide; humans enroll operationally).
- Launch roster: 3 humans + 7 named agents log in via WorkOS; service agents keep bearer tokens.
- Roles: env-based email allowlists (admin/purchasing/accounting); unknown emails allowed as viewer only when domain is on `COMMAND_CENTER_VIEWER_DOMAINS`, else 403 denied page.
- Local dev keeps `COMMAND_CENTER_AUTH_MODE=disabled` fallback; rollback is flipping mode back to disabled.
- Spoofable `x-workos-user-email` header path in `access-control.ts` will be deleted, not retained.
- agent-auth OAuth runtime (auth.md POSTs/JWKS) stays out of scope.
- Found: `/api/product-surface.json` is currently unauthenticated; new middleware will gate it.
**Open threads**: Implement Phase A (SDK, /auth routes, middleware, access-control rework); confirm `@cc.proexteriorsus.net` agent mailboxes receive WorkOS verification mail; add new role env names to `config/.env.example`.

Allowlist confirmed by Chris: admins `chussey@cleverwork.io` + `chussey@aia4.io`; purchasing `roberto@proexteriorsus.com`; accounting `lucinda@proexteriorsus.com`; viewer domains `proexteriorsus.com`, `proexteriorsus.net`, `cleverwork.io`, `aia4.io`.

## Session 2 — Phase A build

**Goal**: Implement WorkOS AuthKit Phase A per docs/34.
**Deliverables**: `@workos-inc/node@10.2.0` added; new `src/lib/workos.server.ts`, `src/lib/session.server.ts`, `src/middleware.ts`, `src/pages/auth/{login,callback,logout,denied}`; reworked `access-control.ts` (header trust deleted, `resolveActorFromSessionUser` role mapping added, `localActor` exported), `auth.ts` (actor-derived shell user), `AppShell.astro` (identity chip + logout POST), API routes read `locals.actor`; `env.d.ts` App.Locals + new env names; `config/.env.example` WorkOS section updated; `astro.config.mjs` gained optional `VITE_CACHE_DIR` override (sandbox/CI builds).
**Decisions**:
- Middleware public list: `/auth/*`, `/auth.md`, `/healthz`, `/.well-known/*`, `/agent/*` (placeholders; NOT `/agents`), `/oauth2/*`, `/api/agentmail/webhook`, `/_astro/*`, `/_image`.
- Logout is POST-only with manual same-origin check (global `checkOrigin` stays off for agent OAuth POSTs); GET returns 405.
- Role permission scoping: purchasing → accounting+operations; accounting → accounting; viewer → read-only all departments.
**Verification**: `astro build` passes; 14-point smoke against built server: disabled mode unchanged (local operator), workos mode → 302 login w/ returnTo, login 302s to api.workos.com authorize URL with state, API 401 unauthenticated, spoofed `x-workos-user-email` header 401, bearer token resolves service agent, healthz/auth.md/.well-known public, logout 405/403/302 matrix, `/agents` gated, `/agent/auth` placeholder still 501.
**Open threads**: Phase B — WorkOS dashboard config (methods, redirects, sessions), provision Chris's user, add env to Coolify, flip `COMMAND_CENTER_AUTH_MODE=workos`, run production checklist (docs/34 §9C). Real-login end-to-end not yet exercised (needs real WorkOS credentials).

## Session 3 — Phase B execution (in progress)

- Committed Phase A as `f0c79e0` and pushed to GitHub (`bb78979..f0c79e0`).
- WorkOS credentials were already in repo-root `.env` and the API key is valid. Environment `environment_01KMSKH8QJY9WFS680J4V1ZNFB`; one pre-existing user `admin@cc.proexteriors.com` (note: `.com`, unverified, created 2026-06-06 — likely a typo'd test user, left in place).
- Redirect URI `https://cc.proexteriorsus.net/auth/callback` NOT yet allowlisted (authorize probe → redirect-uri-invalid). Dashboard-only setting; no WorkOS management API for redirects/auth methods/sessions.
- Created admin users via API: `chussey@cleverwork.io` (`user_01KTQ3JVG0P9HVTD577PHDTM0W`), `chussey@aia4.io` (`user_01KTQ3JVNJ6Z3PDMCG9N51NT1M`), both email_verified.
- Production healthz: live at `bb78979`, `workOsConfigured: true` → WorkOS env values already present in Coolify app env.
- `GITHUB_PUSH_TOKEN` added to repo-root `.env` (fine-grained PAT, ~15-day expiry). Scheduled reminder task `github-pat-renewal-reminder` (9th + 24th, 9am) for rotation. Never print the value.
- Browser automation: Claude-in-Chrome extension not connected; using Control Chrome (AppleScript). Requires Chrome "Allow JavaScript from Apple Events" — asked Chris to enable.
- Cowork sandbox notes: workspace mount blocks unlink by default (use allow_cowork_file_delete); `VITE_CACHE_DIR` + `astro build --outDir /tmp/...` needed for sandbox builds; stale `.git/index.lock` may need manual removal.

## Session 3 results — Phase B complete, auth LIVE

WorkOS dashboard (Cleverwork / Production, app "Pro Exteriors - Command Center" `client_01KTF450QBY957ASEZ8JXZKMV4`), configured via Control Chrome:
- Redirect URI `https://cc.proexteriorsus.net/auth/callback` (default), app homepage `/`, sign-in endpoint `/auth/login`, sign-out redirect `/` — verified via authorize probe (302 → `graceful-square-64.authkit.app`).
- Sessions: max 7 days, inactivity 1 day, access token 5 min.
- Methods: Email+Password ENABLED (strong policy, min 10 chars), Magic Auth ENABLED. Google OAuth NOT enabled — production requires Chris's own Google Cloud OAuth client ID/secret (deferred; WorkOS dialog rejects enable without creds).
- MFA toggle left Disabled (= not required; per spec §7 humans-only enforcement isn't expressible). Sign-up left Enabled to allow viewer-domain self-serve.
- React dialog quirk: inputs need native setter + `_valueTracker` reset + input event; dialogs leave stale `[role=dialog]` remnants — verify against table rows, reload between dialogs.

Coolify (app `command-center`, project dbh70v9wcb1a15c3u0aldytf): Production env updated via Developer view bulk editor — `COMMAND_CENTER_AUTH_MODE=workos`, `COMMAND_CENTER_HUMAN_ADMIN_EMAILS=chussey@cleverwork.io,chussey@aia4.io`, added `WORKOS_API_KEY` (copied in-browser from preview set — production set was missing it), `COMMAND_CENTER_ROLE_PURCHASING_EMAILS`, `COMMAND_CENTER_ROLE_ACCOUNTING_EMAILS`, `COMMAND_CENTER_VIEWER_DOMAINS`. Redeployed → build `f0c79e0` live.

Production verification (all pass): `/` and `/agents` 302→login with returnTo; `/auth/login` 302→WorkOS authorize; API 401 unauth; spoofed `x-workos-user-email` 401; service bearer (ob-accounting) 200; `/auth.md` + `.well-known` 200 public; GET `/auth/logout` 405; `/api/product-surface.json` now gated (401).

Remaining: Chris's real login E2E (magic code), then Phase D roster (Roberto/Lucinda + 7 named agents + TOTP enrollment), Google OAuth creds, named-agent mailbox delivery check.

## Session 3 — Phase D complete

- Created via API: `roberto@proexteriorsus.com` (purchasing), `lucinda@proexteriorsus.com` (accounting) — email_verified, no password (first login via email code), TOTP enrollment is an operational ask.
- Created 7 named-agent users (`maya.chen`, `alex.rivers`, `casey.morgan`, `rowan.vale`, `lena.brooks`, `jordan.price`, `sam.torres` @cc.proexteriorsus.net) with generated 24-char passwords, email_verified (no mailbox dependency).
- Agent passwords written to gitignored `.env.agent-passwords` (matches `.env.*` rule) — Chris must move them to the agent desktop vault and delete the file.
- WorkOS environment now has 12 users (incl. legacy `admin@cc.proexteriors.com`, unverified, never signed in — candidate for deletion).
- E2E production test: authenticateWithPassword as maya.chen → sealed session cookie → `/api/agent/session` returned `maya-chen` named_agent actor with correct reduced permissions (no approval.decide). Full session→actor pipeline verified live.
- Outstanding: Chris first login (last_signed_in still never at check time); Roberto/Lucinda first logins + TOTP; Google OAuth client creds; vault transfer + delete `.env.agent-passwords`; optionally disable Chrome "Allow JavaScript from Apple Events"; consider deleting legacy admin@cc.proexteriors.com user.
