## Session 11:10 — living design system shipped

**Goal**: `/design` brief → a full interactive design system at cc.proexteriorsus.net/design-system with sidebar navigation.
**Deliverables**: `app/command-center/src/pages/design-system/` (18 chapters + `tokens.json`), `src/layouts/DesignSystemShell.astro`, `src/lib/design-system/{nav,tokens}.ts`, `src/components/design-system/*`, `src/styles/design-system.css`, `scripts/design-system-swag.mjs` + 12 fal.ai renders in `public/design-system/swag/`, `docs/112-design-system-site.md`, pointers in CLAUDE.md / CONVENTIONS §11 / config/brand/DESIGN.md / standards/design/v1.md, nav leaf under AI Agents, `--error-surface`/`--error-text` aliases added to global.css, version 0.6.551A → 0.7.x.
**Decisions**: own rail (chapters are the nav); tokens parsed from global.css at build (never copied); contrast measured; site-level dark mode defines the canonical dark mapping and re-points the :root aliases (var() resolves where declared); board specimens via cash-surface.css `.cfx`; swag = references not artwork, provenance in manifest.json; Pantone = nearest solid-coated, physical proof rule C-07; rule ids permanent.
**Open threads**: recorded gaps in docs/112 (no :focus-visible, two resting shadows, IV violet dark accent, two raw prefers-color-scheme surfaces, no mobile rail drawer, long-list adopters outstanding). Client still owes an official reversed logo and roof-only mark. FAL_KEY loads only in interactive zsh (`zsh -lic`).
