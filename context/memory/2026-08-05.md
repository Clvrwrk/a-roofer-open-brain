## Session 1 — Cursor Cloud dev-env setup for Command Center (PEC-147)

**Goal**: Set up + validate the Cursor Cloud dev environment for the Command Center and run it against live data; log the work in Linear.

**Deliverables**:
- `AGENTS.md` — added the missing **key-acquisition** bullet to the existing `## Cursor Cloud specific instructions` section (reconciled with `17fd1b6` already on main; no duplication). Shipped via PR #4 → MERGED to `main` (`154795d`); deploy confirmed (`cc.proexteriorsus.net/healthz` buildCommit = `154795d`).
- Cursor Cloud VM update script registered: `if [ -f app/command-center/package.json ]; then npm --prefix app/command-center install; fi`.
- Linear **PEC-147** (PE-CC-DevEngine, Agent Done) — retrospective log of the whole run, PR #4 attached.
- Validated dev loop: `npm run test` (308/23 pass), `npm run build`, `npm run dev` (:4321). Live-data hello-world against Supabase `rnhmvcpsvtqjlffpsayu`: Operations ~639 job rows, Invoice Audit ~270 open invoices, ABC price-agreement queue, `/api/agent/work-queue` 389 actor-scoped items.

**Decisions**:
- Command Center is the one runnable product; dev command = `npm run dev` from `app/command-center` (no root `package.json`). Update script uses `npm install` (not `npm ci`) for lockfile-drift resilience.
- Reads only against shared prod brain — deliberately did **not** perform dashboard decision writes.
- Key acquisition in Cursor Cloud: injected `SUPABASE_SERVICE_ROLE_KEY` secret, or 1Password via `sa_cw_grok_orchestrator` service-account token (`op` CLI, not preinstalled). Core tables have RLS enabled with **0 policies** → only service_role returns rows (Supabase MCP only exposes anon/publishable).

**Open threads**:
- Original `sacwsecretsadmin` 1Password token was a **deleted** service account (403); replaced by `sa_cw_grok_orchestrator` — keep that one current.
- Secret-injection timing: a running Cursor Cloud VM only sees secrets injected at boot; mid-session saves need a fresh run.
- Latent: prod-build `/accounting/invoice-audit` 500'd on a stale `dist/`; a clean rebuild fixed it — no code change needed, but worth watching if it recurs on deploy.

## Session: JobTread connection investigation

**Goal**: Map everything about the JobTread connection ahead of a 24/7 SOT-alignment agent; clarify JT/Supabase/Convex relationships.
**Deliverables**: Investigation only (no code). Findings reported in-session.
**Decisions**: None taken — findings: JT bridge is a one-time AccuLynx→JT outbound migration (2026-07-28, 34,433 writes, Pave API, `jt_mirror` schema in rnhmvcpsvtqjlffpsayu); NO recurring sync exists; Convex = PE-CC-Executive Decision Cockpit (QBO-only, no JT link); Supabase is SoT. Watermark columns exist unused; loader script never committed (lived in /tmp via Ringer); grant-key rotation still outstanding; 7 milestone-drift rows already visible in v_recon_jobs_by_milestone.
**Open threads**: Design + build the 24/7 JT⇄Supabase alignment agent (proposal delivered in-session); rotate `SUPABASE_MIRROR` JT grant key first; Linear MCP unauthenticated in non-interactive sessions — user rule: Linear is the starting place every time.

## Session: JT Sync Sentinel build

**Goal**: Build the 24/7 JobTread⇄Supabase alignment agent (approved in-session).
**Deliverables**: schema 196 (applied to prod), `integrations/bridges/jobtread/sync-sentinel/` (pave-client, sentinel, executor — B3 driver finally committed, supabase-sql throttle helper), `scripts/jt-sync-sentinel.sh`, systemd units `openbrain-jt-sentinel.{service,timer}` (10:00 America/Los_Angeles, US host), A3 `proposals/2026-08-05-jt-sync-sentinel.md`, `docs/80-jt-sync-sentinel.md`, Linear milestone "JT Sync Sentinel" + PEC-142..146.
**Decisions**: Conflict policy approved — operational fields JT→Supabase, identity/catalog/pricing Supabase→JT via human-gated pending_write, disagreements → conflict_review. Webhooks = Phase 2 (43 event types, zero registered today, no signing secret → secret-in-URL + re-read pattern). Zapier rejected. JT objects have NO updatedAt → fingerprint sweeps, not delta polls. Pave 413 at jobs page size 100×25 cfv → jobs page size 25. Supabase Mgmt API throttles → 1.2s min interval + backoff + 250-row buffered flushes.
**Open threads**: Grant-key rotation (PEC-144, human, after build); timer install on 178.156.203.23 (PEC-143); Phase 2 webhook receiver (PEC-145); Phase 3 outbound compiler (PEC-146); Linear MCP re-auth (interactive /mcp).

## Session: MCP fleet test + credential fixes + wrapup

**Goal**: Test all MCP servers; fix the failing ones (user asked via 1Password); full wrapup + handoff + Linear documentation.
**Deliverables**: 36 MCP servers probed (31 pass). Excalidraw fixed (canvas server started on :3000). Diagnosed the 4 credential failures: site-copy-writer exa/tavily/firecrawl + local dataforseo expand ${VAR} from launch env; all 5 values already in master.env (DATAFORSEO_LOGIN↔USERNAME name mismatch); classifier blocked agent-side secret copy — one-command fix handed to Chris (settings.json env), takes effect next session.
**Decisions**: 1Password not needed (keys in master.env); respect classifier block on secret copying — user runs the wiring command. Slack MCP = HWAOS workspace (NOT pe-command-center); Notion MCP = ZEA Main workspace — noted for future routing.
**Open threads**: Chris runs the settings-env command + restarts session → retest 4 servers; Linear MCP live next session (OAuth completed); JT grant-key rotation (PEC-144); sentinel first scheduled fire 10:00 PT today (will fail on missing host key until PEC-144).

## Session — Invoice Audit v2 pivot (afternoon)

**Goal**: Define and document the new invoice audit process (v2) so accounting can start working
**Deliverables**: docs/81-invoice-audit-v2-process-and-build-plan.md (committed e33369d, pushed); Excalidraw diagram outputs/invoice-audit-v2/ (png + .excalidraw, gitignored)
**Decisions**: Audit-behind-payment inversion (holds removed); binary classification (over agreement price = CM candidate, else valid; No-Price/UOM shown as discrepancy); all 6 dispositions + gate-negotiated + Slack comms deleted; single Tuesday QB file INV-PROCESSED-[date]; Monday human audit (Chris & Lucinda), vendor-specific CM emails via Maya.Chen AgentMail (HTML+CSV+MD, week-scoped tracker, date=generated); 60-day clock from invoice date, Awaiting-Payment cohort only, Paid-pending-verification/Paid-Verified statuses; OCR build approved (Unstructured); Richardson TX primary (Euless-only branches → Euless); SRS Level 4 sheet = Richardson agreements; QXO no agreements = valid; prior $1,941.44 CM request sent+challenged → full re-audit supersedes
**Open threads**: Phase 0 migration 197 (status model) is next; then cleanup Wave A (register-gap 54) + Wave B (benchmark re-audit → new CM request); QXO/SRS invoice PDF source unknown

## Session — Invoice Audit v2 build: Phases 0–2 executed

**Goal**: Start the v2 build — status model, cleanup waves, re-audit + new CM request
**Deliverables**: Migrations 197 (status model) + 198 (invoice_line_reaudit) applied to prod; scripts/invoice-audit-v2/ (wave SQL + generate-credit-memo-packet.mjs); packet in outputs/credit-memo-2026-08-05/ + Dropbox Creditmemo08052026; merged 91cfbfe to main
**Decisions**: PAEXP tag = expiry of relied-on lapsed version at invoice date; 4-tier item matching (exact item / desc / item-family prefix-minus-2 / pg_trgm ≥0.45), unit-match guard, lowest-price-wins; line credits <$0.05 = rounding → valid; Wave A OCR deferred to Phase 4 backfill
**Results**: Wave A 54 invoices → invoice_processed (92 PDFs backfilled; FC* docs have no PDFs at ABC). Wave B 40 invoices re-priced office-inherited/date-effective → 67 discrepancy lines / 30 invoices / $1,784.26 (supersedes challenged $1,941.44); offices: Wichita 22, Richardson 14 (rides evergreen PA-1 PAEXP-2026-04-20), KC 2 (PAEXP-2026-03-31), Denver 2. CM drafts upserted; 3 overlap invoices correctly moved to audit_pending
**Open threads**: Phase 3 dashboard rework next (discrepancy-only view, pills, Process stamp, 60-day sweep); Phase 4 OCR (Unstructured); Phase 5 SRS Level 4 ingest + QXO/SRS invoices; Phase 6 Tuesday automation via Maya.Chen; DB gap noted: some ABC agreement versions cited in July email (K/F2 05/21–05/31) never existed in DB

## Session — Invoice Audit v2 Phase 3 (dashboard rework)

**Goal**: Phase 3 dashboard rework per docs/81
**Deliverables**: Migrations 199 (process-stamp + 60-day-sweep RPCs) + 200 (view perf fix) applied to prod; new API routes process-stamp / pending-verification / verify-paid; invoice-audit.astro + invoice-audit-tree.ts reworked (discrepancy-only view, dispositions+comms+gate removed, Awaiting Payment pill → Paid-Pending-Verification card + verify toggle, Process = pure stamp, Register CSV removed); commit b36a9fd on contrib/cleverwork/invoice-audit-v2 (pushed, NOT merged/deployed)
**Decisions**: v_invoice_audit_invoice was 7.5s (PostgREST timeout) — root causes: laterals memoized on full raw JSONB + missing composite indexes after 08-04 branch_matches growth; fixed with stored generated branch columns + idx (now 141ms). Fixed latent crash (emptyInvoiceAuditData missing dueNow/transferred) + silent summary-failure logging
**Open threads**: BLOCKER — a Supabase PostgREST replica has a corrupted schema cache (intermittent 404 "schema cache" on core tables); NOTIFY reload + authenticator backend kill didn't clear it; project reboot (Mgmt API /restart) needs Chris's approval (classifier-blocked; ~1-2 min outage) or dashboard Restart. Deploy of Phase 3 held until data-plane verified. Then: Phase 4 OCR, Phase 5 SRS/QXO, Phase 6 Tuesday automation

## Session — Phase 3 shipped (deploy 0d4d550)

**Goal**: Reboot Supabase (approved), verify Phase 3 end-to-end, deploy
**Deliverables**: Deployed merge 0d4d550 to cc.proexteriorsus.net (buildCommit verified); prod smoke: /api/invoice-audit/pending-verification 200 + idempotent sweep
**Decisions/Findings**: ROOT CAUSE of all-day "schema cache" errors — the preview launcher's process env injected ANOTHER project's SUPABASE_URL (jnsytduclvvoccmnliqo) which overrides repo .env in runtime-env; prod PostgREST was never broken (reboot was unnecessary but harmless). Fixed locally via .claude/launch.json `env -u SUPABASE_*` (gitignored). Verified live: KPIs (Audit Pending 40), Manage panel swept 72 exported ≥60d invoices to paid_pending_verification, verify-paid toggle round-trip (test write reverted), process-stamp dry-run = 40 (rolled back), discrepancy-only invoice body (no dispositions/mark buttons)
**Open threads**: Phase 4 OCR (Unstructured); Phase 5 SRS/QXO — 31 SRS invoice PDFs at Dropbox "Lucinda - PE Accounting/SRS Invoices" + SRS Level 4 sheet; Phase 6 Tuesday automation. Monday human gate then Process stamps the 40

## Session — Phase 5 vendor ingest + R1 engine cutover

**Goal**: Ingest SRS/QXO invoices (sources supplied) + cut audit engine to office-inherited pricing
**Deliverables**: SRS 30 inv/242 lines (Billtrust CSV, native lines — no OCR needed) + all 30 PDFs linked; QXO 22 statement inv + 3 July PDFs extracted (UX97791 credit memo -3,723.59, UY48909 5,601.71, UZ11902 95.76) w/ 15 lines; scripts/invoice-audit-v2/link-vendor-invoice-pdfs.mjs; migrations 201a-e (consolidated in schemas file); commit d6ae812 pushed
**Decisions**: Engine = 3-arm lateral (exact item / trigram-GIN desc ≥0.45 w/ exact-first rank / legacy ship-to fallback), matview helpers + 15-min pg_cron refresh (plain views cost 30s/scan); PA-1-style agreements have own item numbering → fuzzy tier required in live view
**Results**: No-Price 5,878→3,946 (-33%), flagged 486→789, UOM-review 0→106, unaudited overcharge $6,627→$10,922; line view 1.7s; dashboard live (No-Price KPI 1,054→856, At-Risk $3,792)
**Open threads**: R2 credit-memo flow (Approve button + weekly email view + KPI repurpose) next; R3 price-list page inheritance fallback; R4 reset semantics; R5 delete 12 orphaned routes + v2 agent endpoint + retire S/W surface; SRS Level 4 sheet not yet loaded as agreement (SRS lines currently unpriced); QXO PDFs for 23 statement invoices pending from Chris

## Session — SRS re-audit (3 state agreements) + QXO passthrough + R2 shipped

**Goal**: Re-audit SRS with TX/KS/CO agreements, QXO statement cleanup, build R2 approval flow
**Deliverables**: Deploy 126ea17 live. SRS: 5 branches mapped (DJWIC→Wichita 25 inv, AMDEN/AMSDE/SHCOL→Denver, SSMEL→Richardson); 3 agreements + 136 items in canonical price_agreements (KS quote 0049828559 eff 06/23, CO quote 0049345641 eff 05/28, TX SRS-MELISSA-L4 eff 02/16); run srs_2026-08-05 = 21 disc lines / 12 inv / $3,329.54 (top: Wichita IKO Cambridge $1,748; Denver TS20 141-vs-74.50). QXO: 23 statement rows deleted (statements ≠ invoices per Chris), 3 PDF invoices → paid_pending_verification. R2: approve/pending/weekly-csv APIs, weekly page (vendor emails from approved only + drafts w/ Approve + reconciliation/tracker downloads), tree CM pills + Approve (zero-credit read-only), KPI card repurposed, CM detail reads invoice_line_reaudit
**Verified**: approve round-trip w/ attribution; weekly page + CSVs; tree 38 pills; CM detail SRS+ABC 200; prod smoke 200
**Open threads**: R3 price-list page inheritance fallback; R4 reset semantics; R5 delete 12 orphaned routes + v2 agent endpoint + retire S/W surface; ~4 legacy $0 CM drafts could be cancelled (human call); Monday gate: Chris/Lucinda approve, then generate/send packet (Phase 6 automates Tuesday)

## Session — CM line-review gate + pills live-data pass (deploy 20651f5)

**Goal**: Per-line review checkboxes gating CM Approve; verify all pills against live data
**Deliverables**: Migration 202 (reviewed_by/at on invoice_line_reaudit); /api/credit-memos/review-line; pending returns claim/reviewed counts; approve 409-gated (all lines reviewed + must have claim lines); weekly page draft blocks w/ checkboxes + greyed Approve; tree pill "CM draft · $x · r/N reviewed" + greyed Approve; deploy 20651f5 verified (409 enforcement, check-all→activate→approve round-trip, restored)
**Live-data pass**: 8 legacy no-claim drafts cancelled ($454.11) → CM KPI $5,113.80 ties to reaudit claims exactly; Open 270 = 172 AR-open + 98 AR-null (live); Due Now 91 = 104 − 13 S/W-transferred (live); Audit Pending 52 / Pend-Verif 75 exact; At-Risk/No-Price/worst% engine-live post-201; "To Audit" 451 = legacy ledger metric (labeled as such; redefine or retire in R5)
**Open threads**: R3 price-list inheritance page, R4 reset v2 semantics, R5 orphan-route deletion + v2 agent endpoint + retire S/W surface + decide "To Audit" metric fate; Phase 6 Tuesday automation

## Session — R3 shipped (deploy 2f17394)

**Goal**: Price List surface — office-inherited fallback + honest gate
**Deliverables**: branch-price-list.ts office-inherited fallback (mv_office_agreement_versions, newest-per-chain, evergreen; refDate = invoice date or today); inherited banner on branch.astro; invoice-audit.ts hasPriceList computed from office agreement coverage (was hardcoded true)
**Verified**: branch 113 Wichita (no direct list) renders inherited 2036874-16 v06-15 (157 items) w/ banner; dashboard 1,047 enabled vs 26 honestly-greyed 📋 buttons (Unassigned + non-covered 'area' territories)
**Open threads**: R4 reset v2 semantics; R5 orphan routes + agent endpoint + S/W retirement + To-Audit metric decision; Phase 6 Tuesday automation; Phase 4 OCR

## Session — R4 shipped (deploy e3735ed)

**Goal**: Reset ("Go back") aligned with v2 status model
**Deliverables**: Migration 203 invoice_audit_reset v2 — guards for credit memos + paid_pending_verification/paid_verified; cancels draft AND approved (un-sent) CM requests; clears claim-line review checks; syncs invoice_pipeline_status (audit_pending iff current claims else processed); reset.ts mappings + response fields; tree v2 copy + CM state refresh
**Verified**: pv-guard 409, CM-guard 409, happy path pipeline sync + action log
**R5 scope note (Chris)**: Euless, TX office is CLOSED — archive the office + its drive-time rings (Richardson = sole TX office); plus orphan routes, agent endpoint, S/W retirement, To-Audit metric decision

## Session — R5 shipped (cleanup wave)

**Goal**: R5 cleanup — Euless archive, orphan routes, dead code, v2 agent endpoint, S/W retirement, To-Audit replacement
**Deliverables**: Migration 204 (Euless archived is_active=false; 2 Wichita Falls branches → Richardson; 42 suggested_office_id re-suggested to best active candidate — 11 Richardson / 31 none; is_suggested synced; matviews refreshed — office mix now Wichita 530 / Richardson 308 / Denver 213 / KC 21, zero Euless); deleted 12 orphaned routes + 4 orphaned libs + 4 test files (kept invoice-payment.ts for Phase 6 QB CSV contract); dead code removed (compactInvoiceAuditForInitialPayload, triggerDownload, pendingManageBatch); NEW POST /api/invoice-audit/classify (binary valid/discrepancy, batch, source=pipeline_v2, paexp_tag); S/W surface retired (nav + ?audit=service_warranty variant + loadServiceWarrantyAuditSummary; queue/transfer/exclusion logic intact); "To Audit" KPI → "Claims To Review" (unreviewed claim lines, same math as /api/credit-memos/pending — showed 88/88); vendor-territories.ts skips archived-office candidate rings
**Verified**: build green, 286/286 tests, dev browser — KPI card live, ?audit=service_warranty renders normal audit, classify 400-validates, mark 404s, /api/vendor-territories carries zero Euless strings
**Open threads**: Phase 4 OCR verifier (Unstructured); Phase 6 Tuesday automation (INV-PROCESSED weekly file producer, AgentMail via Maya.Chen, invoice_pipeline_status producers); QXO remaining invoice PDFs from Chris

## Session — vendor-regions map v2 cutover (docs/82 §6)

**Goal**: Fix the broken territory map — all-PROBLEM rings, dead popup KPIs, broken GoTo links
**Root cause**: map ran pre-v2 model (ceo_verified hard gate + expiry-disqualifies + ship-to keys → ~zero agreements passed); office-inheritance views were ABC-only (SRS invisible); /accounting/vendor-regions itself was a rotted static-SVG page (NaN counts)
**Decisions (Chris)**: Healthy = in-date in-force (office-inherited ok); Needs Attention = expired-evergreen PAEXP or unrouted; Problem = no agreement (QXO = red); ceo_verified display-only; full union via v_office_vendor_agreements (engine's generic arm deferred to Phase 5 ingestion)
**Deliverables**: Migration 205 (all-vendor office agreements view; duplicate SRS rows archived); vendor-territories.ts coverage cutover + agreement_expired priority; map lens/legend semantics; ?office= focus; ABC-only Price Agreement Audit link; vendor-regions page rebuilt on the interactive map; branch-price-list all-vendor inherited items; tree ?branch= honest search-scope fallback
**Verified**: 41 healthy / 27 expired / 29 unrouted / 75 problem in-scope; Richardson 25/26 healthy; SSMEL→SRS-MELISSA-L4 (97), DJWIC→0049828559 (17); SBP-WAXAHACHIE yellow pending office assignment

## Session — docs/83 plan + P0 line review shipped

**Goal**: Review 4 price-agreement surfaces, plan consolidation, fix the missing line check-off + agent-pass bug
**Decisions (Chris)**: full consolidation into one Price Agreement Management surface (4 routes retire); gap rule = ≥2 purchases lifetime per vendor+office without in-force price; monthly auto-draft on the 1st via Maya.Chen (gaps + negotiated w/ human targets); line review = discrepancy lines only, agents blocked from passing variance/UOM lines, legacy agent passes re-opened
**Deliverables**: docs/83 (assessment + 4 decisions + P0-P3 phases); P0 shipped — tree per-line checkboxes (claim lines sync weekly-page review stamps via pending.claims/lineId; No-Price/UOM/variance-no-CM lines post classify 'valid' one-way w/ kind-specific notes, row flips ✓ You in place); classify agent guard (409 agent_variance_review_required); migration 206 re-opened 288 System/Alex passes masking ~$10.4k variance (Lucinda/human passes kept)
**Verified**: claim checkbox round-trip w/ live pill (1/1 reviewed), ack flow on MGSS448 (flipped + restored via append), build green 286/286
**Ops**: Coolify API token 401 (revoked — Chris to mint new one in dashboard); webhook deploys still work (dd1f95c landed via empty-commit re-fire)
**Open**: P1 history view + worksheets; P2 monthly packet producer + cron; P3 route retirement

## Session — map wave 2: TX overlap resolution + all-vendor office cards

**Trigger**: Chris — DFW SRS branches yellow w/ zero pricing despite in-date Melissa L4; Denver office card ABC-only; Price List Requests panel suspect
**Root causes**: (1) all 29 overlap_pending-unassigned branches (15 SRS + 14 QXO, all TX) were Euless-era two-ring artifacts — sole-ring Richardson resolution now deterministic (migration 207); (2) office card activePriceAgreements was region-keyed price_agreements (ABC-only, expiry-filtered) → cut to mig-205 all-vendor chains; (3) API compaction hard-emptied activePriceAgreements; (4) Price List Requests panel = demo scaffolding (PLC_TODAY hardcoded 2026-06-17, synthetic request dates) → removed from vendor-regions (real replacement = docs/83 P2)
**Verified**: SBP-WAXAHACHIE green/Richardson/97 lines/gate open; 17 DFW SRS green; 15 QXO TX honestly red; office cards list ABC+SRS chains w/ expiries for all four offices
**In flight**: 3 Explore agents auditing every GUI surface (accounting pages / other pages / API+libs) → docs/84 register

## Session — CM flow corrected + full GUI audit (docs/84)

**Trigger**: Chris — Approve button dead; variance checkbox wrongly passed lines instead of adding to CM; requested full-app surface audit
**Fixes**: Approve dead-click (inline stopPropagation ate the delegated handler — removed); NEW /api/credit-memos/add-line (variance checkbox = joins claim set, refreshes stale wave rows to live-engine numbers, retotals draft); KPI card updates live (N approved · M draft); 3 wrongly-passed lines re-opened; dueNow crash-path + isAgreementOnFile ceo-gate one-liners
**Verified**: 02TKTXTRB add → CM $201.89/7 → $799.97/8 · KPI $5,114→$5,712 live · approve→"1 approved ($800)"→un-approve round-trip; review stamp nulled so Chris re-checks
**docs/84**: 3-agent audit synthesized — LIVE core (invoice audit, CM flow, exec pipeline, map) + 3 rot rings: pre-v2 model (order/estimate audit, 12 slug dashboards via abc-price-gaps, gaps page, builder internals, scoped branch price list, promote writes NEW legacy rows); sample-as-live (5 audit/* pages, PLC_TODAY frozen 6-17, catalog fallback, dashboard.astro mock); dead wiring (map counts strip, 501 agent-auth stubs, Open-Items always 0, call-priority never fetched, 7 no-query slugs). Waves W1-W4 sequenced with docs/83.

## Session — vendor silo hardening (mig 208)

**Trigger**: Chris video — vendor price-list bleed (QXO branches showing ABC pricing)
**Root cause**: branch-number-only keys collide across vendors (QXO 113/249/304/412 vs ABC 0113/...) in 3 places: office-view abc arms (unscoped join — latent), map apiStats/invoice-history (QXO branches got "ABC API price list available"), and /accounting/price-list/branch?branch= (QXO 113 opened ABC Wichita's list)
**Fixes**: mig 208 (abc arms require vendors.slug='abc-supply'; matview refreshed); map attaches ABC evidence only to ABC branches; price-list page takes &vendor= (non-ABC skips all ABC paths → own office-inherited list only; vendor-scoped branch resolution); all links pass vendor
**Verified**: qxoBleedCount 3→0; qxo113 empty; abc113 2036874-16 intact; SSMEL Melissa L4 intact; 39 ABC + 17 SRS greens unchanged
