# 2026-09-16

## Session 14:2x — June recycled invoices closed out (mig 293)

**Goal**: Chris — the seven ABC June 2026 invoices recycled by the triage-race reopen are complete and processed; mark them processed with a note and never bring them back into the audit workflow.
**Deliverables**: mig 293 (applied): `invoice_audit_closeout` register (RLS, service_role), 7 rows; 8 pending lines decided valid as Chris with the reason; 6 `invoice_payment_processed` 'paid' rows (source closeout) — 2011284893-001 keeps its 7/27 paid_pending_verification row; pipeline `invoice_processed`; `invoice_audit_reset()` refuses closed-out invoices (`invoice_closed_out`, probed live); action-log trail. Verified on the live invoice route: 0 pending, paymentStatus paid, toBePaid false. docs/109 F44.
**Decisions**: a closeout is a human decision recorded in its own register — reopen scripts check it first (memory: matview-freshness-before-agent-stamps). Chris's own 9/15 discrepancy + draft CM on 2011009179-001 left untouched for him to cancel/approve.
**Open threads**: cancel/approve the $20 draft CM on 2011009179-001; Lucinda's Pay-It verification queue (incl. 2011284893-001); no app change made — the register-row `disposition` label still reads "In review" for closed-out invoices while the payment column reads paid.
**Addendum 15:xx**: 293b — $20 draft CM on 2011009179-001 cancelled (Chris), its disputed line closed. App: `closedOut`/`closeoutReason` on all three invoice loaders, `deriveDisposition` → "Processed — closed", tree pill + Go back hidden; unit tests 354 green.

## Session 14:3x — PR #9 daily check-in: an open question answered itself

**Goal**: daily check on PR #9 (PEC-221); verify state, reviewers, and the stable figures.
**Deliverables**: docs/107 + PR #9 body record that **branch 305 (Sherman TX) does land in Richardson's ring** — the question docs/107 posed from an address and a distance, with no coordinates. It now carries a `pricing_territory_office_id` and reads `covered`; its 3 invoices / $595.16 left `v_unresolved_branch_spend` (26 → 23 invoices, $27,566.56 → $26,971.40), a delta matching the branch exactly.
**Decisions**: a moving figure is not automatically a defect. Re-measured rather than quoted, re-checked the QUERY before the record, then traced to the branch — and the cause was two pieces of work meeting: **mig 294** recovered Sherman's address from the invoice payload (making the row geocodable at all), and a parallel session's prod **292b** carried the isochrone office onto the numeric stubs holding an alias (Granbury, Enid, Marietta, Sherman). Neither half would have done it alone.
**Verified unchanged**: 4 unreachable agreements / 247 items / AMSDE 128; Denver × SRS `priced_items` = 0; anon privileges on the four coverage views 0; `no_branch_resolved` 0 rows; 95 slug-keyed ABC branches still unreachable by bare number. Main unmoved in 22 h — no fifteenth renumber collision. PR `mergeable_state: clean`; Greptile ✅ and Cursor Security ✅ on head `b6f64c1`.
**Open threads**: PR #9 merge-gated on the docs/108 PII decision (policy scope, replacement token, git history, CI check) — CodeRabbit agrees in writing that no code-only change can clear it. Three other human items: AMSDE == SBP-SOUTHDENVER (128 items); branches 21/39/465/684 geocoded-but-pending; anon-readable `v_office_vendor_branch`/`_inheritance`.

## Session 14:4x — Chris ruled on the named-records question; docs/108 closed

**Goal**: settle the last blocker on PR #9.
**Decisions (Chris)**: (1) **keep all data** — not valid PII; business-card-grade public information and *critical* to the brain, since the whole system exists to build the property layer cake; (2) replacement token N/A; (3) **keep all git history** — no rewrite; (4) **no prevention needed** — no pre-commit hook, no CI check.
**Deliverables**: `docs/108` rewritten from an open worklist into the **standing answer** (status CLOSED, ruling at the top, "if a reviewer raises this again" section). Handoff human-items list 4 → 3. PR #9 body updated. Nothing redacted, nothing restored — the two incidental removals made while the question was open (`8b142f6`, `526e346`) stay out because neither was remediation.
**Reading**: hard rule 2 is unchanged and NOT relaxed. The ruling is about *scope* — this content is not PII for this brain — so the rule is simply not engaged. That keeps the rule intact for secrets and service-role keys.
**Noted once, not an objection**: the `docs/103` Cause B table is residential customers' names against outstanding balances, a customer financial record rather than contact information someone hands out. Chris ruled with that table described to him; the ruling stands and covers it.
**Expect recurrence**: three reviewers raised this through three different routes (inline thread, outside-diff finding, Merge Risk → High). Any generic PII heuristic will raise it again. The answer is docs/108 — do not open a redaction pass.
**Open threads**: none from this decision — it is settled and `docs/108` is the standing answer. PR #9's remaining blockers are the three unrelated human items (AMSDE identity, the four geocoded-but-pending branches, the anon-readable inheritance views).

## Session 15:0x — merge conflict + FIFTEENTH renumber (293-297 → 294-298)

**Goal**: a CodeRabbit finishing-touch flagged a merge conflict on the branch; verify and clear it.
**What was real**: both halves. Main had moved 2 commits (`43cd8d5` mig 293 June closeout, `65eb2d0` memory cap); GitHub read `mergeable: false / dirty`; and main had taken **293**, colliding with this set's first file.
**Deliverables**: add/add conflict in this very daily log resolved by keeping **all three** session blocks (main's 14:2x first, mine renamed 14:3x/14:4x — main owns the file). Fifteenth renumber 293-297 → **294-298**, 33 references moved by line with assertions; both prod view COMMENTs re-issued and read back in full (358/427 chars, now citing mig 294 and mig 297). docs/107 gains the fifteenth addendum. 369/369, build green.
**Decisions**: the sweep ran the way the fourteenth's failures taught — **two unfiltered passes**, one on numbers and one on spelled-out counts, every hit printed and classified by hand. It held: 33 moved while `$2,296.05`, `$1,294.69`, docs/108's `289–295` (a line range into docs/103, not migrations), three history tables, and main's own mig-293 references all correctly stayed put.
**The finding**: three collisions in 24 h (289-291, then 292, then 293). Strongest evidence yet that the branch is mis-scoped rather than unlucky — land schema in its own same-day PR.
**Open threads**: none from the merge or the renumber. PR #9's remaining blockers are the three human items (AMSDE identity 128 items; branches 21/39/465/684; anon-readable `v_office_vendor_branch`/`_inheritance`), plus the standing recommendation to split the five migrations into their own same-day PR.

## Session 15:1x — five findings fixed, then the PR description caught up with reality

**Goal**: clear the review round that landed on the post-renumber head, then make the PR description true again.
**Deliverables**: `d44d871` fixed five findings — two that mattered. (1) *"All five migrations touch no data"* was **false**: 295 `UPDATE`s `vendor_branches`. The handoff now separates three kinds — 294/296/297 view-only, 298 access control, **295 a data backfill**. (2) A **non-deterministic twin lookup** in 297: two independent scalar subqueries ordered on `branch_name` alone, so `likely_canonical_branch_id` and `..._number` could legally describe different rows; both now order on `(branch_name, id)`. The view is applied to prod as `249`, so the file alone was not the change — re-issued against prod and read back (tie-break live; 4 agreements / 247 items / AMSDE 128 unchanged; `COMMENT` intact). Then `0670de9` merged main; 371/371, build green, all 53 threads resolved.
**PR #9 description rebuilt** for the fifteenth renumber: every `293-297` reference moved to `294-298`, the mapping table, the prod `COMMENT` citations (mig 294 / mig 297), the sweep range (281–298), tests 369 → **371**, the daily-verification window to 09-16, unresolved spend to **$26,971.40**, and a new table for the five-finding round. Footer re-dated.
**Two more stale self-references found — by the sweep, not by a reviewer.** The *corrections* list and the *awaiting a human* list each still named the backfill migration as `294`, one number retired. They had survived because nothing in a correctness check reads a prose cross-reference: the filename-equals-header check passes either way. Printing and classifying **every** hit is what caught them; the filtered sweep that failed on the fourteenth would not have.
**Figures re-verified against prod**: 4 unreachable agreements / 247 items / AMSDE 128; anon privileges on the four coverage views 0; unresolved branch spend $26,971.40 / 23 invoices. ABC slug-keyed branches corrected and measured properly: **684** total, **589** now carrying a numeric alias from main's mig 292 (`status = 'resolved'`, tiers postal / city / postal5+city), **95** still without one — those 95 are the un-closed general form of the branch-326 defect.
**Decisions**: a sentence being *rewritten* earns the same whole-sentence audit as one being corrected — finding 1 was mine, introduced in `2fab658`, the commit whose whole purpose was to make that block accurate about migrations. And a count quoted from memory (685) is the same defect as a dollar figure quoted from memory; the query is the record.
**Open threads**: unchanged — the three human items (AMSDE identity, 128 items; branches 21/39/465/684 geocoded-but-pending; anon-readable `v_office_vendor_branch`/`_inheritance`) plus the standing recommendation to land migrations in their own same-day PR. Daily check-in re-armed for 2026-09-17 14:20 UTC with the current state.
**Addendum 15:4x — pagination safety, verified rather than asserted.** CodeRabbit's standing Merge Risk rationale had three clauses; two were closed by `d44d871` and the third ("pagination safety remains unverified") was still open, so it was measured instead of argued. `selectAll` taking a required `orderBy` is not the same claim as *the key is unique* — checked all five callers against prod: `v_office_vendor_branch` (171 rows, key `office_id,vendor_id,vendor_branch_id,agreement_id`), `v_office_vendor_inheritance` (15), `v_office_vendor_gap_exposure` (15), `v_office_vendor_spend` (10), `v_unresolved_branch_spend` (1) — **0 duplicate keys on every one**. `GROUP BY` treats NULLs as equal, so the branch view's nullable `agreement_id` is counted rather than assumed away. `PAGE_SIZE` is 1,000 and the largest view is 171 rows, so paging does not currently engage at all: the total order is a guarantee for when it does. Recorded in the PR body with the table.

## Session 16:1x — Merge Risk recomputed to 🔵 Low; two findings, one real

**Goal**: CodeRabbit finally recomputed against the current head (`42e850d`, marker `coveredCommitId` matching for the first time since `2fab658`) and posted two actionable findings. Verify both.
**Merge Risk 🟡 Moderate → 🔵 Low.** All three clauses of the old rationale are closed: deployment guidance and coverage records by `d44d871`, pagination safety by measurement.
**Finding 1 — real, and wider than reported.** `ACCEPTED_STATUSES` is `{no_book, not_pursued}`; **three** descriptions said "a `no_book` ruling" — the operator-facing paragraph in `builder.astro`, the `gapExposure` docstring, and the `rulingByOfficeVendor` comment. The review named one. Fixed all three in `74ac611`. It was invisible because it was **true of the data and false of the rule**: `not_pursued` is 0 pairs in prod, so every excluded pair today really is `no_book` — the sentence would have failed the first time anyone recorded a `not_pursued` ruling, excluding a pair for a reason its own explanation did not mention. `ACCEPTED_STATUSES` now carries a note naming the three prose copies.
**Finding 2 — declined, re-measured rather than quoted.** "Merge location fields across payloads" (mig 295) is the same finding declined in August. Instead of restating the old decline, re-ran it: **1,120 payloads — 707 full address, 413 name-only, 0 partial** (August: 680 / 413 / 0 of 1,093). 27 payloads have landed since and the partial count is still zero, so the premise — a long `addressLine1` beside a payload holding the `city`/`state` — does not occur. Also recorded on the thread: 295 is a one-shot backfill **already applied to prod**, so recovering more data would need a new migration, not an edit.
**Decisions**: the eighth instance of the standing mistake was avoided by not committing it — a decline is a measurement too, and quoting one from memory is the same defect as quoting a dollar figure from memory. And the guard comment in `builder.astro` was checked before editing: the finding landed on the *exclusion* sentence, not the guarded counts-and-money sentence, so the "fifth finding → delete it" rule did not fire.
**Deliverables**: `74ac611` (371/371, build green); replies on both threads with the evidence; 55 threads, **0 unresolved**; PR body carries the new finding and the re-measured decline.
**Open threads**: unchanged — the three human items (AMSDE identity, 128 items; branches 21/39/465/684; anon-readable `v_office_vendor_branch`/`_inheritance`).
