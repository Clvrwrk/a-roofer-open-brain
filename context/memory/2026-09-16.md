# 2026-09-16

## Session 14:2x — June recycled invoices closed out (mig 293)

**Goal**: Chris — the seven ABC June 2026 invoices recycled by the triage-race reopen are complete and processed; mark them processed with a note and never bring them back into the audit workflow.
**Deliverables**: mig 293 (applied): `invoice_audit_closeout` register (RLS, service_role), 7 rows; 8 pending lines decided valid as Chris with the reason; 6 `invoice_payment_processed` 'paid' rows (source closeout) — 2011284893-001 keeps its 7/27 paid_pending_verification row; pipeline `invoice_processed`; `invoice_audit_reset()` refuses closed-out invoices (`invoice_closed_out`, probed live); action-log trail. Verified on the live invoice route: 0 pending, paymentStatus paid, toBePaid false. docs/109 F44.
**Decisions**: a closeout is a human decision recorded in its own register — reopen scripts check it first (memory: matview-freshness-before-agent-stamps). Chris's own 9/15 discrepancy + draft CM on 2011009179-001 left untouched for him to cancel/approve.
**Open threads**: cancel/approve the $20 draft CM on 2011009179-001; Lucinda's Pay-It verification queue (incl. 2011284893-001); no app change made — the register-row `disposition` label still reads "In review" for closed-out invoices while the payment column reads paid.

