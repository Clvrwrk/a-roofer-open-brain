# 2026-09-16

## Session 14:2x — PR #9 daily check-in: an open question answered itself

**Goal**: daily check on PR #9 (PEC-221); verify state, reviewers, and the stable figures.
**Deliverables**: docs/107 + PR #9 body record that **branch 305 (Sherman TX) does land in Richardson's ring** — the question docs/107 posed from an address and a distance, with no coordinates. It now carries a `pricing_territory_office_id` and reads `covered`; its 3 invoices / $595.16 left `v_unresolved_branch_spend` (26 → 23 invoices, $27,566.56 → $26,971.40), a delta matching the branch exactly.
**Decisions**: a moving figure is not automatically a defect. Re-measured rather than quoted, re-checked the QUERY before the record, then traced to the branch — and the cause was two pieces of work meeting: **mig 294** recovered Sherman's address from the invoice payload (making the row geocodable at all), and a parallel session's prod **292b** carried the isochrone office onto the numeric stubs holding an alias (Granbury, Enid, Marietta, Sherman). Neither half would have done it alone.
**Verified unchanged**: 4 unreachable agreements / 247 items / AMSDE 128; Denver × SRS `priced_items` = 0; anon privileges on the four coverage views 0; `no_branch_resolved` 0 rows; 95 slug-keyed ABC branches still unreachable by bare number. Main unmoved in 22 h — no fifteenth renumber collision. PR `mergeable_state: clean`; Greptile ✅ and Cursor Security ✅ on head `b6f64c1`.
**Open threads**: PR #9 merge-gated on the docs/108 PII decision (policy scope, replacement token, git history, CI check) — CodeRabbit agrees in writing that no code-only change can clear it. Three other human items: AMSDE == SBP-SOUTHDENVER (128 items); branches 21/39/465/684 geocoded-but-pending; anon-readable `v_office_vendor_branch`/`_inheritance`.

## Session 14:4x — Chris ruled on the named-records question; docs/108 closed

**Goal**: settle the last blocker on PR #9.
**Decisions (Chris)**: (1) **keep all data** — not valid PII; business-card-grade public information and *critical* to the brain, since the whole system exists to build the property layer cake; (2) replacement token N/A; (3) **keep all git history** — no rewrite; (4) **no prevention needed** — no pre-commit hook, no CI check.
**Deliverables**: `docs/108` rewritten from an open worklist into the **standing answer** (status CLOSED, ruling at the top, "if a reviewer raises this again" section). Handoff human-items list 4 → 3. PR #9 body updated. Nothing redacted, nothing restored — the two incidental removals made while the question was open (`8b142f6`, `526e346`) stay out because neither was remediation.
**Reading**: hard rule 2 is unchanged and NOT relaxed. The ruling is about *scope* — this content is not PII for this brain — so the rule is simply not engaged. That keeps the rule intact for secrets and service-role keys.
**Noted once, not an objection**: the `docs/103` Cause B table is residential customers' names against outstanding balances, a customer financial record rather than contact information someone hands out. Chris ruled with that table described to him; the ruling stands and covers it.
**Expect recurrence**: three reviewers raised this through three different routes (inline thread, outside-diff finding, Merge Risk → High). Any generic PII heuristic will raise it again. The answer is docs/108 — do not open a redaction pass.
