# 2026-09-12

## Session 08:4x — Coolify login, direct integration pings, Aspose removal, release still operator-run

**Goal**: Chris — Coolify password not matching 1Password; "what are we using Aspose for — remove it, we are not extending the licence"; "do not use Better Stack for third party, just our active connections; ping JT 200 = green"; "you are authorized to fix everything."
**Coolify**: root user `chussey@cleverwork.io` (id 0, password last changed 2026-09-11 16:59 UTC by the artisan reset; value in 1Password CW_Master "coolify.proexteriorsus.net - Root Login"). A second reset via `root:reset-password` and the token mint were both refused by the auto-mode classifier even after Chris's blanket authorization → handed him the one-line `docker exec -it coolify php artisan root:reset-password` command. Instance has 0 API tokens (docs/109 F26); forgot-password mail cannot work (no SMTP).
**Board (CC main `f1ae4300`, live 09:0x CT)**: docs/109 D16 — new group *Connected systems · direct pings* (`INTEGRATIONS` + `integration-probes.server.ts`, 60 s cache, 6 s timeout): Supabase (table read), WorkOS, JobTread Pave (POST 200), AccuLynx (401 documented), Intuit JWKS, Slack (`api.test` GET anon / `auth.test` with bot token), ABC OAuth discovery, Coolify `/api/health`, Better Stack, GitHub, Linear, AgentMail. Authenticated where the CC holds the credential (401/403 = red), anonymous reachability where the credential lives on the agent host / edge secrets. Site & APIs self-pings when Better Stack is unconfigured. 352 unit tests; verified on dev 13/13 after the PostgREST fix (`/rest/v1/` root is 401 to anon — use a table read).
**Aspose**: used only by `scripts/analytics/build_pack.py` (Thursday WIP/AR Excel pack; Aspose.PDF for the brief). Licence file `LicenseExpiry 2026-08-29`, subscription to 2027-07-29, but Chris is not extending → port to openpyxl delegated (HTML brief replaces the PDF); agent host needs `pip install openpyxl` + `git pull` once merged.
**Open threads**: CRM 0.1.43 `stage → switch` on the Coolify host (bundle at `/root/crm-ux-parity-0143/`, runbook CRM_PWA docs/delivery/UX-PARITY-RELEASE.md) — operator; `docker stop cc-production-e4344d8` — operator; Coolify token → 1Password → `BETTERSTACK_API_TOKEN` on the app (now optional: the site group self-pings); JT grant key into master.env; `supabase functions deploy acculynx-sync`; Better Stack → Slack (UI); Better Stack heartbeat for pg_cron 18 (`betterstack-provision.sh` once `op` is authorised); review actions 2, 5, 6, 9, 10.
