# 2026-06-05 Memory

## ABC Supply Production API

- Production credentials are available through repo-root `.env` with `ABC_SUPPLY_ENV=Production`.
  Do not print secrets.
- Added `integrations/bridges/abc-supply/production-sync.mjs`, a native Node read-oriented sync/audit
  harness for ABC production and sandbox. It writes redacted JSON summaries under ignored
  `.production-runs/`.
- Created `integrations/bridges/abc-supply/ABC_Endpoint.md` with the production endpoint access
  matrix, Supabase coverage, and table mirror plan.
- Live Supabase changes were applied to project `rnhmvcpsvtqjlffpsayu`: created
  `abc_product_catalog`, added API mirror columns to `abc_regions` and `abc_vendor_branches`, and
  preserved existing tables because they have foreign-key dependents.
- Production sync results: 331,251 product records fetched from `GET /api/product/v1/items`, 331,249
  distinct `item_number` rows stored in `abc_product_catalog`; 696 production ABC branches upserted;
  32 production account rows upserted (28 Ship-To, 2 Bill-To, 2 Sold-To).
- Read endpoint audit passed product catalog/detail/search/availability/hierarchy/image,
  account detail/contact on sellable Ship-To, branch search/detail, pricing with line status `OK`,
  order history/detail/templates, invoice history/detail. Notification webhook list returned 404
  because no production webhook is registered.
- Write endpoints remain disabled until explicit human approval: order placement, favorite writes,
  webhook register/update/delete. Invoice PDF read remains disabled pending storage/PII approval.
- Supabase advisory to surface: RLS disabled on `public.spatial_ref_sys` and the two ABC backup
  tables `_backup_abc_regions_20260605`, `_backup_abc_vendor_branches_20260605`.

## ABC Supply Full Mirror Backfill

- Added `integrations/bridges/abc-supply/mirror-backfill.mjs`, a read-only production/sandbox mirror
  harness for Ship-To branch access, contacts, pricing observations, orders, invoices, and matches.
  It keeps order writes, favorite writes, webhook writes, and invoice PDF reads disabled unless
  explicitly approved.
- Supabase project `rnhmvcpsvtqjlffpsayu` now has RLS-enabled mirror tables for endpoint logs,
  Ship-To branch access/contacts, pricing observations/lines, UOM evidence, orders, invoices,
  product hierarchy/availability/images, account product signals, webhook configs/events, and
  order/invoice webhook events. Verification showed all 28 new mirror tables have RLS enabled.
- Orders/invoices output: `.mirror-runs/2026-06-05T20-46-13-468Z.json`. The clean rerun covered
  yearly windows from 2000-01-01 through 2026-06-05, fetched/upserted 3,146 order history rows,
  confirmed all 3,146 order details were already present, fetched/upserted 520 invoice history rows,
  confirmed all 520 invoice details were already present, and recorded 0 rate-limit responses.
- Pricing/branch/contact output: `.mirror-runs/2026-06-05T20-49-09-601Z.json`. The full run processed
  887 Ship-To/branch access rows, 591 item-numbered agreement SKUs, and 10,644 pricing requests.
  Results: 9,048 pricing requests passed, 444,849 pricing lines from the full run were upserted,
  160,410 lines were `OK`, 284,439 lines were non-OK, 56 generated API agreement shells were created,
  887 agreement matches were upserted, and 0 rate-limit responses occurred.
- Pricing failures were concentrated in six Ship-To accounts that ABC reported as invalid or not
  registered in the pricing system. Contact lookup also found six non-sellable Ship-To accounts.
- Verified aggregate mirror counts after completion: `abc_ship_to_branch_access` 887,
  `abc_price_observations` 9,049 including the earlier tiny smoke, `abc_price_observation_lines`
  444,851 including smoke, `abc_orders` 3,146, `abc_order_lines` 18,391, `abc_order_shipments` 3,111,
  `abc_invoices` 520, `abc_invoice_lines` 2,462, `abc_invoice_matches` 931, and
  `abc_order_invoice_matches` 520.
