# 2026-07-01

> This session began 2026-06-30 and ran past midnight — the full session blocks are in
> [`2026-06-30.md`](2026-06-30.md). This file is the pickup pointer for the next session.

## Where things stand (Phase 3 — AccuLynx Commercial Cron Hardening)

**Phase 3 is effectively COMPLETE through Wave 5.** All migrations 172–181 applied to
prod (`rnhmvcpsvtqjlffpsayu`); `acculynx-sync` edge fn deployed with all fixes.

- **Wave 4 (03-05)** — security posture (`docs/knowledge-base/acculynx/security/posture.md`,
  D-13) + recovery runbook (`.../ingestion/runbook.md`, D-15); `/gsd-secure-phase 3` = SECURED 12/12.
- **Wave 5 (03-06)** — D-08 gate OPEN; **all 8 AccuLynx accounts enabled** (canary-then-batch,
  zero cross-account bleed) and **fully backfilling** jobs/contacts/estimates.
- **Three live-diagnosed bugs fixed today** (all deployed + verified): (1) reconciliation
  instrument — jobs `last_api_total` grand-total probe (mig 181); (2) contacts/estimates
  pagination — `pageStartIndex` is a PAGE NUMBER (advance by 1); (3) jobs sweep —
  `/jobs` ignores `recordStartIndex`, paginates by `pageStartIndex` as a RECORD offset
  (advance by items.length), 416 = clean end. Docs corrected (read-capability, auth-and-limits).
- Fleet jobs now: colorado 1841/1843, texas 2299/2300, multi_family 352/352, KC/wichita/florida ✓.

## Next task (exact)
Phase 3 is done; no blocking work. **Optional follow-ups** (not blockers):
1. Reconcile `v_acculynx_reconciliation` against the **paginable** count, not the raw API
   `count`, so count-overcount accounts (georgia count=470 vs ~435 unique paginable) don't
   show a false residual delta (currently georgia jobs 7.4%, insurance 7.4% — data is complete).
2. **03-03 alert delivery proof** — still needs 2 human steps: invite `openbrain` bot to the
   private `#ob-ops-conductor`; `select vault.create_secret('<xoxb>','acculynx_alert_slack_bot_token')`
   in the Supabase SQL editor. Then seed→fire→observe per the runbook.

## Session (Phase 3 formal close-out)

**Goal**: Formally close Phase 3 in the GSD workflow (engineering was done + live-healthy but STATE/ROADMAP lagged).
**Deliverables**:
- Live re-verification of all 4 SC against prod (`rnhmvcpsvtqjlffpsayu`): hourly cron active for all 8 accounts; `v_acculynx_cron_outcomes` last 24h = 23 success / 1 in-flight pending (no perpetual pending); `check_acculynx_alerts()` = 0 firing; runbook + posture docs present; security audit CLOSED T-03-01…T-03-11.
- Wrote `.planning/phases/03-commercial-cron-hardening/03-VERIFICATION.md` (verdict COMPLETE).
- ROADMAP: ticked Phase 3 + plans 03-03/05/06 + added COMPLETE status line.
- STATE.md: Phase 03 COMPLETE, 3/7 phases, 13/13 plans, 43%, focus → Phase 04.
**Decisions**: Two residuals accepted (not Phase 3 blockers): (1) georgia/insurance jobs phantom 7.4% delta = AccuLynx `count` overcount vs unique paginable (data complete; fixing needs a persisted sweep-completion signal = edge-fn + migration); (2) alert *delivery* proof still needs the 2 human steps.
**Open threads**: Ready to `/gsd-plan-phase 4` (Sandbox Write-Capability Exploration & Red-Team). Optional follow-ups unchanged (below).

## Durable gotchas (also in the corrected docs)
- **Never `source .env`** — line ~214 has a value zsh executes, aborting the source before the
  AccuLynx keys (empty secrets, digest = SHA-256 of ""). Set Supabase secrets via
  `supabase secrets set --env-file <grepped temp file>`; verify digest ≠ `e3b0c442…b855`.
- **`accountFilter`** (`{"multiAccount":true,"accountFilter":["<acct>"]}`) gives one account the
  full 110s budget — the force-backfill lever.

## Scheduled run — abc-supply-nightly-sync (2026-07-01T11:06Z) — FAILED (env limitation)
env=production; rowsUpserted: catalog=0, branches=0, regions=0 (job never progressed past
"account search Ship-To page 1"). Root cause: the Cowork sandbox runs each bash call in its
own PID namespace with bwrap `--die-with-parent`, so nohup/setsid/disown background jobs are
killed the instant the launching call returns (verified: heartbeat probe stopped at tick 2).
The 45s per-call cap + no cross-call process survival means the 20–45 min full sync cannot run
here. Needs a persistent host (Coolify/Hetzner cron or a foreground-capable runner), not Cowork.

## Session — Phase 4 planned + executed + SECURED (GSD, full cycle)

**Goal**: Take Phase 4 (Sandbox Write-Capability Exploration & Red-Team, REQ-06) from Pending →
discuss → plan → execute → security-verify.
**Deliverables**:
- CONTEXT (D-01…D-05), RESEARCH (38-endpoint enumeration from the vendored OpenAPI index),
  PATTERNS, VALIDATION; 4 PLANs; plan-check PASSED first pass.
- Migrations **182** (`acculynx_write_catalog`/`acculynx_write_probe`) + **183**
  (`acculynx_write_checklist`, 38 rows) applied to prod `rnhmvcpsvtqjlffpsayu` (additive/idempotent).
- New Edge Function **`acculynx-write-sweep`** (hard `assertSandbox` gate, dependency-root seed,
  tiered red-team, pure `shouldStopProbing`) — deployed via `supabase functions deploy` (NOT Coolify).
- Live sandbox red-team: **38/38 evidence-based verdicts**, reconcile gate PASS, human-verified.
- Matrix regenerated (`docs/37` + `docs/knowledge-base/acculynx/api/write-capability.md`),
  phantom `POST /jobs/{id}/measurements` corrected; new `.../ingestion/write-sweep.md`.
- `04-SECURITY.md` — gsd-security-auditor **SECURED 10/10** (T-04-*), ASVS L1.
**Final write matrix (38)**: 12 writable · 5 write-only · 2 fragile-with-guardrail · 2 read-shaped ·
17 blocked-by-dependency · **0 unsupported** (every documented write route exists & is reachable).
**Decisions / live discoveries (durable AccuLynx quirks)**:
- **`jobCategory.id` is Int32** (not a GUID string like every other id) — sending it as a string
  404s "could not be converted to System.Int32" and cascades the whole job seed. Coerce it. (I hit
  this as a mid-run regression when the harness deepening stringified all harvested ref ids.)
- photos/documents need **multipart**; `PUT trade-types` 500s on empty body (send `{items:[{id}]}`);
  `DELETE initial-appointment` needs a `{note}` body (harness doesn't body-seed DELETEs).
- AR/sales-owner POST need a **role-specific `CompanyUserId`** (a plain `/users` id works for
  company-rep but not AR/sales); `payments/paid` needs `accountTypeId`, documents need
  `documentFolderId` — the **sandbox company has neither configured** → legit blocked-by-dependency.
- One verdict corrected in-place at the human-verify gate (DELETE initial-appointment: unsupported→
  fragile). 17 blocked are at genuine diminishing returns (sandbox config we don't control).
**Open threads**: Phase 4 CLOSED. Next = **Phase 5 (Read/Write Action Layer, REQ-08)** — human-
approval-gated write wrappers on a contrib branch, using this write matrix. Optional: `/gsd-verify-work 04`
(formal UAT; goal already proven live). The abc-supply-nightly-sync note above is a separate blocker.
