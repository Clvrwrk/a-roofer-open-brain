# 2026-07-01

> This session began 2026-06-30 and ran past midnight — the full session blocks are in
> [`2026-06-30.md`](2026-06-30.md). This file is the pickup pointer for the next session.

## Where things stand (Phase 3 — AccuLynx Commercial Cron Hardening)

**Phase 3 is effectively COMPLETE through Wave 5.** All migrations 172–181 applied to
prod (`rnhmvcpsvtqjlffpsayu`); `acculynx-sync` edge fn deployed with all fixes.

- **Wave 4 (03-05)** — security posture (`docs/knowledge-base/acculynx/security/posture.md`,
  D-13) + recovery runbook (`.../ingestion/runbook.md`, D-15); `/gsd-secure-phase 3` = SECURED 12/12.
- **Wave 5 (03-06)** — D-08 gate OPEN; **all 8 AccuLynx accounts enabled** (canary-then-batch,
  zero cross-account bleed) and **fully backfilling** jobs/contacts/estimates.
- **Three live-diagnosed bugs fixed today** (all deployed + verified): (1) reconciliation
  instrument — jobs `last_api_total` grand-total probe (mig 181); (2) contacts/estimates
  pagination — `pageStartIndex` is a PAGE NUMBER (advance by 1); (3) jobs sweep —
  `/jobs` ignores `recordStartIndex`, paginates by `pageStartIndex` as a RECORD offset
  (advance by items.length), 416 = clean end. Docs corrected (read-capability, auth-and-limits).
- Fleet jobs now: colorado 1841/1843, texas 2299/2300, multi_family 352/352, KC/wichita/florida ✓.

## Next task (exact)
Phase 3 is done; no blocking work. **Optional follow-ups** (not blockers):
1. Reconcile `v_acculynx_reconciliation` against the **paginable** count, not the raw API
   `count`, so count-overcount accounts (georgia count=470 vs ~435 unique paginable) don't
   show a false residual delta (currently georgia jobs 7.4%, insurance 7.4% — data is complete).
2. **03-03 alert delivery proof** — still needs 2 human steps: invite `openbrain` bot to the
   private `#ob-ops-conductor`; `select vault.create_secret('<xoxb>','acculynx_alert_slack_bot_token')`
   in the Supabase SQL editor. Then seed→fire→observe per the runbook.

## Session (Phase 3 formal close-out)

**Goal**: Formally close Phase 3 in the GSD workflow (engineering was done + live-healthy but STATE/ROADMAP lagged).
**Deliverables**:
- Live re-verification of all 4 SC against prod (`rnhmvcpsvtqjlffpsayu`): hourly cron active for all 8 accounts; `v_acculynx_cron_outcomes` last 24h = 23 success / 1 in-flight pending (no perpetual pending); `check_acculynx_alerts()` = 0 firing; runbook + posture docs present; security audit CLOSED T-03-01…T-03-11.
- Wrote `.planning/phases/03-commercial-cron-hardening/03-VERIFICATION.md` (verdict COMPLETE).
- ROADMAP: ticked Phase 3 + plans 03-03/05/06 + added COMPLETE status line.
- STATE.md: Phase 03 COMPLETE, 3/7 phases, 13/13 plans, 43%, focus → Phase 04.
**Decisions**: Two residuals accepted (not Phase 3 blockers): (1) georgia/insurance jobs phantom 7.4% delta = AccuLynx `count` overcount vs unique paginable (data complete; fixing needs a persisted sweep-completion signal = edge-fn + migration); (2) alert *delivery* proof still needs the 2 human steps.
**Open threads**: Ready to `/gsd-plan-phase 4` (Sandbox Write-Capability Exploration & Red-Team). Optional follow-ups unchanged (below).

## Durable gotchas (also in the corrected docs)
- **Never `source .env`** — line ~214 has a value zsh executes, aborting the source before the
  AccuLynx keys (empty secrets, digest = SHA-256 of ""). Set Supabase secrets via
  `supabase secrets set --env-file <grepped temp file>`; verify digest ≠ `e3b0c442…b855`.
- **`accountFilter`** (`{"multiAccount":true,"accountFilter":["<acct>"]}`) gives one account the
  full 110s budget — the force-backfill lever.

## Scheduled run — abc-supply-nightly-sync (2026-07-01T11:06Z) — FAILED (env limitation)
env=production; rowsUpserted: catalog=0, branches=0, regions=0 (job never progressed past
"account search Ship-To page 1"). Root cause: the Cowork sandbox runs each bash call in its
own PID namespace with bwrap `--die-with-parent`, so nohup/setsid/disown background jobs are
killed the instant the launching call returns (verified: heartbeat probe stopped at tick 2).
The 45s per-call cap + no cross-call process survival means the 20–45 min full sync cannot run
here. Needs a persistent host (Coolify/Hetzner cron or a foreground-capable runner), not Cowork.

## Session — Phase 4 planned + executed + SECURED (GSD, full cycle)

**Goal**: Take Phase 4 (Sandbox Write-Capability Exploration & Red-Team, REQ-06) from Pending →
discuss → plan → execute → security-verify.
**Deliverables**:
- CONTEXT (D-01…D-05), RESEARCH (38-endpoint enumeration from the vendored OpenAPI index),
  PATTERNS, VALIDATION; 4 PLANs; plan-check PASSED first pass.
- Migrations **182** (`acculynx_write_catalog`/`acculynx_write_probe`) + **183**
  (`acculynx_write_checklist`, 38 rows) applied to prod `rnhmvcpsvtqjlffpsayu` (additive/idempotent).
- New Edge Function **`acculynx-write-sweep`** (hard `assertSandbox` gate, dependency-root seed,
  tiered red-team, pure `shouldStopProbing`) — deployed via `supabase functions deploy` (NOT Coolify).
- Live sandbox red-team: **38/38 evidence-based verdicts**, reconcile gate PASS, human-verified.
- Matrix regenerated (`docs/37` + `docs/knowledge-base/acculynx/api/write-capability.md`),
  phantom `POST /jobs/{id}/measurements` corrected; new `.../ingestion/write-sweep.md`.
- `04-SECURITY.md` — gsd-security-auditor **SECURED 10/10** (T-04-*), ASVS L1.
**Final write matrix (38)**: 12 writable · 5 write-only · 2 fragile-with-guardrail · 2 read-shaped ·
17 blocked-by-dependency · **0 unsupported** (every documented write route exists & is reachable).
**Decisions / live discoveries (durable AccuLynx quirks)**:
- **`jobCategory.id` is Int32** (not a GUID string like every other id) — sending it as a string
  404s "could not be converted to System.Int32" and cascades the whole job seed. Coerce it. (I hit
  this as a mid-run regression when the harness deepening stringified all harvested ref ids.)
- photos/documents need **multipart**; `PUT trade-types` 500s on empty body (send `{items:[{id}]}`);
  `DELETE initial-appointment` needs a `{note}` body (harness doesn't body-seed DELETEs).
- AR/sales-owner POST need a **role-specific `CompanyUserId`** (a plain `/users` id works for
  company-rep but not AR/sales); `payments/paid` needs `accountTypeId`, documents need
  `documentFolderId` — the **sandbox company has neither configured** → legit blocked-by-dependency.
- One verdict corrected in-place at the human-verify gate (DELETE initial-appointment: unsupported→
  fragile). 17 blocked are at genuine diminishing returns (sandbox config we don't control).
**Open threads**: Phase 4 CLOSED. Next = **Phase 5 (Read/Write Action Layer, REQ-08)** — human-
approval-gated write wrappers on a contrib branch, using this write matrix. Optional: `/gsd-verify-work 04`
(formal UAT; goal already proven live). The abc-supply-nightly-sync note above is a separate blocker.

## Memory Health

- 2026-07-01 health check: CAP COMPLIANCE flagged — context/MEMORY.md is 2476 chars (within 5% of the 2,500 cap). Consolidate via meta-memory-write before next append. All other checks green. (Addressed at wrapup below — refreshed the "Pick up here" pointer to Phase 5 and fixed the stale "schemas thru 171"→185.)

## Session — Phase 5 discuss → plan → execute Waves 1–2 (GSD)

**Goal**: Advance Phase 5 (Read/Write Action Layer, REQ-08) — human-approval-gated AccuLynx write layer — from Pending through discuss + plan + partial execute.
**Deliverables**:
- **Discuss**: `05-CONTEXT.md` — D-01 reuse Command Center work-queue as the gate; D-02 new `acculynx-write-action` Edge fn = sole prod-write path; D-03 dryRun==execute (one builder); D-04 (amended) 3 proven-safe first lanes (message/payment/external-ref) — **custom-fields DROPPED** (blocked-by-dep, no sandbox CustomFieldType); D-05 payment = first live prod write; D-06 wrap all 17 proven-safe lanes; D-07 dashboard authoritative + D-08 Slack notify-only; D-09 prod needs explicit target flag AND `approval.decide_prod_write` permission.
- **Research** (`05-RESEARCH.md`): central finding RQ-1 — the work-queue had NO generic agent-enqueue path; must add a surface loader + decision fallback. RQ-4: new `acculynx_write_action_log`, NOT atom_access_log (hard rule 6). PATTERNS + VALIDATION written.
- **Plan**: 4 plans / 3 waves; plan-checker PASSED after 1 revision (tightened the 05-01↔05-03 edge-invoke body contract); coverage gates REQ-08 4/4, decisions 9/9; threat models T-05-01…23 (ASVS L1).
- **Execute Wave 1**: 05-01 edge fn (`action.ts`/`index.ts`, 40 Deno tests, dryRun==execute anchor); 05-02 migs **184/185 applied to prod `rnhmvcpsvtqjlffpsayu`** + verified (2 tables/6 idx/3 CHECK/1 FK/RLS/idempotent no-op).
- **Execute Wave 2**: 05-03 CC wiring — `approval.decide_prod_write` (PROD_WRITE_APPROVER_EMAILS allowlist), enqueue route, `loadPendingAccuLynxWriteSurface` (RQ-1), decision.ts fallback + barrier#2 + full-body synchronous edge invoke on approve, Slack notify. Full CC suite 93/93 green, astro check clean. (One executor hit API-overload mid-05-03; resumed cleanly — Task 1 + surface splice were already committed.)
**Decisions**: Executed wave-by-wave, sequential on the contrib branch (not parallel worktrees) for control given prod stakes. Applied the additive migrations to prod during Wave 1 (hard rule 1 compliant, human-approved). Did NOT merge contrib→main at wrapup — main auto-deploys via Coolify, and Wave 3's deploy is deliberately gated.
**Open threads**: **Phase 5 Wave 3 (05-04) is the human-gated finale** — deploy edge fn (`supabase functions deploy`) + Command Center (Coolify) → sandbox-prove the 3 lanes → set `PROD_WRITE_APPROVER_EMAILS` roster (OQ-2) → **FIRST live PROD payment write** (blocking-human, SC3) → converge contrib→main. Resume: `/gsd-execute-phase 5 --wave 3`. Branch `contrib/cleverwork/read-write-action-layer` pushed to origin (local + backed up, NOT merged). Also open: Hermes scheduler; abc-supply-nightly-sync host limitation.

## Session — Phase 5 Wave 3 (05-04) Tasks 1–3: deploy + sandbox proof (STOPPED before prod payment)

**Goal**: Execute Phase 5 Wave 3 up to the human-gated prod payment. User chose "deploy + sandbox only".
**Deliverables**:
- **Task 1**: full suites green (edge 40/40, CC 93/93, no touched-file type errors); started `docs/knowledge-base/acculynx/ingestion/write-action.md`.
- **Task 2 (deploy, explain-then-ship)**: `acculynx-write-action` edge fn deployed ACTIVE v1 via `supabase functions deploy` (from contrib 862482e). Converged contrib→main by clean fast-forward (23 commits, 02e3502→**9da0f84**), pushed origin main, Coolify buildCommit flipped to 9da0f84 (~105s), healthz `status ok / liveSurfaceStatus live`. Edge secrets confirmed (PE_CC_SANDBOX_ACCULYNX_API_KEY set). Roster decision recorded: **primary owners** (not yet set in Coolify — only needed at Task 5).
- **Task 3 (sandbox proof)**: drove all 3 lanes via a **localhost CC in local_operator mode reading prod DB + invoking live edge**, sandbox job `22e22f5d…842f8`. Audit rows: payment(id1,201), message(id2,201), external-ref(id3,201), all `executed`. **Idempotency**: payment re-approve → HTTP 200, no 4th row. **Reject (SC4)**: no edge invoke, no audit row. Baseline was clean (0/0).
**Findings (must fix before Task 5 prod payment)**:
1. **Approver never captured** (SC2/T-05-23 blocker) — `acculynx_write_action_log` has no `approver` column; edge insert + decision-route edge-invoke body omit approver; `acculynx_pending_write.approver` exists but is never set. Needs migration + edge + decision.ts change.
2. **Reject doesn't close pending write** — stays `pending_review` (schema allows `rejected`; decision.ts never sets it). Manually set the test item to `rejected` to keep the live queue clean.
3. **Double-prefixed workKey** (cosmetic) — enqueue should strip a leading `acculynx-write-action:` prefix / callers pass bare workKey.
4. **Slack D-08 notify `not_in_channel`** — notify bot not in target channel; notify-only, non-fatal.
**Decisions**: Used local_operator-on-prod-DB as the sandbox test harness because `approval.decide` is human-only (service agents can't self-approve, by design D-07) and reading root `.env` for a service token is permission-blocked. Did NOT converge the Task 3 doc commits into main (doc-only; would trigger a wasteful Coolify build) — bundle with the findings fixes.
**Open threads**: **Phase 5 Task 5 (first live PROD payment) DEFERRED** per user. Before it: fix findings #1 (approver) + #2 (reject-close), set `PROD_WRITE_APPROVER_EMAILS`, redeploy edge + CC. Branch `contrib/cleverwork/read-write-action-layer` at c6e916c (3 doc commits ahead of main 9da0f84, unpushed). main==origin/main==9da0f84 (deployed).

## Session — Phase 5 Wave 3 findings #1/#2 FIXED + re-verified + deployed

**Goal**: Fix the two sandbox-proof blockers before the (still-deferred) prod payment.
**Deliverables**:
- **Fix #1 (approver capture, SC2/T-05-23)**: no migration — reused the existing `acculynx_write_action_log.actor` column. `decision.ts` passes approver (`email??displayName??id`) in the edge body; edge writes it to `acculynx_pending_write.approver` + `acculynx_write_action_log.actor` + stamps `work_key`. Follow-up fix: the handler rebuilt the persist input WITHOUT approver (line ~258) → first re-verify showed null; caught live, fixed, edge → **v3**.
- **Fix #2 (reject-close)**: `decision.ts` sets `acculynx_pending_write.status='rejected'` (+ rejecter) on reject; schema already allowed it.
- **Tests**: +2 in decision.test.ts (approver-in-body, reject-close); edge 40/40 + deno check, CC **95/95**, astro clean.
- **Deploys**: edge fn `acculynx-write-action` **v3** (`supabase functions deploy`); CC converged contrib→main by fast-forward, buildCommit **bb86ca4** (confirmed live).
- **Live re-verify (local_operator → prod DB → live edge v3)**: approve `p5w3-fix2-approve` → audit #5, 201, pw_approver+audit_actor="Local Operator" ✓; reject `p5w3-fix-reject` → status `rejected`, approver recorded, no audit row ✓. Finding #3 (double-prefix) was my test-input error — bare workKeys render canonically. Finding #4 (Slack `not_in_channel`) is an open config item (invite the notify bot to the channel), not code.
**Decisions**: index.ts remains verified-live not unit-tested (thin Deno.serve wiring); the reconstruct-drops-approver bug is exactly what live sandbox re-verify exists to catch.
**Open threads**: **Task 5 (first live PROD payment) still DEFERRED per user.** Only remaining prereq: set `PROD_WRITE_APPROVER_EMAILS` (primary owners) in Coolify + redeploy, then fire the D-09-gated prod payment. Branch `contrib/cleverwork/read-write-action-layer`; main==origin/main==bb86ca4 (deployed, edge v3). Sandbox test rows (p5w3-*) remain in acculynx_pending_write as executed/rejected evidence.
