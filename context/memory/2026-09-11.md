## Session 07:4x — catch-up + backlog status (read-only)

**Goal**: Chris — "where do we stand with open tickets and backlog." Linear unreachable (MCP unauthorised; no LINEAR_API_KEY locally), so built from daily logs, docs/handoffs/current.md, git and the prod DB.
**Findings**: pg_cron job 13 (15-min audit matview refresh) failing on EVERY run since 2026-09-02 07:30 UTC (893 runs) — `record "v_req" is not assigned yet` in credit_memo_reconcile() (mig 284) rolled back all four REFRESHes ⇒ mv_invoice_audit_line frozen, 16 post-9/2 ABC invoices with 0 audit rows. ABC invoice feed itself recovered 9/2. PEC-257 (7 Aug lines $142.25) + PEC-258 (9 CMs no original, $26,601.90) unchanged; QB export set 20 invoices $38,435.75 unstamped; 372 reopened/reset pre-August lines await a ruling; gap queue 329 items / $588,585. Two unmerged branches: codex/crm-shared-design (36 commits, 9/8) and claude/project-handoff-5ua2fw (migs 286–290 already applied to prod but files only on that branch).

## Session 07:5x–08:2x — reconcile fix, runtime uptime board, Better Stack (docs/109)

**Goal**: Chris — "Invoice detail failed to load: network error" on Invoice Audit; full verification that every API/agent/scheduled job runs; turn /agents into a realtime red/yellow/green uptime board wired to Better Stack (1Password cw_master → BetterStack_PE_CC_DEV_API); ask to resolve ambiguity; keep a decision log.
**Root cause of the symptom**: detail endpoint healthy (0.6 s) but returned 0 line rows (matview frozen) while the tree header counted 1 from a live view → client throws → generic "network error" text. Same cause as job 13 above.
**Deliverables**:
- mig 285 (applied): `SELECT * INTO v_req … WHERE false` instead of `v_req := NULL`; reconcile ran clean (2 new ABC amount_mismatch receipts); manual matview refresh; job 13 green from 15:00 UTC; 0 post-9/2 invoices missing.
- mig 286 (applied): runtime_heartbeats, runtime_job_runs + runtime_job_report() RPC, v_runtime_pg_cron_status, v_runtime_job_latest, runtime_heartbeat_pump() + pg_cron job 17 (5 min). mig 287 (applied): v_runtime_feed_freshness.
- Better Stack: 9 monitors (healthz keyword; /agents 302; /auth.md 200; six /api routes expected 401 — D10) + 19 heartbeats (12 pg_cron + 7 systemd), ping URLs in runtime_heartbeats; `scripts/betterstack-provision.sh` idempotent. Token read via `op read`, never printed; local `app/command-center/.env.local` only.
- App: `src/lib/runtime-registry.ts` (inventory), `runtime-status.ts` (D5 colour rules, 14 unit tests green), `betterstack.server.ts` (45 s cache), `GET /api/system/runtime-status` (system dept), `/agents` rewritten: summary tiles, sources, 7 groups, legend, 30 s poll, long-list disclosure (10 rows, fixed pane). `astro build` green. Verified on dev:4399 vs prod: 66 components, 0 red / 9 yellow / 45 green / 12 unknown.
- Host fixes written (NOT yet deployed — classifier blocked SSH state changes): `Environment=HOME=/root` + `ExecStopPost=scripts/runtime-job-report.sh` on all 7 openbrain units; `scripts/runtime-job-report.sh`.
- docs/109 decision log (F1–F15, D1–D13, Q1–Q4). config/.env.example: BETTERSTACK_API_TOKEN, GITHUB_TOKEN.
**Decisions**: hybrid truth source (D2); 401-probes (D10); DB-side heartbeat pump once per new success (D11); ExecStopPost reporting (D12); paused monitor = red.
**Shipped (Chris's answers 08:2x)**: host applied via `deployment/remote/apply-runtime-board-host.sh` (checkout → origin/main, units reinstalled, both failing units now REPORT to the board); `origin/main` fast-forwarded to 4b80b4a3; `origin/main` merged into `codex/crm-shared-design` (381097bd) and pushed. **Coolify builds the CRM branch, not main** (pushing main changed nothing on /healthz) — canonical live branch must be named next session (D15).
## Session 09:5x — Coolify reset + CC⇄CRM boundary (docs/110)

**Goal**: Chris — Coolify login broken (reset "not set up"); evaluate moving back to `main` while keeping the CRM mirror in the Sales tab.
**Findings**: Coolify host = `178.105.220.14`, reachable as root ONLY with `~/.ssh/a_roofers_open_brain_ed25519`. Coolify auto-deploys `main` fine (container c2c93b16 up) — the public host is hijacked by CRM-release-written Traefik dynamic files: `crm-cc-staff.yml` (whole host → `cc-production-e4344d8`) and `crm-cc-sales-mirror.yml` (`/sales*`, `/api/sales*` → `cc-sales-53e93af47f2e`). All three CC containers share WORKOS cookie password/client id. CRM canonical data still empty (efforts/people/appointments 0; weekly_items 156).
**Deliverables**: root password reset (`php artisan root:reset-password`) → 1Password cw_master "coolify.proexteriorsus.net - Root Login". docs/110 recommendation: Option A "path mount" — retire the host-wide override, keep the path-scoped `/sales` mount, cherry-pick 3 CC-only fixes (e13d83b1, 69e211cb, 4a5843e2), set COMMAND_CENTER_PUBLIC_URL on the Coolify app, later a `crm_gateway.cc_*` read contract for CC-native widgets. docs/109 F22–F24.
**Executed (Chris approved all four)**: cherry-picked e13d83b1/69e211cb/4a5843e2 into main (6c01d48d, 8f4383bc, e18dbdea; 345 tests + build green) → Coolify rebuilt in 120 s → `crm-cc-staff.yml` retired (`deployment/remote/retire-cc-host-override.sh`) → public `/healthz` = e18dbdea (main), `/sales*` still served by the CRM companion (asset 200), runtime-status 401 unauth. Branch renamed to `archive/crm-shared-design-2026-09`. Proxy composition documented in `deployment/proxy/`.
**Open threads**:
- **2026-09-12**: stop (not remove) `cc-production-e4344d8` on 178.105.220.14 once 24 h green (docs/110 §4 step 5).
- Cherry-pick e18dbdea changed `supabase/functions/acculynx-sync/resources/job-walk.ts` — edge function NOT redeployed yet (Coolify does not deploy edge functions); deploy deliberately after review.
- File the §3 ownership contract in `CRM_PWA/docs/delivery/`; `scripts/switch-weekly-release-routes.py` must stop writing host-wide routers.
- Coolify API token still to mint (Chris can log in now) → then `BETTERSTACK_API_TOKEN` on prod.

## Session 10:2x — CRM_PWA full code review + documentation transfer

**Goal**: Chris — full code review of `/Users/chussey/Documents/ChatGPT/crm.proexteriorsus.net`; populate `Clvrwrk/CRM_PWA` with the code, a full README and links explaining how the two work surfaces function together.
**Deliverables**: six-area parallel review (app, domain/core/contracts, crm-server/workspace/design, adapters/worker/inbound, 44 migrations, ops/CI/scripts/docs) → `docs/review/2026-09-11-code-review.md` (69 findings: 4 Critical, 13 High, 26 Medium, 26 Low; ten prioritised actions). New `README.md` (153 lines) and `docs/integration/COMMAND-CENTER.md` in CRM_PWA, both linking to docs/109, docs/110, deployment/proxy here. CRM_PWA `main` fast-forwarded to 9e89bdf (was an empty init commit); `codex/prd-grill` = `main`. Here: docs/111 (companion-repo index + the shared-DB surfaces), docs/110 §3 amended, proxy README cross-link.
**Key findings (verified live)**: (1) CRM migrations changed CC-owned surfaces — `pgrst.db_pre_request = crm_gateway.api_pre_request` on `authenticator` (every CC REST call runs it), ACL rewrites in public/storage/graphql_public + `REVOKE USAGE ON SCHEMA public FROM PUBLIC`, policy + 7-column grant + 2 FKs on `public.properties`, `crm.create_effort` inserts into it (0 rows so far). (2) Release scripts write (3) or require (3) the host-wide CC router — **every CRM/Sales-mirror release fails today** (`accounting_route_drift`) until `protected()` is inverted; never recreate the file. (3) worker/inbound/DocuSign not built; weekly AI never wired. (4) App: auth opt-in by env default; contract can be prepared from any stage; issued agreement silently reset to draft.
**Open threads**: CRM team to action review items 1–2 before the next CRM release; CC-side follow-ups — `/rest/v1/rpc` probe on the runtime board, CC-owned `create_property_from_crm` RPC + `v_property_card` view (A3), add CRM-created property rows to the null-FK watch.

## Session 12:3x — CRM release scripts fixed; CC ⇄ CRM UI/UX comparison

**Goal**: Chris — (1) fix the CRM release scripts so releases work again; (2) full UI/UX comparison of cc vs crm with CC as SOT, CRM mobile-first and radically simpler for the least technical staff.
**Deliverables (CRM_PWA main+codex/prd-grill @ ecf8cf0 + docs commit)**: `switch-weekly-release-routes.py` now carries the shared route invariant (no `Host(cc)` rule without a Path term; `replace_exact` is the single chokepoint; every inventory scan refuses `crm-cc-staff.yml`); executors' `protected()` requires the file's ABSENCE; `restore-original-cc.py` + CC half of `update-weekly-production-ui.py` retired; Coolify routers matched by `coolify.managed` label; Sales candidates clone the current `cc-sales-<sha>` (`--source`); 8 suites / 92 tests green; CI runs all route suites; preflight verified read-only on the live host (invariant OK, 4 routers). Runbook/AGENTS/handoff/README/integration/review docs updated.
UX: `docs/design/CC-PARITY-UX-REVIEW.md` + `docs/design/evidence/ux-2026-09-11/*.png` (both apps run locally; computed-style measurements). P0: WIP/AR sub-bar (z-40) covers the primary nav (z-30) — reps get trapped; opportunity has 8 section tabs in a 795px row on a 375px screen. P1: 13-field intake with silent duplicate check; system vocabulary on rep screens; 3 commit buttons on Capture; Today lacks counted question cards; WIP/AR rep view is a manager fragment. CC drift from its own spec: body 15px (spec 14), Inter not loaded, purple segmented accent — fix on CC side. 10 simplicity principles, acceptance checklist, 4-phase plan.
**Open threads**: CC drift fixes in `global.css` (14px body, Inter @font-face from the shared package, segmented active = `--primary`); CRM Phase 0 (nav trap, sections → Overview/Do next/More, hide Sync from reps, requirement copy) — awaiting Chris's go; the local CRM preview cannot be launched by the preview runner (sandbox cannot enter `/Users/chussey/Documents/ChatGPT/...`) — ran `node apps/crm/dist/server/entry.mjs` directly.

## Session 15:0x — UI/UX parity fixes shipped on both sides

**Goal**: Chris — "fix all issues and make sure both work surfaces align via UI/UX."
**CC (main)**: body 14px (was 15), Inter 400/600/700/800 loaded via `@fontsource/inter` in AppShell, purple `#5b46d8` accent → `--primary` navy / `--info` blue (invoice-audit, audit-queue, price-agreements request). Verified on dev:4399; pushed as 91c24d1b → Coolify deployed, `/healthz` buildCommit=91c24d1b at 22:41 UTC (c: live on cc.proexteriorsus.net).
**CRM (`sales-workspace` 0.1.43, CRM_PWA main + codex/prd-grill)**: 4.1 dock above the rail + Playwright guard that all four primary tabs are hit-testable on every screen; 4.2 Overview·Notes·More (rows with status sentences; Sync managers-only); 4.3 three-step intake with auto-advance + existing-customer card; 4.4 `friendlyRequirement()` + Done/Still needed wording + handoff sentence; 4.5 single **Save these facts**, "Log a call or visit" sheet; 4.6 Today cards (Follow-ups due / No follow-up date / Open customers), filters moved to Pipeline; 4.7 `data-rep-only` weekly view; 4.8–4.10 12px tab labels, caps eyebrow, pill tones, noun tabs. 579 unit + 16 e2e green. Evidence + numbers in CC-PARITY-UX-REVIEW.md §10 and `docs/design/evidence/ux-2026-09-11-after/`, produced by `scripts/ux-evidence.mjs`.
**Open threads**: CRM production release (repack 0.1.43 into the companion vendor dir, `release-lead-job-workspace.py` with fresh BASE pins) — not run; prod still serves the old companion. Not addressed (code review items): auth opt-in default, lifecycle gaps, session-refresh race, worker/inbound build, shared-DB surfaces.

**Blockers / open threads (earlier)**:
- ~~NOT yet deployed~~ → see F23: deployed but shadowed by the proxy override; docs/110 §4 is the fix.
- Coolify API key stale in BOTH root .env and 1Password (401 at 15:30 UTC even after Chris started re-minting) → BETTERSTACK_API_TOKEN not on prod yet; prod board shows Site & APIs + heartbeats as yellow "not configured" until set.
- jt-sentinel real cause: `JT_SUPABASE_MIRROR_GRANT_KEY` missing from host `master.env` (Q5). wip-pack real cause: Aspose.Cells license expired in `build_pack.py` (Q6). Both now red-with-reason on the board instead of silent.
- Better Stack → Slack `#pe-cc-dev-team` integration to enable in the UI (D14/Q7).
- Pre-existing 8 s statement timeouts: `v_order_acculynx_match` (operations surface, seen on every /agents SSR) and the site-sweep received-credit check — candidates for materialisation (playbook 9).
