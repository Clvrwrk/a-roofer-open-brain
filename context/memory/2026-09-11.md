## Session 07:4x — catch-up + backlog status (read-only)

**Goal**: Chris — "where do we stand with open tickets and backlog." Linear unreachable (MCP unauthorised; no LINEAR_API_KEY locally), so built from daily logs, docs/handoffs/current.md, git and the prod DB.
**Findings**: pg_cron job 13 (15-min audit matview refresh) failing on EVERY run since 2026-09-02 07:30 UTC (893 runs) — `record "v_req" is not assigned yet` in credit_memo_reconcile() (mig 284) rolled back all four REFRESHes ⇒ mv_invoice_audit_line frozen, 16 post-9/2 ABC invoices with 0 audit rows. ABC invoice feed itself recovered 9/2. PEC-257 (7 Aug lines $142.25) + PEC-258 (9 CMs no original, $26,601.90) unchanged; QB export set 20 invoices $38,435.75 unstamped; 372 reopened/reset pre-August lines await a ruling; gap queue 329 items / $588,585. Two unmerged branches: codex/crm-shared-design (36 commits, 9/8) and claude/project-handoff-5ua2fw (migs 286–290 already applied to prod but files only on that branch).

## Session 07:5x–08:2x — reconcile fix, runtime uptime board, Better Stack (docs/109)

**Goal**: Chris — "Invoice detail failed to load: network error" on Invoice Audit; full verification that every API/agent/scheduled job runs; turn /agents into a realtime red/yellow/green uptime board wired to Better Stack (1Password cw_master → BetterStack_PE_CC_DEV_API); ask to resolve ambiguity; keep a decision log.
**Root cause of the symptom**: detail endpoint healthy (0.6 s) but returned 0 line rows (matview frozen) while the tree header counted 1 from a live view → client throws → generic "network error" text. Same cause as job 13 above.
**Deliverables**:
- mig 285 (applied): `SELECT * INTO v_req … WHERE false` instead of `v_req := NULL`; reconcile ran clean (2 new ABC amount_mismatch receipts); manual matview refresh; job 13 green from 15:00 UTC; 0 post-9/2 invoices missing.
- mig 286 (applied): runtime_heartbeats, runtime_job_runs + runtime_job_report() RPC, v_runtime_pg_cron_status, v_runtime_job_latest, runtime_heartbeat_pump() + pg_cron job 17 (5 min). mig 287 (applied): v_runtime_feed_freshness.
- Better Stack: 9 monitors (healthz keyword; /agents 302; /auth.md 200; six /api routes expected 401 — D10) + 19 heartbeats (12 pg_cron + 7 systemd), ping URLs in runtime_heartbeats; `scripts/betterstack-provision.sh` idempotent. Token read via `op read`, never printed; local `app/command-center/.env.local` only.
- App: `src/lib/runtime-registry.ts` (inventory), `runtime-status.ts` (D5 colour rules, 14 unit tests green), `betterstack.server.ts` (45 s cache), `GET /api/system/runtime-status` (system dept), `/agents` rewritten: summary tiles, sources, 7 groups, legend, 30 s poll, long-list disclosure (10 rows, fixed pane). `astro build` green. Verified on dev:4399 vs prod: 66 components, 0 red / 9 yellow / 45 green / 12 unknown.
- Host fixes written (NOT yet deployed — classifier blocked SSH state changes): `Environment=HOME=/root` + `ExecStopPost=scripts/runtime-job-report.sh` on all 7 openbrain units; `scripts/runtime-job-report.sh`.
- docs/109 decision log (F1–F15, D1–D13, Q1–Q4). config/.env.example: BETTERSTACK_API_TOKEN, GITHUB_TOKEN.
**Decisions**: hybrid truth source (D2); 401-probes (D10); DB-side heartbeat pump once per new success (D11); ExecStopPost reporting (D12); paused monitor = red.
**Shipped (Chris's answers 08:2x)**: host applied via `deployment/remote/apply-runtime-board-host.sh` (checkout → origin/main, units reinstalled, both failing units now REPORT to the board); `origin/main` fast-forwarded to 4b80b4a3; `origin/main` merged into `codex/crm-shared-design` (381097bd) and pushed. **Coolify builds the CRM branch, not main** (pushing main changed nothing on /healthz) — canonical live branch must be named next session (D15).
**Blockers / open threads**:
- Coolify API key stale in BOTH root .env and 1Password (401 at 15:30 UTC even after Chris started re-minting) → BETTERSTACK_API_TOKEN not on prod yet; prod board shows Site & APIs + heartbeats as yellow "not configured" until set.
- jt-sentinel real cause: `JT_SUPABASE_MIRROR_GRANT_KEY` missing from host `master.env` (Q5). wip-pack real cause: Aspose.Cells license expired in `build_pack.py` (Q6). Both now red-with-reason on the board instead of silent.
- Better Stack → Slack `#pe-cc-dev-team` integration to enable in the UI (D14/Q7).
- Pre-existing 8 s statement timeouts: `v_order_acculynx_match` (operations surface, seen on every /agents SSR) and the site-sweep received-credit check — candidates for materialisation (playbook 9).
