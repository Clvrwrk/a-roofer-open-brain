## Session 1 — wrapup of the 8/6 marathon

Wrapup + full Linear documentation per Chris. Handoff rewritten (docs/handoffs/current.md; prior archived 2026-08-06-0915). Linear: PEC-183 session report, **PEC-184 LEARNINGS LOG (10 rules — retain, do not repeat)**, PEC-185 deferred silo hardening (High). MEMORY.md consolidated to 2,314 chars (silo doctrine + live-call-path rule promoted to standing instructions; schemas thru 223; new 1P item names; Coolify uuid; dev port 4399). Deploy 0b39bc2 verified live; silo_assertions() = 0.

## Session 6 — MC-68 missing payment → job-walk cursor-wrap bug (PEC-187)

Chris: board missing MC-68's 2nd payment ($87,007.25, 7/27). Trace: payment real in QBO (7/28, Customer MC-68); `acculynx_invoices` MC-68-2 still Unpaid, synced Jul 2; **whole invoice mirror frozen — 0/1,660 re-synced in 7d**. Root cause: `job-walk.ts` resume ran from cursor to END with **no wrap** — once an account's initial sweep finished (Jul 2), existing jobs were never re-walked; only new jobs got first-sight walks. Fix: wrap to index 0 on completed sweep (D-16 still skips unchanged); regression test 15/15; deployed via CLI with 1P `SUPABASE_ACCESS_TOKEN` (repo .env copy is 401-stale). Recovery: cursor jumped to pos 335, sync triggered → MC-68-2 Paid $0 → refresh → board Collected $150,587.25 / Balance Due $137,149.75. Full multi-account sweep triggered; hourly cron converges the rest. Commit `1dc9da1`; Linear PEC-187 (Done). Follow-up noted: batch per-skip watermark writes if deadlines bite.

## Session 7 — Maya reply to Lucinda + diagnose/repair gate A3

Lucinda's original email = Maya intake **PEC-186**/CAT-30 (gmail 19fdd6bc666bd43f, 18:10 UTC) — same MC-68 question Chris relayed. Maya replied (AgentMail ob-accounting inbox, to accounting@proexteriorsus.com, cc admin@cc.proexteriorsus.net + chussey@aia4.io, thread 8dd85036…): summary, PEC-187 link, fixed board numbers, what to expect. PEC-186 → Done, related to PEC-187. Wrote **proposals/a3-maya-diagnose-repair-slack-gate.md**: two-phase design — Phase A auto-diagnose (read-only `ob_readonly` role + wip-triage runbook, posts to #ob-approvals), Phase B repair only after Chris's `APPROVE PEC-xxx` Slack reply (user-ID allowlist, plan-hash fail-closed approval row in `agent_fix_approvals`). Pilot scope: Friday WIP/AR discrepancy tickets only. Awaiting Chris: A3 approval + 4 open decisions (channel, allowlist, TTL, auto-run).

## Session 8 — Maya gate BUILT + live (A3 approved)

Chris approved: channel #pe-cc-dev-team (resolved `C0BNVF99Y74`, Maya invited + kickoff posted), approver = Chris only (Slack user `U0B8SGJJZLJ` = admin@cc), Phase A auto-runs on accounting intakes, 7-day TTL. Shipped: **mig 224/224b applied** (agent_fix_approvals fail-closed ledger + ob_readonly role + maya_gate_cursor_jump); `scripts/maya-gate.mjs` (+.sh) — Phase A diagnoses new `[MAYA] Accounting intake` issues (created after 2026-08-07T22Z cutoff; pre-existing 14 grandfathered) via the wip-triage ladder (REST GETs only), comments Linear, posts pilot-class proposals; Phase B honors APPROVE/REJECT PEC-xxx from the allowlist only, recomputes plan_hash, executes ONLY whitelisted `mirror_refresh` (cursor jump → targeted sync → refresh → verify). `openbrain-maya-gate.timer` (15-min) enabled on PE-US-AGENTS; first pass clean. LINEAR_API_KEY provisioned to host (~/.config/cleverwork/linear.env, from 1P). `wip-triage` skill = the PEC-186/187 runbook. **Dry-run staged: proposal row #1 (PEC-186 replay) posted to the channel — awaiting Chris's `APPROVE PEC-186`.** Local repo .env still has the line-214 syntax bug (edit denied; host copy fixed).

## Session 9 — ticket-opened notices

Chris: when Maya's intake opens a Linear ticket, the requester gets a second email with the ticket link ("turned into PEC-xxx, being worked"). Built as a `notices` phase in maya-gate.mjs (mig 225 `agent_intake_notices` dedupe/audit): scans new `[MAYA]` intakes (post-cutoff), parses `Source sender:` email from the intake description, INTERNAL domains only (guard mirrored host-side), sends from ob-accounting AgentMail inbox, comments the Linear issue, one notice per ticket ever. `MAYA_NOTICE_DRY_RUN=1` supported. Dry-run verified against PEC-186 (parsed accounting@proexteriorsus.com, recorded, sent nothing). Live on the 15-min timer for the next real intake. Deploy `730d3f0`.
