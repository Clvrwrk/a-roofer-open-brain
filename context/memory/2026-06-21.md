# 2026-06-21

## Session 1

**Goal**: Scheduled ABC Supply FULL production-sync (catalog + branches + regions).
**Deliverables**: Partial run log `integrations/bridges/abc-supply/.production-runs/full-20260621T145903Z.log` (no JSON produced).
**Decisions / outcome**: FAILED run. env=production; token exchange OK (200, API calls succeeded); productCatalog rowsUpserted=0, vendorBranches rowsUpserted=0, abc_regions rowsUpserted=0. Process killed ~13s in at "account detail 5/32" — the Cowork sandbox runs each bash call in its own bwrap PID namespace with `--die-with-parent`, so the nohup'd background job dies when the launching call returns (45s per-call cap also blocks any foreground run of a 20–45 min job). Same nightly failure as 2026-06-20 (died 7/32) and 2026-06-19 (4/32).
**Open threads**: This long job cannot complete from a Cowork scheduled task. Move it to the Hetzner Coolify agent host (docs/27) via cron/systemd. See [[cowork-sandbox-quirks]].
