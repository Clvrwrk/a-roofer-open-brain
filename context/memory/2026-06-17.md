# 2026-06-17

## Session 1

**Goal**: Design the invoice pricing audit dashboard for Accounting.

**Deliverables**:
- 5 standalone HTML concept mockups under `outputs/invoice-audit-dashboards/` (index + v1 classic two-panel, v2 dense dark console, v3 queue triage, v4 inbox three-pane, v5 executive analytics) sharing `_data.inline.js`.
- Final build wired into the Astro app: `app/command-center/src/pages/accounting/invoice-audit.astro` — V3 queue-triage layout + V2 search/filters + System/Light/Dark theme toggle (localStorage `iaTheme`, scoped to `.ia-dash`). Added "Invoice audit" tab to `components/accounting/AccountingModeTabs.astro` (route `/accounting/invoice-audit`).

**Decisions**:
- Winner = V3 (queue cards + focused detail). Dark mode is a per-dashboard toggle scoped to the surface, not a global app theme.
- Columns/pills per Chris's spec: price-source pills (Negotiated=green, Past Invoices Mean=yellow, API Derived=orange, No Price=red); tolerance pills (Minor 0.01–3% yellow, Moderate 3–6% orange, Major 6%+ red); PDF link on invoice header only. 6 disposition actions (4 accept variants + 2 credit-memo variants).
- Data is sample/embedded (4 invoices). Var $ is per-unit; at-risk rollups = var$ × qty. Tolerance uses absolute variance.

**Open threads**:
- Wire to live data (Supabase price-foundation review surface / `schemas/cleverwork-roofer/81-price-foundation-review.sql`) — client renderer only needs the `INVOICES`/`ACTIONS` shape.
- Disposition actions are UI-only (toast); no persistence/API yet.

## Session 2

**Goal**: Make Invoice Audit the only Accounting dashboard.

**Changes**:
- `AccountingModeTabs.astro` reduced to a single "Invoice audit" tab (gate + overview tabs removed everywhere the bar renders).
- `invoice-audit.astro` no longer renders the (now single-item) tab bar; import dropped.
- `accounting/index.astro` redirect → `/accounting/invoice-audit`.
- Deleted `accounting/overview.astro` (now 404).

**Decision / preserved**:
- Kept `/accounting/invoices` (invoice gate) route alive — it's a live work-queue deep-link target (`live-work.ts`, `command-center.ts`, work-queue decision API). Removed from Accounting nav but not deleted, to avoid breaking work-queue routing. Other slug dashboards (credit-memos, price-agreement-gaps, etc.) untouched and still linked from the audit KPI cards.

## Session 3

**Goal**: KPI drill-downs as standalone HTML mockups, starting with the "Pending Audit" card. First of the six KPI cards on the invoice-audit screen.

**Deliverables**: `outputs/pending-audit-queue/` — index + 5 versions (v1 worklist table, v2 aging board/kanban, v3 triage detail [matches winner], v4 analyst inbox w/ SLA, v5 aging analytics w/ charts). Shared foundation files `_theme.css` (tokens/pills/KPI/theme toggle, same palette as invoice-audit) + `_data.inline.js` (12-invoice sample queue + helpers + System/Light/Dark theme toggle). Each invoice "Open audit →" links to `../invoice-audit-dashboards/v3-queue-triage.html`.

**Decisions**:
- Authored directly (not via workflow) to lock a consistent canonical pattern. Workflow fan-out reserved for the remaining 5 KPI cards (25 dashboards) — each reuses `_theme.css`/`_data.inline.js`.
- Sub-KPI cards on each version are the drill breakdown (In Queue / New / In Review / On Hold / Escalated / Oldest|SLA|Unassigned) and click-to-filter the queue.

## Session 4

**Goal**: Refine Pending Audit winner (V1) + fan out the other 5 KPI drill-downs.

**Changes**:
- V1 winner = `pending-audit-queue/v1-worklist-table.html`. Added `$ At Risk` column between Worst and Status, pill colored by `worstClass(r.worst)` (same color as Worst). Added `atRisk` to each row in `_data.inline.js`. Sortable.
- "Open audit →" now deep-links `?invoice=INV-XXXX`; `invoice-audit-dashboards/v3-queue-triage.html` reads the param on load and auto-opens that invoice (verified for the 4 invoices in the audit sample; others no-op gracefully — sample-data limit, not a logic bug).

**Workflow launched** (background, run `wf_b33a0a72-97c`): `kpi-drilldown-dashboards` — builds `outputs/kpi-drilldowns/<slug>/` for the other 5 KPI cards (out-of-tolerance, at-risk, no-price-lines, credit-memos, avg-resolution), 5 versions each (worklist table / board / triage detail / inbox / analytics) + index, reusing the pending-audit `_theme.css` + `_data.inline.js` conventions. 35 agents (5 foundation → 25 versions → 5 index).

**Workflow result** (`wf_b33a0a72-97c`, 35 agents, ~2.56M tokens, ~10min): all 25 dashboards + 5 indexes delivered under `outputs/kpi-drilldowns/<slug>/`. Verified: `_theme.css` byte-identical to pending-audit reference in all 5 cards; every version links shared files; AUDIT_HREF depth correct (`../../`); all 5 V1s render (rows: OOT 16, at-risk 14 SKUs, no-price 15, credit-memos 12, avg-resolution 16) with correct columns + `?invoice=` deep-links; credit-memos v2 board has 5 status columns; avg-resolution v5 has histogram + 2 bar charts; no console errors.

**Open threads**:
- Pick winning layout per KPI (V1 worklist is the established winner pattern), then port winners into the Astro app like invoice-audit.
- Some agent-generated deep-links use invoices not in the 4-invoice audit sample (e.g. no-price INV-558533) — resolve once wired to live data, or expand the audit mockup dataset.

## Session 5

**Goal**: Run the ABC (ABC Supply) APIs, update all brain tables current as-of-today, and add change-detection + a persistent change archive. (Cron deferred per Chris.)

**Deliverables**:
- DB (Supabase `rnhmvcpsvtqjlffpsayu`, migrations `abc_change_detection_and_archive` + `abc_change_log_ignore_volatile_raw_fields`; mirrored to `schemas/cleverwork-roofer/93-abc-supply-change-log.sql`): `content_hash` column on abc_product_catalog / abc_vendor_branches / abc_regions; new append-only `public.abc_change_log` (combined audit: table_name, record_key, change_type, changed_fields, old_values, new_values, archived_at); generic `public.abc_log_change()` trigger fn + AFTER INSERT/UPDATE triggers on all three tables.
- `integrations/bridges/abc-supply/production-sync.mjs`: `supabaseUpsert` rewritten as a change-gated upsert — computes a stable content_hash (sorted-key canonical JSON, excluding volatile cols + `raw.lastModifiedDate`), prefetches stored hashes, skips unchanged rows, stamps content_hash on writes. New `contentHashOf`/`canonicalize` helpers; `HASH_IGNORE_KEYS` hoisted to module top (TDZ fix).
- `scripts/abc-nightly-sync.sh`: cron-ready full-sync runner (mirrors memsearch-nightly-cron pattern; logs to ~/.abc-sync/logs, never dirties git).

**Run results (today)**:
- Accounts (abc_regions): 32 fetched, 0 changed (already current). Branches: 696/720 refreshed across parallel state-group runs; 20 real updates archived (24 branches not returned by current state searches — likely closed/relocated, retain June-5 data + null hash). Catalog: only ~2,000 of 331,293 rows touched (calibration/probes) → 44 new items + 3 real updates archived.
- Change archive now holds 67 genuine changes (44 catalog inserts, 3 catalog updates, 20 branch updates). Gate verified: re-running any table upserts 0 when unchanged.

**Decisions**:
- `raw.lastModifiedDate` is volatile: ABC bumped it on ~282k catalog items today with no mapped-field change. Excluded from BOTH the content_hash and the trigger comparison (RAW_VOLATILE_KEYS ↔ raw_volatile must stay in sync) — otherwise every catalog row logs noise every run and the gate never skips. Deleted 1,000 such noise rows from abc_change_log after the fix.
- Combined single archive table (not per-table) per best practice — DRY, one-place history.

**Open threads**:
- FULL catalog refresh NOT done: 333 pages / ~90 min sequential ABC pull. Cowork VM reaps any job when the bash call returns (45s cap), so long runs must happen on Chris's Mac/remote. Finish the catalog baseline by running `bash scripts/abc-nightly-sync.sh` there once (idempotent; sets content_hash on all ~331k rows, archives only real changes).
- Cron deferred (Chris). When ready: `30 3 * * * TZ="America/New_York" /usr/bin/env bash /Users/chussey/Documents/a-roofers-open-brain/scripts/abc-nightly-sync.sh` (3:30 AM ET, inside the 3–5 AM window).
- 24 branches with null hash + ~329k catalog rows with null hash get their baseline on the first full Mac run.

## Session 6

**Goal**: Add inventory cycle-counting / ABC review classification so we only revalidate items we actually buy, on a velocity cadence — never-purchased items stay dormant (annual sweep only). Expand activity signals to other ABC tables.

**Key finding**: Of 331,293 catalog items, only 1,722 have ever been purchased (ledger: `abc_line_items` 16,282 rows + `abc_invoice_lines` 2,462, spanning 2023-01 → 2026-06). 1,473 purchased-in-36mo items are in the catalog. So ~99.5% of the catalog is dormant — cycle counting collapses the nightly catalog work from 331k to ~1.5k.

**Deliverables** (migrations `abc_cycle_counting_columns_and_trigger`, `abc_change_log_triggers_source_columns_only`, `abc_recompute_review_schedule_ledger_only`, `abc_review_due_views`; mirrored to `schemas/cleverwork-roofer/94-abc-cycle-counting.sql`):
- Catalog columns: last_purchased_at, purchases_36mo, spend_36mo, is_tracked, review_class (A/B/C/D), review_cadence (weekly/monthly/quarterly/annual), catalog_/price_ last_reviewed_at + next_review_due_at.
- Branch/account activity flags: abc_vendor_branches.is_active/last_purchased_at/purchases_36mo; abc_regions.is_active/last_purchased_at.
- `abc_recompute_review_schedule()` — recency classification from the ledger; touches only ever-purchased items (NULL review_class = never bought = dormant). Branch/account active = transacted in 36mo.
- Views: v_abc_catalog_review_due, v_abc_price_review_due, v_abc_review_summary.
- Archive triggers rebuilt as `AFTER INSERT OR UPDATE OF <source columns>` so recompute/review-stamp writes to operational columns never fire the change-archive (also fixed a 331k-row recompute timeout).
- `production-sync.mjs`: new `--review-due` mode (calls recompute RPC, pulls the due queue, fetches only due items via GET /items/{itemNumber}, gated upsert + archive, stamps catalog_last_reviewed_at + advances next-due per cadence). `--review-limit`, `--skip-recompute` flags.
- `scripts/abc-nightly-sync.sh` reworked: default = nightly cycle-counting (`--review-due`); `full` arg = annual sweep.

**Tiers today** (recency-based, Chris's choice): A weekly 329 items / $3.82M 36mo spend; B monthly 528 / $1.54M; C quarterly 616 / $792k; D annual 45 / $0; ~329k dormant (NULL). 64 active branches, 13 active accounts. Verified --review-due on 5 + 3 items: real changes archived (e.g. `dimensions` changed), reviewed items dropped off the due queue, recompute idempotent (0 rows on re-run).

**Decisions / expand-to-other-tables**: catalog = full cadence; pricing = scheduling layer built (price cadence cols + v_abc_price_review_due + recompute), but the price *fetch+store* loop should run through the existing observation pipeline (`abc_price_observations`/`_lines`, written by `mirror-backfill.mjs`) — NOT yet wired into --review-due. Branches/accounts = lightweight is_active flags only (too small to tier).

**Open threads**:
- Wire price-review execution: when v_abc_price_review_due is non-empty, pull /prices for those items at active ship-to/branch and write observations, then stamp price_last_reviewed_at. (Cadence + due-list already live.)
- Nightly cron still deferred (Chris). Nightly = `abc-nightly-sync.sh`; annual full sweep = `abc-nightly-sync.sh full`.

## Session 6

**Goal**: Port V1 (worklist) drill-downs for all 6 invoice-audit KPI cards into the Astro app.

**Built** (shared framework, not 6 copies):
- `src/styles/audit-queue.css` — design system scoped to `.aq` (same tokens/pills/theme as invoice-audit, light+dark).
- `src/scripts/audit-queue.ts` — generic worklist renderer (KPI cards, filters, sortable table, System/Light/Dark toggle) driven by a JSON payload. Render types: text/mono/money/moneyShort/pct/pill/pillPct/pillMoney/num/days/daysDecimal/action.
- `src/components/audit/AuditQueue.astro` — shell + embeds payload JSON + includes script.
- `src/lib/audit-queues.ts` — 6 exported payloads (columns/filters/kpis/rows + precomputed pill classes + `auditHref`). Sample data biases invoices to the 4 in invoice-audit so deep-links round-trip.
- 6 pages under `src/pages/accounting/audit/`: pending, out-of-tolerance, at-risk, no-price-lines, credit-memos, avg-resolution.

**Wiring**: invoice-audit KPI cards now link to `/accounting/audit/<slug>`. invoice-audit screen reads `?invoice=` on load and auto-opens that invoice (round-trip from a drill-down's "Open audit →").

**Verified**: all 6 routes 200; render with correct columns/rows (pending 12, OOT 16, at-risk 14, no-price 15, credit-memos 12, avg-resolution 16); sort + KPI-filter + search + dark toggle work; round-trip deep-link auto-opens (tested INV-559010); no console errors; `npm run build` (astro build) passes.

**Open threads**:
- Still sample data — swap the raw arrays in `audit-queues.ts` for a live loader (Supabase price-foundation review surface) when ready; column/kpi config stays.
- Only V1 (worklist) ported per Chris; the v2–v5 mockups remain in `outputs/kpi-drilldowns/` if other layouts are ever wanted.

## Session 7

**Goal**: Build the Price List Audit work path (vendor/branch/PE-office pricing-coverage), integrated with the Vendor Map.

**Interview locks**: row grain = item × branch; 80% target = spend-weighted $; map selection drives filters; margin impact = labeled placeholder ("TBD" pill); page layout = map-first, audit-only (remove old branch-mapping content from this page).

**Built**:
- `src/lib/price-list.ts` — 5 pricing categories (Negotiated green / Invoice Mean yellow / API Generated orange / One-Off Project purple / No Pricing red). `buildPriceListPayload(slots)` (item×branch worklist) + `buildCatalog(slots)` (top-200 by spend w/ by-state & by-office aggregates). `branchSlotsFromSurface()` keys sample data to REAL territory branches so map clicks resolve. Coverage = negotiated $ / total $.
- New page `src/pages/accounting/vendor-regions.astro` (STATIC route — shadows `[slug].astro`): full-width territory map on top + Price List Audit (AuditQueue, embedded) below. Map office-markers/branch-pins carry data-office/data-branch; inline script dispatches `aq:filter` → renderer filters (toggle on/off).
- Catalog drill: `src/pages/accounting/price-list/catalog.astro` + `src/scripts/price-list-catalog.ts` — period toggle (Monthly/Quarterly/Annual scales spend & qty) + 4 breakdown panels (Revenue/Qty by State, Revenue/Qty by PE Office) + top-200 table. KPI "Negotiated Catalog" links here.
- Extended shared framework: `audit-queue.ts` (KPI `href` nav-cards + `aq:filter` external-filter event), `AuditQueue.astro` (`embedded`/`heading` props), `audit-queue.css` (`.aq-embedded`, `.plc-*` catalog styles).

**Reverted**: all `[slug].astro` edits — file is pristine (git diff empty). The static `vendor-regions.astro` supersedes the registry route; the old [slug] vendor-regions branch is now shadowed (unchanged, not deleted).

**Verified**: map-first layout (map top, audit full-width below); live surface → 16 branch slots → 107 item×branch rows; Spend Coverage 49% vs 80% target; all 5 source pills; office-marker click → 26 rows, click again clears; catalog period toggle (annual $916k → monthly $76k) + 4 panels; `npm run build` passes; no console errors.

**Open threads**:
- Sample data only (keyed to live branches). Swap buildBaseRows for a live loader (catalog spend/qty + actual pricing authority per item×branch) when ready.
- Margin-impact algorithm is a "TBD" placeholder pending Chris's formula (damage to margin if unpriced item added post-proposal).
- Branch-pin clicks filter by branchNo but sample covers only the first 16 mapped branches; pins outside that set no-op. Office/state/vendor filters always resolve.

## Session 8

**Goal**: Make the Negotiated Item Catalog bars meaningful + add Year filtering (Chris feedback).

**Changes** (`/accounting/price-list/catalog`):
- Bars are now STACKED + color-segmented: by **Vendor** at top level (currently 100% "ABC Supply Co." → one color), and by **Vendor Branch** when a vendor is selected (multi-color, real branch labels + "Other"). A legend communicates the active segmentation dimension.
- Added a **Vendor filter** + **Year filter** (invoice-audit `.aq-select` style) and a prominent **FY pill** + count line ("FY 2026 · Annual · N items · $X") so the user always knows the year/period in view.
- `lib/price-list.ts` `buildCatalog` now returns year-aware raw rows (`CatalogData`: rows×{2024,2025,2026} via YEAR_FACTOR 0.82/0.91/1.0) + vendors/states/offices/years; the client (`scripts/price-list-catalog.ts`) does all aggregation/segmentation/scaling.
- `audit-queue.css`: stacked-bar `.plc-seg` (flex track) + `.plc-legend` + `.fy-pill`.

**Verified**: no-filter → "Segments by Vendor" (ABC only); vendor=ABC → "Segments by Vendor Branch" (11 colors + Other); FY2024 → $751,258 (916,168×0.82), Monthly → $76,347 (÷12); build passes; no console errors.

**Open threads**: Year filter currently only on the catalog; could extend to the main Price List Audit + invoice-audit drill-downs. Still sample data.

## Session 9

**Goal**: Push the Year filter onto the main Price List Audit + all 6 invoice-audit drill-downs (consistency).

**Changes** (shared framework — one place):
- `scripts/audit-queue.ts`: if a payload has `years`, the renderer injects an **FY pill** + **Year `<select>`** (prepended to the filter bar), defaults to the latest year, filters rows by `r.year`, and uses the year's row count as the "X of N" denominator. Coexists with all existing filters/search/sort and the map-click filter.
- `lib/audit-queues.ts`: added `AUDIT_YEARS [2024,2025,2026]` + `withYears(rows, scaleKeys)` (replicates sample rows per FY, scaling $/qty by 0.82/0.91/1.0). Applied to all 6 payloads (pending: total,atRisk · OOT: qty,atRisk · at-risk: dollarAtRisk,totalQty · no-price: qty,extValue · credit-memos: amount · avg-resolution: amount) + `years` on each.
- `lib/price-list.ts`: `buildPriceListPayload` year-expands rows (scales annualQty, annualSpend) + `years`. (Catalog page already had its own Year filter from Session 8.)

**Verified**: FY pill + Year select on pending, credit-memos, and the main Price List Audit; $ scales by FY (pending $12,880→$10,562 ×0.82; price-list $21,330→$19,410 ×0.91; credit-memo $980→$804); year coexists with office filter (FY2025+office → 60/107); category-count KPIs are year-invariant by design (proportions preserved). `npm run build` passes; no console errors.

**Note**: KPI category counts don't change per year (same rows scaled), which is correct for this sample model; only $/qty magnitudes move. Real per-year data swaps in via the same row shape.

## Session 10

**Goal**: Reframe the main Price List Audit (vendor-regions) from item×branch into a vendor→branch→item coverage tree with price-list requests (Chris spec).

**Interview locks**: grain = Vendor row → Branch rows → non-negotiated items (expand); branch shows a PE-office pill green=full/yellow=partial/red=none; in-drive-time branches at top; Request Price List always available, drafts for approval (never auto-sends); tracker = BOTH (columns + dedicated section); headline KPI = in-scope branch coverage (target 100%); create a Supabase table (go-live task).

**Built**:
- `lib/price-list-coverage.ts` — `buildPriceListCoverage(surface)`: vendors→branches→items keyed to live territory branches; in-scope = within 2h drive-time (branch.status covered/overlap_pending) OR ordered-at-no-agreement; listStatus full/partial/none; per-branch request status/date/days-open/next-follow-up. Years 2024-26.
- `components/audit/PriceListCoverage.astro` + `scripts/price-list-coverage.ts` — 3-level expand tree, 6 KPIs (In-Scope Coverage %/In-Scope Branches/Full/Partial/No List/Pending Requests), filters (vendor/office/state/coverage) + in-scope-only toggle + Year filter + theme toggle + map-click filtering. "Request Price List" → draft toast (rep + branch mgr, FYI Lucinda/Roberto) + logs to tracker + bumps Pending KPI. Dedicated "Price List Requests" tracker section (requested date, days open, next weekly follow-up, due flag).
- `vendor-regions.astro` now renders `<PriceListCoverage>` instead of the item×branch AuditQueue.
- `schemas/cleverwork-roofer/95-price-list-requests.sql` — PROPOSED (not applied) table + aging view for the request log/follow-up.

**Verified**: tree (1 vendor → 30 branches), In-Scope Coverage 90%, office pills colored; branch expand → non-negotiated items (or "Fully negotiated" for full); Request action → toast + "Requested · 0d" + Pending 6→7; tracker 30 rows (6 requested/3 need); year scales ($26,110→$21,410 ×0.82); map office-click → 16 branches; build passes; no console errors.

**Open threads / GO-LIVE**:
- Apply `95-price-list-requests.sql` to Supabase (additive) when going live; wire the tracker + Request action to it (currently client-side mock).
- Resolve real recipients per branch: sales rep email/name + branch manager email/name (not in current data) — needed before any request can draft for approval.
- Weekly follow-up = scheduled job selecting `followup_due` rows → draft for human approval → on send bump followups_sent/next_followup_at. No auto-send without approval. (Cron deferred, like ABC sync.)
- Live signals: branch in-drive-time (territory status) + "has order at no-agreement branch" (needs order history) drive in-scope; currently drive-time from live surface, hasOrder is sample. Sample set happens to have all 30 branches in-scope (no out-of-scope to exercise the toggle).

## Session 11

**Goal**: Demo data that exercises out-of-scope + order-triggered cases on the Price List Coverage tree.

**Change**: `lib/price-list-coverage.ts` now assigns branches deterministic PROFILES (cycle of 8) instead of independent cycles, guaranteeing the full mix: in-drive-time full/partial/none, an ORDER-TRIGGERED in-scope branch (out of drive-time, ordered at no-agreement), and genuinely OUT-OF-SCOPE branches. ~half of in-scope no-list branches stay RED "Not requested" (Math.floor(idx/8)%2) so the Request flow has work to demo.

**Verified (30 branches)**: default shows 20 in-scope (16 ≤2h + 4 order-triggered), 10 out-of-scope hidden until the toggle; In-Scope Coverage 80% (4 full + 8 partial + 8 none, 4 requested/4 not); out-of-scope show "—" not actionable. Request on a not-requested branch (459) → coverage 80%→85%, Pending 4→5, tracker "5 requested · 3 need a request". Build passes; no console errors.

**Held per Chris**: do NOT wire to Supabase yet — hold until rep + branch-manager contact data is sorted (go-live task). `95-price-list-requests.sql` remains proposed/not-applied.

## Session 12

**Goal**: Plan go-live (review → deploy) for the Accounting / Price List Audit suite to Supabase `rnhmvcpsvtqjlffpsayu`.

**Deliverable**: `docs/39-price-list-audit-go-live-plan.md` — data-readiness matrix, gap punch-list, build/review/deploy phases, security finding, decisions/gates.

**Key DB findings (live, not empty)**:
- Drive-time in-scope is already modeled: `office` (drive_time_minutes isochrone) + `branch_office_candidate` (72 rows = every office whose boundary contains a branch).
- Negotiated coverage: `price_agreements`(7)/`price_agreement_items`(317) + `abc_price_agreements`(99)/`abc_price_list_items`(1067)/`abc_price_agreement_branch_matches`(887).
- Pricing observations: `product_vendor_price_observations`(2205), `abc_price_observation_lines`(444k API).
- Orders/invoices: `abc_orders`(3146)/`abc_order_lines`(18391), `abc_invoices`(520)/`abc_invoice_lines`(2462), `invoice_documents`(2844).
- **`price_refresh_request` ALREADY EXISTS** = the price-list request/refresh email queue (awaiting_verification→approved→ready_to_send). RECONCILE our proposed `95-price-list-requests.sql` against it — likely drop the new table and extend the existing one. `reps`(7) = sales reps (branch-manager contact still a gap). `credit_memo_requests` tables exist (0 rows).

**SECURITY (surfaced to Chris)**: RLS disabled on `abc_change_log` (we created it), 2 backup tables, `spatial_ref_sys`. Need RLS+service-role policy on abc_change_log; drop/secure backups.

**Decisions pending (no deploy yet)**: reconcile to price_refresh_request? first go-live scope? deploy target (staging vs Coolify prod)? RLS fix approval? WorkOS auth on? Open data gaps (branch-manager contacts, margin algorithm) are go-live tasks, not read-only blockers.

### Session 12 — review pass (verified against live DB)

- Coverage-tree live mapping CONFIRMED: branches=`vendor_branches`(756; has pricing_territory_office_id, manager_name/email, sales_rep_name/email); in-drive-time=`branch_office_candidate.contains` (52 branches, 54 office-assigned); coverage=region-level via `office.region_id`→`price_agreements`(5 active)→`price_agreement_items`; order-trigger=`abc_orders.branch_number` (70 branches).
- **Recipients EMPTY**: manager_email/sales_rep_email = 0/756 → Request display OK, SEND blocked until contacts loaded (held).
- `price_refresh_request` = vendor/region-grained, 1 row (ready_to_send), no branch grain/follow-up → reconcile via additive ALTER.
- Drafted (PROPOSED, not applied): `95-price-list-requests.sql` rewritten as additive ALTER on price_refresh_request (branch grain + scope flags + rep/branch-mgr + weekly follow-up + v_price_refresh_request_aging view); `96-rls-abc-change-log.sql` (enable RLS + service-role/authenticated policies).
- Doc updated: `docs/39` §6b review findings. Next: create Supabase dev BRANCH, apply 95/96 there, build `loadPriceListCoverage()` live loader, verify, then promote. Awaiting Chris go-ahead (branch has cost).

### Session 12 — recipient contacts (Firecrawl ask → API is better)

- Firecrawl-probed abcsupply.com/location (Birmingham): public pages give manager NAME, phone, fax, hours, services — NO emails, NO sales rep. Slug = {city}-{ST}-{zip}.
- KEY: official ABC Location API ALREADY has branch manager firstName/lastName/EMAIL in `abc_vendor_branches.manager_json` (696/720). Better than scraping. → don't crawl for this.
- Blocker = branch-table crosswalk: `vendor_branches`(756) ↔ `abc_vendor_branches`(720) populated semi-independently. Clean keys sparse: legacy_id→abc.id=67 (precise), branch_number=71, lat/long on 149 only. city+state non-unique.
- Decisions: API projection FIRST then Firecrawl backfill; sales rep from `price_agreements.sales_rep` for now (email = go-live task).
- Drafted `schemas/cleverwork-roofer/97-branch-contact-projection.sql` (PROPOSED): tiered crosswalk view `v_vendor_branch_abc_xref` (legacy_id→branch_number→latlong) + idempotent backfill of vendor_branches manager_name/email + sales_rep_name. Expected fill ~70-100 branches (~21-30 of in-scope ~54); rest → Firecrawl (name/phone/hours, no email). Doc 39 §6c updated.
- Firecrawl credits used so far: 1 scrape (5cr) + 1 map. No bulk crawl run.

### Session 12 — APPLIED TO LIVE (rnhmvcpsvtqjlffpsayu), verified

Per Chris "apply to live, verified each step" (Supabase branches carry no prod data → can't verify the data projection; read-only crosswalk on live was the real verification):
- **96 RLS** (migration `rls_abc_change_log`): abc_change_log RLS enabled + service_role(all)/authenticated(read) policies. Verified: rls_enabled=true, 2 policies, 75 rows readable via service-role; security advisor no longer flags abc_change_log. (Backups + spatial_ref_sys RLS still deferred — doc 39 §6.)
- **95 reconcile** (migration `price_refresh_request_branch_grain`): added branch grain + scope flags + rep/branch-mgr recipients + weekly-follow-up cadence columns + indexes + `v_price_refresh_request_aging` view to existing `price_refresh_request`.
- **97 contact projection** (migration `vendor_branch_abc_xref_view` + 2 DML updates): crosswalk view `v_vendor_branch_abc_xref` (legacy_id→branch_number→zero-pad→zip→latlong, prefers email). Filled `vendor_branches.manager_name/email` for **94 branches** (tier1-3 all + tier4 zip for in-scope); `sales_rep_name` for 3 (branch-level agreements).
- **VERIFIED: in-scope 54/54 now have a manager_email** (was 0/756). All 9 previously-missing in-scope branches resolved to real ABC managers (Wichita→Josh Hudson, Ft Worth→Tony Favara, Denver→Andrew Matthews/Craig Scott, KC→Ray Beery, Dallas→Jeffrey Stamp, Waco→Craig Barnum, Longmont→Ryan Reiswig, Austin→Jason Bludau). NO Firecrawl needed.

**Go-live status**: branch-manager contact blocker = RESOLVED (in-scope). Remaining: build live `loadPriceListCoverage()` loader (swap sample→live), wire Request action+tracker to `price_refresh_request`, sales-rep EMAIL (still a task), other surfaces later.

### Session 12 — LIVE coverage-tree loader wired

- Created views: `v_price_list_branch` (in-scope branch-level: vendor/office/state/manager contacts/in_drive_time/has_order/region_negotiated_items) + `v_price_list_branch_item` (purchased SKUs per branch from abc_order_lines via crosswalk, negotiated flag vs region price_agreement_items).
- `lib/price-list-coverage.ts`: added async `loadPriceListCoverage()` (Supabase views + price_refresh_request; sample `buildPriceListCoverage` kept as fallback when unconfigured). Added managerName/managerEmail/salesRepName to CoverageBranch. `vendor-regions.astro` now `await loadPriceListCoverage()`. Request toast names real manager+rep.
- listStatus (live): none if region_negotiated_items=0; else full if ≥80% of purchased items negotiated, partial otherwise (no-order covered-region → full). 
- VERIFIED live on /accounting/vendor-regions: vendor "ABC Supply Co.", 54 branches all w/ manager email, coverage 8 full/15 partial/31 none → In-Scope Coverage 43%, 52 in-drive-time; e.g. Wichita #113 → Scott Adams, 40 items/36 non-neg. Request draft for FtWorth #11 → "Anthony Favara <Tony.Favara@abcsupply.com> + rep Justin Garza". Build passes, no console errors.

**Honest gaps (v1)**: item $ partial (non-negotiated items show $0 — no invoice-price join yet; coverage is the real metric); item_number↔raw_item_number match ~12% so most covered branches read "partial"; Pending Requests=0 (write path held — Request is still client-mock); Year factors still synthetic (no per-year purchase rollup). Other surfaces (invoice audit, drill-downs, catalog) still sample.

### Session 12 — /abc-price-agreement-gaps = Price Agreement Audit workflow HOME

- Repurposed `/abc-price-agreement-gaps` (was "Price Gaps" / ABC gap review queue) into the workflow home: hero "Price Agreement Audit" + 4 entry cards (MetricLinkCard) → Price List Coverage **43%** (live, /accounting/vendor-regions) · Negotiated Item Catalog (/accounting/price-list/catalog) · Agreement Gaps 879 (#gap-queue) · Invoice Audit downstream (/accounting/invoice-audit). Existing live gap queue kept beneath under id="gap-queue".
- Loads `loadPriceListCoverage()` for the headline coverage KPI + existing `loadAgreementGapSurface()` for gaps.
- AppShell nav renamed "Price Gaps" → "Price Agreement Audit" (same route /abc-price-agreement-gaps, activePage pricing).
- Verified on dev server (reads prod DB): coverage 43%, 879 gap rows, all entry links resolve; build passes; no console errors.

### Session 12 — guardrail: Live ⇄ Dev alignment (CLAUDE.md + AGENTS.md)

Root cause documented: this session's app work sat UNCOMMITTED on a stale local `main` (ed37328, behind origin/main ec27d57) while production (cc.proexteriorsus.net) actually runs a different lineage — the Google-Maps vendor map + WorkOS on `contrib/cleverwork/vendor-territory-map-live` / a LOCAL-ONLY `main-perf` (852bcfa, never pushed). Deploy = Coolify building `app/command-center/Dockerfile` from GitHub `Clvrwrk/a-roofer-open-brain` (docs/27).

Added:
- CLAUDE.md: hard rule #11 "Live ⇄ Dev alignment" + new section "Live ⇄ Dev alignment (the deploy contract)" — source of truth = GitHub; confirm the deployed branch before app work (don't assume main); branch FROM the live branch; commit early; merge features back + push (converge); be precise about "live" = (a) dev server/prod DB, (b) merged to live branch, (c) deployed to site.
- AGENTS.md: worktree discipline rewritten to sync-from-live, commit-early, and close-the-loop (merge back + push, no stranded production features).

STILL PENDING (user interrupted the alignment to add these docs): determine the canonical live branch (user said "you determine it"), then unify (local-dev base + graft Google-Maps vendor map from the deployed branch) AND redeploy (user chose "Unify AND redeploy now"). Resume next.
