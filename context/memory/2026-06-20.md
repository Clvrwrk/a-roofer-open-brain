# 2026-06-20

## Session — GPA wireup: PDFs, invoice-date lock, version-comparison engine

Continuation of the Global Price Agreement build-out (see 2026-06-19 for the API-price seed +
columns + PA Audit rework). All committed + pushed to `origin/main`; views applied to shared prod.

**Agreement PDFs (purple pill) — mig 136**: private `agreements` Storage bucket +
`pdf_storage_bucket/path` on abc_price_agreements. `upload-agreement-pdfs.mjs` stored the 7 ABC
PDFs Chris provided; linked 4 to existing agreements (KC #2036874-20 br304; Wichita #2036874-16
Jun/Apr/Sep). `/api/price-agreement/pdf/[agreementId]` signs + 302-redirects (mirrors invoice PDF).
PA Audit loader points `agreementPdfUrl` at the endpoint. Verified: pill → 302 → signed URL → real
PDF (200, %PDF-). Denver/Dallas/ABC-Wichita stored but **no agreement row yet** (no pill).

**Invoice Audit quick wins**: oldest-first sort (FIFO, time-sensitive); purple callout now shows the
PO for every invoice with one (169/172) + client/job when AccuLynx-matched. (Purple callout = the
AccuLynx job match: invoice PO normalized = AccuLynx job number; only 33/172 match → that's why most
had none before.)

**Invoice-date price lock — mig 137** (Chris directive: invoice date assigns + locks the price list):
both v_invoice_audit_line + v_invoice_audit_invoice now compare each line to the agreement in effect
at invoice time = most recent `effective_date <= invoice_date` (locked until superseded; stale expiry
still applies). Was: highest-confidence regardless of date → mis-audited multi-version branches.
`negotiated_agreement_id` records the locked version. **Open $ At Risk $4,437 → ~$3,341** (date-correct).

**Price-list version-comparison engine — mig 138** (Chris): `v_agreement_version` (lineage +
is_current), `v_agreement_version_delta` (per-item % change vs prior version → accept 0-3% / review
3-6% / critical >6% / decrease / new_item), `agreement_version_review` queue +
`refresh_agreement_version_review()` (auto-accepts 0-3%, queues the rest, flags >6% `slack_queued`).
PA Audit: 6th KPI "Price Changes to Review" (64 = 31 review + 33 critical) + per-item change badge.
Verified on real data: 2036874-16 Jun-vs-Apr — Elite $99→$122.90 (+24% critical), Supreme +12%.
**Slack send to Accounting = QUEUED (build later)** per Chris.

**Commits**: 031f634 (PDFs) · 00a3789 (sort+callout) · 43c84a7 (date lock) · 5c40033 (version engine).

**WHERE WE LEFT OFF — Denver/Dallas through the version engine (Chris asked next)**:
The Denver (#49, 09/01/2024) + Dallas (04/21/2025) PDFs are **family-level, 2-column, NO item codes**
(e.g. "Oakridge 3/SQ · 47.68 BD · 143.04 SQ"). Ingesting them = exactly the **item-id-by-description
match** requirement (task #6): parse the 2-column layout → pg_trgm match each description to
abc_product_catalog item_number (confidence threshold + review queue for ambiguous) → create
abc_price_agreements + abc_price_list_items + branch match → link the already-stored PDF → it then
flows through the version-comparison. NOT done — needs careful build (don't auto-create wrong prices
on the live audit). Both PDFs have text layers (pdftotext), so OCR not required — just the 2-column
parser + description matcher.

### Denver/Dallas item-id-by-description engine (task #6) — built + staged, NOT promoted
- mig 139 `price_list_pdf_staging`; `ingest-price-list-pdf.mjs` (parse 2-column text-layer lists →
  221 rows: Denver 113 / Dallas 108, desc + SQ price + uom); `match-price-list-staging.mjs`
  (trigram-containment match to 530 curated catalog families in Node — DB-side word_similarity over
  331K rows times out). Result: **high 98 / review 93 / none 30**.
- Quality: HIGH mostly correct (a few off, e.g. "Starter Strip 105LF"→galvanized metal starter);
  REVIEW genuinely 50/50 ("RoofRunner"→Malarkey ✗, "Pro-Start"→Malarkey ✗); NONE correctly rejects.
- **DECISION PENDING**: promoting to live abc_price_agreements + items needs the review-tier matches
  confirmed first (wrong match = wrong audit price + wrong version-comparison). Options: build a
  review surface / promote high-only / Chris eyeballs the staging. Commit 6c10792.

### Batch: Estimate API price + Denver/Dallas review surface + image chip (Chris order 1,4,2)
- **Estimate Audit API price** (mig 140): v_estimate_audit_line exposes mapped ABC item
  (estimate_scenario_lines.product_mapping_id → estimate_product_mappings.vendor_item_id, vendor
  'abc_supply'); loader joins item×job-branch → API price. 500/510 lines mapped. Verified (OSB
  $16.99/SH, GAF ProStart $67.05/BD). **All 5 line dashboards now have the branch-tied API column.**
- **Price List Review surface** (`/accounting/price-agreement/review`, nav): per staged PDF lists
  each family row + its match (item id, family, score, status), editable item#, confirm/reject, and
  "Promote → agreement". APIs review/update + review/promote (creates abc_price_agreements + items +
  branch match from confirmed/high rows, links the stored PDF, refreshes version-comparison;
  idempotent). Gates Denver/Dallas promotion behind human review. Verified render + confirm.
- **Product image chip** (mig 141): public `product-images` bucket + image_storage_path on catalog;
  fetch-product-images.mjs pulled 91/99 GPA images (ABC catalog images[].href + token). PA Audit
  item table: image chip = first column (32px thumb, placeholder when none), click → full-screen
  overlay, click → close. Verified: 82 chips with real CDN URLs, enlarge works, no console errors.
  **Chris to VISUAL-VERIFY** (his gate). Commits 6c4bad3 (estimate) · 94d9730 (review) · 8301fc1 (chip).

**Still open**: monthly-15th + 30-day price crons (agent host); Accounting Slack for >6% critical
(queued); Chris reviews Denver/Dallas matches → promote; (optional) image chip on Builder/branch
price list too; visual-verify the image chip.

## Session — Full handoff / wrap-up

**Alignment verified**: dev (local `cleverwork/price-agreement-audit`) = `origin/main` = **`39a14c9`**, 0/0 ahead/behind. Clean tree (only untracked `.claude/launch.json`). Coolify auto-builds `origin/main` → `cc.proexteriorsus.net` (deploy verification = `/healthz` buildCommit once it settles; this is a real Coolify deploy, not yet confirmed live this turn).

**Session arc (2026-06-19 evening → 2026-06-20)** built the whole **Global Price Agreement** system — extended the existing vendor-agnostic catalog, didn't rebuild. Migs **132–141** all additive on shared prod:
- 132 best-vendor-price · 133 OCR ingest · 134/135 API price seed (606×150 branches) · 136 agreement PDFs · 137 invoice-date lock · 138 version-comparison · 139 description-match staging · 140 estimate API item · 141 product images.
- **"API Price" column on all 5 line dashboards** (branch-tied). **PA Audit** rebuilt (99 GPA items, new KPIs, purple PDF pill, version-change badges, image chip). **Price List Review** surface for Denver/Dallas family-level PDFs.
- Last fix `39a14c9`: Price List Review text readability (theme-aware colors).

**Memory updated**: `context/MEMORY.md` (2407/2500), `docs/handoffs/current.md` (rewritten; prior archived to `docs/handoffs/archive/2026-06-19-pm-dashboard-reworks.md`).

**WHERE WE LEFT OFF** (see handoff for detail): (1) Chris visual-verify the image chip; (2) review→promote Denver/Dallas in `/accounting/price-agreement/review`; (3) price crons monthly-15th + 30-day on the agent host (next cycle `2026-07`); (4) Accounting Slack for >6% criticals (`slack_queued` flagged); (5) ABC account-expansion request for ~546 unpriceable branches. Optional: image chip on Builder + branch price list.

## Session — ABC Supply nightly production sync (scheduled, autonomous)

**FAILED run** — 2026-06-20 ~11:06 UTC, env=production. rowsUpserted: product_catalog=0, vendor_branches=0, regions=0 (no JSON summary produced). Token exchange succeeded (reached account search + account details), so this is NOT a credential failure. Root cause: the Cowork sandbox runs each `mcp__workspace__bash` call in an isolated bwrap PID namespace with `--die-with-parent`, so a `nohup`/`disown`/`&` background process is killed the instant the launching bash call returns. The node sync (a 20–45 min job) died ~8s after launch at account detail 7/32; log froze at 11:06:19. Confirmed with a heartbeat test (writer stopped at 2 lines on call return) and by the identical recurring failure on 2026-06-19 (died at detail 4/32, no JSON). The catalog upsert is all-at-once after full in-memory page collection, so a killed process persists ZERO rows — no partial progress possible, and chunked 45s runs can't work without script changes (no end-page arg, no per-chunk upsert/resume). Recommendation: run this nightly sync on the Hetzner Coolify agent host via real cron/systemd (docs/27), not via the Cowork scheduled task.

## Session — Open-invoice API-price gap fill (analyze every open invoice → fetch missing prices)

**Goal**: analyze every open ABC invoice; for each (item, branch) line with no API price, GET the live ABC price and populate it.

**Analysis**: 172 open invoices → 418 distinct (item,branch) price points; 308 already priced, **110 missing**. New script `integrations/bridges/abc-supply/fill-open-invoice-api-prices.mjs` (mirrors price-seed.mjs; recomputes the missing set from `v_invoice_audit_invoice`/`v_invoice_audit_line` vs `v_branch_item_api_price`, resolves PE-preferred Ship-To per branch, batches `POST /api/pricing/v2/prices`). All 10 branches priceable.

**Result of GET (all 110)**: ABC priced **69**; **41 genuinely not API-priceable** — ABC returns `"Cannot price item … Call for pricing."` for `NS*` special-order metal + the CSI items, and transactional charges (DELIVERYBR, DELIVERYGRNDBR, FUELSURCHARGE, JURDELFEE, Freight3RD, Fuel3RD, Surchg_Fuel, PROMOCREDIT). Key finding: the 69 priced items existed only in raw `abc_product_catalog`, never promoted to curated `products` — so the API-price view (mig 134) had nowhere to attach. A GET alone fills nothing.

**Fix (Chris approved "promote + populate now")**:
- **mig 142** `142-promote-open-invoice-catalog-products.sql` — promoted the 68 ABC-priceable catalog items into `products` (additive, `WHERE NOT EXISTS`). Sourced from `abc_product_catalog`: name/desc=item_description, base_uom=stocking UOM, manufacturer via explicit supplier→manufacturer alias map (ACM SUPPLIER→AMERICAN CONSTRUCTION METALS, OWENS CORNING SALES LLC→OWENS CORNING, etc.; unmappable→MISCELLANEOUS VENDOR 59 — **no new manufacturer rows**), taxonomy via ABC hierarchy group/category/productType label match (1 'Truck & Van' fallback→Tools&Equipment/Application 33), internal_sku=`M{legacy}-{sku}`. NOTE: audit category comes from `category_key`, NOT products.taxonomy — taxonomy_id is non-audit-critical.
- Re-ran the fill (real) → **69 observations written** (`observer_id='open-invoice-gap-fill'`, `source_ref api:{branch}:2026-06`), **all clean UOM (0 review flags)** since base_uom = ABC stocking UOM.

**Verify**: open-invoice missing lines **180 → 90** (1080/1170 now priced). Remaining 90 = the 41 un-priceable combos (32 in-catalog charges/NS, 9 non-catalog charges). All fillable gaps filled.

**Untracked (NOT committed — awaiting Chris)**: `fill-open-invoice-api-prices.mjs`, `schemas/cleverwork-roofer/142-*.sql`, `outputs/open-invoice-api-price-fill-2026-06*.json`. mig 142 already applied to shared prod (live for dev + cc). Suggest: ABC account/quote follow-up for the "call for pricing" NS/CSI items.

## Session — Observability: Sentry + CodeRabbit wired for the Monday alpha

**Goal**: connect Sentry (errors/replay) + tune CodeRabbit so issues surface + get fixed fast for the alpha.

**Decisions (Chris)**: Slack alerts · Session Replay ON fully-masked · user id+email on errors · human-in-the-loop fix loop · instrument web app + Node runtimes · full tracing (alpha) · CodeRabbit advisory + error-handling focus.

**Built** (branch `contrib/cleverwork/observability-sentry`, merged to main):
- `@sentry/astro` (web SSR) + `@sentry/node` (Slack runtime via `node --import runtime/sentry-instrument.mjs`). Client+server config, full tracing, replay masked (maskAllText/Inputs+blockAllMedia), `sendDefaultPii:false`, beforeSend strips cookies/Authorization. `setUser(id+email)`+`actor.type` in middleware. Nightly ABC sync: guarded `--import` (no-op until `@sentry/node` on the Hetzner host).
- **Key design**: literal DSN fallback gated on prod (a Sentry DSN is PUBLIC, not a secret) → errors+replay work with ZERO Coolify build-arg dependency; dev stays silent.
- CodeRabbit: added path_instructions flagging swallowed errors / missing Sentry capture / PII in error reports.

**Sentry creds**: DSN `…64100fe…@o4511120856449024…/4511599368798208` (org `cleverwork`, project `cc-proexteriorsus`). Build/source-map token = a `sntrys_` ORG token Chris pasted in chat → **rotate post-launch** (transcript exposure). Personal token `SENTRY_PERSONAL_TOKEN` in .env has full scopes but can't mint org tokens via API.

**Coolify** (app uuid `og0rmt02rff8qti9nlfk3nr7`): set `SENTRY_DSN`/`PUBLIC_SENTRY_DSN`/`SENTRY_ENVIRONMENT`/`SENTRY_AUTH_TOKEN`. **Gotcha learned**: Coolify build args need the env field `is_buildtime:true` (NOT `is_build_time`; POST rejects it→ PATCH it). Creds are in root `.env` COMMENTED OUT (`# COOLIFY_…`), NOT `.env.agent-passwords` (skill doc was stale).

**Verified**: deploy `72c70dd` live (`/healthz`); client SDK+DSN baked into served bundle (`/auth/denied`); test event flushed OK to Sentry (release `deploy-smoke-72c70dd`). First build did NOT upload source maps (vars weren't build-time) → flipped `is_buildtime` + triggered rebuild `p9461e7y…` (in progress at log time).

**COMPLETE**: source maps confirmed (build created Sentry release `72c70dd…` after `is_buildtime` flip + rebuild `p9461e7y`). Slack connected (integration `443674` "CleverWork", mycleverwork.slack.com); issue-alert rule `17213565` "New & regressed issues → #cc-proexteriors" created via SENTRY_PERSONAL_TOKEN (has alerts:write+org:integrations; the `sntrys_` build token is releases-only).

**VERIFIED END-TO-END**: sent a unique `[TEST]` error → Sentry created issue CC-PROEXTERIORSUS-2 → alert posted to #cc-proexteriors (`C0BCUJV0MLY`) with Resolve/Archive buttons (confirmed via Slack MCP read). Full loop works: error → Sentry → Slack. Both test issues resolved; dashboard clean.

**STILL OPEN**: (1) rotate the `sntrys_` build token (transcript exposure) → update SENTRY_AUTH_TOKEN in Coolify after. (2) activate nightly-sync Sentry on the Hetzner agent host (`npm i @sentry/node`). (3) the >6% price-critical Accounting Slack alert is still separately queued (unrelated to this).

## Memory Health

- 2026-06-20 health check: CAP COMPLIANCE failed — `context/MEMORY.md` is 2407 chars, within 5% of the 2500 cap. Consolidate via meta-memory-write skill rules. (Capture current, index fresh, USER.md/SOUL.md OK.)
