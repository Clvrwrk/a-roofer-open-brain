# 2026-06-10

## Session 1

**Goal**: Review estimate-to-proposal spec; adapt karpathy/autoresearch pattern into the learning loop
**Deliverables**: docs/33 — new Purpose paragraph; Section 6 twin-loop framing; 6.7 Autonomous Industry Research Loop (concept mapping, daily run loop, hard boundaries); 6.8 Knowledge Domains; 6.9 Two-Loop Integration; 6.10 Finding Quality Score; 4 new approval-matrix rows; kb_* tables in 8.1; 9.7 External Research Sources; MVP criteria, open questions, Phase 8, Definition of Done additions
**Decisions**: Autonomous keep/discard allowed only at evidence trust tier in staging (val_bpb analog = finding quality score); instruction-tier promotion always human-approved; verification harness fixed like prepare.py (agents can't change own scoring); research charter = program.md analog, human-edited and versioned; Researcher stays external-only — staged findings enter brain via gated ingest only; autoresearch cited as re-expressed concept (MIT), not redistributed
**Open threads**: ICC code-content licensing path; initial jurisdiction list; daily run budget; source allowlist owner — all logged in spec Section 12

## Session 2

**Goal**: Validate memory system vs Hermes + MemSearch best practice; fix blockers; port from Scrapes-OS
**Deliverables**: context/SOUL.md (new); .claude/settings.json + .claude/hooks/load-memory-snapshot.js (SessionStart snapshot injection); .claude/skills/meta-memory-write/SKILL.md; context/memory-config.json (reranker authority weights); scripts/memsearch-index-open-brain.sh dedup fix; CLAUDE.md + AGENTS.md memory sections corrected; USER.md team roles added (Chandler CEO, Roberto Ops Mgr, Lucinda Accounting, Chris CTO/AI Admin)
**Decisions**: .memsearch/memory/ is the real turn-capture location (Codex plugin), context/transcripts/ was a dead documented path — docs now match reality; SOUL/USER/MEMORY/daily-log = frozen snapshot (~1.7K tokens measured); local Milvus Lite confirmed for single-user phase
**Tests**: Inject hook → valid SessionStart JSON, 4 sections, ~1,690 tokens ✓; caps 2051/875/1977 all under ✓; index rebuilt from markdown in clean sandbox (110 chunks) ✓; L1 recall hit ABC creds, team roles (1.0), today's spec work ✓; L2 expand returned full section ✓
**Open threads**: Turn capture only fires in Codex CLI (last 2026-06-06) — install memsearch plugin in Claude Code (`claude plugin marketplace add zilliztech/memsearch --scope user`, then `claude plugin install memsearch --scope user`); Mac index rebuild + nightly cron; delete stray `.claude/.write-test`; Cowork sessions rely on daily-log discipline (no hook surface)

## Session 3

**Goal**: Schedule capture/index automation; full handoff and wrap-up
**Deliverables**: Cowork scheduled task `memory-capture-healthcheck` (daily 08:03 — capture landing, index freshness, caps, hook presence; failures appended to daily log under `## Memory Health`); `docs/handoffs/current.md` rewritten for this session; archived copy `docs/handoffs/archive/2026-06-10-1322.md`; MEMORY.md refreshed (backend pending decision closed: local Milvus Lite; active threads updated)
**Decisions**: Health check never rebuilds the index from sandbox (Mac-local `~/.memsearch` state); handoff follows the established current.md + timestamped archive convention; this session's work left uncommitted with suggested commit grouping in the handoff
**Open threads**: Chris's 4-step list in handoff "Next Task — Start Here"; then spec Phase 1 data foundation

## Session 3

**Goal**: Clear Session 2 open threads — install memsearch plugin in Claude Code, rebuild Mac index, set up nightly cron, delete stray write-test
**Deliverables**: memsearch@memsearch-plugins installed at user scope (marketplace was already registered); Mac index rebuilt from markdown (112 chunks, up from 110 — picked up new daily logs); scripts/memsearch-nightly-cron.sh (new — committed cron wrapper: sets PATH for cron's minimal env, logs to ~/.memsearch/logs/index-cron.log outside repo, idempotent); crontab entry `15 3 * * *` nightly; deleted untracked .claude/.write-test
**Decisions**: Cron over launchd (user asked for cron); log lives outside repo so it never dirties git; wrapper is idempotent (drops prior memsearch line before re-adding)
**Tests**: index rebuild exit 0 (112 chunks) ✓; wrapper manual run exit 0, log written, 0 new chunks on re-run = dedup working ✓; crontab -l shows single nightly entry ✓
**Open threads**: macOS TCC — repo lives in ~/Documents (protected); /usr/sbin/cron likely needs Full Disk Access or the 3:15am job fails silently with "Operation not permitted". One-time manual grant in System Settings → Privacy & Security → Full Disk Access. Verify after first run via ~/.memsearch/logs/index-cron.log. Turn-capture activation expected next Claude Code session.

## Session 4

**Goal**: Review handoff; evaluate safishamsi/graphify (MIT, v0.8.36) for speed/token efficiency as the KB grows
**Deliverables**: Review only — no files changed; clone in sandbox /tmp
**Decisions**: Graphify graphs *repo files* (tree-sitter code free; docs via LLM) and serves token-cheap `graphify query` BFS subgraphs — it cannot index Supabase atoms/pgvector, so it is not a brain-retrieval layer. Recommendation: adopt as dev-time repo map (CLI only, no hooks/MCP-stdio, A3 if it becomes an agent skill); brain speed comes from pgvector/HNSW + tiered recall; consider re-expressing graph-query-first over atoms later
**Open threads**: None — Chris decided to skip graphify entirely (no dev-time trial). Full rationale + revisit triggers recorded in `docs/35-decision-graphify-skipped.md`; do not re-evaluate unless a trigger fires

## Session 5

**Goal**: Diagnose and close the recurring Cowork "MemSearch blocked by sandbox" blocker
**Deliverables**: CLAUDE.md Memory Retrieval tiers rewritten — Tier 1 now conditional on `memsearch` being on PATH, with grep over `context/` + `.memsearch/memory/` as the expected Tier 1 in sandboxed surfaces (Cowork); Tier 2 gets a read-the-full-file equivalent
**Decisions**: Root cause = `.memsearch.toml` pins the Milvus Lite index to `~/.memsearch/milvus.db`, unreachable from Cowork's isolated VM (only mounted folders shared; memsearch not installed there), while CLAUDE.md unconditionally ordered `memsearch search` — every fresh VM hit the same wall and improvised. Fix chosen: grep fallback in CLAUDE.md only (no repo-local index move, no per-session VM install — avoids Milvus lock contention and ONNX setup cost). Markdown is source-of-truth, so grep recall is lossless. Agents must never install memsearch in the VM or report the grep path as a blocker.
**Status**: CLOSED — do not re-investigate; if the blocker message reappears, the session is ignoring the updated CLAUDE.md tiers
**Open threads**: None

## Session 6

**Goal**: Pick project back up — commit 6/9–6/10 work per handoff plan; start spec Phase 1 (Data Foundation)
**Deliverables**: 5 commits (0c0756f spec+graphify docs, 2ac0dee memory layer, dc30c95 memory content, bc78893 handoffs, 1b20507 Phase 1 schemas); schemas/cleverwork-roofer/90-estimate-pipeline.sql (24 estimate_* tables) + 91-kb-research-loop.sql (10 kb_* tables); .gitignore now excludes .claude/settings.local.json + ABC smoke-run dirs
**Decisions**: settings.local.json gitignored not committed (machine-local perms); kb_regulatory_snapshots realized as a staging table promoting into existing public.regulatory_snapshot (no parallel snapshot store); kb_research_runs/finding_sources/digests are append-only (no updated_at trigger); schemas follow file-80 pattern (RLS + service-role-only grants, cw_set_updated_at)
**Tests**: Both schema files executed twice clean in PGlite with role/FK stand-ins — idempotent ✓; 34 tables, RLS on all 34, 25 triggers ✓
**Open threads**: Remaining Phase 1 item: convert screenshot templates in "Estimate Tool - Working Files" into estimate_package_templates/estimate_template_lines rows; pre-existing untracked noise still unstaged (ABC bridge files, reference assets, 2026-06-04.md edit) per handoff

## Session 7

**Goal**: Apply Phase 1 schemas to the live Supabase brain
**Deliverables**: 3 migrations applied to rnhmvcpsvtqjlffpsayu (jurisdiction_and_regulatory_snapshot, estimate_pipeline_phase1, kb_research_loop_phase1 — 36 tables live, RLS on all, 26 triggers); schemas 90/91 adapted to live DB; new 89-jurisdiction-snapshot.sql; docs/36-decision-live-db-binding.md
**Decisions**: LIVE-DB BINDING (Chris) — the OB1 property/job/crew core (files 10–30) was never applied to this brain; estimate/kb schemas bind to public.properties (uuid) + public.acculynx_jobs (text GUIDs), crew_ref text until a crew table exists; jurisdiction+regulatory_snapshot extracted into file 89 since the kb loop needs them. Template files 10–30 stay canonical for fresh brains.
**Tests**: PGlite 2x-run idempotency with live-shaped stand-ins ✓; live verify 24+10+2 tables, 36 RLS, 26 triggers ✓; security advisors — new tables only expected rls_enabled_no_policy INFO ✓ (pre-existing: 24 security_definer_view ERRORs, 3 rls_disabled_in_public ERRORs, unrelated)
**Open threads**: Template conversion next (screenshot templates → estimate_package_templates/lines); pre-existing advisor ERRORs worth a separate pass

## Session 8

**Goal**: Convert screenshot templates (T1–T4) into structured rows in the live brain
**Deliverables**: 4 estimate_package_templates + 102 estimate_template_lines + 102 estimate_product_mappings seeded in rnhmvcpsvtqjlffpsayu from roofing-estimate-model-1424-n-24th.xlsx (Templates + ABC Mapping sheets); scripts/seed-package-templates-2026-06-10.sql (idempotent repro of what was applied, with corrections noted in header)
**Decisions**: package_tier left 'custom' with metadata.tier_candidate — GBB tier assignment per zone is a pending human decision (docs/33 §4.3); mapping_status from xlsx match status (Likely match→mapped, Needs review→needs_human, screenshot-unavailable→unavailable); import corrections: T3 OC shingle mapping rows were a spreadsheet TOTAL artifact → NULL item + needs_human; T4 Tamko downgraded Likely match→needs_human (evidence said not in order history)
**Tests**: Per-template counts verified live: T1 26/26, T2 26/26, T3 25/25, T4 25/25; status split 52 mapped / 42 needs_human / 8 unavailable ✓
**Open threads**: Tier assignment (Roberto/zone rules) — 4 templates await good/better/best designation; 42 needs_human mappings (mostly color/profile selection) for human review before live pricing; OC + Tamko + GAF Timberline shingles need live ABC catalog confirmation

## Session 9

**Goal**: Phase 2 (Measurement Extraction) — Roofr CSV+PDF parser, run on 5 samples, load to live brain
**Deliverables**: scripts/extract-roofr-measurements.mjs (CSV scalars + pitch buckets; PDF facets/predominant pitch/imagery via pdftotext; recommended-waste column resolved by char offset in -layout mode; derived squares/waste-adjusted/low-slope; stories/layers inference); 5 estimate_runs + 10 source docs + 5 measurements + 127 measurement fields + 5 open dashboard work items (penetrations missing) + 5 §5.2 verification checks (pass_with_exceptions) loaded to rnhmvcpsvtqjlffpsayu
**Decisions**: Waste-adjusted squares read from Roofr's OWN recommended-column Squares value, never recomputed — property-level table identified by back-computing raw=adj/(1+pct) and matching total OR pitched squares (multi-structure reports list structure tables first, report summary last; flat-roof reports may have no table → 10% default fallback flagged at 0.7 confidence); pitched-scope matches add flat area unwasted; Roofr flat area already contains 0-2/12 buckets → low_slope=max not sum; stories/layers inferred from two-story/two-layer areas at 0.7 confidence; penetrations/vents structurally absent from Roofr → always a dashboard task until CompanyCam hook lands
**Tests**: PGlite full-loader validation 2x (idempotent, 127 fields); waste values verified against all 5 PDFs by hand (17.7@17, 33.77@20+flat, 19.65 fallback, 20.0@19+flat, 55.1@8 property table of 3); live counts equal PGlite counts
**Open threads**: No property links (sample addresses not in live properties table — KC/Wichita/DFW samples vs Collin CAD data); CompanyCam review hook is stubbed (companycam_reviewed=false, no API access yet); 5 open ops tasks for penetrations/vents counts; Phase 3 (Pricing Engine: branch selection + live ABC pricing + UOM rounding + GBB scenarios) is next

## Session 10

**Goal**: Phase 3 (Pricing Engine) — branch selection, mirror pricing, UOM rounding, GBB scenarios, margin checks
**Deliverables**: scripts/evaluate-abc-branches.mjs (geocode via GOOGLE_MAPS_SERVER_KEY when set, Census Bureau fallback; haversine ranking over abc_vendor_branches latlng; Routes API drive time when key exists); scripts/build-estimate-scenarios.mjs (§4.6 qty rules per qty_basis, SQ↔BD conversion, alternate shingle/ridge handling, §4.7 cost components, price=cost/(1-gm-comm-fees)); estimating: section added to roofer.config.example.yaml + client config (all placeholder rates flagged TODO Ops). Loaded live: 25 branch evals, 5 pricing requests, 73 pricing lines, 20 scenario options, 510 scenario lines, 180 cost components, 20 margin checks, 25 verification rows (pricing+scenario, pass_with_exceptions)
**Decisions**: Chris's Maps key is referrer-restricted (*.proexteriorsus.net) → Google REJECTS it for server-side Geocoding/Routes (no referer spoofing); stored as GOOGLE_MAPS_BROWSER_KEY in gitignored .env; GOOGLE_MAPS_SERVER_KEY (IP-restricted) still needed for drive times. Pricing: agreement > price list > order-history avg, ALL stamped static_fallback per §4.5 — live ABC pull is a hard gate before proposal approval. Dual shingle lines in a template = alternates (first primary, rest human-select qty 0) — prevented double-count. Big discovery: VM can reach Supabase REST with service-role key from mounted .env → bulk loads now run from the sandbox directly (no more SQL pasting through MCP).
**Tests**: 5/5 branch picks sensible (Arlington/Garland/N.KC/Burleson/Wichita 3–9mi); 20 scenarios priced to exactly 40% GM; materials reconcile (option cc = Σ line costs, verified); engine alternate-fix validated (T4 materials 5041→2775 after Tamko de-dup)
**Open threads**: GOOGLE_MAPS_SERVER_KEY needed for drive-time ranking; Ops must confirm estimating rates (labor/disposal/commission/etc — all placeholder_config); ship-to selection rules per branch/market pending; OC shingles + 9 other items unpriced in mirror (needs_human); 2732 S Minnesota is all-flat — needs a real low-slope template; Phase 4 (Proposal + Invoice drafting) next

## Session 11

**Goal**: Phase 4 (Proposal + Invoice drafting) + record Chris's pricing waterfall decision
**Deliverables**: docs/33 §4.5 pricing waterfall added (PDF price list → live API w/ Ops+Accounting dashboard approval → invoice history ≤90d → no-price flag); engine unitCost updated to waterfall tiers (order_history_avg fallback removed; invoice_history_90d tier added); scripts/build-proposal-pdf.py (reportlab GBB proposal: roof summary, side-by-side options, 7 §4.8 initials blocks, signature block, decking allowance/change-order, 3-day cancel, DRAFT watermark + footer); 5 proposal PDFs in outputs/proposal-drafts-2026-06-10/; loaded live: 5 estimate_proposals (sha256 in metadata), 5 deposit invoice drafts (amount null until client selects; 30% placeholder), 5 approval tasks (ops_manager), 5 dashboard work items (estimate_proposal_approval), 10 verification rows (§5.5 proposal + §5.6 invoice, pass_with_exceptions)
**Decisions**: PRICING WATERFALL (Chris): ingested PDF price list → live API (pulled price needs Ops+Accounting dashboard approval) → invoice ≤90d → flag; checked 90d invoice coverage for 10 previously order-history-priced items: 6 covered, 4 (Karnak primer, MH cap sheet black, RVG-55F, power vent) would flag no-price — loaded Phase 3 scenarios keep superseded labels until next engine run/live API. Tier labels Good/Better/Best assigned by price order as CANDIDATES (T4 GAF < T2 Highlander < T1 Vista), OC template excluded (unpriced shingle); proposals watermarked DRAFT — approval flow owns watermark removal. Maps server-key instructions sent to Chris (IP-restricted, Geocoding+Routes).
**Tests**: 5/5 PDFs generated; page renders visually checked (p1 GBB table + prices, p3 initials/signature/watermark); option totals in PDF = scenario totals; REST loads verified by count (5/5/5/5/10)
**Open threads**: GOOGLE_MAPS_SERVER_KEY pending Chris; deposit % policy + labor rates + warranty tiers still placeholder (proposal approval gates); live ABC pull (waterfall tier 2) not wired; Phase 5 (AccuLynx + Slack handoff) next — needs AccuLynx API capability discovery (acculynx_api_catalog table exists)

## Session 12

**Goal**: Phase 5 (AccuLynx + Slack handoff) + server Maps key wired
**Deliverables**: GOOGLE_MAPS_SERVER_KEY stored (gitignored .env; Chris will rotate post-testing), Geocoding+Routes verified, 25 branch evals backfilled with drive times — drive-time ranking flipped 3 selections (2226: 1316→041 Dallas; 4521: 312→1330 Mesquite; 1424: 304→006 KC), pricing requests annotated; docs/37-acculynx-write-capability-matrix.md (full §4.10 target × API V2 endpoint matrix from acculynx-api skill + 198-probe history); scripts/build-acculynx-handoff.mjs (fallback packet: job-create fields, measurements+doc hashes, GBB totals, branch, proposal ref, invoice basis, 8-step manual checklist, Slack mrkdwn draft); loaded live: 5 estimate_acculynx_handoffs (dashboard_fallback, fallback_posted), 5 dashboard work items (estimate_acculynx_handoff), 5 §5.9 verification rows
**Decisions**: STRUCTURAL FINDING — AccuLynx API V2 has NO write endpoints for milestones/statuses, invoices, material orders, or crew schedules; the §4.10 fallback is permanent for those four regardless of the API team's reply. Automatable subset once a write-scoped key lands: contact+job create, custom fields, proposal/measurement uploads, worksheet items (POST /financials/{id}/worksheet/items), job messages (write-only), reps, external references (run_id as idempotency anchor per §5.9). All 198 historical probes were GET/OPTIONS — write scope unverified; next probe batch must POST against a test job only.
**Tests**: geocode OK (Google); Routes drive times returned for all 25 candidate branches; handoff REST loads verified by count (5/5/5); Slack draft spot-checked
**Open threads**: AccuLynx write-scoped key + POST probe batch (test job) when API team responds; Slack channel for handoff drafts not yet chosen (drafts stored in packet payload); Maps server key rotation after Chris's testing — evaluator reads env so rotation is config-only; Phase 6 (Order + Schedule drafting) next

## Session 13

**Goal**: Labor intelligence from 7 completed job packages + internal Ops estimates + Phase 6 (order/schedule drafts)
**Deliverables**: schemas/cleverwork-roofer/92-labor-rates.sql applied (labor_observations append-only + labor_rates per office w/ proposed→Ops-approved lifecycle + effective_from/to change tracking); 30 labor observations loaded from KS-72/91/109/111/113/136/139 (7 roofing crew invoices read visually — scanned PDFs, no text layer; gutters/paint/other lump sums at 0.7 confidence from filenames); 10 proposed Wichita rates (base install median $80/sq n=7 range 80-85; decking $10-15/sheet; extra layer $5-10/sq; 8/12 adder $5/sq; soffit vent $15/ea; ridge vent $2/lf; cricket $250; pickup $150); learning event (pricing_rule_candidate) + HIGH-priority Ops work item labor-rates-wichita-v1; scripts/build-internal-estimate.py (landscape Ops doc: GBB margin matrix Material/Labor/Fees, full line detail w/ NEEDS-HUMAN highlighting, placeholder-vs-observed labor comparison) — 5 generated + linked into proposal approval work items; scripts/build-order-schedule-drafts.mjs — 5 material order drafts (§4.11, Better-option pre-selection, fail-closed gates) + 5 schedule recommendations (§4.12, crew-days w/ complexity factors, crew candidates from observations, crew_ref null) + 10 verification rows
**Decisions**: Real Wichita crew labor is $80-85/sq base + itemized adders — roughly HALF the $165/sq engine placeholder (≈$1.4K margin upside on a 17.7sq job); per trust discipline the engine keeps the placeholder until Roberto approves labor-rates-wichita-v1; rates derived as MEDIAN of observations; rate changes close prior rows (effective_to) so labor_rates history = pricing-change track per office; folder 'KS-11 Doc Package' actually contains KS-111 (invoice codes are truth, not folder names); KS-136 payment carries an unrelated KS-79 backcharge — settled_amount ≠ rate evidence
**Tests**: 30 observations + 10 rates live (counts verified); internal estimate p1 visually checked; observed-vs-placeholder math spot-checked (1424: $1,534.50 observed vs $2,920.50 placeholder)
**Open threads**: Ops approval of Wichita rate table → then switch engine to labor_rates model + reprice; production sq/day placeholder (25) has no timing observations; gutter invoices have line detail unread (lf rates extractable later); permit invoice amounts unread (would inform permit allowance); low-slope labor has zero observations (2732 needs them most); Phase 7 (verification + learning loop wiring) largely in place organically — formal pass next

## Memory Health
- 2026-06-10 scheduled check: CAP COMPLIANCE flagged — SOUL.md at 2,051 chars (within 5% of ~2,100 target); consolidate per meta-memory-write rules. Capture, index freshness, hygiene all green.

## Session 14

**Goal**: Finish and publish the vendor-map/Supabase-infrastructure live line, record Ghost and Dolt lab KBs, and reconcile the Git graph so origin/main contains all completed work.
**Deliverables**: Full KB packs for `integrations/bridges/supabase-infrastructure/`, `integrations/bridges/ghost/`, and `integrations/bridges/dolt/`; new preflight scripts `scripts/ghost-lab-preflight.mjs` and `scripts/dolt-lab-preflight.mjs`; enhanced `scripts/supabase-preflight.mjs` public-table Data API grant/RLS warning; updated `/supabase-change-preflight` skill; updated `docs/36-supabase-infrastructure-ops.md`; final handoff archive `docs/handoffs/archive/2026-06-10-1725.md`.
**Git outcome**: Vendor territory map/pricing-waterfall work was already on `origin/main` at `911564d`; Supabase/Ghost/Dolt KB work landed as `bcb24ed` and `d51d80a`; final handoff/memory verification landed as `0dea04f`; the estimate-pipeline line at `ed37328` and the vendor-map/Supabase-infrastructure line were merged so `origin/main` can contain both completed histories.
**Final push audit**: AccuLynx worktree commits `b148b20` (blocker email draft) and `438f958` (API docs/skill resources) were committed, pushed, and merged into the main integration line. Remote branch heads were verified on GitHub. The canonical checkout still has local raw/generated/unreviewed files and was not bulk-committed because repo policy blocks committing raw exports, local logs, screenshots, and likely sensitive accounting/price-agreement artifacts without a separate safety review.
**Verification**: Ghost preflight passed against `pro-exteriors-open-brain-lab` with user-level config access; Dolt preflight passed DoltHub credential auth; Supabase offline preflight passed with expected non-production warnings; Node syntax checks passed for all three preflight scripts; JSON metadata parsed; diff whitespace checks passed; changed KB/script paths scanned clean for the secret-shaped patterns checked.
**Decisions**: Supabase remains the live Open Brain source of truth; Ghost is a disposable Postgres lab for sanitized experiments/restore drills; Dolt is for approved non-sensitive data-diff pilots only; destructive production Supabase work requires named target, fresh backup/clone proof, migration/SQL artifact, rollback path, and exact human approval.
**Open threads**: Reconcile the canonical checkout carefully before using it because it was dirty/divergent; continue vendor territory map audits (Pro Exteriors regions/office verification, ABC branch API-to-vendor matching, weekly API pull/null backfill, price-agreement item completeness audit).
