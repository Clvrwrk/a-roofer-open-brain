# 2026-06-10 Memory

## Session - Vendor Map Live State And Supabase Infrastructure KB

**Goal:** Finish and publish the Supabase/Ghost/Dolt infrastructure knowledge base work, verify Git state, and leave a durable handoff/memory trail.

**Git outcome:**

- Vendor Territory Map pricing-waterfall work was already on `origin/main` at `911564d`.
- Supabase/Ghost/Dolt KB completion landed as `bcb24ed` and `d51d80a` on `contrib/cleverwork/supabase-ops-knowledge-live`.
- `origin/main` was fast-forwarded from `911564d` to `d51d80a`.

**Delivered:**

- Full Supabase infrastructure resource pack under `integrations/bridges/supabase-infrastructure/`.
- Full Ghost lab resource pack under `integrations/bridges/ghost/`.
- Full Dolt data-diff lab resource pack under `integrations/bridges/dolt/`.
- New preflight scripts: `scripts/ghost-lab-preflight.mjs`, `scripts/dolt-lab-preflight.mjs`.
- Enhanced `scripts/supabase-preflight.mjs` with public-table Data API grant/RLS review warnings.
- Updated `.codex/skills/supabase-change-preflight/SKILL.md`.
- Updated `docs/36-supabase-infrastructure-ops.md`.

**Verified:**

- Ghost CLI/MCP/auth/lab database preflight passed with user-level config access.
- Dolt CLI/DoltHub credential preflight passed; the app repo is intentionally not a Dolt repo.
- Supabase offline preflight passed with expected non-production warnings.
- Node syntax checks, JSON metadata parse, `git diff --check`, staged diff check, and changed-path secret scan passed.

**Safety decisions:**

- Do not repeat or store secrets pasted in chat.
- Ghost is a disposable Postgres lab, not the source of truth.
- Dolt is for approved non-sensitive data-diff pilots only.
- Supabase remains the live Open Brain source of truth.
- Destructive production Supabase work requires backup/clone proof and exact human approval.

**Next start:**

- Reconcile the canonical checkout carefully because it was observed dirty and divergent.
- Resume vendor territory map data audits: Pro Exteriors office/region verification, ABC branch API-to-vendor table matching, weekly vendor API pull/null backfill, and price agreement item completeness audit.
