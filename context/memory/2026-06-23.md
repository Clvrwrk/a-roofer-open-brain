## Session — ABC Supply nightly sync (scheduled)

**Goal**: Full ABC production pull (catalog, branches, regions) via integrations/bridges/abc-supply/production-sync.mjs.
**Result**: FAILED (env=production). rowsUpserted — catalog: 0, vendorBranches: 0, regions: 0. No summary JSON produced.
**Root cause**: Cowork sandbox runs each bash call under `bwrap --die-with-parent`; backgrounded/`setsid`/`nohup` process is killed the instant the launching call returns, and the bash tool caps a single (foreground) call at 45s vs the job's 20–45 min. Detached run reached only "account detail 5/32" before being killed; catalog pagination never started. Same truncation in every prior daily log (Jun 19–22) — recurring environmental blocker, not a credential/API failure (creds present, token not yet exchanged at point of kill).
**Fix needed**: run this sync on a persistent host (e.g. the Hetzner/Coolify agent host or a cron on the box), not the Cowork ephemeral VM.

## Session — Sentry hardening (agent host)

**Goal**: Close the two Sentry open threads from MEMORY.md.
**Deliverables**:
- Rotated the exposed Sentry `sntrys_` build token (had been chat-exposed) and updated it in Coolify.
- Configured the nightly Sentry sync on the agent host.
**Decisions**: Sentry work done server-side on the persistent agent host (not the sandbox), same rationale as the ABC sync blocker.
**Verification note**: Confirmed against live DB that the ABC full sync is still owed — `abc_api_sync_runs` shows no run since 2026-06-18 and the last catalog-bearing run was 2026-06-05 (`abc_product_catalog`=331,293 / `abc_vendor_branches`=720 / `abc_regions`=40, consistent with the 06-05 pull). ABC FULL prod-sync remains the standing open item.
**Open threads**: ABC FULL prod-sync still owed (run on agent host); [GPA] Denver/Dallas promote + price crons (2026-07).
