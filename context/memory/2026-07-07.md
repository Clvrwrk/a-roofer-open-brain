## Session — Pipeline KPI consistency + Signed(7d) pill + trailing-7 diagnosis

**Goal**: forensic-audit follow-ups on the executive pipeline KPIs (Chris).

**Shipped** (main 46a56ff, live on cc.proexteriorsus.net):
- Sold Value / Jobs Sold headlines now = sum of their close-date Res/Com split (was closeRate.soldValue/soldCount = all-sold-stages). Avg Ticket headline = blended overall, not residential-only. (commit 19545c6)
- Rep Leaderboard + per-location "sold value" moved to close-date (closed-in-window only) — reconcile with the ~$9.2M headline instead of the ~$18.76M all-sold-stages sum. `queueForRow(row)==='closed'`.
- New **Signed (7d)** KPI pill: jobs that entered Approved in the trailing 7 days (computeSigned7d — real Approved history date, crm approved_date fallback, NEVER milestone_date; obeys filter bar, ignores window selector). Live value = 2 jobs / $0 (matches Prospect→Approved trailing pill; amounts not yet entered).
- Margin % split renders "—" for a segment with 0 cost-data coverage (marginCoverageSplit) instead of a misleading "0%".
- 157 unit tests green (added computeSigned7d test).

**Trailing-7 "Invoiced→Closed 73 jobs / 0 of 73 with history" — DIAGNOSED (data artifact, NOT fixed yet)**: 71 of the 73 share `milestone_date = 2026-07-04` (a Sat holiday) with NO Closed milestone-history entry; the pill falls back to milestone_date and counts them, inflating "$1.53M closed this week." Root: milestone_date is an unreliable close-date fallback (bulk/artifact value); history is the reliable source but lags/absent for these. Honest caption "0 of 73 with history" is surfacing the data problem. Recommended fix (pending Chris): make trailing-7 closed/invoiced pills history-only (drop milestone_date fallback) — removes the spike; genuinely-recent closings appear once the hourly walk ingests their Closed transition.

**Open threads**: (1) trailing-7 history-only fix — awaiting go-ahead. (2) Definitional note: leaderboard is now close-date (credits rep at close, not signing) — Chris chose this; the Signed(7d) pill covers the "signed this week" view.

## Session — AccuLynx links, trailing-7 history-only, AR-aging deep-link (LIVE 3c58de4)

**Shipped** (main 3c58de4):
- **AccuLynx job links fixed**: `proexteriors.acculynx.com` (NXDOMAIN — made-up per-tenant subdomain) → `https://my.acculynx.com/jobs/{acculynx_job_id}` (Chris-confirmed format). Env-overridable via `ACCULYNX_JOB_BASE_URL`. Files: pipeline.astro + scripts/executive-pipeline.ts.
- **Trailing-7 now HISTORY-ONLY** (Chris approved): `resolveTransitionDate` drops the milestone_date fallback → returns null when no real milestone-history transition. Removes the artifact spike (71 closed jobs stamped milestone_date 2026-07-04 w/ no Closed history faked "$1.5M closed this week"). Genuinely-recent transitions appear once the hourly walk ingests them. Updated 5 trailing-7 tests to supply history.
- **Highest AR Aging deep-links to its job**: `computeHighestArAgingInvoice` returns oldest-open-invoice job_id; loader resolves job_number+account_key (`highestArAgingJob`); pill shows a "Job TX-347"-style link that opens (`details.open=true`) the office row below and scrolls/flashes the job (`tr[data-acculynx-job-id]`, `.epl-job-flash`). Live: 579d = TX-347 Weldon James (texas).
- 158 unit tests green.

**Definitional model now canonical (executive pipeline)**: "Sold" = CLOSE DATE (closed-in-window) everywhere — headline, leaderboard, per-location. Milestone-history (`acculynx_job_milestone_history`) is THE per-stage-date source of truth; `updated_at`/`created_at` fallback retired for windowing; `milestone_date` is unreliable (bulk artifacts). Signed(7d) pill = the "signed/approved this week" view.

**Open threads**: (1) `totalMoniesCollectedThisWeek` in computeTrailing7dPillsV2 STILL uses `approved_date ?? milestone_date ?? updated_at` fallback (same artifact risk as trailing-7 had) — candidate for history-only too. (2) Leaderboard credits rep at CLOSE not signing (Chris chose this). (3) 4,609 blank-job-number csv_initial rows ($1.5M leads/cancelled) still un-reconciled. (4) Sandbox git refs lock-jammed all session — commits pushed via `commit-tree`→`push origin <sha>:main`; origin/main=3c58de4 authoritative, local HEAD stale at 5f969e5 (resolves on next fetch).
