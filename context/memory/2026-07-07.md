## Session — Pipeline KPI consistency + Signed(7d) pill + trailing-7 diagnosis

**Goal**: forensic-audit follow-ups on the executive pipeline KPIs (Chris).

**Shipped** (main 46a56ff, live on cc.proexteriorsus.net):
- Sold Value / Jobs Sold headlines now = sum of their close-date Res/Com split (was closeRate.soldValue/soldCount = all-sold-stages). Avg Ticket headline = blended overall, not residential-only. (commit 19545c6)
- Rep Leaderboard + per-location "sold value" moved to close-date (closed-in-window only) — reconcile with the ~$9.2M headline instead of the ~$18.76M all-sold-stages sum. `queueForRow(row)==='closed'`.
- New **Signed (7d)** KPI pill: jobs that entered Approved in the trailing 7 days (computeSigned7d — real Approved history date, crm approved_date fallback, NEVER milestone_date; obeys filter bar, ignores window selector). Live value = 2 jobs / $0 (matches Prospect→Approved trailing pill; amounts not yet entered).
- Margin % split renders "—" for a segment with 0 cost-data coverage (marginCoverageSplit) instead of a misleading "0%".
- 157 unit tests green (added computeSigned7d test).

**Trailing-7 "Invoiced→Closed 73 jobs / 0 of 73 with history" — DIAGNOSED (data artifact, NOT fixed yet)**: 71 of the 73 share `milestone_date = 2026-07-04` (a Sat holiday) with NO Closed milestone-history entry; the pill falls back to milestone_date and counts them, inflating "$1.53M closed this week." Root: milestone_date is an unreliable close-date fallback (bulk/artifact value); history is the reliable source but lags/absent for these. Honest caption "0 of 73 with history" is surfacing the data problem. Recommended fix (pending Chris): make trailing-7 closed/invoiced pills history-only (drop milestone_date fallback) — removes the spike; genuinely-recent closings appear once the hourly walk ingests their Closed transition.

**Open threads**: (1) trailing-7 history-only fix — awaiting go-ahead. (2) Definitional note: leaderboard is now close-date (credits rep at close, not signing) — Chris chose this; the Signed(7d) pill covers the "signed this week" view.

## Session — AccuLynx links, trailing-7 history-only, AR-aging deep-link (LIVE 3c58de4)

**Shipped** (main 3c58de4):
- **AccuLynx job links fixed**: `proexteriors.acculynx.com` (NXDOMAIN — made-up per-tenant subdomain) → `https://my.acculynx.com/jobs/{acculynx_job_id}` (Chris-confirmed format). Env-overridable via `ACCULYNX_JOB_BASE_URL`. Files: pipeline.astro + scripts/executive-pipeline.ts.
- **Trailing-7 now HISTORY-ONLY** (Chris approved): `resolveTransitionDate` drops the milestone_date fallback → returns null when no real milestone-history transition. Removes the artifact spike (71 closed jobs stamped milestone_date 2026-07-04 w/ no Closed history faked "$1.5M closed this week"). Genuinely-recent transitions appear once the hourly walk ingests them. Updated 5 trailing-7 tests to supply history.
- **Highest AR Aging deep-links to its job**: `computeHighestArAgingInvoice` returns oldest-open-invoice job_id; loader resolves job_number+account_key (`highestArAgingJob`); pill shows a "Job TX-347"-style link that opens (`details.open=true`) the office row below and scrolls/flashes the job (`tr[data-acculynx-job-id]`, `.epl-job-flash`). Live: 579d = TX-347 Weldon James (texas).
- 158 unit tests green.

**Definitional model now canonical (executive pipeline)**: "Sold" = CLOSE DATE (closed-in-window) everywhere — headline, leaderboard, per-location. Milestone-history (`acculynx_job_milestone_history`) is THE per-stage-date source of truth; `updated_at`/`created_at` fallback retired for windowing; `milestone_date` is unreliable (bulk artifacts). Signed(7d) pill = the "signed/approved this week" view.

**Open threads**: (1) `totalMoniesCollectedThisWeek` in computeTrailing7dPillsV2 STILL uses `approved_date ?? milestone_date ?? updated_at` fallback (same artifact risk as trailing-7 had) — candidate for history-only too. (2) Leaderboard credits rep at CLOSE not signing (Chris chose this). (3) 4,609 blank-job-number csv_initial rows ($1.5M leads/cancelled) still un-reconciled. (4) Sandbox git refs lock-jammed all session — commits pushed via `commit-tree`→`push origin <sha>:main`; origin/main=3c58de4 authoritative, local HEAD stale at 5f969e5 (resolves on next fetch).

## Session — open-thread cleanup (#1 monies history-only, #2 lead-stage dups, #3a nightly-sync cancel)

**Shipped/done**:
1. **Monies Collected this week = HISTORY-ONLY** (main `503d0b0`): `totalMoniesCollectedThisWeek` now keys on `resolveTransitionDate` (real milestone-history stage-entry), not `approved_date??milestone_date??updated_at` — sheds the same bulk/artifact risk the trailing-7 pills shed. 158 tests green.
2. **4,609 blank-job-number csv_initial rows reconciled**: 4,301 DEAD (excluded from all KPIs — harmless, left as history) + 307 active. Of the active, **295 duplicate a live job by name+city+state → quarantined+removed** (backup `crm_pipeline_leadstage_orphan_quarantine`, reversible); 12 unique legacy leads kept. Fixes minor New Leads count + up to ~$129K Pipeline Value inflation.
3a. **Cowork `abc-supply-nightly-sync` scheduled task DISABLED** (docs/67 §5) — systemd on Hetzner agent host owns the ABC sync; the Cowork copy only ran a few seconds before the sandbox killed it.

**Still open (need Chris — do NOT auto-execute)**:
- 3b. **Automate the daily disposition pass** (docs/67 §6 item 1): DECISION needed — named agents lack `approval.decide` BY DESIGN. Options: (i) mint fresh `AGENT_SERVICE_TOKEN_SHA256_ALEX-RIVERS` + grant approval.decide; (ii) run under a system actor (automate WITHOUT giving a named agent approval rights — recommended); (iii) daily human click (safe, not automated). Pass is one curl away; negotiated gate keeps it safe until decided.
- 3c. **Widen tick timers to the other 7 agents** (docs/67 §6 item 2): staged per the production-gating rule (one agent at a time, human-approved each step) + needs SSH to the Hetzner agent host `5.78.146.161` — NOT reachable from the Cowork sandbox. Do from the host: enable `openbrain-dev-tick@<email-prefix>.timer` + re-enable Hermes per agent.
- 12 unique legacy leads (blank job#, no live match) + 4,301 dead csv rows remain in crm_pipeline (harmless).

## Session — Agent coordination plan (docs/70) + Phase 1 email/Slack correctness

**Goal**: /plan the credit-memo & price-agreement workflows + agent coordination stabilization, then execute Phase 1 (Chris).

**Shipped (docs/70 written; Phase 1 config edits staged, NOT committed/deployed)**:
- **docs/70-agent-coordination-stabilization-and-migration-plan.md** — full plan: current-state map (agents/AgentMail, credit-memo & price-agreement workflows, Slack portal, migration stack), 5 gated phases (email/Slack correctness → reconcile conflicts → close the loop → autonomous layer → gated migration), §7 verification checklists, §11 decisions (all recommended + folded into phase bodies).
- **§11 decisions (baked in)**: 11.1 Sam Torres/QA → route via ob-conductor, no cap raise. 11.2 cadence = config-as-data in config/. 11.3 token policy = strict for identity posts, fallback for system only. 11.4 Convex=defer, Vercel=reject; Greptile + minimal GH Actions = conditional_pilot.
- **P1.1** `agents/profiles/sam-torres.yaml` service_agent_id `ob-auditor`→`ob-conductor` (+ note in `agentmail.ts` omitted roster). No persona references ob-auditor now.
- **P1.2** `scripts/write-cron-jobs.py` + `scripts/deploy-crons.py` — ALL raw cron report delivery repointed from the 3 human-operational channels to `#ob-agents-internal` (C0BD8U44HL3), field + prompt text consistent. Header flags that time-critical posts (storm, expired-agreement urgent) now land internal → Phase 3 must give Conductor an escalation path. Both scripts parse.

**Verification (live, this session)**:
- §7.1 AgentMail roster PASS: code's 10 usernames == 10 provisioned inboxes (roster JSON), cap 10/10, Sam resolves real (ob-conductor).
- §7.3 Slack read-only PASS: 9 bot tokens valid + distinct identities in `pe-command-center`; all 7 named bots members of the 3 operational channels.
- `#ob-agents-internal` (C0BD8U44HL3) is a real PRIVATE channel — only ops_conductor was in it; Chris invited the other 7 report bots → all 8 now members (verified).
- P1.4 post-as-each-bot identity test PASS: all 8 bots posted to #ob-agents-internal under distinct bot IDs and self-deleted. (conversations.invite via ops_conductor blocked on missing_scope groups:write → Chris did manual /invite.)

**Open threads**:
1. **Deploy P1.2**: run `deploy-crons.py`/`write-cron-jobs.py` against agent host `5.78.146.161` (SSH; not reachable from Cowork sandbox) to push new routing.
2. **Persist Phase 1 edits**: 4 files (sam-torres.yaml, agentmail.ts, write-cron-jobs.py, deploy-crons.py) staged in working tree on `contrib/cleverwork/pipeline-milestone-date-rebuild` (unrelated to that branch's pipeline work) — clean path: branch `phase1-email-slack-correctness` off origin/main, move edits over, merge agentmail.ts to main for Coolify.
3. **P1.4 router test**: needs a real human message in #accounting-vendor-intake (router ignores bot posts) + live socket runtime — Chris to type a test line, expect alex_rivers reply as itself not @openbrain.
4. **Phase 2 scoping** in progress: cadence/identity/token reconciliation (docs/71).

## Memory Health

- 2026-07-07 healthcheck: CAP COMPLIANCE flagged — `context/MEMORY.md` at 2477/2500 chars (within 5% of cap). Consolidate via meta-memory-write to bring it back under budget.

## Session — Untangle git tree (Phase 1 edits persisted to main, LIVE 5dca60f)

**Goal**: resolve open thread #2 — Phase 1 email/Slack edits were stranded uncommitted on `contrib/cleverwork/pipeline-milestone-date-rebuild` (stale local HEAD 5f969e5), mixed with already-shipped pipeline changes.

**Diagnosis**: the 5 pipeline source files showed **0-diff vs origin/main** — already live via the earlier commit-tree push; the working-tree "changes" were stale-index noise. True delta vs origin/main = only the 4 Phase 1 files + docs/70 + docs/71 + a superset daily log + a root-level YTD review.

**Done (main `2992147`, `5dca60f`; deployed, healthz buildCommit=5dca60f, status ok)**:
- Cleared the sandbox git `.lock` jam via `allow_cowork_file_delete` + `rm` (the clean remedy per cowork-sandbox-quirks); set local git identity chussey1976/chussey@cleverwork.io (was unset → commits failed).
- New branch `contrib/cleverwork/phase1-email-slack-correctness` off origin/main; force-checkout to drop the redundant pipeline edits (all == main); restored ONLY the genuine deltas from an outputs backup (sha256-verified).
- Commit 2992147 feat(agents): Phase 1 correctness — sam-torres.yaml (ob-auditor→ob-conductor), agentmail.ts roster note, write-cron-jobs.py/deploy-crons.py (reports→#ob-agents-internal), docs/70, docs/71. Scripts py_compile OK; agentmail.ts change is string-only.
- Commit 5dca60f docs(memory): superset daily log + **docs/72** — moved the root-level `pipeline-ytd-sold-value-review.md` finding into docs (YTD "sold value" $45.68M inflated by 819 undated jobs → honest 2026 ~$19.3M).
- FF push to main; Coolify redeployed clean. Local tree empty; `pipeline-milestone-date-rebuild` left untouched (its work was already in main).

**Still open (unchanged by this session — need Chris / the Hetzner host, NOT reachable from Cowork)**:
1. **Deploy P1.2 cron routing** — run `deploy-crons.py` on agent host `5.78.146.161` (SSH). Merged to main = versioned only; agent posting behavior unchanged until then. NB: repointing reports to #ob-agents-internal means storm/expired-agreement urgent posts land internal → Phase 3 must give Conductor an escalation path.
2. **P1.4 router live test** — needs a human line in #accounting-vendor-intake + live socket runtime.
3. **3b disposition automation** (docs/67 §6) — DECISION pending (system-actor rec).
4. **3c widen tick timers** — staged, needs the Hetzner host.

## Session — SSH to agent host: verify P1.2 (already live) + stage 3c (sam.torres)

**Goal**: Chris granted SSH to the Hetzner agent host to unblock P1.2 (cron routing) + 3c (widen tick timers). Full path authorized (recon → P1.2 → staged 3c).

**Access**: `root@5.78.146.161` (pe-ob-agents, Ubuntu 24.04). Reachable from Cowork (:22 egress OK). Key `~/.ssh/a_roofers_open_brain_ed25519` copied into mounted `~/.config/cleverwork/`; loaded at /tmp/ob_key. `master.env` there = big cross-project vault but NO ssh/host → didn't unblock; the ed25519 key did.

**Runtime model discovered** (see memory agent-host-runtime-model): agents = Kasm profiles; crons at `/mnt/kasm_profiles/<email>/<IMG>/.hermes/cron/jobs.json`; **write-cron-jobs.py is the real deployer**, **deploy-crons.py is a dead print-only catalog** (no apply logic — the docker-grep get_container is a red herring). Execution = `openbrain-dev-tick@<prefix>.timer` (~60s) → agent-tick.sh → `docker run … hermes cron tick` (runs due jobs; no-op tick ~2s).

**Findings / done**:
- **P1.2 = ALREADY LIVE**: all 7 agents' deployed jobs.json already deliver to `#ob-agents-internal` (C0BD8U44HL3): maya6/alex4/casey2/jordan3/sam2/rowan5/lena4. No deploy needed. Backed up all 7 → `/opt/openbrain/cron-backup-20260707-2345`. (Host-repo scripts/ still stale human-channel version — functionally irrelevant; converge later.)
- **3c staged**: only `alex.rivers` was ticking (and NOT boot-persistent). Enabled `sam.torres` (`systemctl enable --now`) as the low-risk first widening + boot-persisted `alex.rivers`. Both now `is-enabled` (timers.target.wants symlinks). Sam's first tick verified clean (Result=success, exit 0, 2s no-op — nothing due tonight; his jobs are Wed 9am / Fri 4pm cron).

**Open threads**:
1. **Widen remaining 5 agents** (casey.morgan, jordan.price, rowan.vale, lena.brooks, maya.chen) — one at a time per gating rule; **maya-gmail-poll is every 2m** (recurring LLM+Gmail cost) — decide before enabling Maya.
2. **Host repo hygiene**: `/opt/openbrain/a-roofers-open-brain` on `contrib/cleverwork/host-scheduler` (origin=local bundle), scripts stale vs main — converge if we want the repo copy to match deployed state.
3. Delete the copied key from `~/.config/cleverwork/` + unmount that folder after session.

**UPDATE (same session)**: Chris chose to enable ALL remaining agents. 3c now COMPLETE — all 7 ticks enabled + boot-persistent (alex, sam, casey, jordan, rowan, lena, maya). Verified: all ticks Result=success; real agent-run hermes containers spawn `--rm` and exit clean (no crash-loop); load ~0.44, mem 4.6/31G; no tick failures in 3-min scan. **maya-gmail-poll every-2m is LIVE** (recurring LLM+Gmail cost from 23:54 UTC 7/7). Open thread #1 (widen agents) RESOLVED; #2 (host-repo hygiene) + #3 (delete copied key / unmount cleverwork) remain.
