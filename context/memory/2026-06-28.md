# 2026-06-28 Session Log

## Session — ABC Supply nightly production sync (scheduled, autonomous)

**Goal**: Run the full ABC Supply production data pull (catalog + branches + accounts) via `integrations/bridges/abc-supply/production-sync.mjs --env=production`.

**Run record**: 2026-06-28T13:40Z · env=production · token exchange **200 OK** (scopes returned in full).
- **Accounts → `abc_regions`**: COMPLETE — accountsFetched=32, detailsFetched=32, **rowsUpserted=0**, failed=[] (change-gated; data unchanged).
- **Catalog → `abc_product_catalog`**: VERIFIED reachable but NOT fully pulled — totalPages=333, totalItems=332,632; sampled page 1 = 200, **rowsUpserted=0** (change-gated ⇒ catalog already loaded & unchanged). ~11 s/page ⇒ ~60 min full pull.
- **Branches → `abc_vendor_branches`**: NOT completed — 697 branch-detail GETs (~5–6 min); script has no resume offset for branches (only `--branch-detail-limit`).
- **Audit**: NOT completed — exceeds budget.
- Rate-limited (429) responses: 0 across all phases.

**Outcome**: INCOMPLETE / BLOCKED by sandbox — not a data or auth failure. The Cowork scheduled-task VM reaps background (nohup/setsid) processes at every bash-call boundary, and foreground calls hard-cap at ~45 s. The full job is ~65–70 min, so neither the intended background-nohup path nor a foreground full pull can complete here. Only the 18 s accounts phase fits.

**Decisions**:
- Did not grind the catalog across ~111 foreground chunks: even a complete catalog pull would still leave branches/audit incomplete (no branch resume offset), so a "full pull (all three tables)" is structurally impossible in this environment.
- 0 rowsUpserted is the healthy change-gated steady state (catalog/accounts already loaded, nothing changed), NOT the "no rows upserted = FAILED" case — flagged for clarity.

**Open threads / recommendation**:
- Move this full-sync scheduled job to the persistent agent host (Hetzner/Coolify, `docs/27-hetzner-coolify-agent-host.md`) where `nohup` survives, OR run the script's intended `--review-due` cycle-counting nightly mode (refreshes only items due, not all 331k) instead of a full pull on the Cowork VM.

## Session — Agent runtime verification + Alex operational + daily SOPs

**Goal**: Get refreshed on the agent fleet; verify the runtime; connect agents to the dashboard and record programmatic work as desktop-equivalent. Then build Alex's SOPs.

**Findings (verified)**:
- Fleet is **dormant AND under-provisioned**: 29 crons deployed but all paused; no host scheduler; Kasm up but no agent containers running. App side fully live (13 service tokens, OAuth2 discovery, AgentMail 10 inboxes, `dashboard_action_log` seam exists).
- **Headless architecture proven**: `docker run` baked image (`openbrain-hermes-chrome:1.18.0-20260606`) + mounted profile → `hermes cron tick`. Alex ran a full autonomous headless loop end-to-end.
- **CRITICAL**: agent `.env`s incomplete — Alex missing ABC auth URL/scopes + `SUPABASE_SERVICE_TOKEN` (empty). Maya already complete; Jordan/Sam have NO `.env`.
- **Hard guardrail**: AI cannot launch `--yolo` autonomous agent on prod (classifier blocks even with a permission rule). Host systemd timer runs the agent; AI never launches it. First manual validation is human-run.

**Deliverables**:
- `docs/56-headless-agent-scheduler-design.md` (host-scheduler design + all findings).
- `skills/cleverwork-roofer/nepq-agent-communication/` (authored the fleet-wide comms skill, was missing).
- Installed `abc-supply-api` + `nepq-agent-communication` into Alex's Hermes profile (both enabled).
- `scripts/provision-agent-env.sh` (re-runnable .env provisioning; maps `SUPABASE_SERVICE_ROLE_KEY`→`SUPABASE_SERVICE_TOKEN`; excludes Rowan = external-only). **Applied → Alex now passes ABC OAuth 200 + Supabase 200.**
- `docs/57-alex-rivers-sops.md` — Alex daily SOPs designed: `agreement_ingestion_sweep`, `morning_abc_sync` (tiered variance 0-3% accept / 3-6% Lucinda / ≥6% hold+Casey), `variance_daily_summary`, `agreement_coverage_verification` (gaps→Jordan), `gap_followup_cadence` (7-day SLA, escalate Chris). + Slack approval handler. Agreement-priority branch model (main daily / 2hr weekly / 150 monthly).

**Decisions**: Recording v1 = direct Supabase insert to `dashboard_action_log` (Alex has service token); ABC token endpoint = `<auth_base>/v1/token` (Okta). Variance in ABC pricing UOM. Vendor-agnostic SOPs.

**Open threads**:
- **Alex weeklies/monthlies/quarterly/annual SOPs** (next walkthrough) — weekly owns Tier-2 (2hr) freshness + 0-3% digest.
- Build backlog (docs/57 §5): agreement ingestion + canonical schema, gap-tracking work items, GPA benchmark, Maya→Alex + Alex→Jordan contracts, Track C ≥6% hold, dashboard recording helper, host systemd scheduler.
- Provision Jordan/Sam profiles (no `.env`); fleet-wide `.env` audit + root-cause in `deploy-agent.py`.
- `open-engine/` (Open Engine = DevTeam Linear-queue orchestration) → moved to `agents/dev-engine/`, paths normalized, committed; byproducts gitignored. **Delineation contract = `docs/58`**: DevTeam plane (Open Engine, Linear, no brain) vs Roofing-Ops plane (Hermes crons, dashboard, brain). Hermes seam = separate profiles per plane. Build backlog in docs/58 §6 (dev Hermes profile, provisioning guard).

## Memory Health

- 2026-06-28 health check: CAP COMPLIANCE failed — context/MEMORY.md at 2481/2500 chars (within 5% of cap). Consolidate via meta-memory-write skill rules. All other checks green.

## Session — Alex SOP validation + Invoice Audit rebuild plan + cascade view (Task 1)

**Goal**: Run Alex's 5 daily SOPs one at a time vs prod to validate/correct each; then plan + begin the Invoice Audit UI/UX rebuild (SOP presentation to client as Alex/Maya).

**Deliverables**:
- `docs/57` rewritten — **morning_abc_sync v3**: scope = OPEN+unpaid+≥60d (rolling, daily full set); benchmark **cascade** (agreement → API `v_branch_item_api_price` → recent same-item/same-branch); per-invoice **gross $25 floor**; ≥6% hold+credit-memo, 3–6% weekly, recurring grouped, all-unmatched→Jordan. SOP1 re-scoped to freshness audit; SOP4 owner=Jordan; SOP5 digest rewritten; SOP3 triage = future refinement; §4 superseded.
- `docs/59` (APPROVED) — Invoice Audit rebuild plan: cascade columns, date1↔date2 filter + "Show all" (replaces status selector), Alex/Maya attribution by actor+role, per-invoice "Go back" (append neutral + reverse holds/draft credit memos), **D7 "Org Inv Price" column for credit memos**. 3 red-team passes. Deferred: 2 nest/disposition bugs (#10/#11).
- `schemas/cleverwork-roofer/154-invoice-audit-cascade.sql` — `v_invoice_audit_line_cascade` (applied to prod, additive). **Task 1 done, RT-1 passed.**

**Decisions**:
- Audit matching is NOT broken — "6% coverage" was a history-vs-recent artifact; daily SOP audits aged invoices, not 36mo history.
- Cascade view applied to prod early (additive) + source (154) converged to `main` — migration must match the live view.
- RT-1 fixes: org_inv ref-format (`-1` vs `-001` → base-number match, 0→106 CM lines); `recent` excludes prior credit memos.
- Live branch = `main` (re-confirmed via coolify skill, app `og0rmt02rff8qti9nlfk3nr7`).

**Commits (main)**: `98296aa` docs 57/59 · `b9c9abc` Open Engine v1.0.1 · `83c0dfd` KB · `e73da79` cascade view (migration 154).

**State**: on `contrib/cleverwork/invoice-audit-sop-presentation` (== main == e73da79), tree clean, `main==origin/main`.

**Open threads**:
- docs/59 **Task 2** next (open+60d scope in `invoice-audit.ts`), then UI Tasks 3–6 (preview-verified), RT-2 (go-back), RT-3, deploy.
- morning_abc_sync v3 spec-complete but NOT a running job — fleet dormant + AI can't launch autonomous agent on prod; needs host scheduler (#9). Tomorrow open+60d = 17 inv → 5 action $1,165.46, 36 lines→Jordan.
- Tasks: #6 agreement extractors, #7 staleness reconcile, #9 morning_abc_sync v3 build, #10/#11 nest bugs, #12 Invoice Audit rebuild.

## Session — Invoice Audit rebuild Tasks 2–4 (docs/59) + cascade CM fix (mig 157)

**Goal**: Resume docs/59 at Task 2; build the data + UI tasks on `contrib/cleverwork/invoice-audit-sop-presentation`, preview-verified against prod.

**Tree anomaly (surfaced to Chris)**: session started clean at `02bd71f`, but a ~10:52 burst left **44 uncommitted files from a concurrent DevTeam/SEO workstream** (agents/dev-engine, scripts, proposals, docs/59-endpoint-auth-matrix, schemas 155/156, modified access-control/middleware/prewarm). Not mine. Chris chose **commit only my files** → every commit below stages explicit invoice-audit paths only; DevTeam files left untouched.

**Deliverables (all preview-verified vs prod, astro build clean, 8 unit tests pass)**:
- **Task 2** `d7bb284` — actionable scope (`SCOPE_MIN_AGE_DAYS`, `isInvoiceActionable`, `buildScopeAndTotals`) = open + ≥60d + non-CM; per-invoice `actionable` flag + `InvoiceAuditData.scope` (cutoff + default date range) across all 3 builders. Audit-finding KPIs rescoped to actionable; payment/open/paid/CM-count KPIs kept open-set. **Prod anchor = 17 actionable invoices.**
- **Task 3** `6f99fb1` — contextual 3rd price column (Most Recent / **Org Inv Price** for CMs, D5/D7) + cascade Var%/$ + benchmark-source badge (D6), from `v_invoice_audit_line_cascade` joined per line in the detail loader (revived the never-populated apiPrice).
- **Migration 157** (applied to prod, additive) — **cascade CM-coverage bug fix**: view 154 sourced `abc_invoice_lines` (no CM lines live there) so it returned ZERO rows for every credit memo and D7 never fired. Repointed main CTE at `v_invoice_lines_complete` (line_id=l.id matches v_invoice_audit_line). 930 CM rows now covered, 109 org_inv resolved. Verified CM 2005666536-001 → Org Inv 0% variance (correct reversal).
- **Task 4** `9882e5e` — date1↔date2 range (default [oldest actionable→cutoff], ≤cutoff cap) + **"Show all"** checkbox replacing #iv-status. Verified: default 17/975; Show all reveals paid+CM + lifts cap; date narrowing works.

**Decisions**:
- KPI rescope split: audit-finding KPIs → actionable set; payment loop (to-be-paid/awaiting) + open/paid/CM counts stay open-set (payment loop is independent of the 60-day audit bound). Flag to Chris.
- Migration 157 (new file) over editing committed 154 — preserves history; CREATE OR REPLACE idempotent.
- Task 3 verified via live prod data + DOM (detail loader uses `.eq()` chains the unit-test mock doesn't support) rather than a unit test.

**State**: on `contrib/cleverwork/invoice-audit-sop-presentation` (= `9882e5e`); `main`/`origin/main` still `02bd71f` (NOT merged — deploy is Gate 7). Tree has my work committed + the 44 DevTeam files still uncommitted (per Chris). Schemas mirrored through **157**.

**Open threads**:
- docs/59 **Task 5** next — Alex/Maya attribution by actor+role (dashboard_action_log.actor_type + action_type/source) + agent-vs-human badge. Then **Task 6** (per-invoice "Go back" reset API + RT-2), **RT-3** + deploy (Gate 7).
- Deploy (Gate 7) blocked on the tree: contrib→main merge + Coolify deploy must navigate the concurrent DevTeam uncommitted work.

## Session — Invoice Audit Task 5 (Alex/Maya attribution)

**Goal**: docs/59 Task 5 — map line audit actions to client-facing personas (Alex/Maya) vs human, add agent/human badge. Build + verify vs prod.

**Deliverables** (`421183e`, on `contrib/cleverwork/invoice-audit-sop-presentation`):
- `invoice-audit.ts` — pure `attributeAuditActor(approved_by, source)` → `{label, kind: agent|human|system, persona}`. Rules: `auto_match`/System→Alex(agent); explicit "Alex"/"Maya"→that agent; named person→human ("Maya Chen" stays human, NOT the Maya agent); System+backfill→system (historical seed, not attributed to an agent). Per-line `actorLabel/actorKind/actorPersona` added to InvLine + both loaders (precomputed `const actor`).
- `invoice-audit-tree.ts` — `actorBadge(kind)` (Agent=pill-brand / Human + System=pill-grey); auditCell + disposition lead now show persona + badge; both manual-mark handlers set actorKind=human.
- 6 new unit tests (14 total pass); docs/59 Task 5 marked ✅.

**Verification (live prod)**: API JSON for 3 invoices — 2010452632-001 auto_match→Alex(agent); 2009572164-001 Chris Hussey→human; 2010162348-001 "Maya Chen"→human (not Maya agent). Rendered DOM: "✓ Alex" + 6 "Agent" badges; screenshot of AUDITED column. astro build clean, no console errors.

**State**: `421183e` on contrib branch; `main`/`origin/main` still `02bd71f` (NOT merged — deploy is Gate 7). Concurrent DevTeam/SEO files (44) still uncommitted per Chris — Task 5 commit staged invoice-audit paths only.

**Open threads**:
- docs/59 **Task 6** next — per-invoice "Go back" reset API (`api/invoice-audit/reset`, WorkOS-gated, append-only, reverse holds, cancel draft CMs) + RT-2 (reset safety).
- Then **RT-3** + deploy (Gate 7): contrib→main merge + Coolify, navigating the concurrent DevTeam uncommitted work.

## Session — Invoice Audit Task 6 (per-invoice "Go back" reset + RT-2)

**Goal**: docs/59 Task 6 — per-invoice reset API + UI button; pass red-team RT-2 (reset safety).

**Deliverables** (`215499b`, on `contrib/cleverwork/invoice-audit-sop-presentation`):
- `schemas/cleverwork-roofer/158-invoice-audit-reset.sql` (applied to prod, additive) — broadens `credit_memo_requests.status` to allow `cancelled`; atomic `invoice_audit_reset(invoice, actor_id, actor_type, actor_display)` plpgsql RPC: APPENDS `pending` audit rows for every non-pending line (never deletes), cancels ONLY `draft` credit memos, logs one `dashboard_action_log` row (only when something changed), refuses paid/exported/credit-memo invoices. One transaction.
- `src/pages/api/invoice-audit/reset.ts` — WorkOS-gated route (locals.actor → buildUnauthorizedResponse), calls the RPC, maps errors to 404/409/400.
- `invoice-audit-tree.ts` — "↩ Go back" button in invoice header (hidden when CM / paid / awaiting-payment or auditedLines=0); `resetInvoice()` handler: confirm → POST → toast → reload lines (loadInvoiceLines re-syncs tags) → remove button. CSS in invoice-audit.astro.

**RT-2 verified (live prod, ZERO permanent mutation)**:
- Guards: not_found / credit_memo_not_resettable / invoice_paid all return with no writes.
- Success (2009572164-001, BEGIN/ROLLBACK): re-pends 10 lines + cancels 1 draft CM; **idempotent** (2nd call in-tx → 0 lines/0 CM, only 1 log); **atomic** — ROLLBACK restored prod exactly (10 passed / draft CM / 0 logs).
- Button absent on all 803 paid + 230 CM invoices (DOM scan of 975).
- UI handler round-trip verified via mocked /reset response (correct body, toast "6 line(s) re-pended", button removed) — no prod write. Live UI reset was correctly blocked by the auto-mode classifier (shared-prod mutation); used rollback + mock instead.
- No destructive delete path; sent comms never touched. astro build clean; 19 unit tests pass; no console errors.

**Note**: live UI reset on real invoices mutates shared prod — gated by classifier. Verified via SQL rollback + client mock, which is sufficient and non-destructive.

**State**: `215499b` on contrib branch; `main`/`origin/main` still `02bd71f` (NOT merged). Concurrent DevTeam/SEO files (44) still uncommitted per Chris — Task 6 staged invoice-audit + migration 158 paths only. Schemas mirrored through **158**.

**Open threads**:
- docs/59 **Gate 7** next — RT-3 (integration/security: WorkOS on every new/changed route incl. /reset; regression of audit→disposition→batch→pay; trust/consent; no PII in new columns/badges; Live⇄Dev convergence) + **deploy** (contrib→main merge + Coolify, navigating the 44 concurrent DevTeam uncommitted files). Tasks 1–6 all ✅.

## Session — Gate 7: merge concurrent workstream + RT-3 + DEPLOY

**Goal**: Chris cleared the concurrent DevTeam/SEO files to merge; run RT-3 (integration/security); deploy (docs/59 Gate 7).

**Deliverables**:
- Committed the concurrent workstream (`289c618`, 182 files — the "44" status entries expand via OKF/runbooks/api-dev/systemd dirs; 11,276 insertions, all text, no secrets/PII; .env.example placeholders only). Daily log `20e9a75`.
- **RT-3 clean (1 MEDIUM fixed, 0 HIGH)**: `/reset` WorkOS/bearer-gated + locals.actor; `/api/dev/activity-summary` gated + allowlisted (dev-conductor/session-analyst); `/api/dev/webhooks/github`+`/sentry` HMAC-verified, fail-closed, Linear-only (no brain DB). 28 tests pass (no regression). No PII (normalizedPath collapses invoice#/uuid tokens → `:id` before rollup; rollups store no user ids). Append-only evidence, no trust upgrades.
- **MEDIUM (fixed)**: migrations 155 (`command_center_activity_rollups`) + 156 (`mv_invoice_audit_summary`) were committed but UNAPPLIED in prod — the per-request activity rollup needs 155. Applied both (additive/idempotent); verified rollup writes a normalized PII-free row. Prod aligned through **158**.
- **DEPLOY (Gate 7 done)**: FF-merged contrib→main (10 commits), pushed `02bd71f..20e9a75`; Coolify auto-built. Live verified: `/healthz` status ok, buildCommit `20e9a75`, supabase+workos configured, liveSurfaceStatus live. Auth gates in prod: `/reset`→401, `/api/dev/activity-summary`→401, webhooks→503 (secrets not yet set = fail-closed/inert).

**Decisions**:
- Two prod-mutating actions (live UI reset; push to main) were correctly blocked by the auto-mode classifier; got explicit Chris approval for the push. UI reset verified via SQL rollback + client mock instead (no prod mutation).
- Applied DevTeam's 155/156 to prod as part of convergence (shared prod DB; additive).

**State**: **`main` == `origin/main` == `20e9a75`**, deployed + verified live. All docs/59 Tasks 1–6 + Gate 7 ✅. Tree: docs/59 + this log pending one final docs commit.

**Open threads / follow-ups (DevTeam, not blocking)**:
- Set `GITHUB_WEBHOOK_SECRET` + `SENTRY_WEBHOOK_SECRET` in Coolify to activate the dev webhooks (currently 503/inert).
- Add `dev-conductor:token,session-analyst:token` to `AGENT_SERVICE_TOKENS` for /api/dev/activity-summary.
- `mv_invoice_audit_summary` created WITH NO DATA + unreferenced by runtime — refresh via sync job when wired.
- morning_abc_sync v3 still not a running job (#9, needs host scheduler). Invoice Audit rebuild (docs/59) fully shipped.
