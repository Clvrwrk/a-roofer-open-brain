# 2026-06-29

## Session 1

**Goal**: Scheduled run of the ABC Supply FULL production data sync (catalog + branches + regions).

**Run record**: 2026-06-29 ~11:09Z, env=production — **FULL SYNC NOT COMPLETED (blocked by Cowork sandbox)**. rowsUpserted: productCatalog=n/a, vendorBranches=n/a, regions=n/a (full pull never ran).

**What happened**: The full pull cannot run on this Cowork surface. Each `mcp__workspace__bash` call runs in its own bwrap sandbox (`--unshare-pid --die-with-parent`), so nohup/setsid/disowned background processes are killed the instant the launching call returns (proven with a persist-test: ticks stopped exactly when the parent call exited). Foreground calls cap at 45s; a 3-page catalog probe timed out at 44s (~14–15s/page). Catalog = 333 pages / 332,632 items ⇒ ~80 min for catalog alone, plus 50-state branch search/detail and accounts. Neither background (dies) nor chunked-foreground (~150+ sequential, timeout-risky calls) is viable here.

**Confirmed healthy (bounded probe, 29s, env=production)**: token exchange **200**, all 9 scopes granted (location/product/account/pricing/order/allOrder/notification/invoice/invoice.history .read), token TTL 1800s; catalog read OK (page 1: 1000 items, pagination totalPages=333 / totalItems=332,632); WI branches read OK (21 summaries, 21 details); 23 endpoint requests, **0** rate-limited (429); all `failed` arrays empty. Upsert is change-gated (writes only rows whose content_hash changed) — so on an already-synced brain a re-run legitimately reports near-zero rowsUpserted (catalog page1=0, WI branches=1), which is expected, not an error.

**Decision/where to run**: Execute the full sync on a host that supports long-running background processes — e.g. the Hetzner/Coolify agent host or any normal shell on the repo: `node integrations/bridges/abc-supply/production-sync.mjs --env=production --out=...json`. It is read-only against ABC (upserts only to our abc_product_catalog/abc_vendor_branches/abc_regions). Credentials/scopes verified working today.

**Open threads**: Full production pull still owed; needs a non-sandboxed runner. Left bounded-probe artifact `.production-runs/.healthprobe.json` (could not unlink — sandbox unlink permission quirk).

## Session 2 — Service/Warranty transfer (Commercial ship-to) + Slack per-agent live

**Goal**: Route Commercial-ship-to invoices out of Invoice Audit into a Service/Warranty Audit; transfer the open ones; (earlier) wire per-agent Slack bots + 401 fix + Coolify env.

**Slack per-agent (earlier this session)**: 8 per-agent bots verified live in pe-command-center; canonical apps recorded (Alex A0BD4C9SUPP, Casey A0BD85UG23C, Jordan A0BE2EMAA8Y, Maya A0BD0PAEU2E, Lena A0BD1RH3FPD, Rowan A0BD1RMHFBM, Sam A0BD86ATVHQ, Conductor A0BDG2CCCAJ). Tokens in gitignored config/.env + added to Coolify env (8× *_BOT_TOKEN). 401 weekly-links fix + per-agent posting registry shipped (merged to main 7b4ad8e). Live roll-call demo posted. scripts/coolify-redeploy.sh added (tightly-scoped redeploy helper) — agent self-grant of deploy permission is blocked by design; Chris runs the redeploy or adds the allow-rule.

**Service/Warranty transfer (docs/61)**:
- **Finding**: "Commercial" is in the ABC API data (`abc_invoices.raw->'shipTo'->>'name'`), NOT OCR-only — detection reads the structured field. OCR double-positive deferred until an invoice-OCR pipeline exists.
- **Migration 162** (applied to prod): `service_warranty_audit_queue` (append-only) + `v_service_warranty_candidates`.
- **Engine**: `lib/invoice-audit.ts` excludes queued invoices (`transferred=true`, `actionable=false`); new `totals.transferred`. tsc clean, 14/14 unit tests pass.
- **RAN the transfer**: 16 open Commercial invoices → queue + 16 `transfer_service_warranty` action-log rows. Slack notice posted as Alex to #service-warranty-audit (C0BE05YUQTW): parent + 16 threaded.
- Examples confirmed: 2010446923-001 (06/03), 2009332466-001 (05/06). All ship-to 2036874-17.

**Decisions**: same price-vs-agreement variance for S/W (Phase 2); HITL = Chris (not Lucinda); Phase 1 = transfer+queue+Slack now, mirrored S/W dashboard via `audit_type` discriminator = Phase 2; channel C0BE05YUQTW.

**Open threads**:
- **Engine exclusion is committed but NOT yet deployed** — live dashboard (7b4ad8e) still shows the 16 as actionable until the new invoice-audit code deploys. Converge branch `contrib/cleverwork/service-warranty-transfer` → main + push (also loads the 8 Coolify agent tokens into the running container).
- Phase 2: mirrored S/W Audit surface + morning_abc_sync triage step + invoice-OCR double-positive.
- Tokens pasted in chat (8 bot + config) still need rotation once verified.
