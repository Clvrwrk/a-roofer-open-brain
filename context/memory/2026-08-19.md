
## Session — Orgo + credit-memo re-audit + site cleanup (11:35 CT)

**Goal**: Finish PEC-216, delete orphaned pages (PEC-217), stand up the Layer 2 daily sweep on Hetzner, re-audit every credit memo 3x via Ringer, provision Orgo, then handoff.

**Deliverables**
- mig **232** (prod): phantom $59,639.20 CSV-footer row → `abc_invoices_quarantine` + CHECK (fail closed). ABC total now $2,197,424.94. (PEC-215)
- PEC-216 **both halves**: chunked `.in()` 13.56s→2.9s; rows deferred to client JSON 3.77MB→**309KB (12x)**.
- PEC-217: 11 files deleted (Price Foundation stack, Negotiated Catalog, Vendor Regions, `lib/price-list.ts` fake SKUs). docs/91 has rationale + restore SHA. SW VERSION bumped for cache eviction.
- Layer 2 sweep + systemd units, 06:00 CT Hetzner. docs/92.
- Orgo workspace + `pe-site-qa` desktop provisioned; `scripts/orgo-site-walker.mjs`. docs/94.
- Credit-memo re-audit: **unanimous 3/3** across grok-4.6 / gpt-5.6-sol / claude-opus-5. $4,738.22 → **$4,653.76**. docs/93. Drafts NOT sent.

**Decisions**
- **Quarantine is now the pattern** for malformed ingest rows (archive + CHECK, never delete).
- Redirect stubs KEPT while orphans deleted — removing them would create broken links.
- Two credits withdrawn: item `0150080102` has no in-force Wichita agreement. My earlier $40.26 used a **quote**; a quote is not an agreement, so fail-closed wins.
- WorkOS Agent Auth **deferred** — `/agent/auth` + `/oauth2/token` return live 501; `agent-auth.ts` is discovery-only. Security-critical build, not a provisioning step.

**Hard-won lessons (cost 4 Ringer runs)**
1. Sandboxed workers can only write **inside their task dir** — deliverables elsewhere fail good work.
2. SRS lives in `vendor_invoices`, ABC in `abc_invoices` — an ABC-only join zeroed $2,817.80.
3. `invoice_line_reaudit` has a row **per run_label** — must `DISTINCT ON (invoice_number, line_id)` or totals triple.
4. `unit_match` is true/null, **never false** — null means "not recorded", not "mismatched".
5. **Never trust a URL alone to detect an auth wall** — WorkOS/AuthKit returns 200 at `<tenant>.authkit.app`.
6. Only codex's *disagreement* exposed the duplicate-line bug; grok and opus both returned right-looking wrong numbers.

**Open threads**: Orgo needs a one-time human WorkOS sign-in; CM drafts need review before send; **PEC-220 Maya's Orgo desktop is gone** (runtime down, all ORGO_*_ID stale); PEC-213 Wichita coverage still open.

### CORRECTION (11:45 CT) — Maya's Orgo desktop is HEALTHY

Chris supplied the real URL (`orgo.ai/desktops/20ee4678`). **My "Maya's desktop is gone / runtime down" finding was WRONG** and is retracted.

Ground truth: computer `37b262e0-a915-47e6-8c3b-f180a32ab6fe`, instance `20ee4678`, project **`PE-open-brain`**, status running, created 7/26. On the box: up 16 days; `maya-slack-listener` RUNNING pid 30617 **up 9 days** (since 8/10, matching the release dir mtime); `hermes-gateway` + `websockify` up 16 days. So `orgo-ship-maya-listener.sh` was evidently already completed 8/10 — the "pending since 8/11" note is stale too.

**Two traps caused the false alarm:**
1. `context/MEMORY.md` held stale ids (`f914c60c…`/`c681e86d`) — they 404 correctly.
2. **`GET /api/workspaces` returns 0 while desktops exist** (Maya's is under `PE-open-brain`). It is NOT an inventory endpoint; there is no `GET /computers` list either (405). The only reliable probe is `GET /computers/{id}` against a correctly-recorded id.

I over-generalised from two bad signals instead of verifying. The record had rotted, not the infrastructure.

**Also:** I created `pe-site-qa` in a NEW project `pro-exteriors-open-brain` when `PE-open-brain` already existed — consolidating is open work. PEC-220 rewritten and dropped Urgent→Medium.

## Session — Invoice Audit corrections + credit-memo evidence panel (14:20 CT)

**Goal**: Review /accounting/invoice-audit control-by-control, correct the three defects
found, then give the credit-memo detail page invoice + agreement links and a stated
validation chain (invoice ↔ agreement ↔ PE office). PEC-221.

**Deliverables**
- mig **233** (prod): `v_invoice_audit_invoice.at_risk` no longer excludes `disputed`.
  $0.00 everywhere → **$4,676.63** total / **$3,485.13** actionable across 52 invoices.
  Invoice view and line view now reconcile to the penny (258 overcharged lines
  $10,160.95 = 150 passed $5,484.32 + 108 disputed $4,676.63).
- "Process 0" → "Nothing to stamp" with an honest tooltip. The RPC/endpoint are fine;
  empty is the normal resting state (rows only appear after a reset or re-audit).
- `kpi-pills.ts` fully paginated (was `.limit(1000)`/`.limit(2000)`/`slice(0,500)`).
- **Evidence panel** on `/accounting/credit-memos/<invoice>`: invoice PDF link,
  agreement PDF link, and 7 named checks (office resolution · same-office agreement ·
  in force at invoice date · agreement vs quote · item match method · UOM alignment ·
  citation recorded vs re-derived). Worst state drives the headline + left border.
- `/api/price-agreement/pdf/[id]` now handles uuid (non-ABC) ids instead of 500ing.
- docs/95.

**Decisions**
- at_risk = overcharge not yet accepted as correct (`audit_status <> 'passed'`).
  `disputed` money has NOT come back, so it stays at risk. credit_memo_amount keeps
  tracking the accepted-and-claimed slice separately.
- SRS claims have NO recorded agreement, so the panel re-derives the citation live and
  **labels it re-derived** rather than presenting it as what the auditor cited.
  Fail-loud beats a confident-looking fabrication.
- Did not touch `credit_memo_amount` (also structurally dead, sums to $0) — changing it
  affects other consumers; logged as follow-up in docs/95.

**Hard-won lessons**
1. **`v_invoice_audit_line.variance_ext` is sign-unsafe on negative-qty lines.** Credit /
   return docs have negative quantity, so an UNDERcharge comes out as positive
   variance_ext. 27 lines / $4,614.35 of an apparent $14,775 "overcharge" were this.
   Always filter `quantity > 0` before summing variance_ext, or use at_risk.
   I reported 2004367179-001 as a $2,788 overcharge before checking the sign — it is a
   −89% credit line. Corrected to Chris in-session.
2. `invoice_line_reaudit` already carried office + agreement + match_method + unit_match
   per line; the CM loader was discarding all of it. Read the columns before building.

**Verification**: build clean, 309/309 tests pass, loader exercised live against prod
(SRS 0050033202-002 → derived quote citation, warn; ABC 2009557754-001 → cross-office
FAIL, reproducing the docs/93 withdrawal automatically). Browser render NOT visually
confirmed — port 4399 held by another session's dev server, Chrome extension offline.

**Open threads**: docs/93 write-back to prod still pending; SRS re-audit writer drops
provenance on discrepancy rows; credit_memo_amount repair-or-retire decision.

### Ruling (15:05 CT) — for SRS, the Wichita quote IS the price agreement

Chris: "for SRS the quote in this case is to be considered the price agreement."

- mig **234** (prod): `ceo_verified = true` on `price_agreements` `3e7b261b-…`
  (quote `0049828559`, SRS Wichita). Recorded as a fact about the DOCUMENT, not a
  vendor rule in code. `ceo_verified` is a display badge, never a pricing gate
  (docs/82 §6 dec 3) — no price resolution changed.
- Evidence panel: quote + `ceo_verified` → **pass** ("accepted as the governing price
  agreement, confirmed on the agreement record"). Un-accepted quotes still warn. The
  panel keeps SAYING it is a quote — it shows acceptance, it does not hide the type.
- Effect: all **5 approved SRS claims = $2,024.75** now pass 6/7 checks, all inside the
  quote's 06-23 → 07-23 window. Sole remaining warn is *Citation provenance* (the SRS
  re-audit never recorded the agreement; the panel re-derives and says so).

**Two limits, stated so they don't get lost:**
1. Does NOT revive the ABC claim on `2009557754-001` — that priced an ABC invoice off an
   SRS document, a **vendor-silo** violation (mig 208), separate from quote-vs-agreement.
   docs/93's withdrawal stands.
2. Does NOT elevate SRS quotes generally. The Englewood/S-Denver quote `0049345641`
   (`7246ed93-…`) is untouched; its two claims are already received/cancelled. Asked
   Chris whether to extend.

## Session — Credit-memo work surface + matching protocol (15:25 CT)

**Goal**: Re-nest `/accounting/audit/credit-memos` as PE Office → Vendor Branch → Memo
like every other work surface; add memo PDF + original-invoice link + job link; run an
original-invoice matching protocol over all ABC credit memos. Elevate the Englewood quote.

**Deliverables**
- mig **235**: Englewood/S-Denver quote `0049345641` (`7246ed93-…`) accepted as the
  governing agreement — same `ceo_verified` mechanism as 234, **not** a hardcoded rule.
  Chris: "we are working to get vendor to sign off on the price agreements we build."
- mig **236** (+236b perf): `v_credit_memo_match` — four-tier matching protocol.
  **254/261 matched (97%), was 73 (28%).** exact 73 · confident 82 · probable 42 ·
  weak 57 · none 7. Every tier carries its confidence; a weak tier never renders as
  confirmed.
- New `/accounting/audit/credit-memos`: server-rendered office→branch→memo nesting,
  6 offices / 22 branches / $204,674. Per memo: memo PDF, original-invoice link + its
  PDF, AccuLynx job deep link (or job number as text when no id). 3-control toolbar.
- `lib/credit-memo-tree.ts`; docs/96.

**Decisions**
- `orderNumber` is USELESS for CM→invoice matching — a memo's orderNumber is its own
  invoice root. 0 matches. Recorded in the migration header so nobody re-tries it.
- The 7 unmatched are correct: rebates ($5,000/$3,800 no PO), a GAF manufacturer credit,
  a multi-job memo ("MC-51,52,53"), and two Apr-2025 CSV memos whose originals predate
  our ingest. Forcing a match would be worse than "none resolved".
- Job falls back to the MATCHED ORIGINAL INVOICE's AccuLynx match when the memo has none;
  the row states `job via original invoice`.
- Only a real `acculynx_job_id` becomes a link — a bare PE job number stays text, never a
  dead href.

**Hard-won lesson**
Joining `v_invoice_audit_invoice` for three columns cost **2.9s** (292,581 rows discarded
in the join filter) because that view recomputes the whole pricing CTE. Resolving
office/branch inline = **651ms**, verified identical on all 261 rows. Do not join that
view for metadata — copy its office expression instead.

**Verification**: build clean, 309/309 tests, loader exercised live (1.03s, 261 memos,
254 matched, 154 with jobs, real AccuLynx deep links).

**Open threads**: docs/93 write-back; SRS re-audit writer provenance; credit_memo_amount;
AccuLynx job-id coverage (154 job numbers but only 40 deep-linkable).
