# 2026-06-22

## Session 1

**Goal**: Scheduled ABC Supply FULL production sync (catalog + branches + regions).

**Run log (one-line, per task)**: 2026-06-22T11:06Z env=production — ABC token OK (200), catalog totalPages=333 / totalItems=332,323 confirmed live; run **INCOMPLETE/BLOCKED** in Cowork sandbox. rowsUpserted: productCatalog≈1000 (pages 1–2 only, probe), vendorBranches=0 (not reached), regions=0 (not reached). Not a full sync.

**Decisions / findings**:
- Cowork sandbox cannot run this job to completion. Two hard limits collide: (1) each `bash` call gets a fresh PID namespace, so any `nohup`/`setsid`/`disown` background process is killed the instant the call returns (verified with a detached probe that stopped at the call boundary); (2) foreground `bash` is capped at 45s/call.
- ABC API latency makes every phase exceed 45s: catalog ≈19s/page → ~105 min for 333 pages; a branches(2 states)+accounts slice was killed at the 44s timeout before finishing. 0 rate-limited (429) responses observed — the blocker is runtime model, not throttling.
- Token exchange and Supabase upserts both work end-to-end (catalog probe upserted ~1000 real rows, change-gated). Script is read-only against ABC; no write scopes added.
- Same failure shape as the 2026-06-21 scheduled run (its log died at "account detail 5/32").

**Recommendation**: Run this sync on a host without the 45s/short-lived-process cap — e.g. the Hetzner/Coolify agent host (docs/27) via cron or a one-off `node` invocation — not from the Cowork sandbox. Cowork can verify results afterward by reading the output JSON / querying Supabase counts.

**Open threads**: Full production pull still owed. Leftover probe artifacts in `integrations/bridges/abc-supply/.production-runs/` (probe-catalog-2pages.json, empty full-20260622T110625Z.*) — sandbox unlink is blocked, clean up from host.

## Session 2

**Goal**: Handoff/wrap-up — review, commit, push; align main⇄dev; tie off all worktrees.

**Findings (pre-action audit)**:
- Repo already clean lineage: current branch `contrib/cleverwork/observability-sentry` = `origin/main` = `b6b0169` (0 ahead / 0 behind). No branch had unpushed commits.
- All external worktree dirs (`/private/tmp/*`, `/tmp/vt-live-fix`) were already gone (prunable). In-repo `.worktrees/db-price-foundation-round{2,3,4}` were clean.
- 5 local-only branches confirmed fully merged into `origin/main`; round3/round4 live on their own pushed origin branches (not in main).

**Deliverables**:
- **Worktrees tied off**: `git worktree prune` cleared 8 stale registrations (incl. unlocked `vt-live-fix`); removed the 3 in-repo `.worktrees/` (work preserved on origin). Only the primary checkout remains.
- **Branches pruned**: deleted 5 local-only merged branches (`cleverwork/price-agreement-audit`, `database-playground-plugin`, `db-price-foundation-round2`, `main-perf`, `vt-live-perf`) via safe `-d`. Kept all branches with an origin counterpart.
- **Memory refreshed**: `context/MEMORY.md` — dropped stale `72c70dd` hash for "confirm HEAD via /healthz", softened local-main warning, added ABC full-sync-needs-agent-host open thread. `docs/handoffs/current.md` hash refreshed.
- **Committed + pushed** daily logs + handoff + memory to `origin/main` (Coolify auto-deploy, docs-only). **Local `main` fast-forwarded** to `origin/main` — dev⇄main⇄live aligned.

**Decisions**: Did NOT commit ambiguous/raw untracked items (Estimate Tool working files, supabase Accounting/Price-Agreement PDFs, screenshots, temp probe `.txt`s) — potential PII (hard rule 2) + raw-import staging; left for deliberate sanitization. Did NOT delete branches that have their own origin counterpart (durable record).

**Open threads**: Carried-forward OPEN items unchanged (Sentry `sntrys_` token rotation, nightly-sync Sentry on agent host, GPA items, ABC full prod-sync on agent host). Untracked raw/junk files still on disk in primary checkout — clean or sanitize deliberately.

## Session 07:50 — Invoice-audit workflow overhaul (P1–P3 deployed, P4 foundation)

**Goal**: User-driven UX/correctness overhaul of the price-audit surfaces. Phased P1→P2→P3 now, P4 started.
**Deliverables**:
- **P1 (DEPLOYED, on main)** — one UOM per row + correct variance, all pricing surfaces. New `app/command-center/src/lib/uom.ts` = canonical TS converter (mirrors mig 121 / docs/46; `price_uom` wins; ship→price ×`units_per_price_uom`; refuses to convert when unaligned). Wired into `price-agreement-audit.ts` (+ `price-agreement-tree.ts` display, `abc-price-agreement-gaps.astro` CSS right-align headers), `agreement-package.ts` (Builder), `estimate-audit.ts`. Invoice/Order audit already normalize in SQL — no change. Fixed the screenshot bug: 02MLHLXABB was $147.17/SQ vs $59.32/BD (+148%) → now $147.17 vs $177.96/SQ (−17.3%).
- **P2 (DEPLOYED)** — coverage badge relabeled "Branch coverage" + tooltip; per-branch "X/99 negotiated" pill (new `negotiatedCount` on PaBranch); "Build agreement →" button on <100% branches → `builder?branch=&focus=gaps`; Builder now reads `?branch=` to isolate/open/scroll.
- **P3 (DEPLOYED)** — reusable `src/scripts/progress-checklist.ts` (`initProgressChecklist`, localStorage-backed) + shared `.cc-progress`/`.cc-check` CSS in global.css. Adopted on Price Agreement Audit (per-office "Review progress" bar + per-branch "Reviewed" checkbox, resumable). onChange hook lets DB-backed surfaces mirror.
- **P4 (FOUNDATION, on `contrib/cleverwork/price-list-review-hierarchy`, view applied to prod)** — mig `143-price-list-review-global.sql` → `v_price_list_global`: per PE office + GPA item, lowest OPEN invoice / API / min|max|mean negotiated across the office's 2hr territory, all canonical price_uom. Validated (Denver/02MLHLXABB: open $114, API $177.96, neg $147.17 ×54).
**Decisions**: "Branches within 2hr" = `vendor_branches.pricing_territory_office_id` (the office's 120-min google_routes_bearing_v1 isochrone, schema 70) — REUSED existing drive-time, NOT forked (`branch_office_candidate.drive_minutes` is empty). "Open invoice" = `abc_invoices.ar_status='open'`. Non-negotiated = master items lacking a current agreement (gaps filter). UOM rule applied to ALL pricing surfaces.
**Open threads**: P4 remaining — branch-detail view (current + immediate-last-archived negotiated price), archived-agreements history view (newest→oldest), then the nested Price List Review UI (PE Office → Global Price List → Vendor → Branch → Current/Archived) reusing the P3 primitive + the one-UOM rule. P4 contrib branch NOT yet merged to main. Verify Coolify deployed P1–P3 to cc.proexteriorsus.net.

## Memory Health
- 2026-06-22 health check: FAIL cap — context/MEMORY.md at 2415/2500 chars (within 5% of cap); consolidate via meta-memory-write. (Index freshness unverifiable from sandbox; duplicate file 'context/memory/2026-06-22 2.md' present.)

## Session 08:30 — UOM fix on Invoice & Order Audit (pre-P4)

**Goal**: User reported the live `/accounting/invoice-audit` API PRICE column still compared across UOMs (e.g. $39.75/BD beside a $99.00/SQ line) despite the site-wide UOM normalization. Fix before starting P4.
**Deliverables**: `app/command-center/src/lib/invoice-audit.ts`, `app/command-center/src/pages/api/order-audit/lines.ts` — these two TS loaders were the last surfaces passing the ABC API price through RAW; SQL views already normalized `unit_price`. Now apply `convertPrice(api.price, api.uom → line.uom)` via `v_item_uom_map` (matches Price Agreement Audit pattern). Aligned → line UOM; unalignable → null → "—".
**Decisions**: Confirmed on prod — 02TKFH2WW $39.75/BD ×3 = $119.25/SQ. 6,990 of 38,710 API rows cross-UOM; 4,405 normalize, 2,585 (no UOM map) safely show "—". Committed straight to `main` (canonical/live) per user instruction.
**Deploy**: `bfc0a07` pushed to origin/main; Coolify deployed; `/healthz` buildCommit = bfc0a07 ✓ (state (c) live).
**Open threads**: Begin P4 — `v_price_list_branch` (current + immediate-last-archived negotiated), archived-agreements history view (newest→oldest), then nested Price List Review UI (PE Office→Global→Vendor→Branch→Current/Archived) reusing the P3 progress primitive + one-UOM rule.

## Session 08:55 — P4 complete: Global Price List review hierarchy

**Goal**: Finish P4 end-to-end (cleared to complete without check-in): the nested Price List Review hierarchy PE Office → Global Price List → Vendor → Branch → Current → Archived.
**Deliverables**:
- Migration `144-price-list-review-branch.sql` (applied to prod): `v_price_list_branch_pricing` (per branch+item: current + immediate-prior negotiated price, delta/pct), `v_price_list_branch_agreements` (agreements newest→oldest, current/archived), `v_pl_branch_office` (branch→office territory). Names chosen to avoid pre-existing `v_price_list_branch`/`v_price_list_branch_item`/`v_branch_territory_office` (no destructive changes).
- `lib/price-list-review-hierarchy.ts` loader; `pages/api/price-list-review/branch.ts` lazy endpoint; `pages/accounting/price-list/review.astro` page; `scripts/price-list-review-tree.ts` client; nav entry "Global Price List" → `/accounting/price-list/review`.
**Decisions**: Server-render offices + office-level Global Price List (v_price_list_global / mig 143); lazy-load per-branch detail (15.4k rows never ship up front). Reuse progress-checklist primitive (per-office reviewed, localStorage). One-UOM rule: UOM column is the row unit, price cells plain $x.xx.
**Data note**: Branches currently have ONE agreement generation, so prior-price + archived history are EMPTY today — views are future-proof, populate when ABC's next generation lands; UI states this. 4 offices w/ signal + Unassigned (46 branches lack pricing_territory_office_id), 379 priced items, 76 branches.
**Verified**: local-vs-prod page 200, branch 312 lazy-loads 231 items, progress persists, no console errors. Committed 1336844, pushed origin/main, Coolify deploy triggered.
**Open threads**: P4 done. Pre-P4 UOM display fix also shipped this session (bfc0a07 + 53e1bc7). Future: when 2nd agreement generation arrives, sanity-check prior-price deltas render.
