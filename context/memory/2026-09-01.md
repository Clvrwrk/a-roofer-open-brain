
## Session 09:1x — nightly-feed audit + ABC probe + SRS batch ingest

**Goal**: Diagnose "no invoices to audit / no CMs to claim" + "nightly syncs failing"; ingest new SRS batch.
**Findings**:
- Only ABC is broken. AccuLynx (hourly, all 8 accounts ok), QBO (01:00 UTC, ok), IHM hail alerts (daily rows), WIP/AR (11:00 UTC recompute) all green with fresh rows today.
- ABC invoice-history endpoint returns HTTP 200 `totalItems: 0` for BOTH bill-tos since 2026-08-26 — probed live (scratchpad abc-invoice-history-probe.mjs): token ok, scopes ok, account search still lists the same 2 Bill-To (active, same numbers), yet history for Aug 15–25 (a window we hold ~11 mirrored invoices for) returns 0. Upstream ABC data/entitlement issue → contact ABC partner support. Response shape unchanged (pagination+items).
- Audit queue was legitimately drained (0 undecided discrepancy lines on 170 open invoices); CM claim card empty because all 58 requests are status=sent.
**Deliverables**: Ingested SRS Billtrust CSV (54 invoices incl. 14 credit memos) via ingest-vendor-invoice-csv.mjs; refreshed mv_invoice_audit_line + credit_memo_claims_sync_all/reconcile → 49 SRS invoices / 261 pending lines now on the audit surface. All 20 batch PDFs already covered by CSV.
**Open threads**:
- SRS payment 0049707508-001 (PO co-356, $13,561.33, AmEx 8/21): screenshot is the PRE-payment Billtrust checkout page — no confirmation number. Needs final receipt/conf # for the docs/86 payment-memo flow (human-gated endpoint).
- Add "N consecutive zero-row nights" alert to abc nightly sync (green-but-empty hid for 7 days).
- ABC support ticket for invoice.history returning empty since 8/26.

## Session 09:5x — ABC full-endpoint sweep

**Goal**: Scope the ABC outage — one service or account-wide?
**Findings**: Isolated to the Invoice service ("Partners - Invoice Experience" v1.0.2, MuleSoft). Everything else healthy: account (all 5 endpoints), location, product catalog/detail (336,329 items), pricing (priced a line OK), order history (25 orders Aug 15–Sep 1 — business activity continues) + order detail. Invoice history returns totalItems:0 even for FY 2026-01-01..09-01 (ALL history gone, not just recent). Invoice detail by id returns HTTP 500 MULE:EXPRESSION — ABC's DataWeave error-handler crashes on an EMPTY upstream payload (correlationId 9729326c-ddf3-4750-8490-031c5e71887a, 2026-09-01T12:56:57). Their invoice backend integration is broken; our creds/requests fine. Secondary: product hierarchy/recents/frequents/favorites + order templates now 400 (passed June audit) — not used by nightly sync. Probe scripts in session scratchpad (abc-full-endpoint-probe.mjs).
**Open threads**: send updated inquiry email w/ correlationId to ABC.

## Session 06:0x–06:2x — CM received flow + SRS dedupe fix (mig 282–283, deployed)

**Goal**: Chris spec — received CMs update Chase It (collected vs outstanding), exact match auto-received + QB export dual confirmation, discrepancies to human review; plus SRS "duplicate invoice" report.
**Deliverables**: mig 282 cross-vendor vendor-scoped credit_memo_reconcile (applied; 15 SRS receipts → pending review in Sent CM). mig 283 vendor_line_relink (211 audit + 228 reaudit orphans re-linked). Root cause of "duplicates": CSV re-ingest regenerated random line uuids → audit decisions orphaned → audited invoices re-presented as new (0050033288-003, audited 8/9). Fix: deterministic line ids (invoice_id, line_number) in ingest-vendor-invoice-csv.mjs + re-ingest. SRS pending fell 261→69 lines (16 invoices, genuinely new). Chase It card: outstanding/collected/needs-review; qb-bank-csv cm_actual uses receipt's original invoice (pairs with CM-TBD line). Pushed main 2b98dab.
**Decisions**: discrepancy = ALWAYS human review (only exact vendor-scoped amount match auto-receives). Unpairable orphans stay visible rather than mislinked.
**Open threads**: $1,748 request 0050033288-003 marked received 8/27 by accounting@ with NO linked CM doc (14 new credits sum $1,781.32 ≠ $1,748) — Lucinda to reconcile in Sent CM review. 5 SRS requests still outstanding ($290.25, follow-up 9/4·9/8).

## Session 06:4x — Alex audit parity for manual vendor ingests

**Goal**: Chris — SRS showed No-Price lines as human audit work; Alex should auto-clear them for ALL vendors.
**Findings**: alex_no_price_triage() (mig 229b) IS vendor-agnostic and runs in openbrain-abc-sync.timer 07:30 UTC — but a mid-day manual SRS ingest sits untriaged until next morning. Ran it: 53 stamped (all repeat→agreement_gap_queue). Remaining "no-price pending" (11 SRS + 13 ABC) are ALL credit-memo docs — correctly excluded by surface. True queue now: 3 SRS over-agreement lines only.
**Deliverables**: ingest-vendor-invoice-csv.mjs now calls alex_no_price_triage() after every successful ingest. Verified by idempotent re-ingest: 54 invoices, 0 new stamps needed (deterministic line ids held).

## Session 07:0x — CM review nesting + original-invoice exact match (mig 284, deployed)

**Goal**: Chris — CM review workspace gets invoice-audit-grade detail: request line (light red) with its CM line (light green) nested below, per line; match CMs by the ORIGINAL INVOICE the memo names, never closest-by-amount.
**Findings**: SRS CSV names it — "Orig Inv#: <invoice>" description rows; PDF arm already parsed it (key mismatch). No OCR needed.
**Deliverables**: ingest lifts Orig Inv# → raw.original_invoice_number(s) (CSV + PDF arms). Mig 284: reconcile matches original-invoice-first (exact amount → auto-received; differ → review LINKED to right request); pass 0a credit_memo_reconcile_correct_links() unhooks legacy closest-by-amount links the memo contradicts (8 corrected). sent.astro: receipts nest inside their request block — receipt strip + red request rows + green CM rows paired by item; unlinked credits keep a flat table WITH their memo lines + vendor Reason. Verified on dev vs prod DB: all 5 SRS request blocks pair 1 strip/1 red/1 green; 10 standalone credits show true originals.
**Decisions**: memo-named original beats any amount heuristic; closest-by-amount is context only, never a link. Vendor Reason field surfaces (PRICING AGREEMENT OVERCHARGE vs CUSTOMER MATERIAL RETURN — the review signal).

## Session 07:2x — CM sales-tax breakout + original-invoice audit for unlinked credits

**Goal**: Chris — a CM total includes sales tax; requests are ex-tax, so compare LINE credit vs requested (0050975349-001: $90 lines + $7.65 tax = $97.65 vs $92.25 → SHORT $2.25, vendor credited $10.00/EA not $10.25). And standalone/mismatch receipts need the original invoice's audit data nested the same way.
**Deliverables**: sent.astro — cmBreakdown (tax = |total| − |Σ lines|, derived never guessed) + lines SHORT/over pill on every receipt strip; standalone receipts render original-invoice mv audit rows (billed qty/price/agreement) red above each green CM line. Astro quirk: block-bodied arrows + conditional <tr> fragments inside nested expressions fail the compiler — precompute typed row lists in frontmatter, keep templates expression-bodied.
**Verified**: dev vs prod — nested strip shows "lines SHORT $2.25" on 0050033202-002; standalone CM 0050976695-001 pairs billed-on-0049707508-001 red row over green credit row.
