
## Session — UI/UX Phase 1 + Phase 2 money truth (09:05)

**Goal**: Fix the Invoice Audit Teardown's own number defects, propose a UI/UX build
plan, then execute Phase 1 and Phase 2.

**Shipped** (all live on cc.proexteriorsus.net):
- `b7e8410` Phase 1 — shared theme preference across 5 audit surfaces (`cc.theme`,
  was `ivTheme` + `eaTheme`, so dark reset when you walked between tabs); pill buttons
  name their destination; "No data available — report empty" replaced with why the
  control is inactive. Server template + client re-render changed together.
- `b18b196` mig 246 — a received credit memo now settles its claim lines
  (`credit-received`). at_risk $4,676.63 → $3,679.58, credit_memo_amount $0.00 →
  $997.05, actionable $3,485.13 → $2,692.08.
- `a7a7bca` migs 247/248 — docs/93 written back ($84.45 withdrawn, the adjust
  deliberately not applied); 11 SRS discrepancy rows re-cited, labelled `rederived`.

**Decisions**
- `credit_memo_amount` was NOT a mis-written predicate. 233's model was right; the
  lifecycle step was missing. The tempting one-line repair (repoint at `disputed`,
  $253.19) would have made it a strict subset of at_risk — double-counting.
- Patch a live view via `pg_get_viewdef()`, never restate it from an old migration
  file: rebuilding from 233 would have silently reverted 238 and 244.
- No CHECK on discrepancy provenance — the live add-line path can legitimately insert
  a null agreement_id (65 priced lines have none). Fail closed against data you
  control; detect on data you don't.
- Phase 1's "theme lives on one page" and "docs/84 W3 outstanding" were both wrong —
  six surfaces, and W3 was already shipped. Re-verify docs before planning off them.

**Open threads**
- Phase 2 leftover: SRS re-audit engine never committed → provenance recurrence
  unguarded; Layer 2 sweep is HTTP-only, needs a DB tier to detect it.
- `mark-received` settle path not yet exercised end to end — fires on the next real
  receipt (QA in Command Center).
- Phase 0 still un-run: nobody has watched Alex do the daily pass, which is what
  should order Claim/Chase/Pay/Fix before Phases 3–4.
- `AuditQueue` component fully orphaned — wants a docs/91-style documented removal.
- `.iv-kpis-scope` caption collides with the scope subtext at narrow widths.

## Session — Layer 2 DB tier + PEC-211/222 Colorado PDF review (16:20)

**Shipped**: `993ccb5` DB tier on the daily sweep (5 money-truth checks, read-only,
PostgREST counts not lists); `aa46711` systemd env contract. First prod run: 0 errors
(246/248 confirmed holding), 2 real warnings — 6 active agreements past expiry
(incl. `0049345641` exp 2026-06-27, the SRS Denver/Colorado book) and 8 with no
source PDF. Removed the stale `/api/product-surface.json` probe (deleted in PEC-217).

**PEC-211 / PEC-222 finding**: the SRS Colorado price sheets are NOT stored anywhere.
Both Linear issues have `attachments: []`; the `agreements` bucket's newest object is
2026-06-20; every SRS agreement has `source_pdf_url = NULL`. Root cause is a
capability gap, not a Maya misfire — `lib/agent-intake.ts` types attachments as
`string[]` (filenames only) and posts them to Slack as text. There is no download and
no storage upload on the Gmail path; the Slack path does have one
(`runtime/slack-attachment-processor.mjs` → `slack-attachments` bucket).
Consequence: Colorado is still priced off expired quote `0049345641` (exp 2026-06-27)
and the 8-13 / 8-14 sheets that would replace it were never ingested.

**Blocked**: no SSH key this session — the Hetzner host still needs `git pull` plus
`SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` in `/opt/openbrain/master.env` (known
malformed at 1317/1320) before the DB tier actually runs on the 06:00 timer.
