# 2026-08-20

## Session — UI/UX Phase 1 + Phase 2 money truth (09:05)

**Goal**: Fix the Invoice Audit Teardown's own number defects, propose a UI/UX build
plan, then execute Phase 1 and Phase 2.

**Shipped** (all live on cc.proexteriorsus.net):
- `b7e8410` Phase 1 — shared theme preference across 5 audit surfaces (`cc.theme`,
  was `ivTheme` + `eaTheme`, so dark reset when you walked between tabs); pill buttons
  name their destination; "No data available — report empty" replaced with why the
  control is inactive. Server template + client re-render changed together.
- `b18b196` mig 246 — a received credit memo now settles its claim lines
  (`credit-received`). at_risk $4,676.63 → $3,679.58, credit_memo_amount $0.00 →
  $997.05, actionable $3,485.13 → $2,692.08.
- `a7a7bca` migs 247/248 — docs/93 written back ($84.45 withdrawn, the adjust
  deliberately not applied); 11 SRS discrepancy rows re-cited, labelled `rederived`.

**Decisions**
- `credit_memo_amount` was NOT a mis-written predicate. 233's model was right; the
  lifecycle step was missing. The tempting one-line repair (repoint at `disputed`,
  $253.19) would have made it a strict subset of at_risk — double-counting.
- Patch a live view via `pg_get_viewdef()`, never restate it from an old migration
  file: rebuilding from 233 would have silently reverted 238 and 244.
- No CHECK on discrepancy provenance — the live add-line path can legitimately insert
  a null agreement_id (65 priced lines have none). Fail closed against data you
  control; detect on data you don't.
- Phase 1's "theme lives on one page" and "docs/84 W3 outstanding" were both wrong —
  six surfaces, and W3 was already shipped. Re-verify docs before planning off them.

**Open threads**
- Phase 2 leftover: SRS re-audit engine never committed → provenance recurrence
  unguarded; Layer 2 sweep is HTTP-only, needs a DB tier to detect it.
- `mark-received` settle path not yet exercised end to end — fires on the next real
  receipt (QA in Command Center).
- Phase 0 still un-run: nobody has watched Alex do the daily pass, which is what
  should order Claim/Chase/Pay/Fix before Phases 3–4.
- `AuditQueue` component fully orphaned — wants a docs/91-style documented removal.
- `.iv-kpis-scope` caption collides with the scope subtext at narrow widths.

## Session 1 — Dollar-rank the coverage gaps (PEC-221)

**Goal**: Pick up the 2026-08-19 "Next:" list (Atlanta ABC agreement, QXO, Euless) and make
the price-agreement coverage gaps actionable.

**Deliverables**
- **mig 245** — `v_office_vendor_spend` (invoice count + spend per office × vendor, via the
  mig-244 FK only) and `v_unresolved_branch_spend` (spend reaching no office, by cause).
- **mig 250** — backfilled `city`/`state`/`address` for 7 branches from
  `abc_invoices.raw->'branch'`; flipped them to `geocode_status='pending'`.
- **`docs/100-coverage-gap-dollar-exposure.md`**.
- Command Center: `invoiceCount`/`spend` per vendor, `gapsWithSpend`/`unauditedSpend`/
  `unresolvedSpend` totals, dollar-bearing gap pill + summary line. `gapExposure()` extracted
  pure and unit-tested. Build green, **314/314 tests pass**.

**Decisions / findings**
- **Gaps are not alike.** 9 zero-priced pairs, but 6 have never seen an invoice. Real
  un-audited spend = **$28,362 across 11 invoices**, concentrated in 3 pairs.
- **Priority correction**: 8/19 named Atlanta × ABC ($5,226.90) as next. **Denver × SRS is
  $17,437.63** — 3.3× larger and wasn't on the list. Chase it first.
- **Euless is CLOSED**: `office.is_active = false`. 0 branches is correct, not a defect.
- **Corrected my own first read**: an early query showed Denver × ABC as $427K un-audited.
  Wrong — Denver's agreements attach via `region_id`, not via a Denver-territory branch, and
  it has 153 priced items. Joining only on `vendor_branch_id` undercounted. Verified before
  reporting.
- **Branch 176 = $19,356.94 on a row with no city** — the largest single un-audited bucket.
  Address was already in `raw->'branch'` (Webster, TX). Structured source before OCR, again.
  **Trap**: 10 of its 11 invoices carry only `{"name":"176A"}`; the migration takes the
  *richest* payload, not the newest.
- **305 → Sherman, TX** is ~60 mi from Richardson — may be inside the ring, read as
  `out_of_boundary` only for want of coordinates.
- `no_branch_resolved` = **0 rows**: mig 243's ingest resolution is holding at 100%. All
  unresolved money is a *territory* question, not an identity one.
- 176 and 305 each exist twice across vendors (Ogden UT / Harrisburg PA) — the exact
  collision mig 244's FK was built to survive.

## Session 2 — Reconcile with parallel 245; found an agreement the ring cannot reach

A parallel session pushed `7c12359` (mig 245: close Euless, agreement_status rulings) two
minutes before my migrations, then `b18b196` (mig 246, settle received credit memo) later.
Both merged cleanly; renumbered mine to **250-253** — main is canonical, a feature branch
yields. Applied DB labels still read `245_`/`246_`/`248_`/`249_` — cosmetic; Supabase keys on
timestamp and mine all applied 10:57-11:11 UTC, before the file numbered 246 existed.

- **mig 252** — `v_office_vendor_gap_exposure`: joins Chris's ruling (245) to my dollars
  (246). Chris ruled **QXO `no_book` at all 5 offices** — QXO prices as no-price BY DESIGN,
  so Wichita x QXO ($5,697.47) is NOT work. Ranking on dollars alone would cry wolf weekly.
- **mig 253** — my own 248 gate was **wrong**: it keyed on `live_agreements = 0` (does
  paperwork exist) instead of `priced_items = 0` (can we audit). That hid the biggest find:

  > **Denver x SRS has a live, in-territory, 22-item agreement the office ring cannot
  > reach, so the coverage surface reports `priced_items = 0` for the pair.**
  >
  > **CORRECTED later this day** — see Session 3. "Prices nothing / $17,437.63 audits as
  > no-price" OVERSTATED it: that is true of the office-ring path only, and a separate
  > line-level path prices 11 of the 34 lines. Real un-priced figure: **$13,464.80**.

**Root cause** — `v_office_vendor_branch` still joins agreements to branches by branch-number
**TEXT**, the last survivor of what mig 244 removed from pricing/display. It needs the branch
geocoded AND the numbers to match. SRS South Denver is **two rows for one branch**: `AMSDE`
(no address, no geom, holds the book) and `SBP-SOUTHDENVER` (geocoded, 6.1 mi, holds nothing).

**Latent risk, bigger than one office**: `v_agreement_unreachable` shows **all 3 live SRS
agreements (136 items) are unreachable**. Richardson + Wichita still price only because
**archived duplicate** agreement rows reach the ring. Denver just lacks that crutch. Tidy the
archived rows and 114 more items silently stop pricing.

**Traps recorded**: `v_office_vendor_branch.agreement_id` is MIXED-TYPE text — ABC = legacy
ints ('105'), SRS = uuids; casting throws. Compare text vs both `id` and `legacy_id`.
`CREATE OR REPLACE VIEW` may only APPEND columns, never reorder.

**Not fixed deliberately**: repointing the agreement join is pricing-affecting and earns
mig 244's equivalence proof; merging duplicate branch rows is a human identity decision
(mig 240: a guess cannot become a fact).

**Open threads**
- **TOP ITEM — Denver × SRS, $13,464.80 of line value un-priced**: agreement exists but the ring cannot reach it. Needs either
  (a) confirm `AMSDE` == `SBP-SOUTHDENVER` and repoint the book, or (b) repoint the agreement
  join to the FK with mig 244's equivalence proof. Human decision either way.
- **Do NOT chase QXO** — Chris ruled `no_book`, by design. Superseded my earlier framing.
- **Blocked (needs Chris)**: Atlanta × ABC is already `pending`; Atlanta × SRS and
  Kansas City × SRS are `unrecorded` but have $0 spend, so a ruling can wait.
- **Blocked (no `.env` in container)**: `node scripts/geocode-vendor-branches.mjs` for the 7
  newly-addressed branches; then check whether 305 lands in Richardson's ring.
- Carried from 8/19: QA agent `login` blocked by harness classifier; one-time WorkOS sign-in
  on the QA desktop; 3 stale 1P WorkOS client IDs; `master.env` lines 1317/1320 malformed.
- Not deployed. Work is on `claude/project-handoff-5ua2fw` + draft PR, not merged to `main`.

## Session — Layer 2 DB tier + PEC-211/222 Colorado PDF review (16:20)

**Shipped**: `993ccb5` DB tier on the daily sweep (5 money-truth checks, read-only,
PostgREST counts not lists); `aa46711` systemd env contract. First prod run: 0 errors
(246/248 confirmed holding), 2 real warnings — 6 active agreements past expiry
(incl. `0049345641` exp 2026-06-27, the SRS Denver/Colorado book) and 8 with no
source PDF. Removed the stale `/api/product-surface.json` probe (deleted in PEC-217).

**PEC-211 / PEC-222 finding**: the SRS Colorado price sheets are NOT stored anywhere.
Both Linear issues have `attachments: []`; the `agreements` bucket's newest object is
2026-06-20; every SRS agreement has `source_pdf_url = NULL`. Root cause is a
capability gap, not a Maya misfire — `lib/agent-intake.ts` types attachments as
`string[]` (filenames only) and posts them to Slack as text. There is no download and
no storage upload on the Gmail path; the Slack path does have one
(`runtime/slack-attachment-processor.mjs` → `slack-attachments` bucket).
Consequence: Colorado is still priced off expired quote `0049345641` (exp 2026-06-27)
and the 8-13 / 8-14 sheets that would replace it were never ingested.

**Blocked**: no SSH key this session — the Hetzner host still needs `git pull` plus
`SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` in `/opt/openbrain/master.env` (known
malformed at 1317/1320) before the DB tier actually runs on the 06:00 timer.

## Session 3 — Merge main (6 commits); corrected my own headline (PEC-221)

**Goal**: PR #9 check-in. Main had moved 6 commits; reconcile and verify.

**Collisions handled** (third and fourth of the day)
- Parallel sessions claimed migs **247, 248** — no clash, mine already sit at 250-253.
  Main now holds 245-248; 249 is left unused (the DB label `249_` was spent on my detector).
- **docs/99 claimed too** (`99-credit-memo-money-truth-2026-08-20.md`). Mine renamed
  **docs/99 -> docs/100**. Note the repo already tolerates duplicate doc numbers (two `95-`),
  but two `99-`s dated the same day is needlessly confusing, and main is canonical.
- **`context/memory/2026-08-20.md` add/add conflict** — resolved keeping BOTH sessions'
  blocks, reordered chronologically (09:05, my 2, their 16:20).

**Correction to my own headline — this matters more than the merge**
Their mig 248 (`srs-reaudit-provenance`) revealed that SRS lines are priced through a
**line-level** path, not only the office ring. So my claim "Denver x SRS prices nothing,
$17,437.63 audits as no-price" was **overstated**. Measured:

| | Lines | Line value |
|---|---:|---:|
| Total (4 invoices) | 34 | $15,760.85 |
| Carry a negotiated price | 11 | $2,296.05 |
| **Carry none** | **23** | **$13,464.80** |
| Carry an agreement citation | **0** | - |

Honest figure: **$13,464.80** un-priced, not $17,437.63. ($15,760.85 line value vs
$17,437.63 invoice total = tax/freight.) Corrected in mig 253's header, docs/100 and above.

**What survives, and is worth more than the headline was**: the ring genuinely cannot reach
the agreement, AND **the two pricing paths disagree** — the coverage surface calls the pair
entirely unpriced while the line path prices 11 lines. That disagreement is the real defect.
Zero agreement citations on those lines is the SRS-only gap their mig 248 repairs.

**Lesson**: `priced_items = 0` is a fact about ONE path. I read it as a fact about the money.
Check which path a metric measures before quoting it as an amount.

**Verify**: build green, 314/314 tests on the merged tree.

## Session — Maya QA build-out + narrow host pull (17:45)

**Shipped** (`e08a7a5` chaos scheduler, `0e168d6` sweep env fix, `8bf7ee9` forensic pass,
`320bdbc` orchestrator + 04:30 CT timer):
- Narrow pull on PE-US-AGENTS: HEAD stays `2838bcf`, only sweep/QA scripts checked out.
  Maya's runtime code untouched, as agreed.
- **Site sweep now actually installed and running** — it never was. Its unit pointed
  `EnvironmentFile` at `/opt/openbrain/master.env`, which does not exist; the leading
  `-` made that silent. Env really lives in the repo clone's `.env`, sourced by a bash
  wrapper (the `maya-gate.sh` pattern). Ran on host: DB tier fires, 2 db warnings.
- Maya QA chain proven end to end: systemd → `maya-qa.sh` → orchestrator → Orgo bash
  API → Maya's desktop probe → fail-closed report.

**Decisions**
- Forensic pass is read-only by three layers, the last independent of the others:
  ALLOW view-affecting controls, DENY the mutation vocabulary, ABORT every non-GET at
  the network layer. This app's buttons include Process / Approve & Send / Export —
  a naive click-everything bot would have fired vendor emails nightly.
- Chaos is bounded, not uniform: no page repeats until all 26 have had a turn. Uniform
  random never reaches the long tail, which is where docs/84 found the rot.
- Orchestration is split by capability: Hetzner has the repo (routes), the Orgo desktop
  has Chrome + session (walking). No repo clone on the desktop — private, needs a key.
- **Did NOT delete the PE Site QA desktop.** Maya cannot QA anything yet (no WorkOS
  session), so deleting the only staged fallback before its replacement works is
  backwards. Deletion is one step once Maya's first run is green.

**Blocking, and it is the only thing left**: a human must sign in to WorkOS once, in
Chrome, on Maya's desktop `37b262e0-a915-47e6-8c3b-f180a32ab6fe`, profile
`/opt/pe-cc-agents/maya-qa/chrome-profile`. Agents must not perform that sign-in.

**Open**: two unidentified desktops in PE-open-brain (`c5a0c869`, `5a9542cf`) — did not
touch. Host static-analysis tier reads the stale `2838bcf` checkout, so its `errors=1`
on `price-list.ts` is a phantom (deleted in PEC-217) — resolved by the full pull.
A3 still owed for the QA loop (hard rule 9) before it counts as an approved skill.
