# 2026-06-18

(Continues from `2026-06-17.md` sessions 1–19 — same continuous work stream.)

## Session — 4-surface design review

**Goal**: Review Price Agreement Audit / Price List Coverage / Negotiated Catalog / Price Foundation to live data + the design system.
- **Price Agreement Audit** (`/abc-price-agreement-gaps`): REBUILT workflow-home → agreement-lifecycle dashboard over `v_price_agreement_audit` (schema 102). Finding: all 5 item-bearing agreements EXPIRED; 94 "ok" agreements are empty shells. Filter defaults to "Needs renewal".
- **Price List Coverage** (`/accounting/vendor-regions`): VERIFIED already design-system + live; no rebuild.
- **Negotiated Catalog** (`/accounting/price-list/catalog`): replaced synthetic YEAR_FACTOR with `v_negotiated_catalog` (103) — top-200 items by REAL spend from `abc_line_items` (2023-26, $5.4M).
- **Price Foundation** (`/data-quality/price-foundation`): re-skinned to the design system (scoped tokens, theme toggle, KPI/table/pills); live loader preserved.
- Commits 76b722e (catalog+foundation), e5d9ccb (agreement audit). All deployed.

## Session — Items 1→2→3 (invoice pull, AcuLynx, renewals)

**Item 1 (invoice pull refresh):** fixed a real bug in `mirror-backfill.mjs` (dedupe upsert batches by conflict key — ABC history returns dup invoice_numbers). Refreshed prod invoices 520→**560** (latest 06-17). Re-ran audit backfill. **Key reconciliation finding**: open/paid status is NOT in the ABC API — it's in `invoice_documents.payment_status`, fed from the ABC **portal CSV** (HIL). The "169 active" needs the AR-CSV feed; the data pull alone took open to 227. Commit dbb21bb.

**Item 2 (AcuLynx matching):** KEY discovery — PE job numbers live in `acculynx_jobs.job_name` (`"KS-157: Client"`), not the empty `job_number`. Built `v_invoice_acculynx_match` (104); Invoice Audit now shows job#/client/type on matched invoices (183/560 = 33%). 100% blocked upstream (only 179/1240 jobs carry a PE number). Commit cf7f5ac.

**Item 3 (agreement renewals):** "Request Renewal" now persists to `price_refresh_request` (reason='agreement_renewal', agreement_id link, schema 105) via `/api/price-agreement/request-renewal` — drafted for approval, no auto-send. Commit a0029c7.

## Session — Item 2 build start + vendor API skills + handoff

**Orders (Item 2):** refreshed prod orders 3,146→+32. **Fixed order-sync PO bug** — PO is `raw.salesOrder.purchaseOrder` (`"CO-227: Client"`), read the wrong key, so `purchase_order_number` column was empty (0/3146). 349 orders match AcuLynx jobs. Order lines have NO price (pre-pricing) → Order Audit = AcuLynx verification + coverage, not variance. Commit 9136075. **Order Audit dashboard build = NEXT.**

**Vendor API skills (Chris's standing instruction — stop wasting tokens re-researching):**
- Created `skills/cleverwork-roofer/abc-supply-api/SKILL.md` — full ABC API↔table data map, the complete endpoint catalog, and what's HIL (no AR/paid/due endpoint → portal CSV).
- Added a Brain-data-map section to `skills/cleverwork-roofer/acculynx-api/SKILL.md` (job# in job_name, acculynx_invoices empty, matching rule, TEMP-number design).
- TODO: same for EagleView/GAF/Roofr as touched.

**Decisions captured**: Chris confirmed orders happen pre-approval (nightly order pull); item list for Item 3 = purchased-36mo→~500; magic-link is primary validation; AcuLynx `id` = permanent key with `{Region}-TEMP-{short id}` labels pre-approval.

**Open threads**: Item 1 pending Q (how is `invoice_documents.payment_status` fed?). Item 2 Order Audit dashboard not built yet. Item 3 not started. Full pickup: `docs/handoffs/current.md`.

## Session — ABC Supply nightly sync (scheduled, automated)

**ABC production sync 2026-06-18T11:05Z, env=production — NOT COMPLETED in Cowork sandbox.** Background/nohup jobs do not survive across bash calls here (fresh PID ns + die-with-parent; confirmed via detached-process test) and the 45s per-call cap can't hold a 20-45 min run (probe: ~1.5 of 333 catalog pages per 45s call). Prescribed nohup+poll method is structurally infeasible on this surface. **Verified healthy instead:** prod token/auth OK (catalog fetch returned fetched=1000 totalPages=333); ABC read-only. **Current Supabase end-state (queried directly via PostgREST):** abc_product_catalog rows=331,293 (max abc_last_seen_at 2026-06-17T20:26:40Z — refreshed by yesterday's run), abc_vendor_branches rows=720, abc_regions rows=40. rowsUpserted for a re-run would be ~0 (content-hash gate counts only changed rows; page 1 logged upserted=0). **Recommend:** run full sync from the persistent Hetzner/Coolify agent host (docs/27) or a local terminal where nohup survives; Cowork sandbox is the wrong execution surface for this long job.

## Memory Health
- 2026-06-18: CAP COMPLIANCE failed — context/MEMORY.md at 2,478/2,500 chars (99%, within 5% of cap). Consolidate via meta-memory-write skill rules.

## Session — Item 2: Order Audit dashboard (BUILT, deployed)

**Goal**: Build the Order Audit dashboard under Operations (the marked-NEXT from the handoff).

**Built** (commit 7d20eeb, pushed to `cleverwork/price-agreement-audit` + `main` → Coolify auto-deploy):
- **Schema 106** (`106-order-audit-views.sql`, applied to prod via MCP migration `order_audit_views`): `v_order_audit_order`, `v_order_audit_line`, `v_order_acculynx_match`.
- **Key data-map finding**: ABC order LINES have NO price (pre-pricing) → Order Audit is **verification + coverage**, not variance. PO lives in `raw->'salesOrder'->>'purchaseOrder'` (the `purchase_order_number` COLUMN is empty 0/3178 — sync-bug fix only applies to new pulls, so the views read raw). Order date = `raw.dates.orderedOn`, $ total = `raw.orderAmounts.total`, status = `raw.salesOrder.status`. Lines carry no status_code/status_message (all null).
- `lib/order-audit.ts` (paginated `selectAll` — 3,178 orders + 18,593 lines both exceed PostgREST's 1000-row default), `pages/operations/order-audit.astro` + `scripts/order-audit-tree.ts` mirror the Invoice Audit design system (Office→Branch→Order→Line, theme toggle, ?office=/?branch= deep-link, lazy line render). Nav "Order Audit" added under Operations.

**Live numbers** (verified rendered against localhost:4321, "Supabase live"): 3,178 orders · **354 matched to a PE job (11%)** · 2,824 unmatched · **$7,319,541** ordered · **16,116/18,593 lines (87%) uncovered** by any current negotiated agreement. Match ceiling is the same upstream cap as invoices (only 179/1240 AcuLynx jobs carry a PE#); the high uncovered-line rate is the same expired/empty-agreements story as the Price Agreement Audit.

**Open threads**: Item 1 still pending Chris's answer (how `invoice_documents.payment_status` is fed). Item 3 (vendor-agnostic price-agreement builder) = next in the sequence, requirements locked.

## Session — Order Audit corrected: priced lines + variance + auto-archive + docs-first skill

**Trigger**: Chris caught (a) line Description/Qty/UOM rendering empty, (b) orders ARE priced (`lines.amount`), and (c) the repeated vendor re-research. Pointed me to the ABC API docs (apidocs.abcsupply.com/get-orders).

**Root cause**: `mirror-backfill.mjs orderLineRows()` maps `line.quantity/uom` (don't exist) instead of `orderedQty.value`/`orderedQty.uom`, and never stored `unitPrice.value`/`amount`. So the columns are 0/empty — but `raw` has it all. Lines carry NO description (only `links.href`) → description from `abc_product_catalog` by item_number.

**Built** (commit aa1e5b7, on `cleverwork/price-agreement-audit`; NOT yet on main):
- **Schema 107** (`v_order_audit_line/order` rebuilt, applied to prod): qty/uom/unit_price/extended from raw, description from catalog, **variance vs negotiated** (variance_pct/ext, flagged/at_risk/worst rollups). **Auto-archive via 'system'**, in-view: `disposition='archived'` if status ILIKE 'invoiced' OR `salesOrder.createdDate` < now−60d; `archive_reason` invoiced/aged_60d. Self-maintaining (no cron).
- Order Audit is now a real **pre-invoice variance catch**. Active-scoped KPIs: **181 active orders · $3,658 at risk · 419 flagged · 33 matched · 773 no-agreement**. Archived: 2,997. Top catch e.g. GAF Cobra Snow Country $42.99 vs $17.00 negotiated (+153%).
- `order-audit.ts/.astro/order-audit-tree.ts`: priced line table (Item/Desc/Qty/UOM/Order Price/Negotiated/Var%/Var$), tolerance pills, filters status(active default)/tolerance/match. Verified rendered on localhost:4321 (payload parses, fields populate).

**Skill / standing instruction (the systemic fix Chris asked for)**:
- `abc-supply-api/SKILL.md` now leads with **SOURCE OF TRUTH = official API docs** (live `apidocs.abcsupply.com` + in-repo `ABC_Endpoint.md`), a cross-vendor **"Vendor API rule"** (docs FIRST → data-map skill → fix sync & update skill), corrected order-line paths, the archive rule, and the `orderLineRows` sync bug to fix.
- Elevated the same docs-first order in the `vendor-api-data-map-skills` memory.

**Open threads**:
- **DEPLOY**: corrected build (aa1e5b7) + earlier docs commit (e389b02) are on the dev branch only; `origin/main` is still at the *first* Order Audit (7d20eeb). Needs `git push origin HEAD:main` (user-authorized) to deploy the corrected version.
- **Sync fix** (not done): patch `mirror-backfill.mjs orderLineRows()` to populate quantity/uom + add unit_price/extended_price columns, so views needn't read raw.
- Item 3 (price-agreement builder) still next; Item 1 AR-feed Q still open.

### Session — DEPLOYED corrected Order Audit + sync fix (authorized)
- Pushed `cleverwork/price-agreement-audit` → **`origin/main`** (Chris authorized). main now at `a542082` (= dev branch). Coolify rebuilds; couldn't confirm (c) live from the Cowork sandbox (healthz unreachable).
- **`orderLineRows()` sync FIXED** (commit a542082): maps `orderedQty.value/uom`, `unitPrice.value`, `amount` per the get-orders docs (was mapping non-existent `line.quantity/uom`). **Schema 108** added `unit_price`/`extended_price` to `abc_order_lines` + backfilled all 18,593 rows from raw. Views keep reading raw for robustness. abc-supply-api skill updated to mark the bug fixed.
- Branches converged (main = dev = a542082). **Remaining**: Item 3 (price-agreement builder) next; Item 1 AR-feed Q still open.

### Session — Item 3 slice 1: Price Agreement Builder (workflow-mapped, deployed)
**Approach**: ran an 11-agent foundation-mapping workflow (5 subsystem mappers, each paired with an independent adversarial validator, + synthesis) per Chris's "validation layer for every agent" rule. Validators caught real errors (family_name is 75.6% populated & used; spend $6.14M not $5.35M; no manager_json column; agreements 1 active/1 expiring/4 expired not all-expired; manager_email 94/756 not 0). Full plan → `docs/40-price-agreement-builder-plan.md`.

**Decisions locked w/ Chris**: curation = review-class **A+B by family** (~454 top-level / 857 SKUs / ~87% spend); package grain = **per branch**; prefill = branch's latest negotiated price else 0.

**Built + deployed (commit dbafa9a, on dev + main)**: schema 109 `v_negotiable_items` (A+B, family-grouped) + `lib/agreement-package.ts` (paginated; per-branch prefill via abc_price_agreement_branch_matches→abc_price_list_items) + `pages/accounting/price-agreement/builder.astro` + `scripts/agreement-builder-tree.ts` (family tree → variations, branch picker, prefilled prices, design system) + nav "Agreement Builder" under Accounting. **Read-only this slice.** Verified rendered (454 families, 76 branches, prefill populates).

**Finding**: every branch inherits the SAME central agreement → only ~66/857 A+B SKUs negotiated anywhere (~8%); 791 need negotiating. (Earlier "318 priced for branch 304" was ship-to double-counting; true distinct count ~66.)

**Next (slices 2-5, plan in docs/40)**: editing+persistence (agreement_packages tables) → PDF/CSV → drafted email (human-gated) → magic-link submission. **Open Qs for Chris**: recipients (sales_rep_email 0/756), magic-link TTL, prefill region/national fallback, PDF fidelity.

### Session — Item 3 slices 2-3 (editable builder + persistence + PDF/CSV), validated + deployed
**Decisions (Chris)**: recipient = **Justin Garza** (Justin.Garza@abcsupply.com, ABC national account manager) for all PE offices; **magic-link TTL = 7 working days @ 06:00**; **prefill** = branch negotiated → else PE-office last-invoiced unit price if <60d → else 0; **HARD comms rule**: agents send ZERO email outside the company — agent drafts + notifies Lucinda/Roberto internally (Slack / internal AgentMail), the external send to Justin is a human action from **Hermes / Google Workspace (agents.proexteriorsus.net)** where all vendor email is directed. Enforce via outbound allowlist (slice 4).

**Built + deployed (commits 158d728, 85ccba7, 9f5a71c on dev + main)**:
- **Slice 2** — schema 110 (`agreement_packages`/`agreement_package_items` + `v_recent_invoice_price`). Editable price/variation; family "Set all" cascades to non-overridden variations; variation edit sets is_override. `POST /api/price-agreement/package/items` (auth-gated) get-or-creates per-branch draft, upserts. Loader merges saved edits + the invoice-<60d prefill fallback (branch 113: real prefill 37→202 items).
- **Slice 3** — `pdf-lib`; GET `/api/price-agreement/package/{pdf,csv}?branch=` (auth-gated). PDF mirrors ABC ledger (family-grouped, 35pp), CSV with blank vendor_final_price. Export links in builder toolbar.

**Validation layer (per Chris's standing rule)**: ran an independent adversarial code-review agent on slices 2-3. PASSED: consent boundary (zero external-send path), auth (all routes gate on locals.actor), injection (parameterized). FIXED its real findings: override-on-any-keystroke (HIGH), branch_number string/number normalization (MED), get-or-create race re-fetch (MED), $1M price ceiling (MED), PDF page-break + CSV header (LOW). Re-verified; test rows removed from prod.

**Next**: slice 4 (drafted handoff to Lucinda/Roberto, outbound allowlist, NO external send) → slice 5 (magic-link submission for Justin). Plan in docs/40.

### Session — Item 3 slice 4: drafted handoff + outbound allowlist guard (deployed)
**Built + deployed (commit 719b6be, dev + main)**:
- `lib/outbound-guard.ts` — the code-enforced "zero external agent send" primitive: `classifyRecipients`/`assertAgentSendAllowed` vs an internal-domain allowlist (proexteriorsus.com/.net + cleverwork.io; subdomains internal). Justin Garza @abcsupply.com → external → human-send-required. Verified classification logic.
- `POST /api/price-agreement/package/handoff` (auth-gated) — drafts the package into `price_refresh_request` (reason='agreement_package', status='awaiting_verification', channel='human_send_required', recipient Justin, PDF/CSV links). Idempotent per branch; moves package → pending_review. NEVER sends. "Draft for review" button on the builder.
- **Validation**: full-feature grep confirms the ONLY network calls in the whole agreement feature are 2 same-origin fetches (save + handoff) — no nodemailer/sendgrid/resend/smtp/mailto/transport anywhere. Zero-external-send holds. Test draft removed from prod.
- Fixed a real bug found in testing: dynamic `import.meta.env` access isn't allowed in Vite SSR → switched to `getRuntimeEnv()`.

**Decisions confirmed**: Justin Garza only (no per-branch rep); magic-link = 7 business days @ 06:00.
**Remaining**: slice 5 (magic-link submission page for Justin, TTL 7 biz days @ 6AM). Open: token reuse policy + internal-notify mechanism (Slack vs internal email, human-triggered). Plan in docs/40.

### Session — Item 3 slice 5: magic-link submission — ITEM 3 COMPLETE (deployed)
**Built + deployed (commit de8e8cd, dev + main)**:
- Schema 111 (`agreement_package_submissions`: magic_token, 7-business-day @ ~06:00 Central expiry, single-claim; + vendor_final_price/vendor_note on items).
- `POST /api/price-agreement/package/issue-link` (AUTH-GATED = the human-approval step; idempotent; returns URL for the operator to email — agent never sends).
- PUBLIC `/submit-agreement/[token]` standalone page + `POST /api/price-agreement/submit/[token]` (token-gated, ATOMIC single-claim via .is('claimed_at',null), writes only the token's package items, 410/409 on expired/claimed). Middleware allowlists the 2 public prefixes.
- Builder "Issue link" button copies the URL. Defaults: single-claim token; notify via the review queue.
- **Verified** end-to-end: issue→page→submit→re-submit 409. **Adversarial security review** PASSED core (auth/IDOR, atomic claim, consent, middleware-prefix safety all clean). Fixed findings: removed unused set:html sink (HIGH), canonical-UUID token regex (MED), per-IP rate limit on the public surface (MED). Test rows removed from prod.

**ITEM 3 COMPLETE** — all 5 slices live (schemas 109-111): per-branch builder → editable+persisted → PDF/CSV → drafted handoff (zero external send, outbound guard) → magic-link submission. Recipient Justin Garza. Plan/status: docs/40. The 3-item sequence (Item 1 AR-feed pending Chris, Item 2 Order Audit done, Item 3 done) is now through Item 3.

### Session — Full-site action audit (every page/trigger), audit-first
**Method**: 26-agent static+wiring workflow (12 surface auditors + independent validators + completeness critic) → then a dynamic/data-layer pass (drive running app + before/after Supabase). ~220 interactive triggers across 13 surfaces. Report → `docs/41-site-action-audit.md`.

**KEY LESSON**: the static pass read MIGRATION FILES not the live DB → 3 false positives on its top "dead" findings. Dynamic pass corrected all 3: mark-paid `gate_override` column DOES exist (LIVE); `/accounting/review-queue` renders a real page via `accounting/[slug]` (not 404); `invoice_action_log` table exists. **Always verify against the live DB, not migration files.**

**Confirmed REAL defects**: (1) HIGH **estimate edits not persisted** — estimate-audit.ts toasts "Recalculated locally — not saved", zero fetch (Chris's exact concern). (2) HIGH **"Request Price List"** button (price-list-coverage.ts:190,207-217) = placebo, local-state only, no network — almost certainly the button Chris clicked. (3) MED **marketing dept absent from nav.ts** (routes 200 but unreachable by click). Suspects: price-foundation Reopen field-reset semantics; AuditQueue KPI param-less nav. Cleanup: orphaned abc-price-gaps.ts; ~22 " 2" iCloud dupe files.

**Verified LIVE** (dynamic, before/after + cleanup): invoice "Mark passed" → invoice_line_audit ✓; agreement builder suite ✓ (slices 2-5); request-renewal wired. mark-paid/work-queue/territory/price-foundation wired w/ error handling (schema-verified).

**PAUSED for fix approval** (audit-first per Chris). Recommended order: estimate persistence → Request Price List wire-up → marketing nav + cleanup → suspects.
