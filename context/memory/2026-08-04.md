
## Session 3 (QXO/SRS vendor onboarding)

**Goal**: Reorient + plan QXO/SRS vendor addition + extract branch networks
**Deliverables**: docs/79-qxo-srs-vendor-invoice-onboarding-plan.md; integrations/bridges/{qxo,srs-roofhub}/data/branches-2026-08-04.{json,csv} (QXO 566 stores via beacon-ng.becn.com/v1/store-location API quadtree sweep — 30-item response cap; SRS 448 branches via find-a-branch page + 448 detail pages: phone/fax/hours/geo/manager/team/products)
**Decisions**: generic vendor_invoices/vendor_invoice_lines tables recommended over per-vendor clones; loader-computed UOM (not generated cols); vendor_slug discriminator needed on invoice_line_audit before 2nd vendor data lands
**Open threads**: ABC nightly sync silent since 2026-08-02 (openbrain-abc-sync.timer, 5.78.146.161 — SSH check blocked, needs human or permission); §2 decision points in docs/79 await Chris (samples, table shape, intake order, agreements); vendor-territories.ts slug mismatch srs vs srs-distribution

## INCIDENT — both Hetzner hosts unreachable (detected ~11:30 CT)
- 5.78.124.10 (Coolify/CC, cc.proexteriorsus.net) AND 5.78.146.161 (agent host): 100% packet loss, no TCP 22/80/443. Confirmed from external vantage (Firecrawl) — not local network. Hetzner status: all systems operational → NOT a platform outage.
- Timeline evidence: openbrain-abc-sync.timer (agent host) last fired Aug 2 07:30 UTC; Aug 3+4 runs missing → hosts likely down since ~Aug 2-3, BEFORE today's push. Both hosts down simultaneously → suspect account-level (billing/abuse/suspension) at Hetzner.
- Push 8c2e405 landed on origin/main; Coolify will build when host returns. Supabase (all data work) unaffected.
- HUMAN ACTION: check console.hetzner.cloud server states + Hetzner emails (billing/abuse).

## Session 4 — host rebuild + SSH key custody
- OLD hosts 5.78.124.10 (Coolify/CC) + 5.78.146.161 (agent) CRASHED, deleted by Chris. NEW server **PE.CC.DEV 178.105.220.14** (cpx62, ubuntu-26.04, fsn1) created 2026-08-04 19:02 UTC — bare, no Coolify yet, none of our keys authorized.
- Hetzner API token: 1Password item **Hetzner-PE_CC_DEV** in the **Employee** vault (NOT CW_Master as assumed). Read via `op item get 'Hetzner-PE_CC_DEV' --vault Employee --fields credential --reveal`.
- All 4 SSH keypairs backed up to CW_Master as documents (private + .pub each), SHA-256 verified identical: a_roofers_open_brain_ed25519, hetzner_office, cursor_vps, id_ed25519. NOTE: `op` CLI cannot import into the native "SSH Key" category (reserved private_key field rejects assignment) — documents are the working path; native import is drag-drop in the desktop app.
- 3 public keys registered on the Hetzner account (a-roofers-open-brain, hetzner_office, chussey-cleverwork) so future servers/rebuilds get them automatically.
- BLOCKED: cannot SSH to 178.105.220.14 (keys not on the box). Options: Chris pastes a key via Hetzner console, or authorize a rebuild (server is bare, ~0 data at risk).

### Rebuild progress (PE.CC.DEV 178.105.220.14)
- SSH: key `a_roofers_open_brain_ed25519` authorized by Chris via ssh-copy-id. Host HARDENED key-only (`/etc/ssh/sshd_config.d/99-openbrain-hardening.conf`: PermitRootLogin prohibit-password, PasswordAuthentication no). Stale known_hosts entry for recycled IP cleared (backup ~/.ssh/known_hosts.old).
- Server: ubuntu-32gb-fsn1-2, 16 vCPU / 30GB / 601GB, Docker 29.7.1, **Coolify installed & healthy** (coolify, -db, -redis, -realtime, -proxy, -sentinel all Up).
- DNS (Cloudflare zone proexteriorsus.net, token item CLOUDFLARE_API_TOKEN in CW_Master): cc + coolify A records repointed 5.78.124.10 -> 178.105.220.14, propagated.
- BLOCKED ON HUMAN: Coolify first-run admin registration (account creation + password = agent-prohibited). Chris registers at https://coolify.proexteriorsus.net (reuse COOLIFY_USER_EMAIL/COOLIFY_PASSWORD from root .env), then creates an API token -> agent finishes app recreate + env restore + deploy of 8c2e405 via API.
- Old Coolify API key in .env is DEAD (belonged to destroyed instance); needs replacing after new token issued.

## Session 5 — drive-time rings, sync timer, OCR
- **Drive-time boundaries fixed**: `compactVendorTerritoryMapPayload` hard-set `boundary:null` to slim the payload; homepage map uses deferLoad -> refetches /api/vendor-territories -> rings vanished. Now preserved (5 offices x ~25 pts is trivial vs 1,800 branches). Polygon styling raised to stroke .85/2.4px, fill .13 + legend entry — the 2hr ring IS the shared-pricing territory.
- **ABC nightly timer reinstalled on PE.CC.DEV** (/opt/openbrain/a-roofers-open-brain, Node 22, .env copied 600). Units in /etc/systemd/system, timer enabled, next 03:30 ET. Repo has NO root package.json — bridge scripts use only node: builtins, no npm install needed.
- **BLOCKER — ABC API geo-blocks the German host**: OAuth 200 but `/api/invoice/v1/invoices/history/{billTo}` returns **403 + WAF HTML** from 178.105.220.14 (fsn1). Old host was Hetzner Ashburn (5.78.x, US). Confirmed via abc_endpoint_access_log. Options: move/add a US host (original design had 2 hosts), ask ABC to allowlist the IP (rep Justin Garza), or US proxy for ABC calls only.
- **Wichita OCR done** (`integrations/bridges/abc-supply/ingest-price-sheet-ocr.mjs`, Unstructured hi_res): agreement 105 now 157 items (was 0). Sheet is 2-column and OCR interleaves columns + drops some UOM cells -> 65 rows have unit=null (honest; docs/46 forbids cross-UOM compare; all rows approval_status=pending). Script caches raw OCR to tmpdir so parser iteration never re-bills.
- ABC agreements now: 105 Wichita 157 items (desc-only), 106 Denver 153 (item numbers), 107 DFW 137 (129 w/ item numbers).

## Session 6 — US agent/sync host provisioned (ABC geo-block SOLVED)
- **PE-US-AGENTS `178.156.203.23`** (cpx21: 3 vCPU / 4GB / 80GB, Ubuntu 24.04, **ash / Ashburn VA**), created via Hetzner API with all 3 account SSH keys pre-baked (instant key access — no password step). Hardened key-only. Node 22, repo at /opt/openbrain, .env copied 600.
- **ABC geo-block CONFIRMED + FIXED**: same sync, same creds — Falkenstein = 2 failed / 0 rows (403 WAF); Ashburn = **0 failed, 16 history rows fetched+upserted, 1 detail**. `openbrain-abc-sync.timer` now enabled HERE (03:30 ET); DISABLED on PE.CC.DEV (would only 403).
- Two-host split restored: **PE.CC.DEV fsn1** = Coolify + Command Center; **PE-US-AGENTS ash** = US-egress syncs + future Hermes agents.
- Hetzner ash pricing (this account, net=gross): cpx11 $20.49 / cpx21 $37.49 / ccx13 $50.99 / cpx31 $73.49 / **ccx23 (4 ded vCPU,16G) $102.99** / cpx41 $141.49 / ccx33 (8 ded,32G) $165.99. Resize-up is in-place (power off → resize → boot); disk can never shrink.
- Hermes sizing ref = docs/27: agent-runtime 2-4GB, agent shell runtime 2-4GB, Coolify+Docker 1-2GB.

## Session 7 — Atlanta office was MISSING (site review)
- proexteriorsus.com lists **6** locations; `office` had **5**. Missing: **Atlanta (Jonesboro), GA — 409 Arrowhead Blvd Suite B, Jonesboro GA 30236, (469) 535-1708**, Mon-Fri 7-6.
- Impact: 48 GA vendor branches (ABC 18 / SRS 16 / QXO 14) sat `out_of_boundary` with NO pricing territory — i.e. no negotiated-price expectation for the whole Atlanta market.
- Mig **193**: added GA region + Atlanta office + `set_office_boundary(uuid,text,text,int)` RPC (service_role only).
- **REAL isochrone computed** (`google_routes_bearing_v1`, mean radius 150.0 km, 25 pts, 24 bearings x 6-step binary search). Key lesson: Distance Matrix is DENIED on GOOGLE_MAPS_SERVER_KEY, but **Routes API (routes.googleapis.com computeRouteMatrix) IS enabled** — use Routes, not Distance Matrix. GOOGLE_MAPS_BROWSER_KEY is referrer-restricted and correctly cannot be used server-side. Script: `scripts/compute-office-drive-time-boundary.mjs --office-id=<uuid>`; RPC `set_office_boundary`. The 145 km radius_fallback was an interim step, now superseded (real ring is ~150 km mean — the fallback was close but slightly conservative).
- **Second finding: 605 of 756 ABC branches have `geocode_status='skipped'` and no coords** — pre-existing, they can never territory-match. Geocoded the 18 GA ones (18/18 ok). Recommend a full backfill pass (~$3 at Google rates).
- Atlanta owns **38 branches** on the real isochrone.

## Session 8 — geocode-all + price-agreement territory inheritance
- **Geocoded 589 ABC branches, 0 failures** (512 rooftop, 58 approx, 19 no address). QXO/SRS already had coords from site extraction. `scripts/geocode-vendor-branches.mjs` (resumable) + `assign_branch_territories()` RPC (never overrides human `territory_decided_by`). **138 branches now in a territory**; 29 `overlap_pending` (Euless∩Richardson rings — real, both offices buy there; roster shows them under BOTH).
- **Mig 195 inheritance views** — rules per Chris: agreement in an office ring is inherited by all that vendor's branches in the ring; competing agreements resolve **per-item lowest** with source branch recorded; **EVERGREEN** (lapsed list stays in force until replaced — `is_lapsed` = "Extended", never an error); UOM-safe (docs/46).
- Two bugs caught by validating vs live data: (a) preferring `abc_price_agreement_branch_matches` over the agreement's own `branch_number` reported **14 primaries in DFW instead of 1** (the match table maps an agreement to every ship-to for invoice audit); (b) description-only lists (OCR'd Wichita, SRS sheets) have no item_number so **157 items priced as 0** until the item key fell back to description. Region-scoped agreements (no branch) now flagged `agreement_scope='region'` — 9 Denver branches covered that way, none "primary".
- App: `price-agreement-coverage.ts`, Territory Coverage section on the Agreement Builder, `/api/price-agreement/coverage-export?vendor=&format=pdf|csv` (pdf-lib, paginated, accounting-gated).
- **GAPS FOR REPS: Atlanta has NO agreement with any vendor (39 branches); QXO has NO agreement anywhere (59 branches); KC + Wichita running on extended/lapsed ABC lists.**
- NOTE: transient PostgREST `PGRST205` schema-cache errors after migs 192-195 — resolved by `NOTIFY pgrst, 'reload schema'`. Service-role key is NOT stale (verified against every affected table); prod stayed 200 throughout.
