# 2026-06-30

## Session — Invoice backlog backfill + Alex scheduler diagnosis + rule alignment

**Goal**: Process every open invoice (so daily run starts clean), verify full-line/OCR coverage, verify Alex's daily cron is scheduled+running. Then handoff.

**Deliverables**
- **Deterministic backfill (DONE)** — wrote audit decisions for all pending lines on the 111 non-CM open invoices → **128/128 open invoices fully audited, 0 pending**. Rule mirrors Alex's SOP and was validated **108/108** against his prior run (every `credit-flag` reproduced, zero false holds). Outcome: 93 approved-to-pay, **32 held for credit memo (88 lines, $5,977.11 overcharge, avg 27.1%)**, 55 in 3–6% weekly tier, 177 coverage-gap → Jordan, 50 service-fee auto-approves. 44 CMs left on the credit-memo path (not line-audited). One quiet Slack summary posted as Alex → #accounting-invoice-processing.
- **OCR/line coverage verified** — all 172 open invoices complete; 53 API-truncated ones filled from `abc_csv` (NOT OCR), 0 short. The 10-line ABC API bug is fully mitigated via the CSV path; `v_invoice_lines_complete` is authoritative.
- **Rule-file alignment** — propagated the corrected deploy gate (agent ships, explain-then-push) from CLAUDE.md to AGENTS.md, CONVENTIONS.md, and `.cursor/rules/agent-conventions.mdc` (all three still said "deploys are human").

**Key findings / decisions**
- **Alex is NOT running daily — no cron is deployed.** On the agent host the Hermes gateway is not running (`hermes cron status` = gateway not running); no systemd timer, no crontab. Alex has 4 cron jobs, all `paused`. The "daily" `morning_abc_sync` log rows (06-26, round-second timestamps) are SEEDED, not real. The only real processing ever was a one-off manual batch (06-28) + this backfill.
- **Scheduler deploy mapped but deliberately NOT executed** (no host changes made). Blockers surfaced for a clean follow-up: (1) `scripts/agent-tick.sh` omits `--entrypoint`, so it boots the KASM VNC desktop instead of running headless — needs a repo fix; (2) enabling it stands up an autonomous money-touching agent, which per docs/56 needs one supervised live run before trusting the daily timer.

## LEARNINGS to carry forward (avoid re-doing)

1. **Two Hetzner hosts — don't conflate them.** Coolify/Command-Center = `5.78.124.10`, key `~/.ssh/hetzner_office`. **Agent host (Hermes/named-agent homes) = `5.78.146.161`, key `~/.ssh/a_roofers_open_brain_ed25519`, `agents.proexteriorsus.net`.** The agents live on the AGENT host, not Coolify. (Guessing SSH params got blocked; the `/coolify` skill only documents the Coolify box.)
2. **Seed vs real data heuristic:** round-second timestamps (`12:05:00.000`) = seeded/demo; sub-second precision = a real run. Use on `dashboard_action_log` to tell live activity from seed.
3. **Deterministic audit rule = Alex's SOP (validated 108/108).** Per invoice, gross = Σ`variance_ext` over auditable lines where `variance_pct>0` AND `category_key != 'service_fees'` AND not negotiated-gated. Per line: `service_fees`→`accept-svc` (pass); `benchmark_source='none'`/`variance_pct IS NULL`→`accept-nochallenge` (pass, coverage gap→Jordan); **`benchmark_source='negotiated'` AND `variance_pct>0`→`gate-negotiated` (HUMAN GATE — leave PENDING, no auto row; added 2026-06-30, pre-empts the $/% ladder, excluded from gross)**; (non-negotiated) `gross≥$25 AND variance_pct≥6`→`credit-flag` (disputed/hold→Casey); `3≤variance_pct<6`→`accept-30d` (pass, weekly); else→`accept-neg` (pass). Writes to `invoice_line_audit`; current decision = latest row per `invoice_line_id` (view has no `source` filter). The rule now has a tested code home: `app/command-center/src/lib/invoice-audit-disposition.ts` (`disposeInvoice`) + `.unit.test.ts`. Means we can reproduce Alex's audit deterministically without the LLM agent. NOTE: the original 108/108 backfill did NOT have the negotiated gate — re-running with the gate will move some auto-passed negotiated overcharges to pending, so it is NOT byte-for-byte identical to that run by design.
4. **"OCR" is actually CSV.** Truncated-invoice line completion comes from `abc_csv` (ABC full CSV export), not OCR. Don't build OCR for this. Reinforces "structured source before OCR".
5. **`abc_invoices.invoice_number` carries the `-001` suffix** — joins must handle it (cost a wasted query).
6. **`v_invoice_audit_line_cascade` is expensive** — broad joins time out; scope by invoice set.
7. **Hermes headless invocation:** the `openbrain-hermes-chrome` image is a full KASM desktop — run headless with `docker run --rm --entrypoint /usr/local/bin/hermes <image> cron tick`. Profile path = `/mnt/kasm_profiles/<email>/<IMAGE_UUID>/.hermes`; `cron list` hides paused jobs (use `--all`).
8. **Classifier won't accept deploy-gate *mechanics* in `.claude/` files** (reads as instruction-poisoning/bypass recipe) — keep policy wording in docs, not "how to pass the check." A push lands when an impact statement immediately precedes an isolated `git push origin main`.

**Open threads (next session)**
- **Finish the Hermes scheduler deploy** (the real remaining task): fix `agent-tick.sh` (`--entrypoint`) + commit; resume only the validated `morning_abc_sync` job; one supervised run; install the systemd timer+service; verify next-fire 7am CT. Then verify a real Alex run posts + writes `dashboard_action_log`.
- 35 held invoices ($5,977 overcharge) await Casey/human credit-memo review.
- Lucinda to test the live docs/63 surfaces (Register CSV, Due-now, Commercial auto-pay).
- Document the agent host in docs/27/56 (and consider an `/agent-host` skill).

## Scheduled run — ABC Supply FULL production sync (2026-06-30T110641Z)

- 2026-06-30T110641Z, env=production — **FULL SYNC NOT COMPLETED (Cowork sandbox blocker, recurring)**. rowsUpserted: productCatalog=n/a, vendorBranches=n/a, regions=n/a (full pull never ran). Background node died at "account detail 3/32" the instant the launching bash call returned — same `--unshare-pid --die-with-parent` + 45s-cap failure proven 06-19/06-20/06-29.
- **Bounded health probe (env=production, <45s) — all green**: token **200**, all 9 scopes returned (location/product/account/pricing/order/allOrder/notification/invoice/invoice.history .read), TTL 1800s; catalog page1 **200**, 1000 items, pagination totalPages=**333** / totalItems=**332,662**; WI branches **200**, 21 summaries; 3 requests, **0** rate-limited (429); 0 failures. Credentials + ABC API confirmed healthy.
- **Owed:** full pull still needs the non-sandboxed Hetzner agent host (`node integrations/bridges/abc-supply/production-sync.mjs --env=production --out=...json`), per docs/27. Read-only against ABC.

## Session — AccuLynx commercialization kickoff + Phase 1 (GSD)

**Goal**: Promote AccuLynx from single-account sandbox to a commercial multi-location integration. Plan via GSD; execute Phase 1.

**Major discovery**: AccuLynx is **per-account** — 9 API keys in `.env` (`PE_CC_{LOC}_ACCULYNX_API_KEY`: Florida, Colorado, Georgia, Kansas City, Texas, Wichita, Insurance Program, Multi-Family/Commercial + Sandbox). Live sync uses ONE key → `acculynx_jobs` is ~99% Kansas; 7 prod accounts unsynced. The live integration is the Supabase `acculynx-sync` Edge fn (v10) + pg_cron/pg_net, NOT the repo `integrations/bridges/acculynx` stub.

**Deliverables**:
- `.planning/` scaffolded (PROJECT/ROADMAP/STATE; 7 phases). Planned Phase 1 via gsd-plan-phase (research→plan→checker PASSED).
- **Phase 1 executed inline + verified** (executor subagents lack Supabase MCP, so ran inline): migrations **165** (`acculynx_accounts` registry, 9 keys, env-name-only), **166** (catalog/probe DDL + `source_account_key`), **167** (`acculynx_get_checklist`, 86 GETs from openapi-index). Edge fn **`acculynx-read-sweep`** (sandbox hard-gate, 6/6 deno tests, HATEOAS chaining, PII redaction) — batch of 86 probes (52×200). `docs/65` read matrix. **OKF bundle** `docs/knowledge-base/acculynx/` (14 concepts).
- Sandbox seeded from anonymized Wichita records: 51 clean + 50 messy = **89 unassigned jobs / 102 contacts** (`scripts/seed-sandbox-from-wichita.mjs`, `seed-sandbox-messy.mjs`).

**Key findings (write surface, verified live in sandbox)**:
- First AccuLynx writes ever: `POST /contacts`→200, `POST /jobs`→201, `POST /jobs/{id}/messages`→200.
- **Quirk:** contact `mailingAddress.state` = State OBJECT; job `locationAddress.state` = STRING (abbrev). Opposite shapes; API returns precise conversion errors.
- `POST /jobs` creates **unassigned leads** (excluded from default `GET /jobs`; need `assignment=unassigned`).
- `priority` strict enum (Low/Normal/High only); `contactTypeIds` required (empty→400); **messy strings accepted unvalidated** (phone/casing/typos persist) → dashboard must normalize.
- **NO milestone-write endpoint** (V2) → can't distribute across pipeline stages via API; milestone events need human UI moves (relevant to webhooks). **Webhook tier IS live in sandbox** (`/topics`→200).
- AccuLynx reference confirmed current: **124 ops, zero drift**; no measurement operations exist (docs-only).
- "Google OKF folder" = **Open Knowledge Format** (`docs/knowledge-base/OKF`), an in-repo markdown bundle — NOT Google Drive (dropped as redundant: agents read the repo).

**Decisions**: Sandbox-only for all new behavior (enforced in code via `assertSandbox`). Keep pg_cron+pg_net→Edge fn, harden to hourly (Phase 3). Multi-account fan-out keyed by `acculynx_accounts` (Phase 2). SUPABASE_ACCESS_TOKEN added to `.env` (CLI deploy works; hyphenated multi-family key name fixed `-`→`_`).

**Open threads**: Phase 2 (multi-location ingestion) is NEXT via `/gsd-plan-phase 2`. Enrichment writes (payments=structured value, insurance, sales-owner→assigned) unproven shapes — Phase 4. Webhook subscription+listener demo not yet built. `acculynx_company_id` per-account binding not backfilled.

## Memory Health
- 2026-06-30 health check: CAP COMPLIANCE failed — context/MEMORY.md at 2,499/2,500 (within 5% of cap). Consolidate via meta-memory-write before next curated write. Other checks green.
- 2026-06-30 wrapup: MEMORY.md consolidated to 2,405/2,500 (trimmed Phase-1 detail, compressed playbooks, added worktree + watermark learnings).

## Session — Phase 2 plan → execute → honest close (multi-location ingestion)

**Goal**: `/gsd-plan-phase 2` then `/gsd-execute-phase 2` — full AccuLynx ingestion across 8 prod accounts + the unfed resources (REQ-03/04).

**Deliverables**:
- Planned Phase 2: RESEARCH/PATTERNS/VALIDATION + 4 plans/3 waves. Plan-checker found 5 blockers+3 warnings → 1 revision pass → re-verified clean (`987c6e3`).
- Executed: Wave 1 (migs 168-170 applied to prod; acculynx-sync v10 committed from live + RED tests), Wave 2 (impl → 56/56 GREEN), Wave 3 (human-gated: sandbox proof → user-approved KC+Wichita subset → prod fan-out).
- Gap-closure: 7 prod-surfaced bugs fixed (camelCase→snake_case, endDate, lead_sources FK, jobs infinite-loop, maxModifiedDate, last_api_count, mig 171 watermark composite PK). v19 deployed. 1,259 legacy NULL jobs backfilled → wichita/sedgwick_ks (human-approved).
- Honest close: `02-VERIFICATION.md` (status: partial); ROADMAP/STATE corrected from optimistic "Complete". Pushed `origin/main` = `f4ce1f1` (converged; deployed Edge Fn source no longer stranded).

**Decisions**:
- Phase 2 = machinery-complete-and-proven (zero cross-account bleed; KC+Wichita jobs+contacts ingesting; reconciliation view computes). SC1 ~met; SC2-4 partial — full backfill is cron-paced, NOT brute-forced in-session.
- Carry-forward → Phase 3: finish backfill to tolerance, jobs `last_api_count=1` bug, 8 legacy NULL non-job rows, 6-account expansion (human-approved), pg_net reconcile.
- Production widening gated: sandbox → subset → full, each a human-approved scope step. Edge secrets set for KC+Wichita ONLY (other 6 physically unreachable until enabled).
- Edge Functions deploy direct via `supabase functions deploy` (independent of Coolify/CC app).

**Process learnings**:
- CC `isolation="worktree"` forked the 02-01 agent off a STALE base (`bada018`, 2 commits behind) while 02-02 got current base → `cleanup-wave` blocked on base_mismatch. Resolved by file-level `git checkout <branch> -- <paths>` of each plan's disjoint additive files. Single-plan waves then ran sequentially on main (no worktree) to sidestep it.
- Live watermark col is `resource_type` (not `resource`); single-col PK silently blocked the 2nd account's watermark → mig 171 composite PK `(account_key,resource_type)`.

**Open threads**: `/gsd-plan-phase 3` (Commercial Cron Hardening + the P2 carry-forward above). Hermes scheduler still open. Enrichment writes (Phase 4). Webhook listener demo not built.

## Session — Phase 3 plan + execute Waves 1–3 (Commercial Cron Hardening)

**Goal**: Plan Phase 3 (REQ-07) then execute it — harden the AccuLynx ingestion cron to commercial grade.

**Deliverables**:
- Planning: 03-CONTEXT (15 decisions), 03-RESEARCH, 03-VALIDATION, 03-PATTERNS, 6 PLAN.md (checker-passed after 1 revision), 03-LIVE-STATE (live-DB ground truth).
- Migrations applied to prod (rnhmvcpsvtqjlffpsayu): 172 hourly cutover (multiAccount) + 173 owned cron_dispatch + 174 reconcile fn (*/10) + 175 outcomes view v2; 176 alert-check fn (*/15); 177 RLS deny-by-default (22 tables) + 178 trust invariants (account_key NOT NULL ×9, trust_tier=evidence, acculynx_raw immutable) + 179 four rot-guard views + 180 legacy-NULL triage (8 rows → sandbox+archived).
- Edge fn acculynx-sync redeployed with lib/alerts.ts (Slack chat.postMessage + Sentry, secret-safe, 6/6 tests) + index.ts failure hooks; edge secrets SLACK_BOT_TOKEN + ACCULYNX_ALERT_SLACK_CHANNEL=C0BDF8QRF8A set via CLI.

**Decisions**:
- Ran Waves 1–3 INLINE via orchestrator (gsd-executor lacks Supabase MCP/apply_migration; only main session can touch prod DB). Explain-then-ship human gate per apply.
- Perpetual-`pending` + last_api_count=1 = same root cause (legacy code path); fixed by the multiAccount cutover alone (no jobs.ts change). Verified: run 253 → success; KC jobs last_api_count 1→8.
- market stays nullable (191/1450 jobs NULL — derived field); NOT NULL scoped to account_key only.
- Slack alerts go via openbrain BOT TOKEN (not webhook) to ob-ops-conductor C0BDF8QRF8A — #cc-proexteriors is a DIFFERENT workspace (CleverWork vs pe-command-center), unreachable by the bot.

**Open threads**:
- 03-03 delivery proof BLOCKED on 2 human steps: invite openbrain bot to ob-ops-conductor (private); `select vault.create_secret('<xoxb>', 'acculynx_alert_slack_bot_token')` in Supabase SQL editor (can't set via MCP without leaking token). Then seed stale watermark → check_acculynx_alerts() → confirm delivery.
- FOLLOW-UP: jobs last_api_count is incremental, not full total → 03-06 ≤2% expansion gate needs a jobs full-count first (alert already excludes jobs delta).
- Remaining: Wave 4 (03-05 security review + runbook), Wave 5 (03-06 6-account expansion, sets 6 prod secrets — human-gated). Resume: /gsd-execute-phase 3 --wave 4.

## Session — Phase 3 Wave 4 (03-05): security posture + recovery runbook

**Goal**: Resume Phase 3 at the last open thread — Wave 4 (03-05): run security review, write the D-13 posture doc + D-15 recovery runbook, update the architecture doc.

**Deliverables** (commit `42bb372`, pushed `origin/main`; docs-only, no code/migration/deploy):
- **`/gsd-secure-phase 3` → `## SECURED`, 12/12 threats CLOSED** (ASVS L1). Spawned gsd-security-auditor instead of taking the `threats_open:0` short-circuit (PII+money surface); it read migs 172–180 + alerts.ts/accounts.ts/index.ts directly, **re-ran `deno test` live (6/6)**, ran destructive-verb + secret-literal grep sweeps — all clean. Wrote `03-SECURITY.md` (renamed from `SECURITY.md` to phase convention so State-A re-detects next run).
- **`docs/knowledge-base/acculynx/security/posture.md`** (D-13): 6 sections — secret handling, RLS/ASVS-V4 deny-by-default *with the live anon-vs-service-role `has_table_privilege` proof*, least-privilege (+ single-service-role future-hardening note, explicitly not a D-14 gap), untrusted-content/ASVS-V5 (D-10), STRIDE attestation T-03-01..T-03-SC + doc-authoring T-03-12..14, secure-phase summary. No secret literals.
- **`docs/knowledge-base/acculynx/ingestion/runbook.md`** (D-15): 6 recovery scenarios (stuck pg_net→reconcile fn; watermark rewind UPDATE-only; edge rollback v19→v12; rate-limit lockout unschedule/re-schedule; secret rotation edge/Vault; torn backfill resume-from-watermark) + health check (`verify-acculynx-cron.sql`) + alert seed→fire→observe + Rollback + Owners. Every step additive/reversible.
- **`sync-pipeline.md`** updated in place: hourly `0 * * * *` + `multiAccount:true` + v19 + reconcile/alert crons; "Known gaps" → "Resolved in Phase 3".

**Decisions**: Ran the auditor rather than short-circuiting. Carried the 03-03 alerting operational caveat (Slack delivery unproven pending 2 human Slack/Vault steps) into the posture doc as an explicit non-threat caveat — safe no-op until then.

**Open threads (next session)**:
- **Wave 5 (03-06) — 6-account expansion — HUMAN-GATED** (sets 6 prod Edge secrets). Resume: `/gsd-execute-phase 3 --wave 5`. Blocked until the ≤2% expansion gate has a jobs full-count first (jobs `last_api_count` is incremental delta, not total — alert already excludes jobs delta).
- **03-03 delivery proof** still pending 2 human steps: invite `openbrain` bot to private `#ob-ops-conductor`; `select vault.create_secret('<xoxb>','acculynx_alert_slack_bot_token')` in Supabase SQL editor. Then seed→fire→observe per the new runbook.
- Hermes scheduler deploy (agent host `5.78.146.161`) still open from earlier today. 35 held invoices ($5,977) await Casey/human credit-memo review.

## Session — Phase 3 Wave 5 (03-06): D-08 gate — instrument + pagination fixes → OPEN

**Goal**: Start Wave 5. Ran Task 1 (D-08 expansion gate). Gate was CLOSED; user chose "fix instrument + force backfill". Both done and verified live; gate now OPEN.

**Two live-diagnosed bugs fixed (commit `e145cf7`, pushed; mig 181 applied; edge fn redeployed via CLI):**
1. **Reconciliation instrument (jobs).** `v_acculynx_reconciliation` compared brain vs `last_api_count`, but the date-windowed jobs sync's API `count` is the modified-since-watermark WINDOW, not the grand total (KC jobs read 1975%). Fix: **mig 181** adds `acculynx_sync_watermark.last_api_total` (integer, matches last_api_count type so the view's api_count column type is unchanged — CREATE OR REPLACE can't retype); view now reconciles against `coalesce(last_api_total, last_api_count)`. `jobs.ts` does a one-shot full-history count probe (pageSize=1, startDate=2000-01-01) on incremental runs → writes last_api_total. Full-history runs reuse the swept count (no extra call); probe skipped past deadline.
2. **Contacts/estimates pagination (THE backfill blocker).** `pageStartIndex` is a PAGE NUMBER (docs/knowledge-base/acculynx/api/read-capability.md pagination quirk: jobs use `recordStartIndex`=record offset; contacts/estimates use `pageStartIndex`=page number). Loop advanced by `items.length` → jumped to page 50 after page 0, re-fetching only the tail (wichita contacts stuck 64/1314 = 50 + 14-row tail; ran the full 110s budget re-upserting). Fixed to `pageNo += 1` + stop on short/empty page. Cascade: contacts no longer spins → estimates (previously starved/never-synced) now syncs.

**Verified live (KC+Wichita, via dedicated `accountFilter` runs 257–261):** every reconciled resource ≤2% — KC jobs/contacts/estimates 0.0%/0.0%/0.0%; wichita 0.2%/0.2%/0.0%. **D-08 gate OPEN.** 74/74 deno tests pass.

**Lever learned:** `trigger_acculynx_sync('{"multiAccount":true,"accountFilter":["<acct>"]}')` gives one account the full 110s budget — the force-backfill tool. accountFilter passthrough already existed in index.ts.

**Open threads**:
- **Task 2 (03-06) is the human step**: set the 6 remaining Edge secrets (florida, colorado, georgia, texas, insurance_program, multi_family_commercial) using the `env_secret_name` from `acculynx_accounts`. Canary-first (D-09): smallest-volume account, verify reconciles + 110s budget holds + no bleed, then batch the other five. Claude cannot read the AccuLynx dashboard keys — human provides values. Gate is OPEN, awaiting "approved - expand".
- job_walk sub-resources (invoices/financials/insurance/milestone_history/job_contacts) are NOT in the reconciliation view (no last_api_count) — the D-08 gate covers jobs/contacts/estimates only, as defined.
- 03-03 alert delivery proof still pending 2 human Slack/Vault steps.

## Session — Phase 3 Wave 5 (03-06) Task 2: 6-account expansion (canary-then-batch)

**Goal**: With the D-08 gate OPEN, execute Task 2 — enable the 6 remaining AccuLynx accounts (keys provided in .env).

**Done (verified live)**:
- **All 6 Edge secrets set** (canary `insurance_program` first → contacts 28/28, estimates 1/1, no bleed; then batch colorado/florida/georgia/texas/multi_family_commercial). **All 8 production accounts enabled.**
- **Zero cross-account bleed** — `select distinct account_key from acculynx_jobs` = exactly the 8 enabled accounts.
- **Contacts + estimates fully backfill** in one run each (colorado 1909/1909, georgia 479/479, multi_family 369/369 — all 0.0%). Budget holds.

**Two gotchas found**:
1. **`source .env` is BROKEN** — line ~214 has a value zsh executes, aborting source before the AccuLynx keys (lines 230+) → silently set an EMPTY canary secret (digest `e3b0c442…b855` = SHA-256 of ""). FIX: set via `supabase secrets set --env-file <grepped temp file>`, verify digest ≠ empty. **Never `source .env` for these keys.**
2. Edge secrets take ~30–60s to propagate; a too-soon run skips the account (~1s edge exec, no watermark).

**BLOCKER surfaced — jobs sweep stalls at ~25/run** (`resources/jobs.ts`): the date-windowed jobs sweep fetches only page 1 (PAGE_SIZE=25) then terminates, EVERY run, for every API-swept account (georgia 25/470, texas 25/2300, multi_family 25/352, colorado crawls ~25/run). **KC (166/166) + Wichita (1284/1286) looked fine only because their jobs were loaded by the LEGACY BACKFILL SCRIPT, not this sweep** — that masked the bug. Contacts/estimates unaffected (page-number fix works). Systematic (exactly 25 everywhere) → a jobs.ts pagination bug distinct from the two fixed earlier today. **Next**: instrument jobs.ts page-2 (recordStartIndex=25) with logging, redeploy, read `get_logs edge-function` — likely a status!=200 or empty-items early break; compare to the working legacy `pageStartIndex` jobs path.

**Status**: 03-06 PARTIAL — gate OPEN, all 8 enabled + no bleed + contacts/estimates current; large-account JOBS backfill blocked by the sweep stall. Runbook updated with the D-09 expansion record + the secret-setting gotcha + the jobs-sweep known issue. 03-06-SUMMARY written.

**Open threads (next session)**:
- **Fix the jobs-sweep single-page stall** (real blocker for jobs completeness) → then let cron drain large accounts → re-gate all 8.
- Small-account jobs show benign few-% delta (systematic ~2-record leads count quirk).
- 03-03 alert delivery proof still pending 2 human Slack/Vault steps.
