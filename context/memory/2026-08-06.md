# 2026-08-06

## Session 1

**Goal**: Friday WIP/AR live board — turn AR_WIP_Pack v4 sheet 11 into a nightly-synced Supabase master + editable CC tab + Thursday pack + Maya email; add CPA accrual inputs (est costs, costs-to-date, date cutoffs).

**Deliverables**: mig 215 (+215b/215c fixes) applied to prod — `wip_ar_master`, `wip_ar_master_updates`, `v_qbo_job_cost_lines`/`v_qbo_job_costs`, `refresh_wip_ar_master()` (pg_cron nightly 10:45 UTC), `roll_wip_ar_week()` (Thu 10:30 UTC), `wip_accrual_snapshot(p_cutoff)`; CC page `/accounting/friday-wip` + 5 API routes; `scripts/wip-pack-thursday.sh` + systemd units; `wip-packs` storage bucket; docs/85.

**Decisions**:
- Costs incurred to date come from QBO bill/purchase lines via the Customer:Job name suffix (`…:MC-68`) — bills ~96% tagged, purchases ~50%; ABC material rides QBO bills (direct ABC↔job match only 179/1073, PEC-105).
- `est_total_costs` is a declared gap: AccuLynx worksheet DETAIL endpoint (`raw->'worksheet'->>'_link'`) must be mirrored before it fills; column + `est_costs_source` already on the table.
- Board population = full ledger incl. credit balances (fixes workbook 0_EXCEPTIONS check 18); rows leaving the ledger flip `in_ar_population=false`, never deleted.
- Maya send = AgentMail from `ob-accounting@agentmail.proexteriorsus.net`, recipients from `FRIDAY_WIP_RECIPIENTS`, outbound-guard internal-only; humans forward externally.

**Validation**: first refresh = 115 jobs, Billed AR Σ Q $777,021.55 — exact tie to workbook; georgia $810,521.36 to the penny; inline edit round-trip + audit row verified in prod; accrual CSV 200/~1s after 215c single-pass rewrite.

**Open threads**: Coolify env needs `FRIDAY_WIP_RECIPIENTS` (Tabitha's address unknown — ask Chris) and confirm `AGENTMAIL_API_KEY`; agent host needs `openbrain-wip-pack-thursday.timer` installed; worksheet-detail cost mirror is the next PEC issue; first live Maya send still human-triggered (prod-write-test-first).

## Session 2

**Goal**: Friday WIP/AR round 2 — status display ([AccuLynx] · ref), sidebar collapse, days-in-status chip, six money columns, Estimated $ with auto PIF remainder, rep filter.

**Deliverables**: mig 216 applied (status_since/days_in_status/expected_cash_amount/expense_outstanding + refresh fn v3); friday-wip page/loader/update.ts reworked; AppShell rail-collapse toggle (localStorage) + global.css.

**Decisions**: Days-in-status = as-of − crm_pipeline.milestone_date (all 115 ledger jobs carry it; range 1–895d). Money columns map: Contract Total=L, Invoiced Total=P, Monies Collected=R, Balance Due=N, Expense Realized=QBO costs, Expense Outstanding=est−realized (— until worksheet-detail mirror). Cash map: Estimated $ lands at invoice/cash date, remainder at PIF date.

**Open threads**: **QBO mirror stale — last sync run 2026-07-27; the Thu 7/30 timer on agent host 5.78.146.161 did not fire and SSH from this sandbox times out. Needs a manual check (`systemctl status openbrain-qbo-thursday-sync.timer`) + install `openbrain-wip-pack-thursday.timer` while in there.** Expense Realized numbers are as of Jul 27 until that runs.

## Session 3

**Goal**: Friday WIP/AR round 3 — drop PIF remainder/Prior/Hit-Miss columns, brand zebra striping + shared ivTheme light/dark toggle, recipient defaults, nightly host timers.

**Deliverables**: friday-wip page rework (brand navy/gold theme vars, zebra rows, invoice-audit-style theme toggle sharing `ivTheme`); send.ts DEFAULT_RECIPIENTS (chussey@aia4.io, accounting@/invoices@/chandler@proexteriorsus.com, env-overridable); outbound-guard DEFAULT_INTERNAL_DOMAINS += aia4.io (owner's domain, PEC-113 CC precedent); both systemd timers → nightly (QBO 20:00 CT, pack 06:00 CT — unit names kept).

**Decisions**: Coolify API key in root .env is rotated/dead (401 Unauthenticated) — recipients shipped as code defaults instead; flag key rotation to Chris. Hit/Miss + prior-date fields stay in DB (weekly roll still scores them) — just off the UI.

**Open threads**: Agent host (5.78.146.161) unreachable from sandbox — needs `git pull && systemctl daemon-reload && systemctl enable --now openbrain-qbo-thursday-sync.timer openbrain-wip-pack-thursday.timer` to pick up nightly schedules + investigate why the Jul 30 QBO run missed. Coolify API key needs re-issue for future env-var ops.

## Session 4 — new agent host

**Goal**: Old agent host root@5.78.146.161 is gone; use 1Password to gain access to the new PE-US_AGENTS server and finish the timer work.

**Deliverables**: Found PE-US-AGENTS = 178.156.203.23 via Hetzner API (1P item Hetzner-PE_CC_DEV); root + ~/.ssh/hetzner_office works. Host already ran abc-sync/jt-sentinel; pulled repo to 94ef1b1, installed + enabled nightly openbrain-qbo-thursday-sync.timer (01:00 UTC) and openbrain-wip-pack-thursday.timer (11:00 UTC). Provisioned QBO prod creds from 1P (op inject → scp, no transcript exposure). Fixed host .env line 214 (SLACK_APP_ID_ROWAN missing =). Installed pip + aspose-cells-python + license; DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1 fix committed (94ef1b1). **First pack built and published: wip-packs/AR_WIP_Pack_2026-08-06.xlsx (429KB) — Download button live end-to-end.**

**Blocker**: QBO sync fails `invalid_grant` — the refresh token in 1P (updated 8/2) was rotated by a later use and the successor died with the old host. Needs a human Intuit OAuth re-auth; save the new refresh token to 1P item QUICKBOOKS_PROD_REFRESH_TOKEN, then re-run the op-inject→scp provisioning and `systemctl start openbrain-qbo-thursday-sync.service`.

## Session 5 — WIP/AR scroll fix + price-agreement office silo (mig 217)

**Goal**: (1) Restore lost horizontal scroll on the Friday WIP/AR board; (2) audit + fix cross-office price-agreement leakage in Invoice Audit, then reanalyze processed invoices to retract erroneous CM requests.

**Deliverables**: `friday-wip.astro` — `min-width:0` on `.fw-group` (round 3's `overflow-x:clip` + grid `min-width:auto` had clipped the tables with no scrollbar); deployed `0ada49f`, verified live. `schemas/cleverwork-roofer/217-office-silo-legacy-shipto-arm.sql` applied to prod: the legacy ship-to fallback arm (arm C of mig 201; pri-2 arm of mig 209) now requires the matched agreement to be in the invoice's/ship-to's own office set — four views replaced (v_invoice_audit_line/invoice, v_order_audit_line/order).

**Findings**: The fuzzy matcher attached every 2036874-x national-account agreement to every family ship-to (conf 80), so Denver -9 / KC -20 / Wichita -16 priced each other's offices: 188 lines cross-office-priced, 94 flagged, 65 claim rows on 46 APPROVED CMs = $3,212.04 erroneous; zero sent (sent/received untouched). After fix + `credit_memo_claims_sync_all()`: cross-office lines 0; open CMs 71→47, $5,132.46→$1,920.42 (−$3,212.04 exact), 24 CMs auto-cancelled (`engine_sync_no_claims`).

**Decisions**: Office silo outranks the no-regression doctrine — invoices with no pricing office read No-Price rather than risk a wrong-office challenge (Chris: regional pricing structures make agreements office-specific). Mirrors mig 208's vendor silo.

**Open threads**: 43 leak lines were human-passed `accept-neg` against a wrong-office price (append-only ledger left as-is — flag to Chris if those decisions should be re-reviewed); PEC-175 Intuit re-auth still the standing blocker; Coolify API key still dead.

### Session 5 follow-up — 43 accept-neg lines reset (Chris-directed one-time cleanup)

Chris directed a one-time reset of the 43 passed/accept-neg lines whose accepted price was cross-office (human approval assumed for this pass only; the normal human gate stays for all new invoices). Replayed the pre-217 engine to identify them exactly (43 lines / 27 invoices), appended `pending`/`reset` ledger rows (source `manual`, note cites the mig-217 cleanup + directive). Live engine on those lines after reset: 41 No-Price (own office has no covering agreement), 2 within tolerance — zero new claims. `credit_memo_claims_sync_all()` → no changes (47 synced, 0 cancelled). Weekly CM email verified rebuilt: ABC Supply, 47 invoices / 98 lines / $1,920.42, every claim citing the invoice's own office agreement; final check confirms 0 of 98 claim rows cross-office. Email lives at /accounting/credit-memos/weekly (renders live — no stored artifact to regenerate).

## Session 6 — QBO re-auth + sync restored (PEC-175 resolved)

**Goal**: Chris re-authorizes Intuit; agent re-provisions PE-US-AGENTS and restores the nightly QBO sync.

**Deliverables**: Re-auth via Intuit OAuth 2.0 Playground (app PE-CC-Dashboard — its registered redirect IS the playground URL, so that is the canonical procedure). **Credentials now live in one 1Password item: `QBO - PROD TOKENS` (CW_Master), fields QUICKBOOKS_PROD_{CLIENT_ID,CLIENT_SECRET,REFRESH_TOKEN,REALM_ID}** — supersedes the per-var items from 8/2. Provisioning template at scratchpad qbo-master.env.tpl pattern: op inject → 600-perm scratch → scp to root@178.156.203.23:/root/.config/cleverwork/master.env (unprefixed QUICKBOOKS_* names; sync script + mirror both accept). Sync ran clean 17:09Z: customers 2,863 / vendors 1,364 / invoices 1,809 upserted; ran refresh_wip_ar_master() manually — costs_incurred_asof Jul 27 → **2026-08-06**, costs-to-date $1.19M across 101/115 jobs, Billed AR $777,021.55 tie intact.

**Debug trail worth keeping**: first two restarts failed invalid_grant because the consolidated 1P item initially contained the OLD Aug-2 values byte-for-byte (fingerprint compare via `op read | shasum` caught it — no secrets exposed); Chris re-saved the real playground token 17:01Z and the next run succeeded. Diagnostic pattern: compare value fingerprints, never values.

**Open threads**: mirror does not persist Intuit's rotated refresh token (root cause of both token deaths) — spawn-task chip filed to write the successor back to master.env atomically; Coolify API key still dead.

## Session 7 — QBO refresh-token persistence (PEC-175 root-cause fix)

**Goal**: Chip task — stop Intuit token rotations from stranding the stored refresh token (cause of both token deaths).

**Deliverables**: `mirror-backfill.mjs` — `refreshAccessTokenPersisting()` wraps both refresh call sites: on rotation it updates `process.env` (fixes a second latent bug — mid-run refreshes previously re-read the pre-rotation value) and atomically rewrites the `QUICKBOOKS(_PROD)?_REFRESH_TOKEN` line in `~/.config/cleverwork/master.env` (0600, tmp+rename); logs `qbo_refresh_token=rotated persist=…` events only, never values. Read-only guardrails untouched. Rewrite contract unit-tested against a fake HOME; deployed to PE-US-AGENTS (`530ef5a`), host syntax-checked, and a `--mode=smoke --dry-run` on the host passed end-to-end (company gate OK, no rotation this cycle so the branch stays armed).

**Note**: 1P `QBO - PROD TOKENS` remains the re-auth source of record; after first rotation the host's master.env intentionally drifts ahead of 1P — on any future invalid_grant, mint fresh via playground and re-provision, don't copy 1P over a healthy host file.

## Session 8 — Coolify API key re-minted (last standing blocker cleared)

Chris minted a new Root API token (https://coolify.proexteriorsus.net/security/api-tokens) → 1Password **CW_Master → `coolify.proexteriorsus.net - Root API`** (field `credential`). Agent refreshed the commented `# COOLIFY_PE_OPEN_BRAIN_API_KEY=` line in root `.env` from 1P (value never in transcript) and verified `GET /api/v1/version` → 200. Discovered the app uuid changed with the 8/4 host rebuild: old `og0rmt02rff8qti9nlfk3nr7` → **`lu5txzhyoza7uuz0scwpobv7`** (command-center, main, cc.proexteriorsus.net). Patched the live surfaces (coolify skill, scripts/coolify-redeploy.sh, docs/27) and verified end-to-end via the readval helper + new uuid → app running. Historical daily logs left as-is.

## Session 9 — vendor payment memos → paid-verified (docs/86, mig 218, PEC-180/181/182)

**Goal**: Monthly ABC payment memos (paid-in-full evidence) → Linear issue → process → invoices to paid_verified. Chris design call: raw vendor-agnostic table pair first; (vendor_slug, invoice_number) is the key that populates the working table.

**Deliverables**: mig 218 `vendor_payment_memos` + `vendor_payment_memo_lines` + `vendor_payment_memo_apply()` (outcomes: verified / verified_inserted / already_verified / credit_applied / skipped_returned|void / not_in_mirror / vendor_not_supported; audit-logged; idempotent). `POST /api/accounting/payment-memos/process` (accounting + approval.decide; refuses memos whose line sums don't tie the printed subtotal/CM totals). docs/86 runbook. Backfilled all three memos — scans, no text layer; vision-transcribed 164 lines, validated to the penny against printed totals AND 164/164 in abc_invoices: May 55 verified / June 17 / July-paid 61 (incl. 2009332466-001 flipped from returned_reason='testing' artifact) + 31 credit docs recorded. Paid-pending-verification 75 → 20. Endpoint idempotency proven (June re-post → already_verified:17); totals-guard proven (422). Linear PEC-180/181/182, stamped on memo rows.

**Cautions**: July scan p4 = Billtrust checkout screenshot with card CVV in the clear — Chris to redact Dropbox copy / crop future scans. Credit-memo application (credit_applied → the open/unapplied CM ledger) intentionally not automated yet.
