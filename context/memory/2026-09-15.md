# 2026-09-15

## Session 08:0x — invoice-audit workflow review since 8/26 (Chris: "verify no gaps, all reviewed, all surfaces 100%")

**Goal**: Trace the failure Chris noticed 8/26, prove ABC invoice coverage and audit coverage since then, verify every work surface and scheduled routine, and restart anything lapsed.
**Findings (all verified live — prod DB, agent host, cc.proexteriorsus.net API with the ob-accounting/ob-auditor service tokens)**:
- **The 8/26 failure was two upstream outages, both already repaired.** (1) ABC `invoice.history` returned `totalItems: 0` for both bill-tos 8/26–9/1 (7 nights, nightly job green-but-empty); recovered 9/2 and the 10-day rolling window backfilled every invoice dated 8/25–9/1 on 9/2. (2) pg_cron job 13 failed every run 9/2 07:30 → 9/11 15:00 UTC (`v_req` unassigned in `credit_memo_reconcile`, mig 284) so `mv_invoice_audit_line` froze and Invoice Audit detail threw "network error" on post-9/2 invoices; fixed by mig 285 on 9/11 (docs/109 F1–F3).
- **ABC gap check: none.** ABC API dry-run (`mirror-backfill.mjs --dry-run`, 8/1–9/15) returns 47 invoices; `abc_invoices` holds 47 for the same window; `abc_invoice_history` rows without a detail row = 0. Nightly ingest ran 30/30 nights 8/16–9/15 (host log), 4 new invoices this morning, PDFs land D+2 (the 4 dated 9/14 are "pdf 404" today, same lag as every prior week).
- **Audit coverage: complete.** Every non-credit-memo ABC line dated 8/20 → 9/14 carries a decision (95 auto_match passed by Alex through 9/15, 4 discrepancy by Chris 8/25 → CM requested; the 4 stale `pending` rows are superseded). Latest-per-line pending on any non-CM invoice since 7/1 = 0; live `auditPendingCount` = 0. 13 lines on 5 June/July invoices have no audit row but variance ≤ 0 (valid as billed, hidden by design).
- **Surfaces**: `/healthz` = b4ed4f8 (= origin/main). kpi-pills, pending-verification, credit-memos/pending, agent/work-queue (225 items, live), friday-wip (5–12 s, slow but under limits), invoice detail for 8/25, 9/1 (32-line CM) and 9/14 invoices all 200. Runtime board: 66 green / 8 yellow / 2 red / 4 unknown — reds are jt-sentinel (Q5 grant key) and the Ops Conductor agent that inherits it; yellows are Better Stack + GitHub tokens absent on prod (Q4), site-sweep warnings, 3 service identities without tokens, deploy-drift light needing GITHUB_TOKEN.
- **Jobs**: 14/14 pg_cron green (job 13 fails_7d=316 are all pre-9/11); systemd 6/7 green; maya-gate had transient PostgREST pool timeouts at 05:00 UTC on 9/11 and 9/15 (recovers next tick); wip-pack green since 9/12 (openpyxl).
- **Lapsed HUMAN routines (the real gaps)**: ABC AR open/closed report import last run 8/10 (manual CSV, docs/48; 142 invoices since carry `ar_status` null so paid ones still count as open); Pay-It verification 57 `paid_pending_verification` (53 overdue); weekly QB batch never stamped since 8/25 and not built 9/1 or 9/8; 2 received SRS CMs pending amount-mismatch review; 5 SRS discrepancy lines with no agreement citation (Chris manual, 8/22 + 9/1); `morning_abc_sync` Slack pass still paused (docs/57).
**Deliverables**:
- `exports/inv-processed-2026-09-15/` built PREP-ONLY: ABC 33 inv $48,810.47 (6 with CM requested) · QXO 2 $5,697.47 · SRS 2 $252.72 = 37 / $54,760.66. Nothing stamped — run `--stamp` after Lucinda loads.
- `scripts/site-quality-sweep.mjs`: received-credit check no longer filters the per-row view by a 500-item IN list (statement timeout every night, F16) — first clean run found 2 received CMs still counted as owed; expired-agreement warning honors `renewal_mode` (evergreen books stop warning); design-system chapters exempt from fabricated-data/orphan-page (would have gone red on the next host pull: 7 errors / 17 warnings).
- `scripts/runtime-job-report.sh`: sends `p_started_at` from `ExecMainStartTimestamp` (every host row had null started_at).
- docs/109 §1b (F32–F37).
**Decisions**: none new. Reaffirmed: green-but-empty is the failure class to watch (board feed light covers it; paging still needs Q4).
**Open threads**: Q4 Coolify API token → `BETTERSTACK_API_TOKEN` + `GITHUB_TOKEN` on prod (token in .env still 401); Q5 `JT_SUPABASE_MIRROR_GRANT_KEY` on the agent host; Q7 Better Stack → Slack; `acculynx-sync` edge still v49 (supabase CLI unauthorised locally); Slack connector cannot see the PE workspace channels (channel_not_found) so agent posting was not verified from here; Sentry token not local.

## Session 14:3x — Chris: "hard to believe every invoice passed with zero CMs" → two leaks (mig 289)

**Goal**: Chris challenged the "all passed" result and the outstanding ABC CM requests. Both were real.
**Findings**: (1) Alex's nightly triage ran ~20 s after the ingest while `mv_invoice_pricing_office` had refreshed at 07:30:00 — tonight's invoices had no office row in the pricing arm, read No-Price on every line, and were stamped passed/valid; 162 lines / 37 invoices since June carry that stamp while the audit view prices them, 11 over agreement ($308.75). (2) `credit_memo_reconcile()` only scanned memos dated ≥ 8/1, hiding 7 June/July ABC memos that name invoices with requests sent 8/9. Plus: mix shift to Richardson/Atlanta commercial items (Atlanta has no agreement at all), branch 326 unmapped (2014501859-001).
**Deliverables**: mig 289 (+289b/289c on prod): triage refreshes the office matviews first, own lock_timeout 240 s, fails closed without a pricing-office row, single scan; reconcile ignores the date floor for memos naming an open request; 5 lines reopened ($131.75) — 6 June lines on human-cancelled invoices left for Chris (docs/109 F39). Reconcile run → 7 ABC amount_mismatch receipts (review queue 2 → 9). Triage verified on the live host path (46.7 s, stamped 0). Weekly QB prep rebuilt (36 invoices $45,790.53; 2013736069-001 dropped to In review). docs/109 F38–F41.
**Decisions**: an agent stamp made on stale data is not a decision — reopen; a human cancellation is — surface, never revive. A No-Price reading without the office gate is not a reading (fail closed).
**Open threads**: Chris rulings on the 6 June lines; Lucinda reviews the 9 CM receipts on Sent CM; map ABC branch 326 to a PE office; Atlanta agreement coverage; Pay-It verification (57), ABC AR report import (last 8/10), weekly batch load + `--stamp`; Q4/Q5/Q7 unchanged.
**Addendum 15:1x**: Chris — "put the six lines back for another audit pass" → mig 290 reopened them (cancelled requests untouched). Branch 326 question: the Topeka row exists and is assigned to KC but keyed `topeka-KS-66618-1445`; the pricing join needs bare `326` (F42). Proposed mig 291: re-key the row.
