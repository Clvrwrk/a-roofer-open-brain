# 2026-09-22

## Session — AccuLynx job link (mig 294)

**Goal**: Chris — "why do so many invoices need the AccuLynx job link?" then "fix with your recommendations".
**Findings**: link works whenever the job box/PO carries the PE job number; the drop (82% → 38%) is mix: 42/48 unlinked since June carry account buckets + free-text POs; commercial CP-25-xxxx projects are not in AccuLynx. Matcher gaps: no INS- prefix (12 jobs), no client-name fallback.
**Deliverables**: mig 294 (applied): prefix allowlist + INS, 2–3 letter token regexes, `byname` fallback (unique client name ≥ 5 chars, link_method client_name). 892/1,133 linked (+2); 128 ms. docs/109 F45.
**Open threads**: counter discipline (job number in the job box — Roberto); commercial-project link outside AccuLynx (A3); vendor matcher (mig 250) + v_pe_job_label_parse still seven-prefix.

## Session — SRS 9/22 ingest + weekly export leak (mig 295)

**Goal**: Chris — "audit/add all missing SRS invoices" (two Billtrust CSVs + Dropbox PDF folder).
**Deliverables**: 16 September SRS documents ingested (14 inv + 2 CM, 151 lines, Wichita), 71/71 PDFs linked, claims sync + reconcile run (2 new SRS receipts to review). Found and fixed: `v_inv_processed_weekly` counted undecided valid lines as pending → 85 "Approved" invoices ($459K; all SRS since July) never reached a QB file. mig 295 = app's discrepancy rule; 295b = NOT-paid + payment-ledger guards (46 AR-paid ABC + 15 ledgered would otherwise re-load). Tuesday prep rebuilt: ABC 49 $101,141 · QXO 2 $5,697 · SRS 38 $165,500 (nothing stamped). docs/109 F46.
**Open threads**: Lucinda: check QB for the 14 July SRS bills before loading, then `--stamp`; SRS AR still unknown (statement arm is QXO-only; need the 9/1+ SRS statement or closed-items export and a ~1 h adapter); ABC AR import still lapsed since 8/10 so the NOT-paid guard only protects invoices with AR data.

## Session 13:2x — "run the standard audit/CM review now" + QB bank batch 7/31–8/12 + routine currency

**Run now (all vendors)**: Alex No-Price triage already ran at the 13:06 ingest (96 lines) and at 07:31 nightly; `credit_memo_claims_sync_all()` (synced 3) + `credit_memo_reconcile()` (1 relink) re-run 13:21; `mv_invoice_audit_line` refreshed 13:08 and re-requested 13:21; job 13 succeeded 13:15. The only daily routines that exist for vendor invoices are these three plus the nightly ABC chain — there is no scheduled reaudit (run_labels are `audit_ui` + a one-off `srs_2026-08-05`).
**Currency 13:20 UTC**: ABC nightly sync + ingest + triage green 07:30–07:32 (newest invoice 9/21); SRS ingested 13:06; QXO last ingest 8/05 (newest invoice 7/31 — human CSV cadence); pending lines: ABC 28 (the 289/290/291 reopens still awaiting decisions), SRS 0, QXO 0; CM receipts pending review 13; approved CM requests ABC 2 / SRS 1; site sweep 11:00 (warnings); board 66G/8Y/2R (jt-sentinel Q5, site-sweep warnings).
**QB bank batch**: the producer's preview returned only never-handed rows, so the batch was rebuilt from `qb_bank_export_log` (the exact rows handed 8/10, 8/18, 8/25) + 2 never-handed rows → `exports/qb-bank-2026-07-31_to_2026-08-12/` (ABC 62 rows / $32,072.22 spent, SRS 6 / $22,476.94, QXO 1 / $5,601.71; nothing stamped). Dev server on 4399 was used for the preview because service agents lack `approval.decide`; stopped after.

## Session 06:3x — handoff

**Goal**: `/project-handoff` — full linear handoff of the 9/15 → 9/22 invoice-audit workstream.
**Deliverables**: `docs/handoffs/current.md` rewritten (previous archived as `archive/2026-09-22-0633-pre-invoice-audit-review-handoff.md`); Linear-ready ticket table L1–L14 inside it (Linear MCP unauthenticated).
**Next task**: materialise `v_invoice_audit_invoice` (8 s timeout risk) — see the handoff.


## Session 14:1x — sixteenth migration collision, answered by splitting PR #9

**Goal**: Chris chose "split migrations out" over a seventeenth renumber. Execute it.
**Deliverables**: **PR #12** (`contrib/cleverwork/coverage-migrations`, `996e4e8`) — the five coverage/spend migrations renumbered once to **296-300**, clear of the contended range. **PR #9** (`claude/project-handoff-5ua2fw`, `ff87fc3`) — the five files removed, `docs/107` (23 refs + a sixteenth-renumber addendum), `docs/handoffs/current.md` (now describes two PRs) and `price-agreement-coverage.ts` (2 doc comments) renumbered with them; main merged as a separate commit; PR body rewritten. 371/371, build green on both. Both prod `COMMENT ON VIEW` bodies re-issued and read back at 358/427 chars — `v_agreement_unreachable` now cites mig 299, `v_office_vendor_gap_exposure` mig 296.
**Decisions**: the five renames were never the cost of a collision — the rebase of everything behind them was (coverage library, builder surface, two long documents, none containing schema). Separated, a seventeenth costs one rename in a five-file PR. Prod is untouched either way: all five applied in August under 245/246/248/249+249b/290 and Supabase keys on the timestamp.
**The sweep's new trap**: main's own migrations are *now* 294 and 295, so those digits appear in the handoff meaning main's AccuLynx-link and weekly-export work — a number-keyed substitution would have mislabelled two of another team's migrations. Same shape as `245`, which names both this set's prod label for the spend view and a parallel session's `office-closure-and-agreement-status`. Classify by what the number refers to, never by the number.
**Deliberately unmoved**: the 09-15 and 09-16 daily logs. They are a dated record of how the numbering got here; rewriting them to today's numbers would destroy the only account of it. Same for the three renumber tables in `docs/107`.
**Merge**: `docs/handoffs/current.md` conflicted on four hunks against main's 9/22 handoff. Main wins as the canonical document, but two of this branch's corrections were carried onto main's text rather than dropped, because main's version re-introduces what they warn against — a hardcoded `354 tests` in a verification step, and a written `schemas through 295b` watermark in a row that previously read "through 287" while prod was past 292.
**Open threads**: PR #12 awaiting first review; PR #9 still has four items awaiting a human (AMSDE == SBP-SOUTHDENVER / 128 items, the third coverage state, branches 21/39/465/684, anon-readable `v_office_vendor_branch`). Better Stack app read path still blocked on `BETTERSTACK_API_TOKEN` + egress (PR #11).

### Correction 14:4x — the open-item count, and what "needs a human" means

The Session 14:1x block above says **four items awaiting a human**; `docs/handoffs/current.md`
said **three**. CodeRabbit caught the disagreement. Leaving the original line as written, because
a dated log is a record, not a live document — here is the reconciliation.

Both counts were defensible and that is the actual defect: "needs a human" was doing two jobs.
Three items need a **ruling** only a human can give (Denver branch identity / AMSDE == SBP-
SOUTHDENVER, the four `pending` geocodes, the anon-readable legacy views). The third coverage
state needs **approval to build** — mig 299 line 141 says "Raised for a human", but what it asks
for is a column, a `coverageLabelKind()` branch, a pill and tests, not a decision about data.

Resolved as **four**, with the kinds labelled, in the handoff and the PR #9 body. Note the fix
went the OPPOSITE way to the one proposed: the review suggested dropping the third coverage state
from the daily log to match the handoff's three, which would have quietly retired a real open
item. The handoff was the stale document, not the log. Verify which side is stale before
reconciling two counts — the shorter list is not automatically the right one.

## Session 17:0x — merged all three PRs; a P1 found after the merge

**Goal**: Chris: "merge, commit, and everything is approved."
**Deliverables**: **#9** `2ebe7f2c` (surface + docs, PEC-221), **#12** `6c1f13b2` (migrations 296-300) and **#11** `1ade4158` (Better Stack tests + docs/109 corrections) all merged to main. **#13** opened for the geocode-scope correction. Local main synced; 371/371 then 385/385, build green on each.
**Decisions** — **the mistake worth keeping**: I merged **#12 with three unread Greptile findings on it**. Greptile's *check* read success — its P1/P2 review comments do not fail the check — and I gated on the check instead of reading the comments, which were queued unread. Tenth instance of the standing mistake in a new shape: trusting a green signal instead of the thing it summarises. **A reviewer's check status and its findings are different channels; read both before merging.**
**The P1 was real and bigger than stated.** Greptile: mig 297 demoted branches 21/684 from `ok` to `pending` and nothing repairs them. Measured: **8 rows** hold `geom` but are not `ok`, not the 4 this repo has quoted since August — and **three read `no_address` while geocoded**, a state no document describes. The old framing filtered on `= 'pending'` and so could not see them; `IS DISTINCT FROM 'ok'` is the honest predicate. Cause splits three ways, and the first attribution here was too broad (corrected after review): **21 and 684** are ours — the 2026-08-21 record says mig 297's applied run demoted them, and `updated_at` matches, corroborating the record rather than proving authorship alone; **305, 1278, 185, 1306** belong to 292/292b, whose SQL names exactly those four; **39 and 465** belong to an unowned 13:04 process and are attributable to nothing in particular. Do not read a timestamp as writer provenance.
**Not repaired, deliberately**: for 21/684 the fix is unambiguous, but writing all eight to `'ok'` because two are provably ours is mig 240's boundary — a guess cannot become a fact. Corrected docs/107 + the handoff to 8 with the cause split and the query, and raised the 21/684 repair to Chris as a bounded proposal.
**Also**: CodeRabbit caught F47 in docs/109 claiming "only the read path is missing" while the same entry recorded job 18 unmonitored — two independent gaps stated as one; Q4 had the mirror-image error. Fixed `7fd01011`.
**Open threads**: all four PRs merged (#9 `2ebe7f2c`, #12 `6c1f13b2`, #11 `1ade4158`, #13 `dde5b597`); main green at 385/385. The 21/684 repair awaiting Chris; `BETTERSTACK_API_TOKEN` + job 18 provisioning still need a session with egress to the three blocked hosts; the deploy of `2ebe7f2c` could not be verified from here (proxy 403 on cc.proexteriorsus.net).

## Session 17:2x — all four merged, and the check-vs-comments lesson

**Goal**: finish the merge Chris approved, and stop repeating the mistake that made it long.
**Deliverables**: #9 `2ebe7f2c`, #12 `6c1f13b2`, #11 `1ade4158`, #13 `dde5b597` — all on main. Main at 385/385, build green, tree clean, migrations 296-300 landed beside main's own 294/295 with no collision. Handoff corrected from "two open branches" to the merged table.
**Decisions** — **the lesson that cost the most today, stated once so it does not need relearning**: a reviewer's **check status and its review comments are separate channels**, and a green check does not mean an empty comment list. It cost a bad merge (#12, three unread Greptile findings including a real P1) and then proved itself twice more — **#11 and #13 both read `mergeable_state: clean` with every check green while carrying unresolved findings**. Gate on BOTH before merging, and prefer a watch that reports the two together.
**Review round, all confirmed by the reviewer afterwards**: the payload-merge finding withdrawn on a direct prod measurement (0/0/0 across 37 branches); three test gaps closed on #11, each mutation-tested; F47/Q4/D13 corrected against `runtime-status.ts` rather than on the reviewer's word — the board does NOT go yellow when Better Stack is unconfigured, it self-pings and can read green; and on #13, a stale "4 rows" I left behind **in the commit whose purpose was fixing a stale count**, plus an attribution I had overreached (39/465 are not 292b's).
**Rules earned**: do not read `updated_at` as writer provenance. Match a next-page URL exactly, never by substring — `per_page=250` contains `page=2`. A failure mock that fails every call cannot test an all-or-nothing contract.
**Open threads**: the 21/684 geocode repair awaiting Chris (two rows, known writer, reversible; the other six need his read on intent). `BETTERSTACK_API_TOKEN` + job 18 provisioning still need a session with egress to cc.proexteriorsus.net / coolify.proexteriorsus.net / uptime.betterstack.com. **The POST-MERGE deploy is unverified** — main moved `007a0c39` → `dde5b597` today and `/healthz buildCommit` was never read back against it *from this session*; the proxy 403s on the site. Be precise about the scope: the handoff records a verified deploy earlier the same day (`buildCommit` = `156db5f` at 06:33 PT, a different session, before any of today's four merges). So "today's deploy was verified" is true of `156db5f` and false of everything merged after it. Corrected after review — the first wording said it was not read back at any point today, which reads as though the whole day were unverified and would let the next session take the 06:33 reading as covering the merges.
