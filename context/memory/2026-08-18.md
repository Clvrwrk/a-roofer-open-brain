# 2026-08-18

## Session 1

**Goal**: Friday WIP/AR board UX — scrollable office viewport with frozen columns
**Deliverables**: `app/command-center/src/pages/accounting/friday-wip.astro` (CSS-only): expanded office group = 70vh scroll pane (vertical + horizontal), sticky header row, Job # + Client columns pinned left (72px col 1). Commit `faf7eb4` → merged to main, pushed, Coolify deploy (v0.6.411A).
**Decisions**: sticky cells require `border-collapse: separate` + zebra/hover backgrounds moved from `tr` to `td` (opaque paint under frozen cells). Chris chose 70vh viewport + frozen header + ship-to-live.
**Open threads**: none from this session; standing next task remains Orgo ship script + PEC-209 (see MEMORY.md).

## Session 2

**Goal**: Pull + prioritize PE-CC-DevTeam Maya queue (30 open), then build the approved P0→P3 sequence.
**Deliverables**:
- **mig 230** (prod): `v_invoice_pricing_office` office = servicing branch (`branch_number_extracted`) not ship-to home branch; 49 inv/$119.6K were mis-officed. + `agent_intake_notices` CHECK gains `ack_in_thread` (gate 23514). mv refreshed. Commit `bfa5d25`.
- **PEC-208 fix** (`5f64ca9`, live): sloppy-PO job recovery in qb-bank-csv.
- **agent-intake-guard.ts** (`36227c5`): server-side boundary guard — drops agent self-receipt echoes, folds Google/WorkOS security notices onto `security-notice:<YYYY-MM>` via existing orchestrationKey convergence. 9 unit tests on real historical messages.
- Verification packets posted to PEC-209/210/203/111/177 + security-batch timeline on PEC-149.
- Closed PEC-159/167/169/170 (artifacts). Moved PEC-149..158 + 172 → Agent Needs Input.
**Decisions**:
- **PEC-209: credit NOT supported** — invoice 2009034778-001 billed at/below Wichita terms on every matched line (net −$188). Office error was real; overbilling was not.
- **PEC-210: credit SUPPORTED at $40.26**, not the $22.30 requested ($22.30 implies a $17.30 cap price matching no agreement; defensible benchmark is $19.75 from Wichita quote 0049828559).
- **PEC-111 HELD**: the three "DFW" IKO prices match the **Kansas** book (Cambridge $92 exact, Dynasty $102 vs $103), not DFW ($112/$117). Promoting would cut DFW Cambridge 18% and generate false CM requests. Needs Chris to confirm office from the 7/8 email.
- **PEC-177 HELD**: Tamko Titan $101/SQ is $21 below the latest Wichita SRS price ($122) and $34 below ABC ($134.90). Not promotable without the quote doc.
- **Security batch NOT closed** (deviation from approved plan): underlying emails are 6/6–6/25 (not 8/5–6 as I first said — that was ticket-creation date), and PEC-172 is Billy Cowell added to the PE AIA4 account, unrelated to Maya. Password-reset/account-recovery/OTP events need Chris's yes/no; auto-closing security alerts on inference is not an agent call.
**Open threads**:
- **Wichita has NO active price coverage**: SRS agr expired 7/23/26, ABC agr 7/31/26 → every new Wichita invoice fails closed to No-Price. Renewal needed.
- All **567** price_agreement_items are `approval_status='pending'` — approval workflow never exercised; needs a bulk-verification decision.
- PEC-203 catch-up export (62 rows since 7/1) previewed but `mode=export` is human-gated — Chris fires it, CSV goes to Lucinda.
- Hard-coded `since=2026-08-01` floor in qb-bank-csv.ts should track the last-export watermark so the July seam can't recur.
