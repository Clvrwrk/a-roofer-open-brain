# 2026-06-19

## Session — Invoice audit walkthrough fixes (Phase 1)

**Goal**: Act on Chris's human walkthrough of the invoice audit. Investigate 6 findings + a
segmentation design ask; fix the four bounded accuracy items this round, with verification
and documentation.

**Decisions (from Chris)**:
- Backfilled "passed" audits count as audited / no-longer-at-risk.
- Item 3: fix ALL items with the ×N UOM signature, no per-item review.
- Branch page scopes to the agreement active on the invoice's generation date.
- Segmentation taxonomy = 9 categories: Decking, Underlayment, Shingles, Vents, Skylights,
  Flashing, Accessories, **Labor, Service Fees** (last two seed the labor/service catalogs;
  trip/delivery "Header Charge" lines land in Service Fees).
- RLS on 7 exposed tables deferred to an upcoming DB-health agent pass.

**Deliverables (committed to `cleverwork/price-agreement-audit`, prod DB live)**:
- `schemas/cleverwork-roofer/113-invoice-line-price-backfill.sql` — backfilled 171 null
  invoice lines from `raw` (Item 6).
- `schemas/cleverwork-roofer/99-invoice-audit-views.sql` — effective-price comparison
  (Item 3), un-audited-only `at_risk` + new `credit_memo_amount` (Item 2), raw-fallback +
  `is_auditable` guard (Item 6). Applied to prod via apply_migration.
- `integrations/bridges/abc-supply/mirror-backfill.mjs` — `invoiceLineRows()` nested-key fix
  + post-sync canary warning (Item 6).
- `app/command-center/src/lib/invoice-audit.ts` + `.../accounting/invoice-audit.astro` —
  surfaced Credit Memo Requested KPI, at-risk relabel, auditable-only pending (Item 2).
- `app/command-center/src/lib/branch-price-list.ts` + `.../price-list/branch.astro` —
  branch name/address, agreement-number pill, invoice-date agreement scoping (Item 5).
- `docs/42-invoice-audit-data-integrity.md` — fixes + the two repeating-issue patterns
  (ABC mapping drift; UOM normalization).

**Verified (live, prod rnhmvcpsvtqjlffpsayu + dev server reading prod)**:
- "$ At Risk" $877K → **$6,214** (≈99% was the UOM bug, not real exposure). Decomposition in
  doc 42. Credit Memo Requested $0 (no credit dispositions exist yet). still_null=0.
- Branch 1272 page: address shown, pill "2036874-16" (was "#7"), scoped to 2026-06-03 agreement.
- App pages return 200, no console errors (only unrelated Google Maps deprecation warnings).

**Open threads (Phase 2)**: Item 1 credit-memo tracking (tables exist, empty — wire write
path + replace mock `/accounting/credit-memos` queue), Item 4 daily invoice-PDF auto-pull
(45 PDF-less invoices, all have invoice_id), roof-system segmentation taxonomy (9 cats).
NOT yet deployed to cc.proexteriorsus.net — pending push to origin/main (Chris auth).

- 2026-06-19T11:09:17Z ABC Supply production sync (scheduled) — **BLOCKED in Cowork sandbox**: background process cannot outlive a bash call (bwrap --die-with-parent + --unshare-pid) and single calls cap at ~45s, while a full pull needs 20–45 min; production-sync.mjs has no resume/checkpoint to stitch partial windows. No new full run produced. Current Supabase state (read-only check): abc_product_catalog=331,293, abc_vendor_branches=720, abc_regions=40 (from prior successful sync; last full run summaries are Jun 5). Recommend running on the agent host (Hetzner/Coolify, docs/27) or any non-sandboxed shell where nohup survives.

## Session — Phase 1 deployed + Phase 2 segmentation

**Decisions (Chris)**: deploy Phase 1 now; start segmentation; taxonomy = **12 categories**
(9 + Low-Slope/Membrane + Gutters & Downspouts + Tools & Consumables); UX = collapsible
sections + subtotals, default-collapsed.

**Deploy**: pushed `cleverwork/price-agreement-audit` → `origin/main` (commit fd23a59),
Coolify rebuilds cc.proexteriorsus.net. NB: Phase-1 DB-view fixes were live immediately on
the shared prod DB even before the app redeploy (At Risk already corrected on the site).

**Phase 2 deliverables**:
- `schemas/cleverwork-roofer/114-roof-system-segmentation.sql` — `roof_system_category`
  (12 + uncategorized), `item_roof_system_category` (overrides), `classify_roof_system()`
  keyword classifier (~81% coverage). Applied to prod.
- `99-invoice-audit-views.sql` — `v_invoice_audit_line.category_key` (override → classifier).
- `lib/invoice-audit.ts` — **fixed PostgREST 1000-row cap** (was loading only 1000 of 2,633
  lines + truncated audit/docs) via `fetchAll()` pagination; added categoryKey + categories.
- `scripts/invoice-audit-tree.ts` + `invoice-audit.astro` — collapsible category sections
  with line count / $ subtotal / to-audit / at-risk per section.
- `docs/42` updated (Pattern C: 1000-row cap; Phase 2 section).

**Verified**: build compiles; all 2,633 lines load across 560 invoices; invoice 2010366270-001
renders 6 category sections with correct subtotals; no console errors. Uncategorized ~19%
= metal/tile/siding (future taxonomy call).

**Open threads**: deploy Phase 2 (push to main); Item 1 credit-memo tracking; Item 4 PDF
auto-pull; extend grouping to agreement + estimate surfaces; refine classifier via overrides.

## Session — segmentation rolled out site-wide + design guide

**Decision (Chris)**: before deploying, document the Phase-2 pattern in the design guide and
roll it out site-wide ("all our other dashboards are built the same so they should function
the same"); next focus = agreements + estimates.

**Design guide**: `docs/40-dashboard-design-system.md` — added §5a (roof-system category
sections: classifier source, collapsible default-collapsed UX, pending-only hide rule,
uncategorized-visible rule) + checklist items (segmentation; paginate `.range()`).

**Rolled out the category grouping to all line-list dashboards** (collapsible,
default-collapsed sections ordered by `roof_system_category.sort_order`, per-category pills):
- **Agreements / branch price list** (me): generated `abc_price_list_items.category_key`;
  `v_branch_price_list` exposes it (override-aware); `branch-price-list.ts` + `branch.astro`
  group items; search auto-expands matching sections.
- **Order audit** (agent): `v_order_audit_line.category_key`; loader/tree/astro grouped.
  Perf fix — added generated `abc_product_catalog.category_key` so the 18.5k-line order view
  reads a precomputed column instead of running the regex classifier per row per page
  (view 3.0s → 1.7s; the residual ~1.7s/22s app load is the pre-existing 18.5k-line volume,
  not segmentation — flag as a separate order-audit perf item).
- **Estimate audit** (agent): `v_estimate_audit_line.category_key` (classify on description,
  no item_number/override — estimate lines lack item codes); grouped with Cost/Price
  subtotals (it's an editor, not a pass/fail auditor); also fixed its 1000-row cap.

**Verified**: full `npm run build` passes; branch page shows ordered category sections scoped
to the invoice agreement; estimate page renders 180 category sections; order view fast +
sane distribution; no console errors. All schema changes applied to prod (additive).

**Still open**: deploy this rollout (push to main); order-audit large-list perf; Items 1 & 4.

## Session — site speed (client-blocking) + order-audit perf

**Mandate (Chris)**: site too slow, "will not be accepted by the client" — site speed now top
priority. Profiled all dashboards (curl, warmed, prod DB): only **order-audit** was slow
(6.85s warmed / 22s cold); invoice-audit 1.23s; everything else <250ms.

**Root cause**: order-audit eager-loaded all 18.6k order lines (19 paginated reads of a 1.7s
view) to render a summary tree, when only 181 of 3,178 orders are active (the default view).

**Fix** (commit pending): loader scopes to the active window by default (`?status=` →
`scope active|all`) and **no longer loads lines** — added `/api/order-audit/lines?order=` so
the tree lazy-fetches one order's lines on expand. Order-summary rollups come from
`v_order_audit_order` so KPIs stay complete; archived count via a head-count.
- **Active 6.85s → 0.45s; archived 8.8s → 2.0s; all → 1.9s.** Build passes; lazy load + 9
  category sections verified on order 2011014809; no console errors.
- Tradeoff: order-summary search no longer includes line-item text (lines aren't in the
  payload); order#/PO/job/branch search unchanged. Status change reloads (scope switch).

**Reusable pattern**: lazy-load line detail via API for any >~thousands-of-rows drill-down.
invoice-audit (1.23s, 2.6k lines) is acceptable; candidate for the same pattern if needed.

**Still open**: Item 1 credit-memo tracking; Item 4 PDF auto-pull; Metal/Tile/Siding taxonomy;
RLS DB-health pass; (optional) apply lazy-line pattern to invoice-audit.

## Session — Item 1 credit memos (distinguish + match, read-only slice)

Built the verifiable read-only slice of Item 1. Note: post-Phase-1 backfill, CM lines now
carry unit prices (earlier "null price" finding no longer holds), so unit-price matching is
straightforward.
- `schemas/cleverwork-roofer/115-credit-memo-audit-view.sql` — `v_credit_memo_audit`: per CM
  (is_credit_memo OR negative total), resolves the original invoice via
  original_invoice_reference (root → held `-001`) and compares each CM line's unit price to
  the original line's by item_number → match_status (matches/mismatch/partial/no_reference)
  + rollup. Applied to prod.
- `app/command-center/src/lib/audit-queues.ts` — `loadCreditMemoQueue()` builds the live
  AuditQueue payload from the view (replaces the mock `cmRaw` placebo).
- `app/command-center/src/pages/accounting/audit/credit-memos.astro` — awaits the live loader.
- Verified: 130 real CMs; KPIs Matches 15 / Needs Review 9 / $Mismatch $10,105 / No Reference
  85; rows show CM#, original invoice, branch, credit $, "X/Y match", status; build passes,
  no console errors. The `/accounting/credit-memos` queue is no longer a placebo.

## Session — Item 1 credit-memo WRITE path (disposition)

Chris chose to build the write path; verified end-to-end against prod.
- `api/credit-memos/disposition.ts` — POST {invoiceNumber, decision: approve|review|reject}
  → upserts `credit_memo_requests` (UNIQUE invoice_number) with mapped status
  (approved|needs_more_evidence|rejected), recomputing credit/line facts server-side from
  v_credit_memo_audit; packet jsonb holds match summary. Internal record only — NO external
  send (SOUL boundary). Auth-gated (locals.actor).
- `lib/credit-memo.ts` — `loadCreditMemo()`: CM header + per-line unit-price match vs the
  resolved original + current disposition.
- `accounting/credit-memos/[invoice].astro` — REPURPOSED from the old variance-candidate
  packet (orphaned once the queue went live) to the received-CM detail: line match table +
  Approve/Needs-review/Reject buttons (client POST → API, live status update + toast).
- `audit-queues.ts` — queue overlays a recorded disposition over the match-derived status
  (e.g. "Approved" not "Matches") + filter options for the disposition statuses.
- Verified: opened CM 2004899544-001 (matches, $161.25, line 05SERAD42 $75=$75) → clicked
  Approve → credit_memo_requests row persisted (status approved, by Local Operator, packet
  match_status=matches) → queue row showed "Approved". Test row then DELETED (table clean,
  0 rows). Build passes; no console errors.

## Session — Item 1 COMPLETE: requested-CM flow + lifecycle

Closed Item 1 (Chris chose to finish it). Verified end-to-end on prod, test data removed.
- `schema 116` — `credit_memo_requests.request_kind` (received | requested) + CHECK.
- `api/invoice-audit/mark.ts` — credit-flag/credit-noflag dispositions now AUTO-CREATE a
  'requested' credit_memo_request for the invoice (recomputes credit-flagged exposure from
  the audit ledger; preserves lifecycle status). Best-effort; never fails the audit write.
- `api/credit-memos/disposition.ts` — now handles BOTH flows: received (approve/review/reject)
  and requested lifecycle (mark-sent → sets sent_at + 14d follow_up_due_at; mark-received;
  close). No external send (SOUL); "sent" only logs a human sent it.
- `lib/credit-memo.ts` — `loadCreditMemo` falls back to the requested detail (disputed lines:
  invoice price vs negotiated + request status) when the invoice isn't a received CM.
- `accounting/credit-memos/[invoice].astro` — renders kind-appropriate header + actions
  (received: Approve/Review/Reject · requested: Mark sent/received/Close).
- `audit-queues.ts` — queue merges Received + Requested with a Type column/filter and
  requested KPIs ($ Requested, Awaiting Vendor).
- Verified: credit-flag line 02MLVIA3SG ($780 over) in invoice-audit → requested draft
  auto-created → packet "Mark sent" → status sent + follow-up set → queue showed
  Requested/Sent to vendor. Build passes, no console errors. Test rows deleted (0/0).

**Item 1 DONE** (received-CM match+approve AND requested-CM tracking). Still open: Item 4 PDF
auto-pull (host-only); Metal/Tile/Siding taxonomy (Chris decision); RLS DB-health pass;
optional auto-approve-all-matching + lazy-line pattern for invoice-audit.

## Memory Health

- 2026-06-19 health check: index freshness unverifiable from sandbox (host `~/.memsearch` unreachable) and markdown changed today — run `bash scripts/memsearch-index-open-brain.sh` on the Mac to refresh recall. MEMORY.md at 2371/2500 (near cap, watch). All other checks green.

## Session — wrap-up / handoff

**Full session deliverables, all deployed at commit `dffc50d`** (dev `cleverwork/price-agreement-audit` = `origin/main` = local HEAD, verified equal; tree clean except unrelated stray `excalidraw.log`):
- Phase 1 invoice-audit fixes (items 2/3/5/6) · Phase 2 segmentation (12 cats) rolled out site-wide + `docs/40` §5a standard · order-audit perf 22s→0.45s + 1000-row cap fix · Item 1 credit memos COMPLETE (received match+approve AND requested-CM tracker draft→sent→received). Schemas 99,101,107,113,114,115,116 (+ generated category cols) all applied to prod, additive.
- Repeating-issue playbooks in `docs/42` (ABC mapping drift · UOM `effective_unit_price` · PostgREST 1000-row cap).

**Handoff refreshed:** `docs/handoffs/current.md` (prior archived to `docs/handoffs/archive/2026-06-18-2017.md`). `context/MEMORY.md` updated (2,488/2,500).

**Open (all gated, in handoff):** Item 4 PDF auto-pull (agent-host only); Metal/Tile/Siding categories (Chris decision); RLS 7 tables (DB-health pass); optional auto-approve-all-matching + invoice-audit lazy lines.

## Session 11:30 — Chris punch-list (7 fixes: territory map + invoice audit)

**Goal**: Fix 7 reported issues before next items. Branch `cleverwork/price-agreement-audit` (= live lineage → origin/main → Coolify).

**Diagnosed + fixed (verified against prod DB + dev server):**
- **IA-1 branch/office wrong for EVERY invoice** — `v_invoice_audit_invoice` derived branch from ship_to→price-agreement match (always collapsed to one branch, "152 Edmond OK"); ship_to addr is just Richardson HQ. Real selling branch is in `abc_invoices.raw->'branch'` (481 Aurora CO, 113 Wichita KS ×248, 011 Fort Worth ×50…). Fixed view to key branch/office off `raw->'branch'`, joined to vendor_branches by `ltrim(branch_number,'0')`. Now groups Wichita/Richardson/Denver/KC correctly.
- **IA-3 UOM variance inverted** — negotiated price (abc_price_list_items, per SQ) compared raw vs invoice per-BD price. 02TKTXTRB showed −59.6% ("saving") when truly +21% overcharge. Fix: divide negotiated by `raw->priceQty.priceConversionFactor` when ordered UOM ≠ priced UOM. Both views. Migration `schemas/cleverwork-roofer/117-invoice-audit-branch-uom-fix.sql` (applied to prod via execute_sql).
- **IA-2 PDFs** — 45/560 window invoices lacked PDFs (ABC pdf endpoint was gated). Chris approved. `integrations/bridges/abc-supply/backfill-invoice-pdfs.mjs` → fetched+stored all 45 (560/560 now). On-demand: `src/lib/abc-invoice-pdf.server.ts` + wired into `pdf/[invoiceNumber].ts`. Verified live (302; row persists; source must be `portal_sync` per `invoice_documents_source_check`).
- **IA-4/IA-5** — `invoice-audit-tree.ts`: Price List greys out (non-clickable span) when branch has no negotiated lines (113 do); Price List + Invoice now matching left-justified pills; "PDF"→"Invoice" (always-active, on-demand-backed).
- **TM-1** — logo spans rail width (`.brand-lockup img` width:100%; was 160px). Verified 244px = nav button.
- **TM-2** — `currentAgreement` null for ALL branches (5 CEO-verified agreements all EXPIRED; `isAgreementCurrent` drops expired). Added `findAgreementOnFile`/`agreementOnFile` (ignores expiry) → 42 branches now show `[PA <number>] [Expired <date>]` pills in popup + side card. `currentAgreement` semantics (map green) untouched.

**State**: code in working tree, NOT committed/pushed. Migration 117 + PDF backfill LIVE on prod DB now (shared DB → cc.proexteriorsus.net data reflects immediately). Frontend (buttons/pills/logo/on-demand endpoint) local-only until committed + Coolify deploy. `npm run build` clean.

**Deploy note**: on-demand PDF needs `ABC_SUPPLY_CLIENT_ID`/`ABC_SUPPLY_CLIENT_SECRET` in command-center Coolify env (web tier had no ABC client before). Backfill covered all current invoices; on-demand only fires for future un-synced ones.

**Open**: commit/push + deploy is Chris's call (did not auto-push). → committed fafa2c3 on Chris's "YES PLEASE COMMIT" (still not pushed).

## Session 11:54 — Price Agreement Audit rework

**Goal**: Rebuild PA Audit (`/abc-price-agreement-gaps`) to match the other dashboards.

**Built (verified on dev + prod DB):**
- **Restructured to PE Office → Vendor/Branch → Item Category → Item** drill-down (was a flat agreement table). New `src/scripts/price-agreement-tree.ts`; full rewrite of `src/lib/price-agreement-audit.ts` + `src/pages/abc-price-agreement-gaps.astro`. Source = abc_price_agreements + abc_price_list_items (category_key) + abc_price_agreement_branch_matches; branch→office via `ltrim(branch_number,'0')` → vendor_branches.pricing_territory_office_id (same join as invoice audit).
- **KPIs**: Coverage Rate (avg per office = covered branches / assigned branches; 24%), Expired (59 branches), Expiring ≤30d (6), + Negotiated Agreements (7) and Priced Items (825 distinct).
- **Item counts are per-distinct-agreement** (an agreement covering N branches is one catalog) — first cut summed per branch and showed 16,111; fixed to 825.
- **API price lists** (`agreement_number LIKE 'API-%'`, 92 branches, non-negotiated, no items, all out-of-territory e.g. Pooler GA) render with an `API · non-negotiated` badge + filter (Chris's chosen option: flag on branch, exclude from items). Request-renewal action preserved (per branch's agreement).
- Decisions taken: coverage = branch coverage; API flagged-not-excluded; #3 deferred.

**Design notes captured** (`docs/43-price-list-lock-and-api-pricing-rules.md`):
- #3 invoice↔price-list **hard lock on first match**, overridable only in Invoice Audit (lock table; views read locked agreement). To build with Price List Coverage.
- #4 API agreements **out-of-territory only + must be labeled non-negotiated** on orders/invoices/estimates. PA Audit done; extend to order/invoice/estimate lines.

**State**: working tree (not pushed). No new migration — pure app + lib. `npm run build` clean. → committed 3d83b0e.

## Session 12:00 — Agreement Builder rework (Phase A)

**Goal**: Rebuild `/accounting/price-agreement/builder` per Chris's 7-point review. Decisions: cost = projected + savings; review unit = per-family; export naming `PA-<VENDOR>#<BRANCH>-<PA#>`; build invoice-volume + two phases.

**Phase A built + verified on dev (committed):**
- **6-level drill-down**: PE Office → Vendor → Vendor/Branch → Category → Item(family) → Variation. Default = ALL offices (no pre-filter). Branch skeleton from new overview lib; per-branch catalog lazy-loads via `/api/price-agreement/branch-detail`.
- **Cost roll-up** (#2): per branch, set price = proposed → branch negotiated → historical avg; projected = Σ set×(36mo qty); savings = historical − projected. Rolls Branch→Vendor→Office. Needed per-branch volume → new view **`v_branch_item_spend`** (migration 118; aggregates abc_invoice_lines by real selling branch raw->branch × item, 36mo). Applied to prod.
- **#7 exports**: PDF/CSV download as `PA-ABC#113-2036874-16.{pdf,csv}` (via `&name=`). **#5**: methodology page `/accounting/price-agreement/methodology` (renders docs/44) + "📘 ABC classes" button in toolbar + per-branch action bar (next to Issue link).
- Kept per-branch Save draft / Draft for review / Issue link (now per-branch in the action bar). `agreement-package.ts` now tags each family with a roof-system category (from abc_price_list_items.category_key).
- Files: `lib/agreement-builder-overview.ts` (new), `scripts/agreement-builder-tree.ts` (rewrite), `pages/.../builder.astro` (rewrite), `api/price-agreement/branch-detail.ts` (new), `methodology.astro` (new), `lib/agreement-package.ts` + `package/pdf.ts` + `package/csv.ts` (edits), schema 118.
- KPIs: PE Offices 4, Negotiable Items 857, 36mo Spend $937k, Projected $951k, Savings −$14k (current negotiated prices net slightly above recent avg — real signal, not a bug).

**Design notes**: `docs/44` (ABC classification methodology, the #5 README content) + `docs/45` (#4 Communications Dashboard — single source of truth / approval gate; lifecycle states + open questions).

**Phase B — NOT built (next)**: #6 per-family review checkboxes + per-branch progress bar + confetti + Submit (→ draft-for-review into the comms gate). Needs an additive `reviewed`/`reviewed_at` on agreement_package_items. Confetti canvas already in the page, unused.

**State**: working tree; build clean. Migration 118 + v_branch_item_spend LIVE on prod DB.

## Session 14:30 — UOM/pricing normalization (the once-and-for-all fix)

**Goal**: Solve UOM-driven mispricing across ALL pricing dashboards. Seed items `02MLVIA3SG` / `02MLVIA3WP` (Mal Vista shingles, 3 BD/SQ).

**Root cause**: `abc_invoice_lines.quantity`/`.uom` ingested inconsistently — usually from `priceQty` (SQ) but ~8.5% from `shippedQty` (BD) — so the same SKU stored $46.50/BD on some invoices and $132/SQ on others, while agreements are always per SQ. `effective_unit_price` (generated = ext/qty) inherited it. Orders had NO UOM handling → live −80% to −100% artifacts. Migration 117 patched only one view, fragile.

**Fix (Option A, chosen w/ Chris; display in pricing UOM)** — migrations applied to prod + schema files:
- **119**: raw-derived generated cols on `abc_invoice_lines` (`ship_uom/qty`, `price_uom/qty`, `price_conversion_factor`, **`price_per_uom` = ext/priceQty.value**); + keystone view **`v_item_uom_map`** (item → ship_uom/price_uom/units_per_price_uom). Immune to ingest writer; self-backfills history.
- **120**: `v_invoice_audit_line` + `v_invoice_audit_invoice` → canonical `price_per_uom`, agreement compared only when `price_uom == pli.unit`, else `uom_mismatch` flag (variance NULL). Retired 117 neg_factor.
- **121**: `v_order_audit_line` + `v_order_audit_order` → convert order price (ordered UOM) to agreement unit via `v_item_uom_map`; flag mismatch.
- **122**: `v_recent_invoice_price`, `v_branch_item_spend` (qty in price UOM), `v_invoice_line_audit_eval` (auto-pass gate, unit-aligned), `v_abc_invoice_lines_with_pdf` (expose canonical cols).
- **App**: `abc-price-gaps.ts` + `credit-memo.ts` repointed off inconsistent `pricePerUnitAmount`/`unit_price` → `price_per_uom`; `invoice-audit-tree.ts`/`order-audit-tree.ts` show `/UOM` suffix + "UOM mismatch / Review (UOM)" badges (threaded `uomMismatch`+`negotiatedUom` through libs + order-audit API).

**Validated (prod SQL + running dev server)**: both items now 100% SQ; BD rows resolve to 139.51/SQ (−9.36%). Order audit: 03MLLAAB −24.94% (was −81.24%), 03CTPRIRWW now uom_mismatch (was fake −81%). Auto-pass gate: 0 unit-mismatched matches. System-wide UOM mismatches = 1 invoice line + ~14 order lines (flagged for review, not fabricated). `npm run build` clean.

**Docs/memory**: `docs/46-uom-pricing-normalization.md` (full contract); one-liners added to CLAUDE.md (Working style) + AGENTS.md. MEMORY.md playbook #2 updated (supersedes 117), schemas→122.

**State**: DB live on prod (dev + cc.proexteriorsus.net share it → dashboards already corrected). App-code changes committed to a branch, **NOT yet pushed to main** — awaiting Chris's live validation before the public code deploy. Confirmed live branch = `origin/main` (buildCommit matched after fetch).

**Open threads**: push app code → main → verify buildCommit (after Chris validates); adjacent (out of scope): agreement effective/expiry-date matching in audit views; ship-to `…-21` branch-coverage gap (negotiated null).

**DEPLOYED 15:xx**: pushed to `origin/main` (`9789b47..6b9cabb`), branch converged 0/0; Coolify auto-built; `cc.proexteriorsus.net/healthz` buildCommit = `6b9cabb` stable across 10 checks. UOM/pricing fix now COMPLETE site-wide (DB + app live). Live site is WorkOS-gated so final visual check is Chris-side; dev-server + prod-SQL validation already green.

## Session 15:50 — WorkOS agent-auth skill + handoff

**Goal**: Build a reusable skill so OB agents can reach the WorkOS-gated live Command Center; then wrap up.

**Delivered**: `.claude/skills/workos-agent-auth/SKILL.md` (now live as `/workos-agent-auth`). Discovered the app already ships the full WorkOS Auth-for-AI-Agents surface:
- **Path A (works today)**: service bearer token on `/api/*` — `Authorization: Bearer <token>`, validated in `app/command-center/src/lib/access-control.ts` against `AGENT_SERVICE_TOKENS` (root `.env`, CSV `agentId:token`) or `AGENT_SERVICE_TOKEN_SHA256_<AGENTID>`. `middleware.ts` honors bearer on API routes ONLY; HTML pages stay human/WorkOS-gated. `healthz` shows `agentServiceAuthConfigured=true`, `agentServiceTokenCount=13` (full roster ob-accounting…hermes). This is how agents read the live site.
- **Path B (discovery live, NOT usable)**: WorkOS `auth.md` OAuth — `/auth.md`, `/.well-known/oauth-*`, `/oauth2/token`, scopes `open-brain.read|tasks.request|tasks.write|memory.read`, issuer cc.proexteriorsus.net. `/auth.md` says registration/token-minting/revocation return `not_implemented` until signing key + token store + issuer list + replay protection + human-session bridge land. Code: `src/lib/agent-auth.ts`, `src/pages/{auth.md.ts,oauth2/*,agent/auth*,.well-known/oauth-*}`.

**One-liners added**: AGENTS.md (roster intro) + CLAUDE.md (Working style) → point at `/workos-agent-auth`.

**Note**: auto-mode classifier (correctly) blocked extracting a real prod token to hit the live site — left as operator/authorized action; mechanism is code-verified + `/api/*` path proven on dev earlier.

**Commits (LOCAL only on `cleverwork/price-agreement-audit`, per Chris "leave local")**:
- `5533e6c` UOM/pricing normalization — **PUSHED + DEPLOYED** (on `origin/main` as `6b9cabb`, live).
- `da3b91c` `/workos-agent-auth` skill + AGENTS/CLAUDE one-liners — **NOT pushed**.
- (handoff) MEMORY.md + daily-log update — **NOT pushed**.

**Where we left off / next session**:
1. UOM fix is complete + live; nothing pending there except the adjacent items (audit-view agreement effective/expiry-date matching; ship-to `…-21` no-agreement coverage gap).
2. `/workos-agent-auth` skill is local — push `da3b91c` (+ handoff commit) to `origin/main` when ready (no-op prod rebuild; app doesn't read `.claude/`).
3. To actually USE agent auth on prod: an authorized run can `curl -H "Authorization: Bearer <token from .env AGENT_SERVICE_TOKENS>" https://cc.proexteriorsus.net/api/...` (HTML pages won't work — API only).
4. Builder **Phase B (#6)** remains the next feature.

**Alignment at wrap**: `origin/main` `6b9cabb` == live `buildCommit` (app 100% aligned, nothing app-affecting stranded). Local branch is 2 commits ahead of `origin/main` — both are docs/skill/memory only (zero app impact), intentionally unpushed.

## Session 18:50 — ABC invoice AR backfill + API-vs-report validation (pre-Phase-6)

**Goal**: Use ABC closed-report DUE_DATE as date_paid; validate open data + API numbers vs the 2026-06-19 open/closed report download; audit mismatches; document an ABC API-team conversation thread.

**Deliverables**:
- `schemas/cleverwork-roofer/123-abc-invoice-ar-status.sql` (additive AR cols on abc_invoices) — **applied to prod**.
- Backfilled **556** invoices (171 open / 385 paid) from report CSVs; date_paid = DUE_DATE, fallback invoice_date for 184 blank-DUE_DATE closed docs (date_paid_is_proxy).
- Staging tables `abc_invoice_ar_import` (971), `abc_invoice_ar_line_import` (12,061) kept as import audit trail.
- `docs/47-external-abc-api-open-conversations.md` (ABC API team thread).
- `docs/48-abc-invoice-ar-and-api-validation-audit.md` (audit record).

**Key findings**:
- **Invoice totals match 100%** — API total_amount = report TOTAL_DUE for all 171 in-DB open invoices, $0 variance. AR dollars trustworthy.
- **🔴 ABC API truncates invoice lines at 10** (dataset-wide: max raw lines = 10, 152 invoices at exactly 10, 0 over). Header totals fine but line detail silently lost for >10-line invoices; ~$42.8K line value missing across open set, worst single $4,191 (2010874108-001). Breaks line-level price/UOM audit for those invoices. → docs/47 item #2.
- API has no date-paid/due-date/AR-status field; report CSV is the only AR source. → docs/47 item #1.
- API window Dec 2025+ only; 415 historical invoices (~$1.65M) exist only in reports. → docs/47 item #3.
- 1 open invoice missing from DB = sync lag (2010370311-002, dated 06-18, after 06-18 09:45 sync). 4 DB-only invoices = $0 docs excluded from AR reports.

**Decisions chosen by Chris (interview)**: persist AR to abc_invoices (additive) · backfill existing only (don't import 415 historical) · invoice_date fallback for blank DUE_DATE · invoice + line-item reconcile depth.

**Open threads (for Chris review — see docs/48 §⚠️)**: 10-line truncation remediation path; date_paid-as-DUE_DATE proxy sign-off; 184 invoice_date fallbacks; whether to import 415 historical report-only invoices.

### 19:20 — Live ABC API verification of the 10-line cap
Confirmed endpoints (all 200): history/{billTo} (header-only list, paginated), id/{invoiceId} (full header + capped lines), pdf/{invoiceId}. History pull 2023-01-01..2026-01-19 for bill-to 2036874-1 = 72 invoices. **10-line cap is the ABC API itself, not ingestion**: live by-id returned 10 lines for invoices with 14/18/26 real lines (subTotal/total correct). No line pagination — itemsPerPage/pageSize/includeAllLines/lineItemsPerPage/pageNumber=2 all still 10. Interim full-line source = invoice PDF. docs/47 item #2 + docs/48 updated.

### 20:05 — CSV line-completion (Phase 1) + PDF OCR proposal
Verified the ABC report CSV has FULL untruncated line detail (2010452632-001 = all 26 lines = 13296.14 subtotal). Closed the API 10-line gap from CSV (no OCR needed for current data).
- mig 124: abc_invoices truncation flags; new table abc_invoice_lines_full (2,251 complete CSV lines for 145 truncated invoices, canonical UOM); view v_invoice_lines_complete (API lines for un-truncated + full CSV for truncated, no double-count, no deletion of API atoms).
- **145** truncated invoices (precise; CSV lines > API lines). All 145/145 reconcile to subtotal to the penny. View no-double-count verified (3,434 = 1,183 + 2,251).
- **Phase 2 NOT done**: ~8 audit views + 5 libs still read abc_invoice_lines directly → live audit still truncated until repointed at v_invoice_lines_complete. Recommended as first Phase-6 cleanup item (touches same dashboards).
- PDF OCR ingest scoped as deferred A3: proposals/2026-06-19-invoice-pdf-ocr-line-completion.md (go-forward automated coverage for new/out-of-window invoices; CSV detail = ground-truth oracle for the parser).

### 20:15 — Decision: Phase 2 line-repoint = first Phase-6-cleanup item
Chris decided the audit-view repoint (~8 views + 5 libs → v_invoice_lines_complete) is the FIRST task of Phase 6 cleanup, with per-dashboard verification. Until then the live line-level audit is incomplete for the 145 truncated invoices (Phase-1 data + canonical view already in place). Recorded in docs/48.

### 21:30 — Phase 6 item 1 DONE: audit views repointed to complete lines (migs 126-127)
Repointed all 8 audit views + 3 app libs (credit-memo.ts, abc-price-gaps.ts, mark-paid.ts) from abc_invoice_lines → v_invoice_lines_complete. live-work.ts/decision.ts only had a sourceTable label (left). 
- mig 126: dropped invoice_line_audit FK to abc_invoice_lines.id (so CSV-line audit decisions can be recorded; col+index kept; lines never deleted). Extended complete view with created_at/updated_at. Recreated 8 views.
- mig 127: RLS + SELECT grants on abc_invoice_lines_full + v_invoice_lines_complete.
- VERIFIED: non-truncated byte-identical (line_count 1183, at_risk 327.02). Truncated now full: audit_line 1450→2251, item_uom_map 2633→3434, worst invoice 26 lines.
- Newly surfaced on the 145: 903 overcharged lines, 1269 no-agreement, **$8,656.75 at-risk** previously hidden by the 10-line cap.
- Re-derived audit decisions (mirror mig 100): 538 pre-2026 backfilled passed, 55 2026 auto_match passed, 1658 pending/surfaced.
- npm run build clean. DB live immediately on shared prod; app libs deploy via Coolify on push.

### 22:40 — Go-live: ABC open CSV = source of truth; 3 audit surfaces in lockstep
Aligned open/paid to the ABC open/closed report; Invoice Audit + Price Agreement Audit + Agreement Builder now share v_invoice_lines_complete.
- **Open = 172** (= open CSV exactly), paid 803, total 975 (560 API + 415 imported from report).
- invoice-audit.ts: open/paid now from abc_invoices.ar_status (not the invoice_documents gate). KPI "Open Invoices" 227→172.
- Gate reconcile: 56 ABC-paid/gate-open invoices marked paid via sanctioned gate_override (provenance stamped). 0 reverse.
- Imported 415 (414 historical paid Apr25-Apr26 + 1 open sync-lag 2010370311-002) → abc_invoices + abc_invoice_lines_full, ar_source='abc_csv_history', lines_truncated_by_api=true. Lines reconcile to subtotal (0 mismatch). Historical paid lines marked passed (settled); open one stays active.
- 4 zero-$ docs tagged paid (ar_source=zero_dollar_doc).
- Retired Price Foundation / Negotiated Catalog / Price List Coverage from nav (lib/nav.ts; pages kept). Verdicts: Price Foundation obsolete (review queue empty); other two superseded for audit flow.
- Lockstep verified: v_invoice_lines_complete = v_invoice_audit_line = 5803; sample invoice 26 lines across surfaces. OPEN at-risk $4,437 (actionable), 1,110 open to-audit lines.
- Full record + assumptions ledger: docs/49. npm run build clean.
- NOTE: gate reconcile + historical import + zero-$ tag are DATA ops on prod (documented in docs/49); schema unchanged this round (migs 123-127 already live).

### 19:00 — KPI cards scoped to OPEN only
invoice-audit.ts totals now sum per-invoice fields over open (unpaid) invoices; invoice-audit.astro adds an "open invoices only" scope note + Credit Memos sub → "open · unapplied". Open-scoped baseline: 172 open / 1,110 to-audit / $4,437 at-risk / 728 no-price / 44 credit memos (was global 2,221 / $8,984 / 2,163). Paid/closed remain browsable via "All invoices" filter + office tree. Build clean.

### 19:10 — Fix: historical-import branch mapping (office attribution)
The 415 imported historical invoices used CSV BRANCH_NUMBER ("462A") which is the API branch + trailing letter; they all landed Unassigned. Learned the crosswalk from invoices in both API+report (462A→462), stripped trailing letter from raw->branch->number. Now 414/415 map to real PE offices (1 blank-branch report gap remains); 0 alpha codes left in v_branch_item_spend. Price Agreement Audit + Agreement Builder spend now clean with history. docs/49 updated. (data + doc only, no app code.)

### 19:25 — Order Audit: already aligned (no work needed)
ABC order API does NOT truncate (max 28 lines, 829 orders >10). v_order_audit_line/order (mig 121) already use canonical v_item_uom_map + raw->branch — same spine as invoices, and improved by the invoice line-completion (v_item_uom_map 2633→3434). Sanity: 3178 orders, 312 at-risk, ~$30,108. Lone gap: 9 orders (0.3%) on 5 ABC branches (36/37/38/79/1271) not assigned to a PE office in vendor_branches → business mapping decision, not a bug. Verdict + gap recorded in docs/49.

### 19:45 — Frequently Ordered ground-level anchor (6 offices loaded + audited)
Parsed 6 scrape files (1 csv + 5 xlsx) → frequently_ordered_import (390 rows). 100% of items in every scrape are items we actually buy; 24-38 new-to-catalog per office (surface as "frequently ordered, no agreement yet"). UOM aligns (gap = BD↔SQ shingle conversion).
- mig 128: frequently_ordered_office_map (6 scrapes→5 offices; Denver=Englewood primary+Aurora alt) + v_office_ground_price (anchor normalized to canonical UOM vs negotiated vs recent actual).
- Gap signal: Richardson(FtWorth) + Euless(WichitaFalls) = 0 office-scoped negotiated coverage; KC = 4 agreements WORSE than current shelf (stale); Denver 27/27 beat shelf; Wichita 14/80.
- Next: Phase B builder UI (reviewed checkbox + progress bar + confetti + submit) + anchor column in builder.

### 20:00 — Agreement Builder Phase B BUILT (review UI)
- mig 129: agreement_package_items.reviewed/reviewed_at/reviewed_by (additive).
- items.ts persists reviewed (sent on every save so price-only saves preserve it).
- agreement-package.ts: NegVariation.reviewed loaded from package items.
- agreement-builder-tree.ts: per-family "Reviewed" checkbox + per-branch progress bar (reviewed families / total + %) + confetti burst at 100%. Submit = existing "Draft for review" handoff (drafts into comms gate, never sends).
- builder.astro: CSS for checkbox + progress bar.
- Verified: npm build clean; dev page renders 200, no console errors, confetti+progress present. Interactive flow validates live (authed lazy-load). Anchor column = fast-follow (data live in v_office_ground_price).

### 20:25 — Variations analysis + Monday invoices + price-list PDFs (3 artifacts)
- **Variations**: 99 frequently-ordered items → 53 families / 177 SKU variations (avg 3.3). Active universe is ~53 families; supports tightening methodology to family-level (1,300 SKUs = long tail). docs/50 §C.
- **Monday invoices** (mig 130, monday_invoice_queue, 27 loaded): all in DB; 24 open + 3 already-paid; due window 5/31-6/30. 7 null-due = ALL credit memos w/ terms → filter needs effective_due_date = COALESCE(due_date, derive(invoice_date, terms)); "2nd End of Month" = EOM+2. Need to backfill terms (API raw lacks it, report CSV has it). docs/50 §A.
- **Price PDFs** (mig 131, abc_price_list_pdf_import): scanned, no text layer. PA_2036874-16 = NEW SKU agreement (Justin Garza, eff 6/4-6/30); page1 loaded (56 items); 19 prices DROPPED vs our catalog (Highlander 147.17→126.00/SQ, Vista→139.50) = real improvement; 29 new items. Wichita branch-113 list = family-level, not yet loaded. Remaining pages need OCR/Justin's source file → deferred review phase promotes PA-16 to live agreement. docs/50 §B.

### 20:45 — OCR pipeline validated (Unstructured.io) for the Global Price Agreement
Unstructured.io is the OCR engine for vendor price-list ingest. Key in .env (gitignored), placeholder in config/.env.example. POST api.unstructuredapp.io/general/v0/general strategy=hi_res → Table elements w/ text_as_html. Proven on both scanned ABC PDFs. Normalization: match OCR item# to abc_product_catalog → PA agreement 66/73 exact (90%), 4/7 misses auto-fixed via pg_trgm (02MLVIASHE→02MLVIA3HE, 141P81716→14IP81716), 3 ambiguous → review queue. This is the recurring multi-vendor ingest loop. Next (Chris's choice): build Global Price Agreement schema (global_catalog_item / vendor_item_xref / vendor_price / v_best_vendor_price) + seed ~150 canonical (53 freq-ordered families + top Class A), ABC as vendor #1.
