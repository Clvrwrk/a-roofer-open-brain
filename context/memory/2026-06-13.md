# 2026-06-13

## Session — Price Foundation Migration Review dashboard (MVP)

**Goal**: Build the Command Center "Data Quality → Price Foundation" review dashboard from Chris's spec.

**Key discovery**: The spec's `price_foundation_*` tables ("Round 4") do NOT exist. Only source tables do. Chose (confirmed by Chris) to **derive the 3 queues live** from source tables + one additive overlay table for resolution state, and to match existing app auth (no new RLS this pass).

**Real partition** (over `abc_price_list_items` = 1067; wireframe's 37/604/426 were illustrative):
- Immediately reusable: 46 (approved · has_sku · item#)
- SKU Review Queue: 233 (has_sku=false)
- Business Rule Queue: 788 (has_sku, pending approval)
- Branch Review Queue: 56 (match_type=generated_api_new) — orthogonal

**Deliverables**:
- Migration `81-price-foundation-review.sql` → applied as `price_foundation_review_actions_overlay` (additive overlay, RLS, service-role-only).
- `src/lib/price-foundation.ts` (derivation + overlay join), API `…/price-foundation/review.ts` (resolve/reject/defer → overlay + `dashboard_action_log`).
- Pages: overview + queues (tabs/filters/table/side panel), 2 client scripts, `PfStatCard`, AppShell nav + `global.css` Data Quality block.
- `docs/38-price-foundation-review-dashboard.md`.

**Verified**: `npm run build` clean; both pages 200 with live numbers; POST resolve persisted overlay+audit and flowed to summary (SKU 233→232 open/1 resolved); smoke-test rows deleted after.

**Open threads**: `is_one_off`/`is_reusable` not modeled on `price_agreements` (leakage approximated); WorkOS-role RLS deferred; Phase 2 agent-proposed resolutions (`proposed_by_agent` column already present). Not committed — left for Chris.
