# 2026-07-06

## Session 1

**Goal**: Scheduled ABC Supply FULL production sync (abc_product_catalog, abc_vendor_branches, abc_regions).

**Run**: 2026-07-06T11:05Z, env=production — **BLOCKED (did not complete) in the Cowork sandbox.** rowsUpserted: catalog n/a, branches n/a, regions n/a (no run reached its final summary). Auth OK (token exchange 200; runs got past auth into fetch+upsert). DB intact and unchanged: abc_product_catalog 331,390 / abc_vendor_branches 720 / abc_regions 40.

**Decisions**: Tables are already fully populated, so this is an incremental refresh — change-gated idempotent upserts mean the killed partial runs made no harmful/duplicate writes (counts verified unchanged before and after). Did NOT chunk the full ~331-page catalog across 50-70 sequential 45s calls (impractical/unreliable for a production sync).

**Open threads**: The Cowork sandbox cannot run this 20-45 min job: each bash call is capped at 45s and background/nohup processes are killed between calls (fresh PID namespace, `--die-with-parent`), so the task's "nohup + poll" model is not available here. Measured throughput: catalog >8 pages/45s→timeout; branch phase reached 28/697 detail calls in 45s (~12 min for branches alone). **Run this sync on the persistent Hetzner/Coolify host (docs/27) or any machine where nohup survives — not the Cowork VM.**

## Memory Health
- 2026-07-06 healthcheck: CAP COMPLIANCE flag — context/MEMORY.md at 2443/2500 chars (within 5% of cap); consolidate via meta-memory-write. All other checks green.

## Session 2

**Goal**: Fix the disjointed Invoice Audit screen, restore the invoice pipeline, get agents + Slack running (deliverable was a week behind).

**Deliverables**: docs/67 (full incident record). Merges to main: `6ab1b75` (invoice-audit payable/held consistency: Process button counts payable, held surfaced, client predicate aligned, mark/reset cache invalidation, regression test) + `e74705d` (run-disposition endpoint wiring disposeInvoice, human-gated, dryRun/office/maxInvoices) + host-scheduler branch (portable abc-nightly-sync + daily invoice ingest step + openbrain-abc-sync systemd units + agent-tick --entrypoint fix). Host: repo clone + scoped .env provisioned, systemd timers enabled (abc-sync nightly, alex.rivers tick), stray debug container removed, nepq skill collision resolved, alex-morning-abc-sync re-enabled.

**Run results**: invoice catch-up 20 invoices/136 lines (current thru 7/2); disposition pass 62 invoices/191 decisions (5 credit-flags, 16 negotiated-gated pending human); Alex summary posted to #accounting-invoice-processing ts 1783351625.

**Decisions**: daily-all processing per docs/63 confirmed by Chris; scheduler lives on Hetzner agent host; Slack scope = outbound only; disposition pass stays human-gated (named agents lack approval.decide by design); source='backfill' is the SOP-pass value (CHECK constraint).

**Open threads**: docs/67 §6 — Cowork task cancellation (HUMAN), daily pass automation (bearer/permission fork), widen ticks to 7 agents staged, cron prompt rewrite, returned-invoice pill.

## Session — Executive pipeline sold-value data-integrity fix

**Goal**: Investigate why YTD "sold value" ($45,683,303) looked like all-time, per Chris.

**Findings**: Two stacked defects, one bad-data layer. (1) Double count: 819 legacy `csv_initial` rows with NULL `acculynx_job_id` duplicate live jobs already counted under their real JobID; NULL id defeats `dedupePipeline`. (2) Those rows carry no stage dates, so `computeCloseRate` windowing falls back to `updated_at` (=2026-04-20), dropping the whole phantom into YTD. Source of truth for per-stage dates already exists: `acculynx_job_milestone_history` (16,277 rows, 6,357/6,463 jobs = 98%; Lead/Prospect/Approved/Completed/Invoiced/Closed/Cancelled dated).

**Action taken (prod DB, immediately live)**: Backed up 969 confirmed-duplicate `csv_initial` NULL-JobID rows (all with a live twin present in crm_pipeline; $31.1M) to new table `crm_pipeline_orphan_quarantine`, then DELETEd them from `crm_pipeline`. Left 4,609 blank-job-number csv_initial rows ($1.5M leads/cancelled) untouched — not confirmable as dupes. Rollback = re-insert from quarantine table.

**Results**: all-time sold $59.5M→$33.1M (965 jobs), matches milestone-history truth ~$32.8M; remaining NULL-JobID sold rows 0 (was 819); dashboard YTD sold now $19.29M (566) vs $45.68M before.

**Decisions**: Chris — rebuild pipeline off milestone-history spine; exclusive buckets (value once, by current status); "sold YTD" measured by **Close Date**. True close-in-2026 = 239 jobs / $7.66M (target after rebuild).

**Open threads**: Rebuild not yet started — (a) derive current status = latest-dated milestone; (b) capture per-stage entry dates; (c) retire `updated_at` fallback, window sold on Close Date; (d) regenerate crm_pipeline from truth layer; (e) coverage caption. Code: `computeCloseRate` in app/command-center/src/lib/executive-pipeline.ts. Diagnosis docs at repo root: pipeline-data-integrity-diagnosis.md.
