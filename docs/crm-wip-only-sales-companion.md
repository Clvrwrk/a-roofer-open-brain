# Desktop Sales WIP-only entry and shared source-detail adoption

2026-09-08 · PEC-299 ↔ CAT-188. CRM_PWA source commit `82e65ec85e7440d4679b4e10d4e859ed74439f3d`; reciprocal root receipt `docs/delivery/INCOMPLETE-SOURCE-UI.md`. Companion remains on its existing reviewed branch and retains AppShell, WorkOS/BFF authorization, and the Friday shared workbench.

The prior desktop Sales wrapper mounted the general SalesWorkspace. Its bootstrap needed `/efforts` before establishing the viewer; a correctly restricted WIP-only native staff principal therefore could not enter its weekly review. Every activated desktop Sales URL now checks `/api/sales/v1/session`, verifies `wip_read`, and mounts the shared **WeeklyWorkspace** directly. It makes no prospect/effort/intake call. Lead management, prospecting, inspections, agreements, and sales performance are explicitly disabled for this release. Existing effort URLs explain that those workflows are deferred; they do not open an unauthorized alternate editor.

The wrapper uses existing versioned design tokens and embedded heading levels under AppShell. Session failures show a safe retry/sign-in state without exposing provider error bodies. Dirty review state retains beforeunload protection; pending saves/uploads also block host link navigation and form submission until completion. The underlying weekly component retains its existing selection, draft, save, and submission guards. The Accounting Friday component is unchanged by this wrapper correction.

Package adoption is restricted to `@proexteriors/sales-workspace` **0.1.30**, SHA-256 `dba55c4e5035b2520eae1d616e4a9775fc7c9617484ee99fc19ec06289bdf4b6`. Its root-reviewed changes expose incomplete source facts without inventing amounts or implying completion. Contracts and server packages remain 0.1.17. Only the root local-dependency pointer and the installed sales-workspace package entry change in package-lock; no registry dependency changes.

Validation before commit: **463 tests / 36 files passed**, plus a production build, in `/private/tmp/crm-cc-wip-only-20260908/app/command-center`. The test copy uses current tracked companion source, new wrapper files, and cloned existing reviewed dependencies with the exact local 0.1.30 tarball extracted. No dependency downloads or package install scripts were executed. Initial broad-copy and offline-install attempts were incomplete and not used as passing evidence; the final test source was copied by tracked paths and the dependency version was verified.

A fresh isolated Chromium profile ran **12 synthetic browser checks**: actual shared WeeklyWorkspace session→weekly-cycle bootstrap without efforts; five disabled pillars; 390px and 320px reflow; safe denied-session state; successful explicit retry; missing-capability denial; draft beforeunload protection; pending host-link/logout protection; completion/unmount cleanup; and no runtime errors. The actual weekly child used an empty synthetic cycle list; navigation guard callbacks used a deliberately isolated test child. Logs: `/private/tmp/crm-cc-wip-only-browser-7.log`. Earlier fixture failures remain in browser logs 2–6: late Vite route registration and unbundled CommonJS/JSX dependencies, fixed by correct route registration and bundling the existing package. These were fixture failures, not accepted evidence.

The browser harness is `app/command-center/src/components/sales/sales-wip-shell.browser.mjs`, run from CRM_PWA root with `CC_WIP_TEST_APP` pointing at the isolated companion app. It uses existing local Vite/Playwright/esbuild tooling, synthetic API interception, and a separate temporary cache/profile. It does **not** inspect user Chrome, cookies, authentication, or any hosted origin. It does not establish live WorkOS acceptance, hosted staff onboarding, a full populated review journey, or a 90-point visual score.

The normal repository commit hook maintains the version marker. A build after that marker update is required before the parent deploys the companion commit. No hosted deployment, provider configuration change, or staff communication is performed by this adoption task. Independent wrapper review and the parent release record must identify the final commit and any remaining live acceptance gates.
