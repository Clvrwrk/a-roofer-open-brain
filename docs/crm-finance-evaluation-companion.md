# Finance evaluation tabs in the isolated CC Sales mirror

September 9, 2026: adopt the reviewed CRM Job Finance tabs, Canceled with balance and Invoice issues, through the existing Sales-only consumer. The original Command Center Accounting UI and workflows remain unchanged. This companion performs no deployment, database write, route change or upstream push.

The exact shared workspace archive is version 0.1.39, SHA-256 `699ada2b19094106b5f6ba37a4802cbdd704225f62c0c1c54f8b3cc4fce4adef`. Contracts and server remain 0.1.21. The package was copied without repacking. The package lock retains every prior dependency entry; only the root workspace pointer and its npm-generated package entry change. Offline npm initially lacked peer metadata; preserving the existing lock graph and rerunning normal offline installation restored its peers. No dependency versions were relaxed.

CrmSalesMirror matches the CRM WipRelease source after only the previously documented component name, stylesheet import and explicit host URL/logo substitutions. Normalized source SHA-256 is `9f79a014f7b8ec4923b1f363326b076d807ddbbc4434e604feef4aa5ea3017ec`; stylesheet remains byte-identical, SHA-256 `a349549df1f3550363d8c06f73944742636dcfd58e688b9c87588546185bb2bd`. Query links remain on the current Sales path and preserve unrelated parameters. Native dirty-navigation protection, immediate pending guards and session gating remain shared with CRM.

Verification: 182 tests pass across sales-wip-shell, sales-host, crm-access.server, crm-staff.server, crm-legacy-preservation.server and friday-wip-legacy-guard. The scoped Sales/Friday consumer and access resolver TypeScript check passes. The production build with `CRM_SALES_MIRROR_ASSETS_PREFIX=/sales-mirror-assets` passes. All 42 Accounting components/pages/API files, Friday consumer/helpers and global AppShell remain byte-identical to original production source `e4344d8bbd145d7697428fe41c01e011f0d7e714`. The six-file source set includes a new test for both finance queue labels, Sales-relative query links and installed shared parser behavior; the normalized source test binds all shared navigation safeguards. CRM's independent 13-case finance suite and full 558-test unit run passed before companion adoption.

The normal commit hook remains enabled and bumps the app marker. Parent owns publication, staged/runtime health, exact Sales-only route replacement and signed-in visual acceptance. Local source parity and these tests do not establish deployed acceptance. Accounting, global assets/auth and every non-Sales route must continue using the retained original runtime; canonical Friday remains disabled there and on the isolated candidate.

## Table preview follow-up: workspace 0.1.40

The live evaluation table revealed unusually long source issue summaries. This follow-up adopts the frozen 0.1.40 archive, SHA-256 `a15eb883005ed73b1fa294771ace2f9f81f63ac2ee59cfc953a7301ee08942d7`, without repacking. Only the table's issue preview is visually limited to three lines and labeled “Open record for full evidence.” The customer button still selects the complete record. Independent source review confirms the full issue summaries remain unchanged in WeeklyHeldReview's exception list and explicit confirmation dialog; source references, pins and audit payload are unchanged. The normalized mirror shell and copied stylesheet hashes above remain unchanged.

All 182 scoped consumer tests and the scoped TypeScript check pass again. The prefixed production build passes. All 42 original Accounting/global/Friday files still match the same e4344d8 baseline byte-for-byte. Normal offline installation changed one package; exact tarball dependency metadata and integrity were verified while retaining every unrelated lock entry. The normal commit hook records the next app marker. Parent retains publication, routing and live visual acceptance responsibility.
