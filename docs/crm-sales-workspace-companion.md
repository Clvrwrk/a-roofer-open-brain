# CRM sales workspace companion

2026-09-06. Isolated `codex/crm-shared-design` branch. Fresh upstream `origin/main` remains `40a917e`; initial companion base `0decefa`. No production deployment/merge or database write occurred. CRM source work is in `Clvrwrk/CRM_PWA`, `codex/prd-grill`, based on `bd08b48` with subsequent shared-route changes identified by this exact private package artifact.

Installed `@proexteriors/sales-workspace` 0.1.1, SHA-256 `d3801a1eb32399834e74b3d31529951e28a53ce4581bc7aa3a91e1f8d97f651e`, and its matching contracts tarball. Eighteen installed runtime/declaration/style/readme files matched the CRM artifact sources byte-for-byte. Package entry points use compiled ESM; React 19.2.8 is shared with CRM. Astro's official React integration 5.0.7 is compatible with this consumer's current Astro 6 build. No new paid service or license was introduced.

Set `CRM_SALES_WORKSPACE_ENABLED=true` only in the isolated local candidate to render the shared feature at `/sales`, `/sales/pipeline`, `/sales/wip-ar`, `/sales/more` and `/sales/efforts/:uuid`. The default preserves the existing sales pages. The existing AppShell/department navigation remains; unrelated legacy sales slugs retain their existing handling. Deep links validate UUID/query/tab values. Native page/customer navigation uses shared leave protection; tab changes retain the mounted draft and replace the current URL. Actual browser history/unload behavior is still unverified.

`/api/sales/*` deliberately returns 401 for non-WorkOS/local/agent identities and 503 for WorkOS humans until a verified CRM staff-session/scoped canonical BFF is wired. It does not use a service role or infer CRM permission from the legacy full-access actor. This is a closed integration boundary, not live data acceptance. No CRM service worker or offline packet is installed on this host. The existing CC worker bypasses sales HTML/API paths, rejects rendered sales snapshots and purges prior sales cache entries; other department entries remain intact. Real worker upgrade/claim needs browser proof.

Validation: 318 tests passed (314 existing plus four host/cache cases), build passed with Sentry release/upload disabled. Loopback HTTP checks with blank database credentials rendered all five sales routes with one main landmark, existing Accounting navigation and no-store headers. Bad UUID returned 404; API session returned 401. JavaScript was not executed in those checks. Temporary port 4341 process stopped afterward. Source red team fixes include initial-effort retry retention, intake navigation/cancel/unload protection, unique dialogs and no nested main. The Mac was locked, so no new browser or ≥90 UX pass was awarded.

The network dependency audit identified 16 pre-existing vulnerable packages (9 high, 5 moderate, 2 low); every vulnerable node's version matches the pre-integration lockfile. These remain release blockers. An offline install's zero advisory count is not a remediation result. Framework/adapter and transitive remediation must be tested before promotion.

Reciprocal CRM receipts: `docs/delivery/X1-DESKTOP-CONSUMER.md`, `docs/delivery/X1-DESKTOP-RED-TEAM.md`, `docs/delivery/evidence/sales-desktop-package.json`, `desktop-sales-http.json`, `desktop-dependency-audit.json`. Full shared records, Friday WIP/AR workflow, staff auth, real provider/document capabilities, browser/device and ≥90 acceptance remain open. Rollback is the prior companion commit plus disabling the local feature switch; no production rollback is required because nothing was deployed.
