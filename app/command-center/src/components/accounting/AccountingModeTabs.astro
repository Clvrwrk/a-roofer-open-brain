---
interface Props {
  active: "invoice-gate" | "invoice-audit" | "overview" | "other";
}

const { active } = Astro.props;
---

<nav class="accounting-mode-tabs" aria-label="Accounting dashboard mode">
  <a class:list={["accounting-mode-tab", active === "invoice-audit" && "is-active"]} href="/accounting/invoice-audit">
    Invoice audit
  </a>
</nav>
