---
// The territory map now lives at the home route `/`. Keep this path working
// for existing deep-links (live-work items, command-center routing) by
// redirecting to home.
return Astro.redirect("/", 301);
---
